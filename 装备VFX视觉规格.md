# 装备/羁绊 VFX 视觉规格 (用户口述, 实装参照)

> 现有 VFX 库没有的特效, 用户给具体视觉 → 照此实装(复用现有 primitives 或新建)。
> 用户没给的, 我照同风格做 + F5 眼验。
> 现有 primitives 可复用: _fire_projectile(直线投射,带纹理/大小/时长) · _fire_barrage_bolt(弹幕) · _play_fireball · _play_lightning_strike · _play_chain_bolt · _play_crystal_beam · _play_fire_sweep · _play_equip_vfx(帧动画 lightning/burn) · CPUParticles2D(爆破/拖尾) · Line2D(光束/折线)。

---

## p2eq_053 霰弹贝 (2026-06-23 用户定) ⏳ 待实装
- 从龟**中心**打出**十几颗子弹**(逻辑已 12/14/18 发, 按星级 1/2/3)。
- **火焰开火特效**(枪口 muzzle flash, 龟口火光)。
- 子弹在 **40° 扇形**角度内, **每颗夹角相同**(均匀分布: 角 = -20° 到 +20° 等分 N 份)。
- 飞向**敌方**(左队朝右/右队朝左)。
- **按命中掉血**(逻辑已有: 每颗 0.22×ATK, ≥8 发命中→眩晕)。
- **实现**: 复用 `_fire_projectile`/弹珠 primitive, 40° 内等分角度从龟中心发 N 颗 + 龟口火光(CPUParticles 或火帧动画) + 飞行到敌命中。逻辑层(伤害/眩晕)已实装, 本次加视觉。

## p2eq_039 竹叶 (2026-06-24 用户定) ⏳ 待实装
- 用户称"竹制弓箭", 实为唯一竹系装备 **竹叶**(机巧/召唤系, `+生命80/140/200`, 效果=施法后强化攻击+回血+永久+100生命, 镜竹叶龟充能机制)。
- **加生命值特效用竹叶龟那个** = `_spawn_bamboo_orb`([BattleScene.gd:3552](scripts/scenes/BattleScene.gd#L3552), 绿生命球抛物线飞回龟+落点爆 bamboo-charge-burst + 绿拖尾 _bamboo_trail_dot)。
- 实现: 竹叶装备触发"回血/永久生命"时调 `_spawn_bamboo_orb(from,to)`(同竹叶龟充能追击)。逻辑已实装, 本次加此视觉。

## 枪系 VFX 补齐 (2026-06-24 用户"去补齐") ✅ 已实装 (批2: 048/049/050/051/057/058, 见 VFX实装账本.md 批2; 052 早接 revolver-bullet)
枪系(枪 type) = 048黄铜手铳 / 050加特林贝壳 / 051激光手枪 / 052左轮 / 057狙击长管。**052 已接 revolver-bullet 投射**。
- **048 黄铜手铳**: 射 4/5/6 发沿途首个单位 → 快速单发弹珠(复用 `_fire_projectile`, 小弹连发)。
- **050 加特林贝壳**: 射 20/30/60 发随机敌+减甲 → 加特林扫射(复用 `_fire_projectile`, 大量快速弹珠喷射)。
- **051 激光手枪**: 同列首敌物理+流血 → **红色激光束**(复用 `_play_crystal_beam`, 从龟射向那一列敌; 名副其实)。
- **057 狙击长管**: 最低血%敌+击杀递归 → 狙击长程曳光/细光束(复用 `_play_crystal_beam` 细 或 高速曳光弹)。
- 金弹(枪羁绊满阈值)已有 `goldbullet` 金色弹道 VFX (gun_fire_shot:379)。
触发点: 各 handler 的 fx 里加 `"vfx"` 字段(052/051基础枪/030 已是范本), 复用现有不新建。

## p2eq_010 激光长刃 横扫 VFX (2026-06-24 用户问) ⏳ 待【新建, 不可复用】
- 激光长刃 = 传说剑(cost5), 授予【横扫】0龟能主动(横扫一列/3★全体 + 单体竖斩+身后50% + 回血35/80/100%)。**逻辑全实装**(sweep() phase2_equip_runtime:1591), 但横扫**一点特效都没有**(只伤害飘字)。
- **必须新建【剑刃横扫弧】VFX** —— 用户明确: 这是【剑在横扫】, **不能套 cyber-beam-sweep**(那是赛博激光束, 视觉不对)。要一道**剑气/刃光月牙弧**扫过那一列/全体敌(白/能量色弧形, 从携带者扫向目标列, 快速~250ms): Line2D 弧 / 绘制月牙斩 sprite / 粒子拖尾。
- 触发点: p2Sweep handler(skill_handlers.gd:52) 或 sweep() 的 fx 加 `"vfx"` 标记。

> **原则(用户2026-06-24纠)**: VFX 必须**视觉匹配**装备 —— 剑→斩击弧 / 枪→子弹 / 激光枪→光束。**不能只因"有现成的"就套视觉不对的**(如剑横扫套激光束)。剑系(001/005/006/007/009/010)斩击 VFX 基本都需新建。

## 灵物触手 (#553) 修 — 实装不符规格 (用户 2026-06-24 两次纠) ⏳ 待修
规格原文(类型效果-实装规格.md:128): "激活时 1/2 个【场边】触手登场…每回合朝一目标拍击, 对【沿途敌人】(4%目标最大HP+55) 物理。"
当前实装(BattleScene _spawn_tentacles:13834 / _tentacle_slap:13877)错 3 处:
1. **位置**: 用 `_find_summon_slot` 塞进 6 格召唤位 → **错**。改: **战场边缘(屏幕侧边)放, 不占格、不进 slot 网格**(参考场边/屏幕侧坐标, 非 _slotKey)。
2. **拍击范围**: 打 `same_column`(目标那列) → **错**。改: **"沿途敌人" = 触手→目标连线上的敌人**(一条扫击线穿过的敌人)。
3. **视觉**: 🐙 emoji + 通用 basic-slam-impact → 没真触手。改: **程序画触手从场边伸出 → 拍向目标 → 缩回**(Line2D 触手身/粒子, 拍击落点冲击)。
位置/拍击=逻辑+渲染修; 视觉=新建。**逻辑数值(伤害/追击)不动, 只改位置+范围语义+加视觉**。

## p2eq_036 孵化器 进度条 + 升级特效 (2026-06-24 用户定) ✅ 已实装
- **机制确认**: 孵化进度 `_incubatorProgress` **0-100**; 任意单位死亡 → 持有者 +进度(敌死+10/我死+15, `BattleScene.gd:6989` `_incubator_on_death` → `EquipmentRuntime._incubator_add`); **满 100 → `_incubatorTempLevel`+1**(临时等级+1, 进度回卷)。
- 用户要两个:
  ① **图标下加 0-100 进度条** —— 像法器法力条(`_build_equip_column` ~1506 的 14×2px 条)那样, 在孵化器图标下加一条, 读 `_incubatorProgress/100`, 随死亡刷新(`_refresh_slot` 链里加更新)。
  ② **每满 100 升级时放特效** —— 在 `_incubator_on_death` 6995-6996 升级处(现只飘 "🥚临时Lv+N" 字), 加**蛋裂/金光升级 VFX**。
- 注: 批3 做的是孵化器"满级全队盾光"(另一回事); 这进度条+升级特效是**单独一块**, 批3 后做(同写 BattleScene 故串行)。
- **实装(2026-06-24)**: ① 进度条 `_build_incubator_bar`/`_update_incubator_bar`/`_has_incubator` (BattleScene.gd ~1556), 仿法器法力条 14×2px(🥚标+暖橙条,满金), 仅持 p2eq_036 或有 `_incubatorProgress` 者显; meta `incubator_bar` 存在 **slot 节点(root)≠hud**(因 `_refresh_slot` 检 slot 节点 — 法器条 meta 误存在 hud 上故那条 refresh 链其实不命中, 我没复制此潜伏 bug), 建挂在 `_make_slot_view` 装备列后, 刷新挂进 `_refresh_slot`(死亡加进度自动刷)+ `_incubator_on_death` else 分支补"涨进度未升级也刷"。 ② 升级特效 `_play_incubator_hatch`(BattleScene.gd ~6894): 金光爆环(复用 `_play_aoe_ring` 金色)+蛋壳碎片 CPUParticles(米白带重力下坠)+金色升腾微光; 接在 `_incubator_on_death` `_incubator_add()>0` 处(升级飘字之后)。视觉需 F5。

## VFX/UI 调优 (用户 F5 反馈 2026-06-24, z 修后能看到了) ⏳ 待调 (等法力 agent 腾出 BattleScene)
- **商店栏太大** → 优化缩小。查商店 UI(`_rebuild_battle_shop` / 商店面板/卡片尺寸/栏高), 缩小占屏。
- **048 黄铜手铳 子弹太快看不到** → 弹珠飞行 `dur` 0.18 太快 → 放慢到 ~0.4-0.5s; 4/5/6 发要**逐发错开(stagger)**让每颗都看得到(用户强调"有很多颗子弹")。
- **057 狙击长管 应一枪射到尽头** → 现在是短弹/到目标止, 用户要**一道长射线贯穿到屏幕边缘**(狙击感, 射到尽头)。改: 复用 `_play_laser_beam` 但终点延长到屏幕边/穿透线, 而非止于目标。

---
(后续装备视觉规格按此格式累积)
