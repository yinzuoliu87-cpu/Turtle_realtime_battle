# 训龟大师 7 技审核报告（2026-07-30）

需求原文（用户 2026-07-30）：**「训龟大师的每一个技能效果和特效需要审核一边」**
方案书：[docs/plans/20260730b-局内HUD改造+大师审核+地图提升.md](plans/20260730b-局内HUD改造+大师审核+地图提升.md) §4.3
拍板 U9：**先出报告，数值改不改由用户定** —— 本报告**没有改任何数值**。

> ★口径：**读代码，不读 tooltip**。工具 `python tools/pet_effect_dump.py --trainer`。
> 由来是无头·灵魂打击那次：我按文案报「0.9A 物理 + 20% 当前生命」，代码一直是「0.5A 魔法 + 10%」。

---

## 0. 一句话结论

**7 技的数值本身全部与文案一致，没有发现算错的地方。** 但审出**两类基础设施问题**，都已修（值不变）：

1. **一个"看起来是事实源"的常量其实是死的** —— `HOOK_VULN_MULT` 在游戏代码里零读者，
   真实行为来自 `_mitigate_incoming` 里硬编码的 `1.25`。而门禁 `verify_trainer_desc`
   **拿这个死常量去推文案该写什么**。→ 改常量能让门禁要求一个错的文案，并判它正确。
2. **三个技能的实质数值门禁一个都不查** —— 怒火药水 / 口哨 / 冰川只查了射程和 CD，
   因为它们的数值是散在 `trainer_system.gd` 里的裸字面量（那个文件原来**一个 `const` 都没有**），
   没有常量可以推期望值。

另有 **6 条「代码里有但文案没写」**，都不是错，是要不要告诉玩家的问题（见 §3，需你定）。

---

## 1. 逐技对照（代码真值 ↔ 玩家文案 ↔ 特效）

文案来源：`scripts/scenes/TrainerConfigScene.gd` 的 `SKILLS[].desc`。

### ① 魔法石（被动）

| 项 | 代码真值 | 文案 | |
|---|---|---|---|
| 攻击力 | `TRAINER_ATK_MAGIC_STONE 10.0` vs `TRAINER_ATK 1` | ×10 | ✅ |
| 普攻附带 | `MS_MAXHP_BASE 0.02 + 0.001×lv`，`lv` 钳 1~10 | (2+0.1×大轮等级)%，Lv1 2.1% → Lv10 3.0% | ✅ |
| 每层攻速 | `MS_HASTE_PER_STACK 0.05` | +5% 攻速（可叠） | ✅ |
| **叠层清零时机** | `_dl_start_fight()` 里 **每路开打清零**（[dual_lane_flow.gd:352-355](../scripts/scenes/battle/dual_lane_flow.gd#L352-L355)） | 「持续到**本场**结束」 | ⚠ **见 §3-a** |

实现链：`_tick_trainer_attacks` → `_fire_trainer_rock`（弹道）→ `_trainer_magicstone_onhit`（结算）
特效：抛物线石头（`lava-rock.png`）✓ · 命中紫字 `#c86bff` ✓

### ② 钩锁（CD 20 / 射程 600）

| 项 | 代码真值 | 文案 | |
|---|---|---|---|
| 眩晕 | `HOOK_STUN 4.0`（吃韧性） | 眩晕 4 秒 | ✅ |
| 受伤加成 | `HOOK_VULN_MULT 1.25` | +25% | ✅（**但这条原来是假的，见 §2-1**） |
| 拖拽 | 每 `1.0s` 拽一下 × `70` 码，4 秒内 4 下 = 280 码 | 「一段段拽向大师」 | ✅ 定性一致 |
| 空放 | `HOOK_CD_MISS 10.0`（返还 10 秒） | 「空放返还」 | ✅ 定性一致（未写具体秒数） |
| 施法站定 | `_cast_lock_until = _t + HOOK_WINDUP(0.35)` | — | ⚠ 未写 |

实现链：`_cast_hook` → `_hook_grab`（到达才结算，走 `_pending_shots` 不等 tween）→ `_tick_hooks`
特效：前摇举钩 → 中速飞（`HOOK_MISSILE_SPD 570` 码/秒）→ 到达；红环易伤标记 ✓ 素材 `trainer-hook.png` ✓

### ③ 怒火药水（CD 16 / 射程 700）

| 项 | 代码真值 | 文案 | |
|---|---|---|---|
| 生效半径 | `FURY_RADIUS 300.0` | 300 码 | ✅ |
| 持续 | `FURY_SEC 5.0` | 5 秒 | ✅ |
| 攻速 | `FURY_HASTE 1.3` | +30% 攻速 | ✅ |
| 龟能充能 | `FURY_ECHARGE 1.25` | +25% 龟能充能 | ✅ |
| 移速 | `FURY_MOVE 1.25` | +25% 移速 | ✅ |
| 只给友军 | 同侧且非大师 | 友军 | ✅ |
| 落地才生效 | `_pending_shots` 延时（不是丢出去就生效） | — | ⚠ 未写 |

实现链：`_cast_fury_potion`（主文件·薄包装）→ `_fury_apply_buffs`
特效：抛物线飞行 + 橙红 splash + 受益者红橙脚环 5 秒 + 身体红光 5 秒 ✓
（注释自承「素材原图待 R1g，暂用火光占位」→ 这是**已知的占位**，不是本次审出的）

### ④ 口哨（CD 14 / 无需瞄准）

三支**等概率**随机（`_battle_rng.randi() % 3`，各 1/3）。

| 支 | 项 | 代码真值 | 文案 | |
|---|---|---|---|---|
| ① 临时血 | 数值 | `WHISTLE_TEMPHP 700.0` | 700 临时生命 | ✅ |
| | 持续 | `WHISTLE_TEMPHP_SEC 5.0`，到期**按比例削**当前血 | — | ⚠ **未写时长**，读起来像永久 |
| ② 灵体气波 | 伤害 | `WHISTLE_WAVE_DMG 200.0` 物理 | +200 物理 | ✅ |
| | 击飞 | `WHISTLE_WAVE_KB 100.0` | 击飞 | ✅ |
| | 削甲 | `WHISTLE_SHRED_MULT 0.7`（−30%） | 削甲 30% | ✅ |
| | 削甲持续 | `WHISTLE_SHRED_SEC 5.0` | — | ⚠ 未写时长 |
| | 判定 | 直线带宽 80 码，穿透沿途敌 | — | ⚠ 未写 |
| ③ 狂暴 | 攻击力 | `WHISTLE_BERSERK_ATK 0.2`（+20%） | +20% 攻 | ✅ |
| | 生命偷取 | `WHISTLE_BERSERK_LS 20`（定值） | +吸血 | ✅ 定性 |
| | 持续 | `WHISTLE_BERSERK_SEC 4.0`（攻/吸血/免死**同为 4 秒**） | 「免死 4 秒」 | ⚠ 4 秒只挂在"免死"上，容易读成攻/吸血更久 |

实现链：`_cast_whistle` → `_whistle_temphp`/`_apply_temp_maxhp` · `_whistle_spirit_wave` · `_whistle_berserk`/`_whistle_berserk_on`
特效：头顶 ♪ 前摇 ✓ · 绿光柱+绿环（临时血）✓ · 灵体小龟 `spirit-turtle.png` + 蓝气波 `qibo-ball.png` ✓ · 金盾闪+血滴（狂暴）✓

### ⑤ 冰川（CD 17 / 射程 500）

| 项 | 代码真值 | 文案 | |
|---|---|---|---|
| 长度 | `GLACIER_LEN 500.0` | 500 码 | ✅ |
| 持续 | `GLACIER_SEC 6.0` | (6 秒) | ✅ |
| 减速 | `GLACIER_SLOW_MAG 0.6`（−40%） | −40% 移速 | ✅ |
| 受伤加成 | `GLACIER_VULN_MULT 1.2`（+20%） | 受伤 +20% | ✅（**原来连常量都没有，见 §2-1**） |
| 只冻敌方 | 同侧 skip，且 skip 大师 | 站上的敌 | ✅ |
| 判定带宽 | `GLACIER_WIDTH 90.0` | — | ⚠ 未写 |

实现链：`_cast_glacier`（主文件·只登记区域）→ `_tick_glaciers`（逐帧施加）
特效：冻地冰河 `glacier-ground` 平铺 + 冰脊 `glacier-crystals` 逐根升起 + 寒雾 + 冲击环 ✓
（注释记着 2026-07-26 已按「重新设计·别复用」重做过一轮）

### ⑥ 猎龟令（CD 30 / 射程 600 / 锁头弹道）

| 项 | 代码真值 | 文案 | |
|---|---|---|---|
| 标记时长 | `HUNT_SEC 15.0` | 15 秒 | ✅ |
| 受伤加成 | `HUNT_VULN 1.15`（+15%） | +15% | ✅（这条**一直**是真事实源） |
| 嘲讽半径 | `HUNT_TAUNT_R 400.0`，圈随目标移动 | 400 码（圈随它移动） | ✅ |
| 弹道 | `HUNT_MISSILE_SPD 1400` 锁头，到达才生效 | — | ⚠ 未写（但"锁头"是用户 2026-07-28 明确要的） |
| 空放 | `HUNT_CD × 0.5`（返还一半） | — | ⚠ 未写（钩锁写了，这里没写 → 不一致） |

实现链：`_cast_hunt_order` → `_hunt_mark` → `_tick_hunt_taunt`
特效：`_hunt_dramatize` ✓ 图标 `hunt-order-icon.png` ✓

#### 逐技目视（2026-07-30 补·`VFXPREVIEW=tr_hunt` 走真入口 `_cast_active`）

先纠一条**我自己看图看错的**：我最初判「印记没贴在目标身上」——错。探针实测第 2 帧起偏差 **0.00 m**，一直贴着。看着像没贴上，是因为圈**大到看不出圆心在龟身上**。

| 项 | 实测 | 判 |
|---|---|---|
| 施放 | `_cast_active(hunt_order)` → 成功 | ✅ |
| 锁头弹 | 帧 2→6（t=0.21→0.39s）屏幕 x 从 ~400 推到 ~929，tween 每帧重读目标位置 = 真锁头 | ✅ |
| 命中判定 | 走 `_pending_shots`（delta 制），**不依赖 tween** —— 符合 CLAUDE.md §3.5 | ✅ |
| **印记直径** | `HUNT_TAUNT_R*2*WS/128` = **19.20 m**；战场 ARENA 只有 **38.3×17.5 m** → 占宽 50%、**比纵深长 110%** | ❌ 已改 **2.60 m** |
| **像素密度** | 0.150 m/texel，龟 ≈0.042 → **粗 3 倍**，破碎环糊成红板砖 + 黄块 | ❌ 已改 **0.020 m** |
| **首帧位置** | `_hunt_sigil` 建节点**没设初始 position**，靠 `.set_delay(0.03)` 循环 tween 归位 → 头 2 帧在世界原点，偏差 **16.21 m** = 地图正中闪一下 | ❌ 已修 |

**根因一句话：把「效果半径」当成了「贴片尺寸」。** 这两个概念必须分开 —— 400 码是嘲讽范围，
不是印记该有多大。所以门禁不只验数字在带内，还验它**不再由 `HUNT_TAUNT_R` 推导**（只钉数字的话，
有人换个半径就又崩）。见 `verify_trainer_hunt_tame` ⑨ 组 11 条，三条修复各做过反向验证（改回旧写法 → FAIL x3 / x1 / x1）。

**嘲讽范围圈不画**（用户 2026-07-30 拍板）：范围本不需精确告知玩家，画它必然要一张 19 m 尺度的专用低频素材（细虚线环）。

**顺带修的同类问题**：驯服 `_tame_rune` 有**同一个原点闪帧毛病**，同批修；尺寸（3.6 m）本来就对，只是换成显式常量 `TAME_RUNE_D_M`。

**观察未改（记账）**：印记是 y=0.06 的贴地贴片，透明排序下会画在龟立绘腿部之上，读感略"浮"。项目里贴地环的既有惯例是 `no_depth_test + render_priority`（`_splash_ring_bold`），但那会让它**恒画在立绘之上**、更浮，所以没动。P3 高低差落地时要一并考虑。

### ⑦ 驯服（CD 60 / 射程 600 / 锁头弹道）

| 项 | 代码真值 | 文案 | |
|---|---|---|---|
| 重生血量 | `TAME_REVIVE_PCT 0.30` | 30% 最大生命 | ✅ |
| 重生无敌 | `TAME_REVIVE_SEC 2.5` | 2.5 秒无敌 | ✅ |
| 每秒衰减 | `TAME_DECAY_PCT 0.02` | 每秒损失 2% 最大生命 | ✅ |
| 不重复标 | 已驯服 / 已标记的跳过 | — | ⚠ 未写 |
| 空放 | `TAME_CD × 0.5`（返还一半） | — | ⚠ 未写 |

实现链：`_cast_tame` → `_tame_mark` → `_tick_tame_decay`
特效：`_tame_dramatize` ✓ 图标 `tame-icon.png` ✓

---

## 2. 审出的问题（已修 · 值一律不变）

### 2-1 ★`HOOK_VULN_MULT` 是死常量，而门禁拿它当事实源

**修前**：

```gdscript
# RealtimeBattle3DScene.gd  const 区
const HOOK_VULN_MULT := 1.25          # ← 游戏代码【零读者】

# _mitigate_incoming 里
d *= 1.25                              # ← 真实行为在这, 硬编码
```

而 [verify_trainer_desc.gd:45](../tests/verify_trainer_desc.gd#L45) 是：

```gdscript
["受伤加成", "+%d%%" % int(round((K["HOOK_VULN_MULT"] - 1.0) * 100.0))]
```

**两个 1.25 只是碰巧相等。** 把常量改成 1.5 会发生：门禁要求文案写 +50% → 改了文案门禁绿 →
**游戏里仍然是 +25%**，也就是门禁**把错误的文案判成了正确的**。
冰川那条更彻底：`d *= 1.2` 连常量都没有。

**已修**：两处改成读 `HOOK_VULN_MULT` / 新增的 `GLACIER_VULN_MULT`（值不变）。
**反向验证**：把 `HOOK_VULN_MULT` 改成 1.60 → `verify_hook` 的「被钩→受伤×1.25(100→125)」
报 `160.0` 并 FAIL。修之前改这个常量**什么都不会发生**。

### 2-2 三个技能的实质数值门禁一个都不查

`trainer_system.gd` 原来**一个 `const` 都没有**，数值全是函数里的裸字面量。后果两条：
① `pet_effect_dump` 抽不到（它靠展开具名常量）② `verify_trainer_desc` 没常量可推期望值，
于是 `want` 表里怒火药水/口哨/冰川**只有射程和 CD**。

**已修**：提出 15 个常量（`FURY_*` / `WHISTLE_*` / `GLACIER_SLOW_MAG` / `MS_HASTE_PER_STACK` / `WHISTLE_SHRED_MULT`），
门禁覆盖的数字 **20 → 32 个**。
**反向验证**：`FURY_HASTE 1.3→1.5` → 报「描述里找不到 +50%攻速」；
`GLACIER_VULN_MULT 1.2→1.35` → 报「描述里找不到 +35%」。

### 2-3 `pet_effect_dump --trainer` 第一版七技全是"效果 0 条"

看着像"大师没有任何效果"。**探针**（不是推理）打出 `_hook_grab` 的函数体，看到
`battle._damage._stun(target, battle.HOOK_STUN, "hook")` —— 数值是**跨文件常量**，
而 `expand_consts` 只展开同文件的。已加 `expand_cross()` 处理 `battle.CONST` 形式。

另外六个主动里有三个的入口是主文件里的**薄包装**（`_cast_glacier` 只往 `_glacier_zones`
塞个字典就返回，效果全在 `_tick_glaciers`），而 `func_scope` 只跟同文件子函数 →
`TRAINER_SKILLS` 改成**每技一条函数链**（13 环），并加了「链上函数失踪」检查
（它当场抓到我把 `_tick_hunt_taunt` 写成了 `_hunt_taunt_tick`）。

现在：**7 技 / 13 环全在位 / 12 条效果 / 文案未提 0 条**。

### 2-4 附带发现（**没改**，超出本次审核范围）：装备域有同一个反模式

给新门禁写"硬编码倍率不许回来"这条断言时，我第一版写成「全文件不许出现 `d *= 1.2`」，
结果抓到的不是冰川，而是 **装备·靶向器055** 那行：

```gdscript
if not is_self and _t < u.get("eq_marked_until", 0.0):
    d *= 1.2                                     # 靶向器055: 被标记目标受伤 +20%
```

**它也是硬编码、也没有常量。** 装备的文案审计器 `tooltip_number_audit.py` 是拿代码里的值
去核文案的，所以目前不会出错；但它和钩锁那条是同一个形态，哪天有人给它加个"事实源常量"
就会重演 §2-1。**本次没动它** —— 装备不在需求 3 的范围内，而且改它要连 59 件装备的口径一起看。
记在这里，等做装备那轮时一起。

---

## 3. 需你定：6 条「代码里有但文案没写」

这些**都不是 bug**，是"要不要给玩家看"的设计问题。我没动它们。

| # | 内容 | 我的建议 |
|---|---|---|
| **a** | **魔法石攻速叠层是【每路】清零，文案写「持续到本场结束」。** 代码注释自己也把"本场"当"每路"用。若玩家把"本场"理解成整场对局（上+下+决胜），这句是错的 | **改文案为「持续到本战场结束（换战场清零）」** —— 这是玩家能感知的机制，说不清会让人以为能攒三路 |
| b | 口哨①临时生命 **没写 5 秒时长**，读起来像永久（且到期是按比例削当前血，不是直接掉） | **补上「5 秒」** —— 700 血永久和 5 秒是完全不同的技能 |
| c | 口哨②削甲 **没写 5 秒时长** | 补 |
| d | 口哨③的 4 秒**同时**是攻/吸血/免死的时长，文案只把 4 秒挂在"免死"上 | 改成「4 秒内 +20%攻 / +吸血 / 免死」 |
| e | 猎龟令、驯服 **空放返还一半没写**（钩锁写了「空放返还」）→ 三个技能口径不一致 | 补，或三个都不写 |
| f | 施法期间大师站定（钩锁/怒火/冰川）、冰川判定带宽 90 码、气波带宽 80 码、驯服不重复标 | **不写** —— 属实现细节，写进去只会让说明变长 |

---

## 4. 特效审核结论

7 技**全部有专属演出函数**，13 个素材文件**全部在位**：

| 技 | 演出函数 | 素材 |
|---|---|---|
| 魔法石 | 弹道 + 命中紫字 | `lava-rock.png` ✓ |
| 钩锁 | `_hook_dramatize` / `_hook_dramatize_miss` | `trainer-hook.png` ✓ |
| 怒火药水 | `_fury_dramatize` | 火光**占位**（注释自承「原图待 R1g」） |
| 口哨 | `_whistle_note` / `_whistle_spirit_dramatize` / `_spawn_spirit_turtle` | `spirit-turtle.png` `qibo-ball.png` ✓ |
| 冰川 | `_glacier_dramatize`（四层） | `glacier-ground` `glacier-crystals` ✓ |
| 猎龟令 | `_hunt_dramatize` | `hunt-order-icon.png` ✓ |
| 驯服 | `_tame_dramatize` | `tame-icon.png` ✓ |

**唯一的美术缺口是怒火药水的药水瓶还在用火光占位**（代码注释里早已标注，不是本次审出的新问题）。

> ★注意本节的判据限度：这是**静态检查**（演出函数存在 + 素材文件存在），
> **不等于"屏幕上真的看得见"**。memory `project-vfx-library-rich` 那条教训是
> 「美术断言要查素材真的显示进 `_world`，不能只判定生效」。
> 逐技目视需要开调试场（`DEBUG_EDIT=1`，用 `_edit_trainer_active` 切技能）—— **这一步还没做**。

---

## 4.5 ★逐技目视补完后审出的最大问题：钩锁根本不是 skillshot（已修）

§4 那张"7 技全有演出函数 + 13 素材在位"的表是**静态检查**，它给的是绿灯。
用户 2026-07-30 让我「一个个仔细看」，逐技目视之后审出的最重的一条**和美术无关，是逻辑**：

**`_cast_hook` 在出手那一刻就判定命中。** `_hook_first_target` 沿方向选定目标 → 算好 `arrive`
→ `_pending_shots` 定时到点**必钩**（唯一条件只是"还活着"）。飞行期间目标走出路径 / 跑出射程 /
绕到背后**全部无效**。代码注释自己写着「返回是否**将**命中」。

**决定性证据**：`HOOK_MISSILE_SPD` 那行的注释是
「用户2026-07-26 再−40%: 950→570·**更像可躲skillshot**」——
用户当时降速 40% 的意图就是让它可躲，而判定在出手瞬间，**那次降速根本没让它变可躲**，
只是把"假可躲窗口"从 0.6 秒拉长到 1.05 秒。用户 2026-07-30：「lol锤石的Q哪有这么锁的？」

**成因**：注释写「结算逻辑从演出里抽出可测（照海盗钩索 / CLAUDE.md §3.5）」——
§3.5 的目标是**别让数值结算依赖演出 tween**（海盗钩索那次连红三次的教训），这是对的；
但实现时把**命中判定**也一起挪到了出手瞬间，**过头了**。两者本可兼得。

**已修**：钩头按 delta 逐帧推进（挂 `_tick_hooks`，它本来就在 sim tick 里 → 不依赖 tween、无头可测），
每帧做 `HOOK_HIT_R = 70` 碰撞检测。返回值语义从"是否将命中"改成"是否成功放出"
（游戏内三处调用都不看返回值，只有门禁在断言）。CD 出手即进 20，飞满射程未命中才返还成 10。

**门禁** `verify_hook` ③ 组重写成 6 小组，核心是**「飞行中走开躲得掉」**。
反向验证：把碰撞检测改回"无视距离"（复现旧的锁定行为）→ 这条立刻红。

★**这会让钩锁变弱**（原来必中）。要不要调 CD / 眩晕时长补偿，**待用户拍板**。

★另一个遗留问题（这次没改）：`_hook_chain` 每帧都在调，但**屏幕上看不到从大师连到钩头的铁链**。
旧的 tween 版本也一样。

## 5. 门禁沉淀

- **新增 `verify_trainer_audit`（107 条断言 · 四组各自反向验证过）** —— 守的不是数值，
  而是"审出来的那两类基础设施问题不许回来"：
  ① 两处易伤倍率必须**读常量**（退回硬编码 → 红）
  ② `trainer_system` 那 15 个常量必须**真的被对应函数用上**（定义了不用 = 死常量 → 红）
  ③ 7 技齐全 + 每技演出函数 + 图标素材在磁盘上 + 实现链 13 环全在位（改名/搬家 → 红）
  ④ 与 `tools/pet_code_scope.py` 的 `TRAINER_SKILLS` **对齐**（一边改了另一边不知道 → 红）
- `verify_trainer_desc`：覆盖数字 20 → **32 个**（两条反向验证过）
- `verify_hook`：因 2-1 的修复，现在**真的**能守住受伤倍率（改常量会红，此前不会）
- `tools/pet_effect_dump.py --trainer`：7 技代码真实效果清单 + 链上函数失踪检查
- `tools/pet_code_scope.py`：加 `expand_cross()`（跨文件常量）+ `TRAINER_SKILLS` 函数链 + 5 类大师专用效果规则

**不升版本号** —— 本轮没有任何玩家可感知的改动（数值一律不变，只是口径与门禁）。
§3 那 6 条要不要改文案由你定；改了文案才算玩家可感知，那时再升。
