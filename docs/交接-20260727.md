# 交接文档 · 2026-07-27

> 给**下一个对话 AI / 续窗**用。读完这份 + `memory/` 三篇 + `docs/plans/` 近期方案书，就能无缝接上。
> 项目：斗龟场·实时版（Godot 4.6.3 / GDScript）。工作约定见 `CLAUDE.md`（**先读它**）。

---

## 1. 当前状态（截至交接）

| 项 | 值 |
|---|---|
| 分支 | `main`，HEAD `7bb15cb`，**工作区干净、0 未推、CI 全绿** |
| 版本 | **0.15.13**（`project.godot` config/version）· tag v0.15.11/12/13 已推 |
| iOS 包 | `C:\Users\Louis\Desktop\turtle-0.15.13-unsigned.ipa`（完整可装 · Sideloadly 装机）|
| 远端 | `git@github.com:yinzuoliu87-cpu/Turtle_realtime_battle.git`（公开）|

**一切已提交+已推+memory 已写。没有悬空的活。** 可安全新开对话，git + memory 接得上。

---

## 2. 这一整段做了什么（按时间）

1. **训龟大师返工方案书全套**（`docs/plans/20260726b-*`）：
   - R1 技能装配**五选一**（魔法石被动 + 钩锁/怒火/口哨/冰川，`GameState.trainer_skill` 单字段）
   - R2 战场**瞄准指示器** + 持续 buff/debuff 光环 + PC 按住 Q 瞄准
   - R3 钩锁演出重做（4 下×70 码 + 专属铁链 + LoL 式命中闪 + 扎目标 + 易伤）
   - R4/R5 怒火/口哨/**冰川**演出重做
   - R6-A 配置页选的**形象真进战场**；R6-B **三形象真 4 方向 + 走路/扔石头/待机动画**（PixelLab template 动画）
2. **冰川特效整层重做**：专属美术 `glacier-crystals/ground.png`（不复用），从大师**脚下**沿方向铺满 500 码。
3. **battle_aim 抽取还债**：瞄准指示器 ~168 行从主 sim 文件抽到 `scripts/scenes/battle/battle_aim.gd`（主文件 8629→8462，低于原 8476）。顺手修好 `battle_hud` 里指向不存在方法的圆盘回调。
4. **调试场**：加装备 popup + 大面板体 **自适应可视区**（治"够不到"）；手机选龟**笔刷栏抬进安全区**（不撞 home 手势条）+ 横滑提示。
5. **大师立绘脚底贴地**（实测悬空 ~0.29m，改 offset）。
6. **图鉴装备框色**：2 费**蓝→绿**、3 费绿→蓝（原来两色反了）+ 删死代码 `P2EQ_RARITY_COLOR`。**真截图验过**（`CODEX_SHOT` 钩子）。

---

## 3. ★本机铁律 / 踩过的坑（最重要，别重踩）

- **BSOD**：开窗口的**重载 3D** Godot（战斗场景）**必蓝屏/强制关机**（WinDbg 查过=内核 FS 过滤器栈崩于重文件 I/O）。本 session 崩过一次（开窗口截战斗）。
  - **验证一律 headless**：数值/逻辑走 `.gd` 门禁 harness 打印断言；**别开 3D 窗口截图**。
  - **2D 界面**（图鉴/菜单等）**文件 I/O 轻，可开窗口截图**：用 `CODEX_SHOT` 钩子（见 §4）。这轮框色就靠它真截图验的。
  - 详见 memory `project-machine-bsod-during-tests`。
- **GODOT 路径**：`C:\Users\Louis\Desktop\Godot_v4.6.3-stable_win64.exe`（**不在** Program Files；空变量会假绿）。跑测试加 `--audio-driver Dummy` + 小批次。
- **本地 commit 不 push**，除非用户点名要推（他这轮点名要 IPA = 授权了 push）。
- **玩家可感知的改动升版本 4 处**（`project.godot` / CHANGELOG / `export_presets.cfg` short_version+version/name）+ tag + CHANGELOG；`verify_version` 守四处一致。dev 工具改**不升版本**。
- **push → CI**（`.github/workflows/tests.yml` 门禁 + `ios-build.yml` 出包到 Release `ios-latest`）。**每次 push 的新 iOS 构建会覆盖 release、冲坏正在下的 IPA** → 下包挑"无 push"的稳定窗口，下完 `unzip -t` 验完整。
- **arch 预算棘轮** `tools/arch_budget.json`：主文件 `RealtimeBattle3DScene.gd` 冻结在 **8462** 行，往主文件加代码会红→抽出去或（有理由地）抬档。
- **rng 棘轮** `tools/rng_discipline.py`：不许新增裸 `randf/randi`，视觉抖动走 `_juice_rng`、sim 走 `_battle_rng`。
- CI 状态查（无 token 公开 API）：`curl "https://api.github.com/repos/yinzuoliu87-cpu/Turtle_realtime_battle/actions/runs"`。

---

## 4. 验证工具（headless / 轻量截图）

- **headless 逻辑门禁**：写 `tests/verify_*.gd`（+ `.tscn`）自动纳入 `run-tests.sh`；或临时 harness（放项目根 `_tmp_*.gd`+`.tscn`，跑完删）。实例化 `RealtimeBattle3DScene` 即编译全链。
- **新增 class_name 后必 `--import`** 一次（注册全局类缓存），否则 `Xxx.new()` 编译失败。
- **图鉴/2D 界面截图**：`CODEX_SHOT=<秒> SHOT_TAB=equips SHOT_SCROLL=<px> SHOT_OUT=<路径> Godot --audio-driver Dummy res://scenes/Codex.tscn`（**不加 --headless**，轻量 2D 窗口，抓完自退）。钩子在 `CodexScene._codex_selfshot`。
- **战斗 VFX 预览**（谨慎·3D 窗口有 BSOD 风险，本 session 没再用）：`VFXPREVIEW=<eff>`；`SELFSHOT` 自截图；`TRAINER_APPEAR=<id>` 强制形象。
- **Python 审计器**（无 Godot·安全随便跑）：`data_integrity / tri_audit / tooltip_number_audit / docs_authority_lint / workflow_lint / rng_discipline / arch_budget`。

---

## 5. 还开着的（都不紧急、不影响已交付）

- **调试场手机横滑选龟**：改动完成、headless 验过；但它在**重载 3D 场景**里，真机横滑观感**得人在真机（DEVTOOLS 包）点一下确认**——我这边不能开窗口截它（BSOD）。
- **更深的 Inspector 调试旋钮**（改攻/防/星级/直接填数值 / 换布局）：**只有用户点名才做**——测试反馈里报的（笔刷栏/单龟配置/大师可测/折叠）早已实装，这轮只补了"面板不溢出"。
- 见 memory `project-pending-debug-trainer-vfx`（调试场 + 训龟页 + 技能特效的挂起需求全景）。

---

## 6. 关键位置速查

- **事实源**：代码 `scripts/` `autoload/` > 数据 `data/*.json` > 三权威文档（`docs/design/28龟技能设计-权威.md` / `实时版-系统机制权威.md` / `docs/实时版-路线图与待办.md`）。别"按文档改代码"。
- **战斗上帝文件已拆**：`RealtimeBattle3DScene.gd`（核心 sim）+ `scripts/scenes/battle/battle_*.gd`（world_builder/vfx/render/targeting/damage/ballistics/spawn/hud/**aim**/debug_arena，RefCounted + `battle` 注入）。
- **训龟大师**：`scripts/systems/trainer/trainer_system.gd`（技能）、`scripts/scenes/TrainerConfigScene.gd`（配置页）、`assets/sprites/trainer/`（立绘）+ `anim/`（4 方向帧表）。
- **图鉴**：`scripts/scenes/CodexScene.gd` + `scripts/scenes/codex/{list_builder,detail_views}.gd`；`COST_COLOR`（1灰2绿3蓝4紫5金）。
- **本轮方案书**：`docs/plans/20260726b-*`（训龟返工）、`docs/plans/20260727-图鉴装备框色+调试场手机选龟.md`。
- **memory**（每 session 自动加载 `MEMORY.md` 索引）：`project-trainer-master-art` / `project-machine-bsod-during-tests` / `project-pending-debug-trainer-vfx` / `project-god-file-decomposition` 等。

---

## 7. 怎么接着干（给下一个 AI）

1. 先读 `CLAUDE.md`（§1 事实源、§3 地雷、§7 工作方式）+ 本文件 + `MEMORY.md` 里相关条目。
2. 新需求：**先写 `docs/plans/YYYYMMDD-*.md` 方案书**（验收清单一次列全子需求 + 已知风险/未决点）→ 用户拍板未决点 → 实现 → 门禁（headless）→ 版本号 → push → CI 亲自看绿 → 出包。
3. **报绿前先证明检查会 FAIL**（改坏一个值确认门禁会红）；**断根因先写探针打数值**，别推理。
4. 视觉验证：2D 用 `CODEX_SHOT` 真截图；3D 用 headless 断言（**别开 3D 窗口**）。
