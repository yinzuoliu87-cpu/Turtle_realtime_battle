# 096 小木斧九个形态重做

状态：实施中（v0.19.393 = A 批 + B 批斧头本体；v0.19.394 = D 余烬处决激光；v0.19.395 = C 炽天使回旋镖；v0.19.396 = E 亡灵·全息；F 九段重录 未做）

## 需求原文

用户 2026-09-15 看完九段形态视频后（逐字）：

1. 「96是因为有多种形态，所以每一个都要分开看」
2. 「首先3/9这个竖劈大小要大一倍」
3. 「然后5/9这个梯形本来是4秒内每0.5秒加100码是吧，现在改为每0.25加30码，并且梯形的宽度减少20%，包括特效和实际伤害范围」
4. 「然后是提醒内的6边形需要大小大一倍，你自己看看怎么调，以及为什么只有中间一块发光了？你看看图片」
5. 「然后6/9亡灵斧你这是在敷衍我啊，完全没按标准来」
6. 「7/9的回旋镖同样是在敷衍我啊，回旋镖是什么？以及特效和实际伤害范围完全不一样啊，也没有命中特效」
7. 「8/9也是，完全没达标」
8. 「9/9处决没余烬的专属特效啊，还是这个特效不明显？我需要你参考lol比较新版本的无限火力里击杀敌人时一道激光从天击中敌人的那种处决」
9. 「我觉得斧头召唤物这个形象做的太差了，你能不能用blender搞一把3D的就那种在空中的斧头，应该要九把吧，因为9种形态，然后他们移动，idel，攻击，有什么被动的话你就让斧头挥动起来，做多种攻击运动，然后也做好挥舞特效，就是像刀光那样吧，斩击特效」
10. 「全部做好把9种形态视频再发我」

### 口径清单（动手前从头读一遍）

- 否定：**不许敷衍** —— 程序细环、白球、BoxMesh 方块、LINE_STRIP 线条这一类代码现算图形，在 6/9、7/9、8/9、9/9 上已被点名否掉。
- 否定：**特效范围不许和伤害范围不一样**（7/9 点名）；5/9 改宽度要**特效与伤害一起改**。
- 数值只改用户点名的：梯形增长（每 0.25 秒 +30 码）与梯形宽度（−20%）。其余数值不动。
- 3/9 竖劈「大一倍」、5/9 六边形「大一倍」都是**尺寸 ×2**。
- 斧头本体：Blender 3D、悬空、**九把**（九个形态各一把）；动作至少 移动 / 待机 / 攻击；有被动的形态要**挥动起来、多种攻击动作**；挥舞配**刀光斩击特效**。
- 交付：九个形态全部做完后**重录九段视频**再发。

## 调查

| 项 | 现状（代码事实） | 位置 |
|---|---|---|
| 3/9 竖劈尺寸 | `cleave()` 贴图边长 = `atk_range × 1.1` | `scripts/scenes/battle/axe_passive_vfx.gd` `cleave` |
| 5/9 梯形增长 | `CHARGE_STEP 0.5` / `CHARGE_H_PER_STEP 100` / `CHARGE_TIME 4` ⇒ 满蓄 800 码；`charge_height` 按格向下取整 | `scripts/gamedata/axe_evolution.gd` |
| 5/9 梯形宽度 | `TRAPEZOID_NEAR_W 300` / `TRAPEZOID_FAR_W 900`；`in_trapezoid` 按满蓄高插值半宽；演出 `_field_mesh` / `half_w_at` 同一套常量 | 同上 + `axe_passive_vfx.gd` |
| 5/9 六边形 | `_hex_mesh` 外接圆半径 `R = 16` 码（按塞恩 Q「格子/角色 ≈ 0.67」量的） | `axe_passive_vfx.gd` `_hex_mesh` |
| 5/9「只有中间一块发光」 | 砸下 `charge_clear(flash)` → `hex_burst` → `_hex_mesh(h, full=false)`：**只铺砸击点周围 max(60, h×0.11) 码内的格子**。2026-09-03 按塞恩 Q「命中点周围局部闪现」刻意做的；蓄力期 `Fill` 才是满铺 | `axe_passive_vfx.gd` `_hex_mesh` / `hex_burst` |
| 5/9 文案 | effectDesc2 用 `{C:AxeEvolution.CHARGE_STEP}` / `{C:AxeEvolution.CHARGE_H_PER_STEP}` 占位符，改常量文案自动跟 | `data/phase2-equipment.json` |
| 6/9 亡灵 | 环 = `_ring_mesh` 程序细环（ADD）；吸取 = `ImmediateMesh` LINE_STRIP 绿线；重生 = 6 根 BoxMesh 聚拢 | `scripts/scenes/battle/axe_final_vfx.gd` |
| 7/9 炽天使 | 判定 `seraph_boomerang_settle`：身前**半宽 300 码、不限长**的直线带，出手当帧全部结算；演出 `seraph_boomerang`：0.55×0.10×0.16 米的 BoxMesh 直线飞过 0.45 秒（不回来）；**没有命中特效** | `scripts/systems/equip/axe_final_forms.gd` / `axe_final_vfx.gd` |
| 8/9 全息 | 法阵 = 两个程序环 + BoxMesh 斧；脉冲 = 改透明度；普攻给友军盾无演出 | `axe_final_vfx.gd` |
| 9/9 余烬处决 | `ember_execute` = 0.34×2.6×0.34 米红色 BoxMesh 竖条，0.35 秒 | `axe_final_vfx.gd` |
| 斧头本体 | 九个形态共用一套 PixelLab 2D 帧表 `vfx/eq-axe-{idle,walk,attack,cast,charge,cleave,sweep,smash,slam,throw,plant,execute}.png`（80px 画布，`ART_FACES_RIGHT` 登记朝右）；形态只换图标 `equip/axe-*.png` | `RealtimeBattle3DScene.gd` 帧表登记 / `axe_system.play_action` |
| 参考 | 9/9 无限火力处决激光：已派只读研究代理下视频逐帧（`scratchpad/ref_urf_execute/`） | — |

## 出入（与文案 / 原设想的差异，我定的）

- **回旋镖**：改成「飞出去 → 回来」。判定仍是**半宽 300 码的带**（文案数值不动），但：
  ① 带长不再无限：飞到身前被打中的最远那个敌人再折返（等价于现在「身前全吃」的集合，不改谁挨打）；
  ② 伤害在**镖真正经过那个敌人时**结算（现在是出手当帧全部结算 = 演出与伤害脱节那一类）；
  ③ 每把镖每个敌人只吃一次（去程或回程先碰到的那次），不因为回来再打一遍 —— 否则伤害翻倍 = 擅自改数值；
  ④ 镖的视觉尺寸 = 判定尺寸：旋转的镖身外径就是 300 码半宽，不是一根小方块。
- **5/9 砸下发光**：改成**整个梯形判定区的六边形全部亮起**（亮到哪打到哪），不再只亮砸击点周围 —— 用户问的就是这个。
- 文案「沿直线飞过」要改成回旋的说法（数值占位符不变）。
- **斧头帧格 80 → 112**：80 格时砸 / 劈 / 处决 / 插地挥到最低点刃被切掉。画布放大后引擎通用规则「身高 ÷ 帧高」会让斧头只剩 0.7 m，所以所有动作统一用 `AxeArt.TEXEL_M`（待机斧高 45 像素 = 龟身高中位 1.40 m），`battle_vfx._play_action` 与 `AxeSystem.play_action` 各加一个统一尺寸口。
- **旧人形素材 12 张删除**（`eq-axe-*.png`），`tools/vfx_discipline_debt.json` 同步销账。

## 方案

### 批次与顺序

| 批 | 内容 | 版本 |
|---|---|---|
| A | 3/9 竖劈 ×2；5/9 增长/宽度/六边形 ×2/砸下满铺亮 | v0.19.393 |
| B | 斧头本体：Blender 九把悬空 3D 斧 + 动作帧表 + 刀光 | v0.19.393 |
| C | 7/9 回旋镖（判定时序 + 往返 + 命中特效 + 烘焙镖身） | v0.19.395 |
| D | 9/9 余烬处决激光（照参考逐帧） | v0.19.394 |
| E | 6/9 亡灵、8/9 全息（分段表 + 烘焙素材） | v0.19.396 |
| F | 九段重录，逐帧看完再发 | |

（每批各自：方案分段表 → 实现 → 门禁 + 变异 → 全套门禁 → 版本号五处 + tag + CHANGELOG + 路线图 → commit 不 push。）

### B 斧头本体（草案，建模前再细化）

- 一个参数化 Blender 脚本：斧柄 + 斧刃几何按形态换材质与刃形（木 / 石 / 铁 / 金 / 钻石 / 亡灵 / 炽天使 / 全息 / 余烬）。
- 动作：待机（悬浮上下浮动 + 轻摆）、移动（前倾 + 拖尾）、普攻（前劈）、竖劈（高举下劈）、横扫（水平 180° 挥）、强化砸（重砸）、蓄力（高举抖动）、猛砸（砸地）、甩镖（回旋掷出）、插地（全息）、处决（余烬）、施法（主动举起发光）。
- 刀光：挥动轨迹单独烘焙成刀光帧（与斧头同一套动作时间轴），不用程序三角带。
- 侧视单方向渲染，朝左靠翻转；像素化走 `tools/pixelize_sheet.py`，每形态一套调色板。
- 每张新素材一份逐帧研究（`docs/studies/`，「共 N 帧」）。

### C 炽天使回旋镖

用户：「7/9的回旋镖同样是在敷衍我啊，回旋镖是什么？以及特效和实际伤害范围完全不一样啊，也没有命中特效」

判定口径（数值一个不动）：命中名单 = 出手时身前（rel·dir ≥ 0）的可选中敌人；飞出距离 = max(`SERAPH_BOOM_MIN_OUT` 750, 身前半宽 300 码带里最远那个的纵深)；每个模拟步用「上一步中心 → 这一步中心」的折线扫，敌人中心到线段 ≤ `SERAPH_BOOM_R` 即命中；每把每敌一次；伤害 = 出手时 1×ATK 魔法（先过魔抗）+ 8 层灼烧。新增两个**演出时序常量**（不是平衡数值）：`SERAPH_BOOM_SPEED` 1600 码/秒、`SERAPH_BOOM_MIN_OUT` 750 码。

| 段 | 说什么事 | 多长 | 亮暗怎么变 | 用什么实现 |
|---|---|---|---|---|
| 出手 | 斧头甩出一把镖（每 0.4 秒一把，4 秒 10 把），朝最近敌人 | 瞬间；斧头本体播 `axe_throw` 帧表 | 镖在出手点出现即满亮 | `AxeFinalForms.seraph_boomerang_launch` 只登记在途、**不结算**；`AxeSeraphVfx.boom_spawn` 贴地 Sprite3D（`ground_basis`，离地 0.3 m，alpha 裁切 + 深度测试 ⇒ 圈里的龟腿以上照常露出） |
| 飞出 | 镖贴地旋转着沿直线飞出去；**转起来扫出的那一圈就是打到人的那一圈** | 飞出距离 ÷ 1600（750 码 ≈ 0.47 秒） | 恒满亮不淡；翼尖后三段流光 光晕金 → 浅金 → 橙 由粗到细，表达旋转拖尾（像素画没有半透明，只能靠色阶 + 变细） | `tick_boomerangs`（挂 `EquipSystem.tick_global` → `AxeSystem.tick_global`）每步推进，`boom_step` 搬节点到当前中心、按飞行时间 20 帧/秒切 8 帧；素材 `eq096-seraph-boomerang.png`（Blender 俯视烘焙，正交宽度 = 2R ⇒ 帧宽 × pixel_size = 600 码） |
| 折返 | 到最远处立刻掉头 | 同一步内折返 | 同上 | `_boom_advance` 把折返点放进这一步的折线顶点，扫线不抄近路 |
| 飞回 | 朝斧头【当前】位置飞回，到了就收（斧头死了飞回出手点；斧头离场整条作废） | ≈ 同飞出 | 同上；到达即收掉（被接住，不淡出） | `_boom_advance` 每步现读斧头位置；`boom_free`；源头不在 `battle._units` 里 ⇒ 作废（照 `eq_bow_batch._alive_here`） |
| 命中火花 | 镖经过某个敌人时它身上一闪（每把每敌一次） | 0.4 秒（6 帧 × 15 帧/秒） | 前 70% 满亮、后 30% 淡（`hold_fade`） | `_boom_sweep` 结算之后 `hit_spark` 公告板，跟着被命中者走；由同一条战斗时钟推进（不用 tween，时停天然冻住）；素材 `eq096-seraph-boom-hit.png`（Blender 侧视烘焙：四角星芯 + 八道放射 → 羽毛飘开） |

### D 余烬处决：天降轨道激光

参考：LoL 无限火力「轨道激光」终结特效，1080p60 实拍逐帧 135 帧（研究在草稿区 `ref_urf_execute/`）。烘焙 `tools/blender_ember_laser.py` → `tools/pixelize_sheet.py`（新加 `--cell-w` 竖长格、`orbital` 调色板）；引擎 `scripts/scenes/battle/axe_ember_vfx.gd`。

| 段 | 帧（20fps） | 说什么事 | 亮暗 | 实现 |
|---|---|---|---|---|
| 击杀闪光 | 0 | 目标被处决的那一刻 | 全段最亮：白芯青边放射 | 光柱层 |
| 上行信号 | 1–2 | 天上锁定了这个目标 | 青色细线，中亮；脚下小红盘 | 光柱层 + 地光层 |
| 蓄力细光柱 | 3–4 | 激光要下来了 | 细白芯 + 红晕，火舌沿柱下移 | 光柱层 |
| 展开 | 5–6 | 光柱张到半宽 → 全宽，第一次黄色脉冲在柱中 | 迅速变亮 | 光柱层 |
| 砸到 + 持续灼烧 | 7–19 | 5 次脉冲砸到目标（帧 7 / 12 / 15 / 17 / 19，间隔递减）；地面炸开橙光、穹顶罩住、碎石飞起回落 | 高亮；脉冲到底那帧地光鼓起 | 光柱 + 穹顶（整层 45% 透明）+ 地光 |
| 收窄 | 20–24 | 能量衰减 | 变暗偏红，穹顶缩没，地光缩小 | 三层 |
| 细线 | 25–28 | 余光 | 暗红细线 | 光柱层 |
| 断开消散 | 29–31 | 结束 | 下半段先没，上段擦除 | 光柱层 |

- 结算不动：处决伤害与 +150 龟能仍在 `AxeFinalForms.ember_on_hit` 同步结算，同一个调用里建激光；帧 0 击杀闪光 = 死亡那一刻。
- 三层同一个 tween 推帧（`AxeEmberVfx.apply_frame`），不会错帧；tween 只管画，lambda 捕获实例 id。
- 尺寸与龟立绘同一像素尺寸 0.0425 m：光柱帧高 26.6 m、穹顶帧高 8.5 m，两层帧底都在地下 0.6 m（底边落地由门禁按真像素行量）；地光格 3.4 m、脉冲帧可见直径 2.89 m。
- 光柱为什么 26.6 m：第一版 8.5 m 帧、柱顶 7.9 m，门禁用战斗镜头投影量出柱顶在屏幕 y=407/1280（脚在 696）—— 平切的柱顶落在屏幕中上部，读成悬空柱子。现在门禁要求柱顶投影在屏幕上沿之外。
- 地光第一版脉冲帧包围盒满格 (0,0)-(127,127)：光芒撞到方形面片的边被切成方角；材质加圆形收边。
- 台子录像（18.9 秒起）又看到两件事：① 地光直径 4.8 m，一炸盖住左右两只龟，而且不测深度、整圈画在邻居身上 ⇒ 缩到 2.9 m（台子相邻两龟中心距约 2.9 m）+ 地光层开深度测试；② 假人锁血时每一拳都劈一道激光 ⇒ 激光只在目标真的死掉时放（与 +150 龟能同一个判断）。
- 与参考的出入：参考 2.13 秒 7 次脉冲，这里 1.6 秒 5 次（只压缩持续灼烧段，间隔仍递减）；穹顶是 13 帧不变的静态罩，参考里有流纹 —— 已知不完美，等实拍看。

### E 亡灵之斧 / 全息斧 演出（2026-09-15 · 并行代理 B）

**口径**（动手前从头读）：
- 用户原话「6/9亡灵斧你这是在敷衍我啊，完全没按标准来」「8/9也是，完全没达标」。
- 他同一段对别的形态用的尺子：**演出读得出机制 / 范围 = 判定 / 每个效果有自己的画面 / 命中·生效有反馈**。
- 否定（一个都不留）：程序细环 `_ring_mesh`、`ImmediateMesh` 线、`BoxMesh` 方块、「改透明度」当脉冲。
- 文案就是规格（p2eq_096 effectDesc3）：亡灵 = 300 码环内每秒 1% 最大生命魔法伤害 + 每个环内敌人回 0.3% + 死亡 2.5 秒后 40% 复活；全息 = 普攻给血量最低友军 60 盾 + 5 龟能 / 主动插地 4 秒（30% 减伤）/ 600 码内友军每 0.5 秒 +100 血 +5 龟能 +30% 攻速。
- 数值一个不动；结算仍在 `axe_final_forms.gd` 的同步函数里，演出在**同一个模拟步**建出来。

**分段表**（素材一律 Blender 几何 + 发射着色器、俯视/侧视正交渲 → `tools/pixelize_sheet.py` 锁调色板 `undead` / `holo`）：

| 段 | 说什么事 | 多长 | 亮暗怎么变 | 实现 |
|---|---|---|---|---|
| U1 亡灵领域 | 斧头脚下 300 码是一片会吸血的死地，**边缘就是判定圆** | 斧头活着就一直在；8 帧循环 8fps | 恒亮不闪：边缘一圈冥火最亮（边界要看得清），骨纹符文环慢转，里面暗、稀疏的墓雾往里卷 | `eq096-undead-field.png` 贴地，外径 = 2×`UNDEAD_RING_R`；挂 `battle._follow_vfx`（跟斧头、按游戏钟切帧），斧头死亡当步释放、复活后重建 |
| U2a 领域内收 | 这一跳「领域在吸」 | 0.5 秒 8 帧 | 前 6 帧满亮，一圈亡魂从边缘往中心收；末 2 帧变暗 | `eq096-undead-pulse.png` 贴地同尺寸；`undead_ring_tick` 结算那一步建 |
| U2b 魂被抽走 | **每个被扣血的敌人**身上抽出一缕魂飞回斧头（几个敌人几道） | 0.45 秒 | 先 3 帧从身上钻出，后 5 帧拖尾飞行；前 70% 满亮 | `eq096-undead-soul.png` 公告板，按两点弧线飞；同一步建 |
| U2c 吸到了 | 斧头回血 | 0.6 秒 8 帧 | 魂点汇入 → 第 3 帧绿白一亮 → 往上冒回血魂火，末 2 帧暗 | `eq096-undead-drink.png` 公告板跟斧头；只有真回了血（环内有人）才建 |
| U3a 崩散 | 斧头死亡那刻碎成亡魂（**不播死亡动画**） | 0.6 秒 8 帧 | 第 0 帧裂光最亮 → 碎骨下落、魂火外散 → 沉到地面 | `eq096-undead-shatter.png` 公告板，死亡位置固定（`_anim_fx`）；`undead_on_death` 那一步建 |
| U3b 倒计时 | 「它会回来」+ 还剩多久：一圈 8 支魂烛逐支点亮，骨灰旋涡收紧 | **= 死亡到复活的实际间隔**（`_axe_revive_at − 死亡时刻` = 2.5 秒） | 16 帧按进度切：越接近复活越亮 | `eq096-undead-wake.png` 贴地；帧率 = 16 ÷ 间隔，走游戏钟；复活结算那一步释放 |
| U3c 复活 | 爆出、斧头重新立起 | 0.6 秒 8 帧 | 地面一闪起柱 → 魂火盘旋上升 → 第 3 帧斧头轮廓一亮（本体同一步重新显示）→ 散 | `eq096-undead-rise.png` 公告板跟斧头；`undead_tick_revive` 成功那一步建 |
| H1a 数据流 | 从斧头到**血量最低的友军**一串全息数据块 | 0.3 秒 | 数据块匀速飞，不淡 | `eq096-holo-bit.png` 公告板，数量按两点距离分段；终点 = 那个友军；`holo_on_hit` 那一步建 |
| H1b 护盾展开 + 龟能入账 | 友军身上一圈六角全息盾从脚下扫描立起，头顶两枚青色箭头上升（+5 龟能） | 0.5 秒 8 帧 | 扫描线上升 → 第 4 帧整罩最亮 → 收成轮廓，箭头升到头顶变暗 | `eq096-holo-shield.png` 公告板跟友军；同一步建 |
| H2a 插地 + 展开 | 斧头插进地里，法阵从插点展开到 600 码 | 0.5 秒 8 帧 | 圈从中心往外推，边到哪亮到哪 | 斧头播 `axe_plant` 循环帧（**4 秒内一直是插地**，原来 0.75 秒就回待机）；地面 `eq096-holo-deploy.png`，外径 = 2×`HOLO_AURA_R` |
| H2b 法阵循环 | 这里是治疗法阵、范围到哪 | 0.5 → 3.6 秒；8 帧循环 8fps | 恒亮：外圈双线 + 刻度（判定边）最亮，六角格很淡 | `eq096-holo-field.png` 贴地同尺寸，每模拟步按插地已过时间切帧 |
| H2c 收拢 | 法阵结束 | 最后 0.4 秒（**收拢结束 = `_holo_until` 到期那一步**） | 展开帧倒放 | `eq096-holo-deploy.png` 倒序；到期那一步释放 |
| H2d 减伤护罩 | 插地期间 30% 减伤 | 4 秒；8 帧循环 | 六角格护罩恒亮，扫描线上下走 | `eq096-holo-guard.png` 公告板罩住斧头；到期释放 |
| H3a 脉冲 | 每 0.5 秒一跳：一道波从插点推到 600 码边 | 0.5 秒 8 帧（与跳同长，下一跳正好接上） | 前 6 帧波环满亮外推，第 6 帧碰边一亮，第 7 帧暗 | `eq096-holo-pulse.png` 贴地同尺寸；`holo_aura_tick` 那一步从第 0 帧建 |
| H3b 治疗/龟能反馈 | **范围内每个被治疗的友军**冒绿色十字（+100 血）与青色箭头（+5 龟能） | 0.45 秒 8 帧 | 从脚下升到头顶，末 2 帧暗 | `eq096-holo-boost.png` 公告板跟友军；同一步建，范围外不建 |
| H3c 加速标记 | +30% 攻速：友军脚下一圈青色箭头快速转圈 | 攻速 buff 在就在（`haste_until`），离开范围 buff 到期即收 | 恒亮循环 | `eq096-holo-haste.png` 贴地跟友军；`_follow_vfx` 的 `until_key = haste_until` |

**接线（全部在结算的同一个模拟步）**：`undead_ring_tick`（U2）、`undead_on_death`（U1 释放 + U3a + U3b）、`undead_tick_revive`（U3b 释放 + U3c）、`AxeSystem._tick_undead_ring`（U1 保证存在）、`holo_on_hit`（H1）、`begin_active` 全息分支（H2 起）、`tick_active` 全息分支（H2 切帧 / 收拢 / 到期释放 + H3）。新文件 `scripts/scenes/battle/axe_undead_vfx.gd` / `axe_holo_vfx.gd`；`axe_final_vfx.gd` 删 `undead_ring` / `undead_leech_line` / `undead_revive` / `holo_field` / `holo_pulse`。

**E 的出入（我定的，待主会话确认）**：
- **插地期间 `no_move`**（备份/还原，同被动 6 蓄力的写法）。原因：结算的圆心是 `ax.pos`，斧头若在插地的 4 秒里走动，「法阵钉在插地点」与「范围 = 判定」只能二选一；文案是「插入地下」，被动 6 头注也把全息插地列为「同一类不动的招」。**普攻不禁**（「每次普攻给盾」照常）。演出圆心每步读 `ax.pos`，所以被击退时仍与判定一致。**这是行为改动（不是数值）**，主会话不认可就删 `begin_active` 里那两行 + `tick_active` 的还原，演出本身不依赖它。
- **插地帧 4 秒内保持**：`play_action(loop=true)` 在 `_advance_anim` 里并不循环（有 `anim_action` 就播完回待机；插地表 8 帧 10.67fps = 0.75 秒）。变异 M11 实测：去掉续帧后，插地 0.9~3.8 秒逐步采样 174 次、0 次是插地帧。现在插地期间每步检查、掉了就续上（正在播的普攻招式不打断），到期切回待机。

**E 的已知不完美**：
- 台子 `p2eq_096_undead` 敌人不还手 ⇒ 看不到崩散 / 倒计时 / 复活柱的实拍，只有门禁逐模拟步量与逐帧研究。
- zoom 2.2 下亡灵领域的冥火舌与内收的魂很大、画面偏满；全息法阵在 zoom 1.0 下六角格很满（常驻透明度 0.80），单位靠渲染顺序压在上面。都没有给用户看过。
- 数据块 12 格太小，顶边 1~2 颗像素被重索引到治疗亮绿档。
- 魂与数据块的飞行是 0.3~0.45 秒的纯观感 tween（`_reg_tween`，时停冻得住）；终点取建出那一刻的位置，飞行途中目标移动不追。
- `AxeFinalVfx` 里 `ring_breath` / `aura_pulse` / `revive_gather` 三个纯曲线函数的产品调用者随被删的 5 个函数一起没了，只剩 `verify_axe_finals` ⑥ 在调（没删，任务书只让删那 5 个）。

## 验收清单

- [x] 3/9 竖劈贴图尺寸 = 原来 ×2 —— `tests/verify_axe_vfx.gd` ⑤x（量真实节点 154 码 = 攻击距离 70 × 2.2）；变异 A1 红
- [x] 5/9 梯形每 0.25 秒 +30 码、4 秒满蓄 480 码 —— `tests/verify_axe_passives.gd`「0.25 秒一格」阶梯表；变异 A5 红
- [x] 5/9 梯形近边 / 远边宽度 ×0.8（240 / 720）—— `tests/verify_axe_passives.gd`「宽度减 20% 进了判定」+ `tests/verify_axe_vfx.gd` ① 每格角点逐码对齐；变异 A4 红
- [x] 5/9 六边形外接圆半径 ×2 —— `tests/verify_axe_vfx.gd` ⑤y（从真网格反推 32.0 码）；变异 A2 红
- [x] 5/9 砸下时整个梯形内的六边形都亮 —— `tests/verify_axe_vfx.gd` ⑤z（亮 546 / 满铺 546 个三角形）；变异 A3 红（改回局部时亮 30 / 546）
- [x] 5/9 文案渲染出「每 0.25 秒增高 30 码」—— `tools/text_golden.py` / `tools/text_value_golden.py` 快照（只动小木斧 effectDesc2）
- [x] 炽天使回旋镖飞出去再回来；镖身视觉外径 = 判定半宽；伤害在镖经过时结算；每把每敌一次；有命中特效 —— `tests/verify_axe_finals.gd` ③（出手当帧 0 伤害、中心到 400 码那一步才掉血、先远离 27 步再回来 29 步回到斧头、1300 码外也打到、横向 360 码 / 身后 200 码不掉、魔抗 17 vs 100、只吃一次 + 8 层、节点外径 600 码且每步在中心、素材 8 帧外沿 97% 半格、3 次命中 3 个火花、离场作废收节点、斧头挪动 / 死亡的回程终点）+ ⑦（真入口 4 秒甩 10 把、甩出那一刻 0 伤害、经 `EquipSystem.tick_global` 推步后掉血 1000、在途清空）、`tests/verify_axe_batch10.gd` E10（量镖身节点真实位置飞到 1293 / 747 码）、`tools/frame_study_audit.py`（`docs/studies/20260915s-096炽天使回旋镖逐帧.md` 14 帧）；变异 M1~M16（当帧结算 / 不回来 / 不去重 / 半宽 ×1.5 / 不判身前 / 离场不收节点 / 离场不作废 / 外径 = R / 不补远 / 不建火花 / 不过魔抗 / 节点不跟 / 全局步没接 / 不加灼烧 / 回程不跟斧头 / 死了不回出手点 / 不贴地）各红在自己那条
- [x] 余烬处决 = 从天而降激光（照参考逐帧；三层素材逐帧研究 `docs/studies/20260915d-096余烬处决激光-{光柱,穹顶,地光}逐帧.md`）—— `tests/verify_axe_ember_laser.gd` 39 条（真入口 `AxeSystem.on_hit` 处决同一调用里建出激光、被护盾吸住没死不放激光、落在被处决者脚下、光柱/穹顶底边按真像素行落地、光柱顶端投影出战斗镜头上沿、穹顶半透明、地光贴地 / 测深度 / 不贴格边 / 场上直径 ≤ 3.0 m、三层同帧、32 帧不丢、播完收掉）；变异 M1~M15 各红在自己那条（不建激光 / 全轴公告板 / 帧底不埋地 / 穹顶不透明 ×2 / 推帧漏层 / 不收节点 / 线性过滤 / 丢末帧 / 根节点离地 / 光柱帧高回 8.5 m / 地光不贴地 / 地光不测深度 / 没死也放激光 / 地光节点放大 1.7 倍）；台子 `p2eq_096_ember` 配 `mortal_enemies`（录像锁血时处决打不死）
- [x] 亡灵之斧：分段表（方案「E」U1~U3）+ 7 张 Blender 烘焙帧表 `eq096-undead-*.png`（`tools/blender_axe_undead.py`，逐帧研究 `docs/studies/20260915h-096亡灵之斧演出逐帧.md`），程序环 / 线 / 方块已删 —— `tests/verify_axe_undead_holo_vfx.gd` ①②③（领域贴地、外径 600 码、跟随、死亡当步释放；一跳 N 个敌人 N 缕魂且同一步；崩散 + 倒计时活到复活上一步、复活当步收掉并起柱）；变异 M1 外径 / M2 不跟随 / M3 不释放 / M4 只建 1 缕 / M5 晚一帧 / M6 倒计时按 0.9 秒 / M7 不崩散 各红
- [x] 全息斧：分段表（方案「E」H1~H3）+ 8 张 Blender 烘焙帧表 `eq096-holo-*.png`（`tools/blender_axe_holo.py`，逐帧研究 `docs/studies/20260915h-096全息斧演出逐帧.md`），程序环 / 方块 / 透明度脉冲已删 —— `tests/verify_axe_undead_holo_vfx.gd` ④⑤⑥⑦（数据流终点 = 最低血友军；法阵外径 1200 码、钉在插地点、展开 → 循环 → 倒放收拢到期前一步落第 0 帧、到期当步释放、插地帧 4 秒一直在播、no_move；每 0.5 秒一跳同一步从第 0 帧起播；反馈与加速圈只在吃到治疗的人身上；全部 NEAREST）+ `tests/verify_axe_batch10.gd` 宿主先释放 0 条 lambda 报错；变异 M8 指错友军 / M9 外径 / M10 到期不释放 / M11 不续插地帧 / M12 不禁移动 / M13 脉冲晚一帧 / M14 反馈发全体 / M15 LINEAR / M16 收拢正放 / M17 lambda 捕获节点 各红
- [x] 斧头本体：九把 Blender 3D 悬空斧 × 12 个动作 + 刀光 —— `tests/verify_eq_body_sprites.gd`（108 张：规格 / 不贴边 / 单次动作回起手 / 待机乒乓朝右）、`tests/verify_axe_anim_runtime.gd` ⑥（按形态换表、普攻与招式同一像素尺寸）、`tests/verify_summon_art.gd`（朝向 / 身高 1.40 m）、`tools/frame_study_audit.py`（九份逐帧研究）；变异 B1~B5 各红
- [ ] ⏳ 九把斧进台子实拍、逐帧看（F 批一起录）
- [ ] ⏳ 九段视频重录、逐帧看完、发给用户

## 已知风险与未决点

- **斧头本体还没进台子实拍**：门禁量的是素材与节点，九把在真实战斗镜头下的大小、悬空高度、刀光亮度要 F 批录像逐帧看。
- **横扫第 5 帧**（跟随）斧身几乎正对镜头只剩细条；**施法第 3 帧**刃侧对镜头只剩一条竖线（自转中）。
- **兜底帧表手抄了一份帧率**：`ActionElite.TABLE` 与主文件两张表里斧头那几行指向木斧新表，fps 与 `AxeArt.FPS` 同值，改一边要改另一边。
- **工时大**：九把 3D 斧 × 十来个动作 ≈ 百张帧表，加四个形态的特效重做与参考研究，是多天量级。
- **回旋镖时序改动**：伤害从「出手当帧」改成「镖经过时」，会让远处敌人晚几百毫秒挨打（数值不变）。回程要不要跟随斧头当前位置，实现时定。
- **5/9 增长规则改动**：满蓄从 800 码降到 480 码（用户点名），`verify_axe_vfx` / `verify_axe_passives` 里按旧常量写死的断言要跟着改。
- **9/9 参考**：已下到视频并逐帧研究（135 帧）。激光实拍还没录：五次脉冲是三档离散高度、碎石 2~4 像素、都要实拍看。
- **余烬处决不穿护盾 / 免死锁血（现状，没改）**：处决伤害走 `_apply_damage(tru)`，护盾照吸、`deathfloor_until` 照挡 ⇒ 带厚盾的目标到了处决线也不会死，之后每一拳再判一次（现在不放激光、不给龟能）。要不要改成无视护盾是玩法问题，没动。
- **余烬种子与余烬之光仍是程序细环**（`AxeFinalVfx.ember_seed` / `ember_light`）：用户这轮点名的是处决，这两个没动，同一类，可能被否。
- **六边形 ×2 与满铺亮**：满铺在满蓄梯形上格子数会翻倍多，注意单网格顶点量。
- 064 浮囊（上一轮）贴在脚边、后半圈没被身体挡住，未修（没排进 A 批），排在 096 之后与 064 触发 / 破裂两段一起做。

## 决策记录

- 2026-09-15 我定：回旋镖「飞到最远命中者再折返、经过时结算、每把每敌一次、镖身外径 = 判定半宽」（见出入）。
- 2026-09-15 我定：批次顺序 A → B → C → D → E → F。
- 2026-09-15 我定：全息斧插地期间 `no_move`（代理 B 提出、主会话确认）—— 文案是「插地 4 秒」，插在地里本就不走；不禁的话「法阵钉在插地点」与「范围 = 判定」只能二选一。普攻不禁。
