# 大轮赛制 v2（周赛制）—— 方案书

日期：2026-09-16　|　状态：**草稿**

> 用户 2026-09-16 提出（原稿 2026-09-13 写在桌面）。**本篇只做调查、整理与意见，一行代码没动。**
> 用户原稿逐字存档：[docs/design/斗龟场大轮赛制方案v2-用户原稿.md](../design/斗龟场大轮赛制方案v2-用户原稿.md)（与桌面文件逐字节相同）。
> 相关旧文：[20260817-上线与后端](20260817-上线与后端.md)（草稿，U1~U8）、
> [20260820-阵容上传后端](20260820-阵容上传后端.md)（已作废：用户 2026-09-04「别管后端啊」）、
> [V2模式策划-异步PvP生命赛季](../design/V2模式策划-异步PvP生命赛季.md)（2026-06 旧设计，数值已过时）。

## 1. 需求原文

用户 2026-09-16：

> 「继续，把仓库搞好，准备好弄下一个方案，方案有个v2在我桌面上你也可以先看看然后整理方案书」
> 「提意见」

原稿要点（逐字摘录，全文见存档）：

> 适用范围:大轮内战斗制度重构。单场对局内部规则(上路→下路→终极战场、蛋机制、10 秒/无限破蛋窗口、40 秒决胜状态、装备层数三战场继承)一律不变;本方案只改大轮结构、匹配方式、赛程、经济供给与观赛。
>
> 核心公平原则:**任何一场对局的双方,经历过的经济供给(结算次数、货架刷新、经验)必须相同;允许存在的差距只能来自战绩本身。** 实现手段 = 同标签匹配 + 配额补发。
>
> 一大轮 = 一个自然周:周一休赛期 / 周二~周五积分赛(8 命 + 24 场配额) / 周六闯关赛(4 胜晋级 / 3 负出局,严格同战绩匹配) / 周日决赛日(上午 32 人分桶赛,晚上冠军签表)
>
> 确定性模拟是全部前提:同种子 + 同双方阵容摆位 ⇒ 必然同结果。……现有 vanilla JS 代码需先行扫描改造,此项排期在赛制上线之前。
>
> 结算权在服务端:结算一次、存档一份(种子 + 双方阵容包 + 结果摘要);客户端只做重放渲染。

## 2. 调查结论（现状，以代码为准）

### 2.1 大轮 / 命 / 经济（现在是本机单人）

| 项 | 现状 | 证据 |
|---|---|---|
| 大轮长度 | 5 天，**从这台设备第一次开局起算**（每台设备各自滚动，不是自然周） | `scripts/gamedata/phase2_config.gd:14`；`autoload/GameState.gd:1335-1347` |
| 命 | 8，只有输才掉命 | `GameState.gd:582`、`:1354-1356`；`RealtimeBattle3DScene.gd:7547-7548` |
| 0 命 | 主菜单锁「开始战斗」与商店，只能「设置→重置存档」重开（用户 2026-07-24「淘汰锁定」）；结算里的表演赛分支因此走不到 | `scripts/scenes/MainMenuScene.gd:212`、`:235`、`:716-718`；`RealtimeBattle3DScene.gd:7543-7545` |
| 场次配额 | 没有 | 搜 quota / max_battles / 场次上限 无结果 |
| 每场深海币 | `8 + 余命 + 2×失命 + 胜 6` —— **与 v2 公式一致** | `RealtimeBattle3DScene.gd:7549-7550` |
| 其它币来源 | 奇械羁绊铸币、035 装备、糖果罐大奖、卖装备 | `scripts/systems/equip/gadget_synergy_system.gd:34`；`scripts/systems/equip/equip_tick_system.gd:336-339`；`GameState.gd:1375-1381`；`scripts/scenes/inventory/equip_ops.gd:128-132` |
| 经验 / 容量 | 每场 +2；升级表照抄云顶；全队装备上限 `(等级−1)×2`、单只 ≤3；4 币买 4 经验 —— **与 v2 一致** | `RealtimeBattle3DScene.gd:7552`；`phase2_config.gd:26-32`、`:51-56` |
| 商店 | 打过第一场才解锁；打完新的一场自动刷新；手动刷新 2 币；10 格；三同款同星自动升星 | `scripts/scenes/ShopScene.gd:58-60`、`:119-122`、`:38`、`:211`；`GameState.gd:1488` |
| 统领 | 3 只，整轮锁定 | `scripts/scenes/TeamSelectScene.gd:72`、`:188-195` |
| **按「打了几场」长的机制** | 096 小木斧每打完一场 +10 砍伐经验；糖果罐赢 +1、输 +4 | `RealtimeBattle3DScene.gd:7553-7554`；`GameState.gd:1230`、`:1389` |
| 排行榜 | 只在本地；按 `season_eggs_killed` 排，而这个数**每赢一场 +1**（名叫碎蛋，实为胜场） | `RealtimeBattle3DScene.gd:7555-7557`；`scripts/net/backend.gd:207-217` |
| 跨大轮保留 | 切轮清掉命/币/等级/装备/统领等；保留 `install_uid`、龟等级、历史战绩；**没有头衔/称号** | `GameState.gd:1641-1674` |

### 2.2 单场对局（v2 说不变的部分）

| 项 | 现状 | 证据 |
|---|---|---|
| 路 | 上路 → 下路，1-1 才打终极战场 | `scripts/gamedata/phase2_duallane.gd:43-47` |
| 破蛋窗口 | 普通路团灭后 10 秒；定局路不限时（蛋承伤 ×5 + 每秒自损最大生命的 5%） | `scripts/scenes/battle/dual_lane_flow.gd:733`；`RealtimeBattle3DScene.gd:90-92` |
| 决胜 | 本战场开打 40 秒进入，之后每 5 秒增伤一档、治疗减半 | `RealtimeBattle3DScene.gd:1508-1511` |
| 三战场继承 | 竹弓 / 哑铃 / 温泉蛋等层数在路之间保留 | `dual_lane_flow.gd:815`（`EQ_CARRY`） |
| 呈现间隔 | 每段呈现 5 秒；摆位不限时 | `RealtimeBattle3DScene.gd:587` |
| 一场多长 | 离线机器人对局（跳过摆位与呈现）**9457 场**：中位 100.8 秒、P90 131.0、P95 141.2、P99 162.3、最长 225.1 秒（3.75 分）。超 3 分钟 **0.17%**（16 场）、超 3.5 分钟 0.03%、**超 4 分钟 0 场**。没有硬上限，但长尾比原先记的窄得多 | `tools/autoplay/**/cohort-battles*.csv` 全 29 份的 `帧` 列 ÷ 60（det 模式 1 帧 = 1/60 游戏秒）。★上一版这行只吃到一半文件，结论方向没错但把长尾说重了 |

### 2.3 匹配 / 后端 / 身份

| 项 | 现状 | 证据 |
|---|---|---|
| 匹配 | 对手全部来自**本地快照池**（内置种子队 + 拉到的远端快照）；按本轮总场数分 9 档；本档没有就往低档找，全空才出机器人；排除自己和最近打过的对手 | `backend.gd:36-45`、`:366-378`；`scripts/scenes/MatchmakingScene.gd:69-75` |
| 「匹配中」 | 固定 2.2 秒动画，不是排队 | `MatchmakingScene.gd:79` |
| 后端 | **关着**：`backend_url=""`；原 Deno 端因超免费额度被封；只有「传快照 / 拉快照」两个接口，从不跑战斗 | `project.godot:62`；`server/deno/main.ts`；`docs/plans/20260820-阵容上传后端.md:3` |
| 上线节奏 | 用户 2026-08-18「那上线感觉可以往后推, 先再优化本体吧」 | `docs/实时版-路线图与待办.md:1744` |
| 身份 | 没有账号 / 登录；只有首次启动生成的 `install_uid` | `GameState.gd:37-49` |
| 观赛 / 重放 / 押注 | 仓库里都没有。龟投是另一个项目（桌面 `龟投原型设计/` 设计稿 + `Turtle-Project-L_Demo/parent-site/` 前端） | 全仓搜 回放 / replay / 押注 / 龟投 无实现 |

### 2.4 确定性（v2 说的「全部前提」）

| 项 | 现状 | 证据 |
|---|---|---|
| 种子 | 只有设了环境变量 `TURTLE_SEED` 才定种子并进确定性模式；正常游玩每局随机 | `scripts/scenes/battle/battle_world_builder.gd:371-376` |
| 步长 | 确定性模式每帧恰好推一步；正常游玩走固定步长累加器 | `RealtimeBattle3DScene.gd:2204-2233` |
| 裸随机 | 全仓裸随机调用由 `tools/rng_discipline.py` 守「只减不增」；大多是纯视觉抖动，**有一小撮会改战斗结果**，例：骰子龟命运暴击 | `tools/rng_discipline.py`；`scripts/systems/skills/dice_system.gd:198` |
| 伤害挂在补间动画末尾 | 不少技能的伤害 / 控制在 `tween_callback` 里结算，补间走真实帧时间 | 例：`scripts/systems/skills/gambler_system.gd:122-125`；CLAUDE.md §3.5 |
| 战斗中的实时操作 | **玩家在战斗里实时操控自己的训龟大师**（键盘 / 摇杆每帧读） | `RealtimeBattle3DScene.gd:2190` |
| 已有门禁 | 同一进程里 1v1 跑两遍指纹相同；帧切块大小不影响结果 | `tests/verify_battle_determinism.gd`、`tests/verify_interactive_determinism.gd` |
| 没有的 | 跨机器（Windows / Linux / iOS）同结果从没验过；没有录像日志；战斗离不开场景树（相机 / UI / 导航网格） | `docs/plans/20260817-上线与后端.md:163`（U4 未验） |

## 3. 需求 ↔ 代码的出入

1. **「现有 vanilla JS 代码 / 禁止裸调 Math.random()」** —— 本仓库战斗全是 GDScript；JS 只有后端几个文件，且没有 `Math.random`。那句话对应的是旧网页版仓库 `Turtle-Project-L_Demo`。本仓库对应的守卫已经有：`tools/rng_discipline.py`。
2. **「大轮 = 自然周、全服统一赛程」** —— 现在是每台设备各自滚动的 5 天；没有服务器时间，也没有任何「周几 / 收盘时刻」逻辑。
3. **「命尽 → 积分赛结束 → 观赛 / 押注 / 表演赛」** —— 现在 0 命直接锁战斗和商店（用户 2026-07-24 定的「淘汰锁定」）。v2 等于把「淘汰锁定」改回「淘汰后还有事做」。
4. **「按本轮已打场数匹配（第 N+1 场只配打过 N 场的人）」** —— 现在是 9 个宽档、可往低档回落、最后出机器人；对手全是离线快照，不是真人。
5. **「同步撮合 20 秒、服务端结算、重放」** —— 现在没有在线服务（后端关着）、没有服务端战斗、没有录像。
6. **「确定性模拟是全部前提」** —— 同意；但缺口比原稿估计的大（§2.4）：不只是随机数，还有**补间动画里结算伤害**、**训龟大师实时操控**（「阵容 + 种子」不是全部输入）、跨平台浮点与导航网格、战斗离不开渲染层。
7. **「3 分钟一局」** —— 9457 场实测纯战斗中位 100.8 秒、P99 162.3 秒、最长 225.1 秒；**超 3 分钟的只占 0.17%，超 4 分钟的一场都没有**。
   注意区分两种场景：**周日桶内放的是重放，没有摆位阶段**（阵容已锁定）⇒ 重放时长 ≈ 纯战斗 + 固定呈现（`DL_PRESENT_SEC = 5.0` 秒/幕，两路 25 秒、三路 35 秒）；
   而玩家自己打的那局还要加上不限时的摆位。⇒ 只有重放节奏才受这条约束，见 D11。
8. **「成长类层数跨对局清零」** —— 原稿只列了三战场之间的层数。**按「打了几场」长的机制**（096 小木斧砍伐经验、糖果罐）不在原稿的补发里 ⇒ 被补发的人这部分没补到，**违反原稿自己的公平原则**。
9. **「经济供给相同」** —— 同场数下仍有与战绩无关的币差：奇械铸币、035、糖果罐大奖（跟阵容选择走）。不一定要改，但要明确「这算允许的差距」。
10. **「排行榜不按碎蛋数」** —— 代码里的「碎蛋数」本来就是胜场；排行榜目前只在本地。
11. **「横扫场数（2-0）」** —— 单场里有两路胜负，但整轮没有记录每场是不是 2-0。
12. **「头衔跨大轮保留」** —— 代码里没有头衔。
13. **「龟投联动」** —— 龟投不在本仓库，是另一个项目。

## 4. 方案（分阶段路线；每个阶段开工前单独写方案书）

**总体建议：先把「不需要服务器」的部分做成可玩、可测，再决定要不要上线。**
原稿里只有「积分赛规则」和「经济贯穿线」能在现在的离线架构里落地；周六 / 周日同步赛事、服务端结算、观赛、押注全都依赖一个**现在不存在、而且用户暂停过**的在线服务。

| 阶段 | 做什么 | 依赖 | 大致会动哪些文件 |
|---|---|---|---|
| **A 离线周赛骨架** | 赛程常量化（周一~周日各阶段，时区见 U4）；积分赛 8 命 + 场次配额；0 命 / 打满配额后的状态（改「淘汰锁定」见 U9）；按已打场数分档匹配（对本地快照池）；种子排序 + 每场记录是否 2-0；配额补发（范围见 U6）；终榜排序 | 无 | `phase2_config.gd`、`GameState.gd`（切轮 / 新字段）、`RealtimeBattle3DScene.gd` 结算段、`backend.gd`（分档）、`MainMenuScene.gd`（锁定）、`MatchmakingScene.gd`、`LeaderboardScene.gd` |
| **B 确定性加固** | 伤害从补间里抽出（照 CLAUDE.md §3.5 海盗钩的办法）；改结果的裸随机改走 `_battle_rng`；种子从 `GameState.battle_seed` 进战斗；训龟大师实时操控的处理（U2）；跨机器指纹对比（本机 Windows vs Linux CI） | 无（工作量大） | `scripts/systems/skills/*`、`battle_world_builder.gd`、`tests/verify_battle_determinism.gd` 扩到满阵容满装备 |
| **C 本地录像与回看** | 每场存「版本号 + 种子 + 双方阵容包 + 结果摘要」；能回看自己的历史对局；版本变了怎么处理（U7） | B | 存档新字段 + 回看入口 |
| **D 在线** | 账号 / 昵称、服务端排行榜、快照上传、按标签匹配、服务端用无头 Godot 复算（抽检或全量） | U1、B、C | `server/`、`scripts/net/*` |
| **E 周六闯关 / 周日分桶与签表** | 同战绩标签匹配、桶内同步回合、签表、快照代打 | D | 新场景 + 服务端赛程 |
| **F 观赛与押注** | 重放剧场、焦点对决、红蓝押注接龟投 | D、E、U5 | 跨项目（龟投） |

### 4.1　A 阶段（离线周赛骨架）详细实施步骤

顺序是有依赖的：**A1 常量 → A2 存档字段 → A3 结算接线 → A4 开局拦截 → A5 读数 → A6 匹配 → A7 补发 → A8 终榜 → A9 收口**。
每步都写清「改哪、门禁怎么写、怎么证明门禁会红」。带 ⚠ 的是已经踩过的坑，别重犯。

**A1　常量落位**　`scripts/gamedata/phase2_config.gd`
在现有「V2 赛季」区（`:14` `SEASON_DURATION_SEC` 那一段）后面加一组：积分赛配额、补发地板（币 / 经验）、晋级线（比例 + 保送胜场）、
闯关线（几胜进 / 几负出 / 配额）、赛程各阶段的起止。**全部集中在这一个文件**，不散进场景脚本。
⚠ 门禁不要断言「常量等于某个数」——那是把参数抄两份（[[fb-refactor-creates-the-drift-it-removes]] 同族）。常量的正确性由 A3~A7 的行为门禁间接守。

**A2　存档字段**　`autoload/GameState.gd`
新增字段：积分赛已用场次、横扫数、补发已发场次（幂等用）、赛程阶段、自然周锚点、闯关战绩（胜 / 负）、是否已晋级。
每个字段必须同时改**五处**，漏一处就会在切轮或重置后漂：声明（`:579-585` 那一批旁边）、保存（`:1081-1089`）、
载入（`:1149-1165`，照 `int(data.get("xxx", 0))` 的写法）、`reset_save`（`:1300-1309`）、`start_new_season`（`:1641-1653`）。
门禁（新建 `tests/verify_week_season.gd`）①存档往返保值 ②`start_new_season()` 后全部归零。
反向验证：注释掉载入那一行 ⇒ ① 红；注释掉 `start_new_season` 那一行 ⇒ ② 红。

**A3　结算接线**　`scripts/scenes/RealtimeBattle3DScene.gd:7531` `_settle_season`
- 在 `:7551`（`season_total_battles += 1`）旁边记积分赛配额消耗；**闯关赛 / 决赛日的场次不吃积分赛配额**，按赛程阶段分流。
- 横扫：结算这一刻 `GameState.lane_results` 还在（写入点 `dual_lane_flow.gd:750`），
  条件是「两路都打完 + 不需要终极战场」（判定函数 `phase2_duallane.gd:39-47`）⇒ 在 `:7556` 附近把横扫数 +1，并写进 `record_match`（`:7581`）那条记录。
- 补发要用到的「按场数成长」在同一段里：096 砍伐经验 `:7553`、糖果罐 `:7554`（见 A7 与 U6）。
门禁③：直接调 `_settle_season(true/false)`（`verify_combat_sanity.gd:61-65` 就是这么调的），量配额计数与横扫计数。
反向验证：把横扫条件改成恒真 ⇒ 1-1 打完终极的那局也被记成横扫 ⇒ ③ 红。
⚠ 门禁里的场数、命数一律写死，不要用常量算期望值（[[fb-gate-tautological-when-it-spans-a-frame]] 同族）。

**A4　开局拦截**　`scripts/scenes/MainMenuScene.gd`
现在只有「0 命淘汰」一个闸（`:212` 取 `eliminated`、`:235` 商店锁、`:716-718` toast）。改成三种原因各自一条提示：命尽 / 配额已打满 / 当前不是能打的阶段。
门禁④照抄现成写法 `tests/verify_season_elim.gd:24-45`（实例化主菜单 → 数 toast → 断言 `dual_active` 没被置 true）。
反向验证：去掉配额那一条判断 ⇒ 配额打满还能开打 ⇒ ④ 红。

**A5　读数（界面）**　主菜单 `:524-529`、结算屏 `scripts/scenes/battle/battle_hud.gd:1395-1412`
主菜单赛季面板加「剩余场次」一行；结算屏的数据块加同一个读数；副标题（`_result_subtitle`）在配额打满 / 出局时各说各的。
⚠ `tests/verify_mainmenu_layout.gd:209-210` **断言赛季面板恰好四行**——加一行就会红。这是**要改的账**（把分母改成五并写清为什么），不是放松判据。

**A6　匹配标签**　`scripts/net/backend.gd`
快照里本来就带着原始场数（`:482`），所以**不用迁移旧池**：桶键仍是现在的 9 档（`pool_add`），
在桶内按「场数完全相同」过滤（`pool_find` `:123-137` 加一个参数）。找不到同场数的，按顺序回落：同桶其它人 → 机器人（`make_bot` 用同标签参数）。
⚠ 原稿要「永不跨标签」，但离线池子小，严格执行等于永远打机器人 ⇒ 建议**回落要记账**（记下这一场是精确 / 同桶 / 机器人），
让「有多少场是真同标签」变成可量的数，而不是拍脑袋。这条与 U3 一起拍板。
门禁⑤：池里放场数不同的三份快照，我打过 N 场 ⇒ 必须选中场数 N 的那份。反向验证：去掉精确过滤 ⇒ 选中别人 ⇒ ⑤ 红。

**A7　配额补发**　新函数放 `GameState`（与 `add_season_xp` / `candy_jar_add` 同一层）
触发：积分赛阶段结束且该玩家晋级；补发场数 = 配额 − 实打场数；每场补币与经验（地板值见 A1）。
**按 U6 拍板决定要不要连带补 096 砍伐经验与糖果罐**——不补的话，这两样的持有者被补发后仍然吃亏（§3 第 8 条）。
幂等靠「补发已发场次」字段：只补差额，重复调用不再给。
门禁⑥：合成一份存档（打了 K 场、晋级），补发后币 / 经验 / 成长项等于「打满配额的最低供给」——**量真实字段，不套公式**。
门禁⑦：连调两次，第二次一分不加。反向验证：去掉幂等字段 ⇒ ⑦ 红。

**A8　终榜排序**　`scripts/net/backend.gd:207-217` + `scripts/scenes/LeaderboardScene.gd:37`、`:58`、`:80`
排序键从「碎蛋数」换成字典序「胜场 → 余命 → 横扫」；快照里补上这三个数（`:474-483`），旧快照缺字段当 0（不作废旧池）。
标题与表头文案一起改（现在写的是「本赛季击杀龟蛋数」）。
门禁⑧：构造同胜同命、横扫不同的三份快照，断言顺序。反向验证：比较器改回只比碎蛋数 ⇒ ⑧ 红。

**A9　收口**　`bash run-tests.sh`（只认 `GATE_RC=` 与 `ALL PASS`）→ 把附录 A 列的 19 个旧门禁逐个改账 →
版本号五处 + tag + CHANGELOG + 路线图 → 方案书回填（实际改了哪些 / 与方案的偏差 / 新发现的坑）。
⚠ 门禁跑的时候不要改仓库（[[fb-no-repo-edits-while-gate-runs]]）。

### 4.2　B 阶段（确定性加固）详细实施步骤

**B1　先把尺子做出来（不动产品代码）**：把 `tests/verify_battle_determinism.gd` 从 1v1 扩到「3 统领 + 3 小将、每只带装备、打到分出胜负」，
指纹取每 N 步的血量与坐标摘要。**先跑一遍证明它现在就会红**——这一步的产出是「缺口有多大」的实测数，而不是猜。
**B2　12 处 `process_frame` 协程**（附录 B）改成走 sim 队列：弹道进 `_pending_shots`，定时进 `equip_tick_system.schedule`，等待用 `_wait_sim`。一批一跑门禁。
**B3　48 处 tween 回调**：数值结算抽成能同步调用的函数，tween 只留演出（CLAUDE.md §3.5 海盗钩索的办法）。先做附录 B 里「按固定节拍排队」那批，最机械。
**B4　四处难点单独做**：小熊按立绘帧号结算（`:4303-4310`）、星星在 `tween_method` 里逐渐染帧结算（`star_system.gd:362-391`）、
宽刃用帧 delta 扫描（`equip_system.gd:945-969`）、踩背 0.833 秒协程（`hiding_system.gd:366-388`）。每一处都要重新表达成「按 sim 步推进」。
**B5　种子接线**：现在只有环境变量能定种子（`battle_world_builder.gd:371-376`）⇒ 改成也能从 `GameState.battle_seed` 进来，并把种子存进战绩记录。
**B6　跨机器验证**：CI 加一个 job，用同一组种子跑同一批对局，指纹与本机逐字比对——**这是 U4「确定性从没端到端验证」真正被关掉的那一刻**。
⚠ 改结果的裸随机（约十来处，例 `dice_system.gd:198`）在 B3 顺路改走 `_battle_rng`；`tools/rng_discipline.py` 只守"不许新增"，不等于"同种子同结果"。

### 4.3　C 阶段（本地录像）详细实施步骤

**C1** 每场结算时把「版本号 + 种子 + 双方阵容包 + 结果摘要」写进战绩记录（`GameState.record_match` `:1005` 那条记录加字段）。
**C2** 回看入口：从战绩屏点一条记录 → 用记录里的种子与阵容重跑一遍（`TURTLE_SEED` 那条路已经存在）。
**C3** 版本对不上的旧记录怎么办，按 U7 拍板（保留旧程序复算 / 存逐帧事件 / 旧记录标记为不可回看）。
⚠ C 阶段做完才谈得上 D（服务端结算），因为服务端要的正是 C1 那份存档格式。

### 4.4　训龟大师改造（用户 2026-09-16 拍板）

**用户原话**：「训龟大师估计得调整为一个不能移动的形象和每个周期固定投放一个技能这样子，不再由人来操控」
「把一些技能的射程提升到 2000 码，只带一个，最近的，保留，同规则，法术盘可以留冷却显示，还有魔法石来显示层数，
大师现在改为不可被打，相当于一个装饰品了，不在拥有血量，但还是可以攻击，移除掉训龟大师的信息栏」

**现状（已核实）**：
- 玩家每帧操控：`RealtimeBattle3DScene.gd:2190` → `trainer_system.gd:117-129`（摇杆优先，否则键盘八键）→ `:133-144` 移动，移速 `TRAINER_MOVE_SPD 130`（`RealtimeBattle3DScene.gd:339`），钳在 `ARENA`（`:56`，1596×728）内。
- **敌方大师本来就是 AI**：`trainer_system.gd:867-883` —— ① 每 0.8~1.8 秒随机游走（半速）② 冷却一好就朝最近敌人放技能。**「自动放技能」的骨架已经存在**。
- 技能七选一（`TrainerConfigScene.gd:9-17`，存 `GameState.trainer_skill`）：魔法石（被动）/ 钩锁 CD20 射程600 / 怒火药水 CD16 射程700 /
  口哨 CD14 无需瞄准 / 冰川 CD17 射程500 / 猎龟令 CD30 射程600 / 驯服 CD60 射程600（表在 `RealtimeBattle3DScene.gd:364-374`）。
- 自动普攻已有：每 1.5 秒扔石头，射程 2000（`RealtimeBattle3DScene.gd:337-338`，`trainer_system.gd:166-191`）。
- 血量与受击：`TRAINER_HP 500`（`:321`）；受到任何伤害（含真伤）钳成 1（`:4498`）；不被主动索敌（`battle_targeting.gd` 八处）；不计入团灭；每路满血重建。
- 法术盘已经在显示魔法石层数与档位环色（`battle_render.gd:25-32`）。

**改造内容**：
1. **不可移动**：删玩家输入路径（`trainer_system.gd:117-129`、`:153-159` 与 `RealtimeBattle3DScene.gd:2190` 的调用）与 AI 游走（`trainer_system.gd:871-878`）；出生点 `battle_spawn.gd:557-558` 保持后排。⚠ `TRAINER_MOVE_SPD` 会变成孤儿常量 —— 按本仓规矩是**删掉**，不是置 0（`verify_trainer_audit` 验「常量存在且被使用」）。
2. **射程**：把够不到的主动技能射程提到 2000（与普攻同口径）。⚠ 站在后排不动时，中线距离约 758 码，原来的 500~700 射程**全都够不到对面**——这是「不能移动」必须连带改的，见 D1（哪几个技能提到 2000 待你点名）。
3. **自动放技能**：把敌方那套 `_trainer_ai_step` ②（冷却好 → 朝最近敌人放）提成两侧共用；节奏用各技能自己的冷却（D2）。
4. **不可被打、没有血量**：`is_trainer` 直接免疫（伤害入口 `:4498` 由「钳成 1」改成「0」），去掉 `TRAINER_HP`、不建血条、不进任何血量统计；但**保留普攻**（扔石头）与技能。
5. **UI**：移除大师信息栏与摇杆；法术盘保留为只读（冷却 + 魔法石层数）。
6. **敌我同规则**。
7. **玩家保留赛前选择**（选形象 / 选那一个技能；`TrainerConfigScene`）。

**会跟着红的门禁**（要一并改账，不是回归）：`tests/verify_trainer_move.gd`（整份验移动：速度 130、半推、朝向、`ARENA` 钳制、PC 八键、摇杆优先）、
`tests/verify_trainer.gd:60-74`（500 血 / 伤害降 1）、`tests/verify_trainer_audit.gd`（常量必须被使用）、
`tests/verify_battle_hud_r1.gd` 与 `tests/verify_whistle_no_aim.gd`（走真实法术盘 / Q 输入路径）、`tests/verify_hook.gd`（施法定身时序）、
`tests/verify_trainer_place.gd`、`tests/verify_trainer_target.gd`、`tests/verify_salvo_trainer.gd`。

### 4.5　后端与账号（用户 2026-09-16 拍板：重开、海外、几百人、要邮箱账号）

**用户原话**：「后端需要重开，那么由于我几天后会去英国，所以最好海外服务器吧」
「玩家应该集中海外吧，暂时不用国内的了，账号的话能绑定邮箱吗，前期我们估计几百人吧，隐私政策看看怎么做」
「快照的话得仔细想想怎么做，也就是前三天同场次的才匹配吗」「时间以英国时间先为标准，要做可以调整的」

**2026-09-16 查到的现行免费额度**（三家官网 / 汇总页，用于选型，不是抄进代码的数值）：

| 候选 | 免费额度 | 对本项目的要害 |
|---|---|---|
| **Supabase** | 数据库 500 MB、5 GB 出站、**邮箱登录 5 万月活**、边缘函数 50 万次/月、2 个项目 | **自带邮箱账号**，一次解决身份问题；⚠ 一周没请求会自动暂停项目、免费档无备份 |
| Cloudflare Workers + KV | 请求 10 万/天；KV **写只有 1000/天**、读 10 万/天、存储 1 GB | 请求量够；**KV 写入上限太低**（每场传一次快照就能打满），要改用 D1 |
| Netlify | **300 credits/月**（硬上限、不自动续）；Edge Functions 另算 100 万次/月；Personal $9/月 1000 credits | 普通 Functions 的点数太少，只有全走 Edge Functions 才够 |

**我的建议**：走 **Supabase**（邮箱登录 + 数据库 + 边缘函数一家搞定，几百人完全在免费档内），备选 Cloudflare Workers + D1。
⚠ 玩家集中海外后，「国内能不能连」不再是硬指标，之前否掉 Cloudflare / Fly 的理由（被墙）不再成立。

**快照与匹配口径（我的方案，待你拍板 D5）**：
- 现在对手池是「本地快照 + 按本轮总场数分 9 档」；周赛制要的是**同场次**匹配。
- 建议：快照按 `(赛季周, 已打场数)` 存成切片，每个玩家每个场次只留最新一份（同键覆盖）；匹配时只在「本周 + 同场次」里找。
- 上传时机要从「只有打赢才传」改成**每场都传**，否则同场次的池子只有赢家、越往后越偏强。
- 「前三天同场次才匹配」这条**建议改成「本周同场次」**：积分赛是周二~周五四天，卡三天会让周五开打的人匹配不到周二的对手。
- 人不够时的回落顺序与记账见 A6。

**⚠ Supabase 解决不了的那一半（D 阶段要正视）**：它能存数据、发邮箱登录、跑边缘函数（TypeScript / Deno），
**但它跑不了 Godot** ⇒ 原稿「结算权在服务端、服务端结算一次」这条，Supabase 给不了。
D 阶段要在两条路里选（新未决点 D7）：① 另租一台能跑无头 Godot 的小机器做复算；② 先不做服务端复算 —— 客户端算、服务端只做账目校验与抽检。
**Supabase 自身的风险**（都是免费档的已知条件，不是猜）：免费项目一周没有请求会自动暂停（赛季间歇期要注意，有活跃玩家不会触发）；
免费档没有备份 ⇒ 要自己定期导出；涨到超额要上 Pro（按官网当前价 $25/月）。

### 4.6　周六 / 周日的匹配与分桶（用户 2026-09-16 拍板）

**用户原话**：「我们还用快照这个机制吗，因为最重要的点是双方玩家必须是同总场次的才能互相打，而不再是按档位分，
快照也应该可以用吧，但只限于积分赛的，不包括周六周日吧，上传的话就是每场都传」
「32 桶的话比如有 25 人，那就让一些人轮空并补发经济，如果小于等于 16 的话就是 16 桶，依次类推」
「0 命的话就只能等到周 6 周日观赛了，不再打表演赛」

- **积分赛（周二~周五）**：可以打快照；硬条件是**双方总场次必须相同**（不再是 9 档分档）。每场都上传快照（不再只传赢的）。
- **周六闯关赛**：优先真人；同战绩标签排不到真人时，按原稿兜底链走 —— **同标签 30 分钟内的新鲜快照 → 机器人**，
  **永不跨战绩标签**（放宽战绩比用快照更伤公平：闯关赛的全部意义就是「同战绩的人互相淘汰」）。
- **周日决赛日**：**不用快照**匹配，只在真人之间按桶规则走（人数不整由轮空 + 补发兜底）。
  注意与 D8 区分：周日**掉线代打**用的是该玩家最后锁定的阵容快照，那是「替谁打」不是「跟谁打」。
- **周日分桶容量随人数自适应**：超过 16 人 → 32 人桶；≤16 人 → 16 人桶；再少依次减半（8 / 4）。
- **人数不整** → 高种子轮空，**轮空要补发经济**（与积分赛补发共用同一套幂等记账：按一轮应得的轮次币与经验补）。
- **0 命之后**：不再有表演赛，只能等周六 / 周日看比赛。⇒ A 阶段的「淘汰锁定」保持锁死，但要把提示改成「等周六观赛」，并在 F 阶段接观赛入口。
- ✅ U3b 已定（2026-09-16 第二轮）：**恢复原稿的兜底链**。这修订了同日早些时候「快照不包括周六周日」那句 —— 周六可以用快照，但只作兜底。仍要定的实现细节：排队等多久才降级到快照、快照「30 分钟内」按上传时刻还是按对局时刻算。

### 4.7　重放寿命与版本维护期（用户 2026-09-16 拍板）

**用户原话**：「重放的话只有周六周日周一这三天可以观看重放，过完后过去对局就删掉了，然后我们可能也要有版本维护期这样子，停服啥的」

- **重放只在周六、周日、周一可看**，周一过完就删。⇒ 服务端只需保存最近一个周末的对局存档（种子 + 双方阵容 + 结果摘要），存储压力几乎为零。
- **版本维护期放在周一休赛期**：停服 → 发版本 → 开服。这条同时把「改了平衡旧重放会算出不同结果」这个问题**从根上解决**——
  因为重放最长只活到周一，而版本只在周一之后才换；跨版本的重放根本不存在。
- 由此推出的实现要求（写进 D / F 阶段方案书）：
  ① 对局存档带版本号，播放前比对，不一致直接不给播（兜底，正常情况下不会发生）；
  ② 周一维护窗口要有停服开关与公告位；客户端在停服期给明确提示，而不是连不上就静默失败；
  ③ 删除是**到期自动删**（服务端定时任务），不靠手动。

### 4.8　参数与规则总表（原稿 §九 逐条对账，2026-09-16）

「沿用」= 照原稿做，没有异议；「已改」= 用户这两轮改过；「待定」= 还需要拍板。

| 原稿参数 | 原稿值 | 状态 | 备注 |
|---|---|---|---|
| 积分赛配额 | 24 场 | 沿用 | 与 8 命咬合；实际强度要等 A 阶段用离线机器人对局跑一轮 |
| 命数 | 8，不回命 | 沿用 | 与现在代码一致（`GameState.gd:582`） |
| 结算公式 | 8 + 余命 + 2×失命，胜 +6 | 沿用 | **与现在代码逐字一致**（`RealtimeBattle3DScene.gd:7549-7550`），不用改 |
| 补发地板（积分赛） | 16 币 + 2 经验 / 场 | **已改** | 用户定：**不补砍伐经验与糖果罐**，只补币与经验 |
| 种子排序 | 胜场 > 余命 > 横扫 | 沿用 | 横扫要新增记录（A3） |
| 晋级线 | 前 30% + ≥13 胜保送 | 沿用 | 几百人规模：约 30% 进周六 |
| 闯关线 | 4 胜进 / 3 负出，配额 6 场 | 沿用 | |
| 闯关匹配 | 严格同战绩标签 | **待定 U3b** | 原稿兜底链允许「同标签新鲜快照」，与用户「周六不用快照」冲突 |
| 补发地板（闯关） | 8 币 + 2 经验 / 场 | 沿用 | |
| 排队降级阈值 | 20 秒 | **待定** | 取决于 U3b：周六若不用快照，这个阈值改成「等多久给机器人 / 判轮空」 |
| 新鲜快照时窗 | 30 分钟 | **待定 D10** | 积分赛的快照要不要也限新鲜度？还是只认「本周 + 同总场次」不看时间 |
| 周日入场 | 全员自动，不在线快照代打 | **已定：按原稿** | 用户 2026-09-16「周日的话按原稿的来」 |
| 桶容量 | 32 人 | **已改** | 随人数自适应：>16 → 32 人桶；≤16 → 16 人桶，依次减半；人数不整高种子轮空并补发经济 |
| 桶内节奏 | 3 分钟购物 + 3 分钟重放 | **待定 D11** | 9457 场实测：纯战斗超 3 分钟的只有 0.17%、超 4 分钟 0 场。重放另加固定呈现 25~35 秒 ⇒ **放宽到 4 分钟即可全覆盖**，不必做加速播或硬时限 |
| 签表开赛 | 20:00 | 沿用 | 时区已定：英国时间，且做成可调 |
| 签表掉线兜底 | 沿用上一轮阵容 | 沿用 | 与周日快照代打同一套 |
| 押注截止 | ≤ 开播时刻 | **作废** | 用户定：押注不做（主站做） |
| 决赛奖励形态 | 仅头衔 / 称号 | **待定 D12** | 代码里**没有头衔系统**：要定有哪些头衔、存哪、在哪显示（跨大轮唯一资产） |
| 收盘时刻 | 周五 / 周六 23:00 | 沿用 | 英国时间、可调 |

**原稿 §六 观赛系统**：押注、开撕台、表演赛三项按用户决定去掉；剩下的重放剧场 / 战绩榜与对阵流 / 焦点对决 / 周日同步开播
**范围待定 D13** —— 尤其是「积分赛（周二~周五）的对局要不要也存重放」：用户定了只有周六周日周一能看重放，
那周二~周五的对局是根本不存，还是存下来周末一起放。

### 对原稿规则本身的意见

1. **小规模时怎么办。** 原稿三个量级从 1,000 人起算；现在是 sideload 包、没有账号，真实人数更可能是几人到几十人。那时周六池只有个位数，「严格同标签、永不跨标签」几乎每场都落到快照 / 机器人，周日 32 人桶开不满。建议补一条「人数不够时的降级规则」（例如周六改打同标签快照、周日直接进签表）。→ U3
2. **积分赛默认异步。** 在线人数少时，20 秒真人撮合几乎总是失败，体验等于「每场先等 20 秒再打机器人」。建议积分赛默认直接打同标签快照，真人同步只留给周日。
3. **前两天输光的人整周没事做。** 「命尽不回命 + 0 命锁定」下，原稿用观赛 / 表演赛填空，而这两样都要在线功能。离线阶段需要替代（比如无奖励的练习赛）。→ U9
4. **补发要连「按场数成长的机制」一起补。** 补发只补晋级者，逻辑自洽；但 096 小木斧、糖果罐这类按场数长的，不补就会让持有者被补发后仍然吃亏（§3 第 8 条）。→ U6
5. **横扫作第三排序键没问题**（同胜同命时场数必然相同），但要从现在起记录每场是否 2-0。
6. **重放与版本。** 服务端只存「种子 + 阵容」的重放，一改平衡或修 bug，旧重放就会播出不同的结果。建议只在周一休赛期发版本、重放记录带版本号；跨版本的旧重放要么保留旧程序复算，要么改存逐帧事件。→ U7
7. **押注的合规风险。** 只要押注用的货币能用真钱买、或能换成有价值的东西，就可能碰上商店审核与法律问题。建议押注只用不可购买的积分，并单独做一次合规评估。→ U5
8. **训龟大师实时操控与「就绪即锁定、服务端瞬间结算」直接冲突。** 三选一：排位里训龟大师改 AI 托管 / 录玩家输入（那就不能瞬间结算，只能边打边传）/ 排位禁用。→ U2
9. **收盘时刻的时区。** 「23:00 收盘」要定是北京时间还是按玩家时区；全服统一赛程只能用服务器时间，客户端时间可以被改。→ U4

## 5. 已知风险与未决点

### 未决点（动手前需要用户拍板）

| # | 问题 | 状态 | 说明 |
|---|---|---|---|
| U1 | 后端要不要重开、用哪家 | **已定 2026-09-16**：重开、海外；「玩家应该集中海外吧，暂时不用国内的了」⇒ 国内可达性不再是硬指标 | 宿主具体选哪家仍未定 → D4 |
| U2 | 训龟大师实时操控 vs 服务端结算 | **已定 2026-09-16**：不可移动、每周期自动放技能、不再由人操控；不可被打、没有血量、仍能攻击；只带一个技能、打最近的、敌我同规则；保留赛前选择；法术盘留作冷却 + 魔法石层数显示；移除大师信息栏 | 细节见 §4.4；仍要定 D1（哪几个技能射程提到 2000）与 D2（放技能节奏） |
| U3 | 人数少时的降级规则 | **部分已定 2026-09-16**：周日桶容量随人数自适应（>16 → 32 人桶；≤16 → 16 人桶，依次减半），人数不整高种子轮空并补发经济 | 周六还没定 → U3b |
| U3b | 周六同战绩标签凑不到真人时怎么办 | **已定 2026-09-16（第二轮）**：恢复原稿的兜底链 —— 同标签真人排队 → **同标签 30 分钟内的新鲜快照** → 机器人，**永不跨战绩标签** | 这一条**修订了同日早些时候的「快照不包括周六周日」**：周六闯关赛允许快照，但只作兜底、且必须同标签 + 新鲜。周日不受影响（有轮空规则兜底，见 §4.6）|
| U4 | 时区与收盘时刻基准 | **已定 2026-09-16**：「时间以英国时间先为标准，要做可以调整的」⇒ 赛程时区做成可配置，默认英国 | 具体收盘时刻仍按原稿 23:00 |
| U5 | 押注货币与合规 | **已定 2026-09-16**：「押注我们先不做，因为主战会做的」⇒ 本项目不做押注，F 阶段只保留观赛 | — |
| U6 | 补发是否覆盖按场数成长的机制 | **已定 2026-09-16**：「不要补发砍伐经验和糖果罐」⇒ 只补币与经验 | 意味着长期挂机不打满配额的人，096 / 糖果罐进度会真的落后（这是有意的） |
| U7 | 版本更新后旧重放怎么处理 | **已定 2026-09-16**：「只有周六周日周一这三天可以观看重放，过完后过去对局就删掉」+ 设版本维护期（停服发版） | 重放寿命 3 天 ⇒ 跨版本重放的问题自然消失（见 §4.7）；维护窗口放在周一休赛期 |
| U8 | 阶段顺序 | **已定 2026-09-16**：A（离线积分赛规则）与 B（确定性加固）**并行**，再做 C，D~F 在后 | 两者不冲突：A 动赛程/结算/匹配，B 动战斗内部的补间与随机源 |
| U9 | 「淘汰锁定」改不改 | **已定 2026-09-16**：「0 命的话就只能等到周 6 周日观赛了，不再打表演赛」⇒ 保持锁死，提示改成「等周六观赛」 | 观赛入口在 F 阶段；离线阶段 0 命就是没事可做 |
| D1 | 哪几个主动技能的射程提到 2000 | **已定 2026-09-16**：「射程全部提升到 2000」⇒ 钩锁 / 怒火药水 / 冰川 / 猎龟令 / 驯服 全部 2000 | ⚠ 钩锁变成全场可勾、驯服 / 猎龟令全场可点名，强度要在 A 阶段实测重估（我会量，不擅自改数值） |
| D2 | 自动放技能的节奏 | **已定 2026-09-16**：「用各自冷却」⇒ 沿用 14~60 秒的各自冷却 | — |
| D3 | 账号形态 | **已定 2026-09-16**：「先匿名加补绑吧」⇒ 首次进入匿名建号，随时可补绑邮箱 | 补绑要处理「同一人两个匿名号」的合并，A/D 阶段各自写清 |
| D4 | 宿主最终选哪家 | **已定 2026-09-16**：Supabase | 它跑不了 Godot ⇒ 服务端复算另议，见 D7 |
| D5 | 快照与匹配口径 | **已定 2026-09-16**：快照保留；积分赛（周二~周五）正常用，硬条件是双方总场次相同（不再按 9 档）；每场都上传。**周六闯关赛只作兜底用**（同标签 + 30 分钟内新鲜，见 U3b）；周日不用 | 「本周同场次」的周界与轮空补发细节写在 §4.6 |
| D6 | 隐私政策怎么挂 | 🔲 待拍板 | 已有草稿 `docs/隐私政策.md`，缺一个公开 URL 与联系邮箱；可以挂在 Supabase 站点或 GitHub Pages |
| D7 | 服务端复算战斗放哪 | **已定 2026-09-16**：两步走 —— 先做②（客户端算 + 服务端账目校验与抽检），**架构上预留复算接口**；B 阶段确定性加固完成后再决定要不要真开那台机器 | ⇒ 前期不租机器；但 Supabase 侧的表结构与上传协议要从一开始就带上「可复算所需的全部输入」（种子 / 双方阵容 / 版本号），否则将来切①要重做一遍
| D8 | 周日掉线 | **已定 2026-09-16**：「周日的话按原稿的来」⇒ 不在线用最后锁定的阵容快照代打、奖励照发、可随时上线接管 | 与「周六周日不用快照」的口径冲突只留在周六（U3b） |
| D9 | 开工时机 | **已定 2026-09-16**：「我们先不急着开始，我们先把一切都规划好」⇒ 规划全部收敛后再动代码 | — |
| D10 | 积分赛快照的新鲜度 | 🔲 待拍板 | 只认「本周 + 同总场次」，还是再加一条「N 分钟内上传的才算」 |
| D11 | 一场打太久怎么办 | **已定 2026-09-16**：**不给每场对局设硬时限**（用户原话）⇒ 采用「桶内节奏放宽到 4 分钟」，也就不需要加速播 | **9457 场实测**：纯战斗中位 100.5 秒、P99 164.4 秒、最长 225.1 秒；超 3 分钟的只有 **0.2%**（7/3098），超 4 分钟的 **0 场**。重放没有摆位阶段 ⇒ 时长≈纯战斗+固定呈现。⇒ 我建议**把桶内节奏从 3 分钟放宽到 4 分钟**（0 场会超，不用做加速播也不用砍时限）。若坚持 3 分钟，才需要硬时限或加速播 |
| D12 | 头衔系统 | 🔲 待拍板 | 代码里没有。要定：有哪几种头衔、怎么存（跨大轮唯一保留的资产）、在哪显示（主菜单 / 排行榜 / 对局内） |
| D13 | 观赛范围 | 🔲 待拍板 | 去掉押注后还剩：重放剧场 / 周六战绩榜与对阵流 / 焦点对决 / 周日同步开播；另外积分赛对局要不要存重放

### 风险

| # | 风险 | 状态 |
|---|---|---|
| R1 | 确定性无法一次证明：跨平台浮点、导航网格可能让 iOS 客户端与 Linux 服务端算出不同结果 | 未验 |
| R2 | 把伤害从补间里抽出来会动很多技能文件，回归面广 | 未验 |
| R3 | 本机 CPU 有确诊硬件故障，大批无头复算 / 门禁是高危负载（CLAUDE.md §2） | 已知 |
| R4 | v2 的配额、晋级比例、补发数额没有做过模拟；可以先用离线机器人对局 `tests/_cohort.gd` 跑经济与晋级率 | 未验 |
| R5 | 「经济供给相同」下仍有阵容相关的币差（奇械铸币、035、糖果罐大奖） | 未决（算不算允许的差距） |
| R6 | 旧文 `docs/design/实时版-系统机制权威.md` §1 的装备槽公式、0 命行为、分档表已与代码不符，A 阶段要一并改权威文档 | 已查实 |

## 6. 验收标准

本篇是草稿，验收项写的是「各阶段做完怎么证明」，每条都要能反向验证（故意改坏必须红）。

- [ ] 🔲 U1~U9 用户逐条拍板后，本篇状态改「已拍板」，并拆出 A 阶段方案书
- [ ] ❌ A 阶段：打满积分赛配额或命尽后不能再开积分赛 —— 门禁走真结算入口（`RealtimeBattle3DScene.gd` 结算段），场数与命数在门禁里写死、不从常量推；反向验证：去掉配额判断必红
- [ ] ❌ A 阶段：补发后晋级者的币与经验等于「打满配额的最低供给」，按场数成长的机制同样补到（范围按 U6）—— 门禁用合成存档量真实字段，不从公式常量推
- [ ] ❌ A 阶段：每场是否 2-0 被记录，终榜排序按「胜场 → 余命 → 横扫」—— 门禁构造同胜同命、横扫不同的两份存档
- [ ] ❌ B 阶段：满阵容满装备的对局，同种子同阵容在本机与 Linux CI 上指纹逐字相同（扩展 `tests/verify_battle_determinism.gd`）
- [ ] ❌ B 阶段：战斗路径上会改结果的裸随机清零（在 `tools/rng_discipline.py` 里加「战斗路径零容忍」一类）；反向验证：塞回一处 `randf()` 必红
- [ ] ❓ 「3 分钟一局」够不够：需要带摆位与呈现的完整时长分布（现有 `tools/autoplay/cohort-battles-s*.csv` 不含这两段）

## 7. 决策记录

- 2026-09-16 用户：「继续，把仓库搞好，准备好弄下一个方案，方案有个v2在我桌面上你也可以先看看然后整理方案书」「提意见」⇒ 原稿逐字存档到 `docs/design/`；本篇整理成七节并附意见；**一行代码没动**。
- 2026-09-16 我建议：先离线落地 A（积分赛规则）并同时做 B（确定性加固）；在线部分（D~F）等用户重开后端再议。**未拍板。**
- 相关历史决定（原话见各文）：2026-07-24「淘汰锁定」；2026-08-18「那上线感觉可以往后推」；2026-08-20 沿用 8 心 / 5 天 / 现币公式（20260817 U3）；2026-08-27 打赢才传快照；2026-09-04「别管后端啊」。
- **2026-09-16 用户拍板（逐字）**：
  - 「后端需要重开，那么由于我几天后会去英国，所以最好海外服务器吧」
  - 「训龟大师估计得调整为一个不能移动的形象和每个周期固定投放一个技能这样子，不再由人来操控」
  - 「玩家应该集中海外吧，暂时不用国内的了」「前期我们估计几百人吧」「隐私政策看看怎么做」
  - 「把一些技能的射程提升到 2000 码，只带一个，最近的，保留，同规则，法术盘可以留冷却显示，还有魔法石来显示层数，
    大师现在改为不可被打，相当于一个装饰品了，不在拥有血量，但还是可以攻击，移除掉训龟大师的信息栏」
  - 「时间以英国时间先为标准，要做可以调整的」
  - 「不要补发砍伐经验和糖果罐」
  - 「押注我们先不做，因为主战会做的」
  - 用户问「Netlify 收费吗」「账号的话能绑定邮箱吗」「快照的话得仔细想想怎么做，也就是前三天同场次的才匹配吗」⇒ 我的答复与建议见 §4.5 与 D3~D5。
- **2026-09-16 第二轮拍板**（我把四个未决点摆开问，用户逐条选）：
  - **U3b 周六兜底** = 恢复原稿的链：同标签真人排队 → 同标签 30 分钟内新鲜快照 → 机器人，永不跨战绩标签。
    ⚠ 这**修订了同日早些时候的「快照只限于积分赛，不包括周六周日」** —— 周六闯关赛现在允许快照兜底。周日仍不用。
  - **D7 服务端复算** = 两步走：先「客户端算 + 服务端账目校验与抽检」，架构预留复算接口；B 阶段确定性加固完成后再决定要不要真开那台机器。
  - **U8 阶段顺序** = A 与 B 并行。
  - **D11 桶内节奏** = 用户原话「不用给每场设置时限的」⇒ 不设硬时限；据 9457 场实测（超 4 分钟 0 场）采用「节奏放宽到 4 分钟」，从而也不需要加速播。
    （若用户本意是「保持 3 分钟、只是不设时限」，这条要回来改。）
- **2026-09-16 实测补充**：对局时长分布改用全部 29 份 `cohort-battles*.csv`（9457 场）重算 —— 中位 100.8 秒、P99 162.3 秒、最长 225.1 秒。
  上一版方案书那行只吃到一半文件，把长尾说重了（"3 分钟装不下长尾"实际只有 0.17% 的对局超过 3 分钟）。

## 附录 A：A 阶段插入点（2026-09-16 调查产出；拆 A 方案书时直接用，别重新查）

**结算的唯一入口**：`scripts/scenes/RealtimeBattle3DScene.gd:7531` `_settle_season(won)`。单路走 `_check_end()`（`:7509`）、双路走 `_dl_sys._dl_flow_check()`（`:7512`）、投降走 `:1240-1260`，**三条路都汇到它** ⇒ 配额判断放这一处就能覆盖。
教学沙盒早退在 `:7533-7535`；`_had_season` 闸在 `:7538-7539`；0 命表演赛分支在 `:7543-7545`（现在走不到，见 §3 第 3 条）。

它写进存档的东西（A 阶段每一项都要想清楚配额与补发怎么算）：

| 位置 | 写了什么 |
|---|---|
| `:7548` | 掉命 |
| `:7549-7550` | 深海币公式 |
| `:7551` | `season_total_battles`（**配额计数就是它**） |
| `:7552` | 大轮经验 +2 |
| `:7553` | 096 小木斧砍伐经验（按场数长） |
| `:7554` | 糖果罐（按场数长） |
| `:7556-7557` | 胜场 / 碎蛋数 |
| `:7562-7565` | 打赢才传快照 |
| `:7569-7571` | 奇械铸币并进本场奖励 |
| `:7572-7575` | 四个羁绊 `reset_match` |
| `:7576` | 钱包入账 |
| `:7578-7583` | 总战绩 + `record_match` |
| `:7584` | `save()` |

商店刷新跟着 `season_total_battles` 走：`scripts/scenes/ShopScene.gd:121`、`:149`。

**存档字段的生命周期**（加新字段必须同步五处，漏一处就会在切轮 / 重置后漂）：
声明 `autoload/GameState.gd:579-585`、保存 `:1081-1089`、载入 `:1149-1165`、`reset_save` `:1300-1309`、`start_new_season` `:1641-1653`；
轮次滚动在 `ensure_season` `:1335-1341`（自然周锚点要改的就是这里）。

**2-0 横扫怎么拿**：`GameState.lane_results`（`:112`，**只在内存、不进存档**），唯一写入点 `scripts/scenes/battle/dual_lane_flow.gd:750`；
决胜路破蛋会回填 `:718-721`；`:757-758` 本来就在判「2-0 横扫」；判定函数在 `scripts/gamedata/phase2_duallane.gd:39-69`。
结算那一刻 `lane_results` 还在 ⇒ 横扫标记可以在 `:7556` 附近算出来，但**今天没有任何地方持久化它**。

**匹配标签**：分档 `scripts/net/backend.gd:36-45`、反解 `:48-58`、`find_opponent` 的低档回落循环 `:366-378`
（「严格同标签」= 去掉这个回落）；快照里已经带了原始场数 `:474`、`:482-483` ⇒ 改成「精确同场数」成本很小。
唯一调用点 `scripts/scenes/MatchmakingScene.gd:71`；假排队动画 `:79`、`:83`。

**要改文案的界面**：主菜单赛季面板 `scripts/scenes/MainMenuScene.gd:524-529`（大轮 / 命数 / 深海币 / 战绩四行）、淘汰与商店锁提示 `:716-721`；
结算屏读数 `scripts/scenes/battle/battle_hud.gd:1403-1412`、比分行 `:1334-1337`、副标题 `:1395-1398`；
排行榜 `scripts/scenes/LeaderboardScene.gd:37`、`:58`、`:80` 与排序 `backend.gd:209-217`。

**会跟着红的门禁**（A 阶段要一并改，不是回归）：
`tests/verify_combat_sanity.gd:61-65`（直接调 `_settle_season` 验掉命）、`tests/verify_onboarding.gd:55-57`（沙盒不计场 + 非沙盒对照）、
`tests/verify_season_elim.gd:25-36`、`tests/verify_season_flow.gd:56-60`、`tests/verify_incense_stone.gd:190-191`、
`tests/verify_axe.gd:233-244`、`tests/verify_axe_evolution.gd:150`、`tests/verify_dual_egg_decider.gd:21-53`、
`tests/verify_bracket_gear.gd:36-53`、`tests/verify_ghost_seed.gd:29-45`、`tests/verify_ghost_chest.gd:59-137`、`tests/verify_remote_pool.gd:51`、
`tests/verify_nav_paths.gd:71-99`、`tests/verify_ios_ui.gd:188-189`、
**`tests/verify_mainmenu_layout.gd:209-210`（断言赛季面板恰好四行 —— 加一行配额进去就红）**、
`tests/verify_equip_pool.gd:120`、`tests/verify_holy_shield_grant.gd:39-77`、`tests/verify_settings.gd:50`、`tests/verify_equip_cap.gd:45`。
非门禁但同样手抄了币公式的：`tests/_cohort.gd:436-437`。

**要新加的存档字段**（每个都要过上面那五处）：配额已用（或沿用 `season_total_battles` + 一个配额常量）、补发已发次数（幂等，防重启重复补）、
横扫数、赛程阶段、自然周锚点、闯关战绩、**精确匹配标签**（还要加进快照 `backend.gd:474-483` 与 `data/ghost_seed.json`，
否则严格同标签匹配一个人都找不到）、`match_history` 每条记录加横扫标记（`GameState.gd:1006`）。

## 附录 B：B 阶段清单 —— 挂在真实帧上的战斗结算（2026-09-16 调查产出，抽样逐行复核过）

**目标形态仓库里已经有了**：`_sim_step`（`scripts/scenes/RealtimeBattle3DScene.gd:2236`）驱动的 `_pending_shots`（`:701`，由 `scripts/scenes/battle/battle_ballistics.gd` 推进）、
`equip_tick_system.tick_delayed / schedule`（`scripts/systems/equip/equip_tick_system.gd:628`、`:669`）、各系统自己的 `tick()`、以及走战斗时钟的 `_wait_sim(secs)`（`:1715`）。
B 阶段做的就是把下面这些搬进这套里 —— 哑铃早就这么搬过（`:3523-3528` 有注释）。

**总量：60 处（48 处 tween 回调 / `tween_method` + 12 处 `process_frame` 协程），分布在 24 个文件；其中 45 处直接改血 / 盾 / 控制 / 状态。**
集中区：`RealtimeBattle3DScene.gd` 9 处、`scripts/systems/skills/hiding_system.gd` 7 处、`scripts/systems/skills/star_system.gd` 5 处。

最难搬的几处（都逐行看过）：

| 位置 | 为什么难 |
|---|---|
| `RealtimeBattle3DScene.gd:4303-4310` | 小熊的伤害挂在**立绘帧号**上（`kt += get_process_delta_time()` 推出 `bear.frame`，`frame >= 3` 才结算）⇒ 帧率一变，打击时刻就变 |
| `scripts/systems/skills/star_system.gd:362-391` | 伤害写在 1.16 秒的 `tween_method` lambda 里、**每个渲染帧跑一次** ⇒ 命中次数随帧率变 |
| `scripts/systems/equip/equip_system.gd:945-969` | 扫描式命中判定用 `battle.get_process_delta_time()` 推进，命中即给盾 |
| `scripts/systems/skills/hiding_system.gd:366-388` | 0.833 秒的踩背协程：每 0.1 秒一跳 DoT、中途可被控制打断、同时改攻守双方 `pos` |
| `RealtimeBattle3DScene.gd:5605-5613` | 气波前摇用 `process_frame` 挪人，之后再定身 |
| `scripts/systems/skills/two_head_system.gd:104-106` | 炮弹用 `process_frame` + 帧 delta 飞行并逐帧判碰撞（应改成 `_pending_shots` 弹道） |
| `scripts/systems/skills/cyber_system.gd:291` | 5 秒 `tween_method` 持续写 `mref["maxHp"]`（机甲成长） |
| `scripts/systems/skills/pirate_system.gd:299 → 309 → 318` | 三段链式 tween：眩晕 → 0.3 秒拉回 → 命中，每段读上一段改过的坐标 |
| `RealtimeBattle3DScene.gd:6365` | `_sk_dmg` 的多段波是**很多技能共用**的通道，改它牵一发动全身，还牵扯随机选靶顺序 |
| `scripts/systems/skills/ninja_system.gd:129` | 逐帧行进，多目标命中顺序跟帧节奏走 |

**按固定节拍排队的（相对好搬）**：`ice_system.gd:312-314`（每 0.5 秒一跳）、`lightning_system.gd:143-145`（每 0.075 秒一道雷）、
`rainbow_system.gd:211`、`gambler_system.gd:122`、`:220`、`crystal_system.gd:579`、`bubble_system.gd:146`、`candy_system.gd:76`、
`phoenix_system.gd:341`、`ninja_system.gd:248`、`two_head_system.gd:149`、`:304`、`elite_system.gd:440`、`cyber_system.gd:234`、
`equip_system.gd:856`、`lava_system.gd:682`（5 秒后回收大招加的属性）。

**纯位移 / 上锁类**（不直接改血，但位置决定谁被打到，同样要搬）：`hiding_system.gd:248`、`:287`、`:407`、`shell_system.gd:265`、
`headless_system.gd:211`、`rocket_system.gd:90`、`hookbomb_system.gd:703`、`lava_system.gd:565`、`:566`、`:575`、
`star_system.gd:253`、`:296`、`:406`、`cyber_system.gd:169`、`RealtimeBattle3DScene.gd:6250`、`:3878`、`:3887`、`:5742`。

**已确认纯演出、不用动**：`battle_vfx.gd:334`、`:748`、`dragon_system.gd:131`、`smolder_system.gd:88`、`:92`、`:144`、
`lightning_system.gd:60`、`hookbomb_system.gd:359`、`RealtimeBattle3DScene.gd:4969`、`:8246`、`:8282`；开发 / 测试用的定时器另算。

★ 这不是「顺手改」的量级。B 阶段要按文件分批，每批跑扩展后的 `tests/verify_battle_determinism.gd` + 该技能自己的门禁，
并且照 CLAUDE.md §3.5 的办法：**数值结算抽成能同步调用的函数，演出末尾只负责好看**。
