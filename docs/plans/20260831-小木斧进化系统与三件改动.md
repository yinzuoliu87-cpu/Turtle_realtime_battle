# 小木斧进化系统 + 靶向器改名 + 白鲸气环 + FPGA板 + 古灵精怪枪

日期：2026-08-31　|　状态：**实施中**

> **这份方案书的体量与之前几份完全不是一个量级。**
> 前面几份是「改名 + 换图 + 改一个数」；这份里的**小木斧**是一整套
> **跨战斗、跨商店、带进化树、带四选一终局**的新系统，它需要的东西有一半在仓库里还不存在。
> 工时估计与拆期建议写在「已知风险与未决点」，**动手前先看那一节**。

---

## 需求原文

用户 2026-08-31 一次给出，逐字照抄（**不改写、不合并**，后面每条验收都回指这里）：

> 靶向器改名为病毒箭头，重做图标，白鲸气环的三环伤害改为25/40/70+目标2/3/5%最大生命值真实伤害，PFGA板加个新效果：登场时给对方随机1/2/3个敌人提供1把古灵精怪枪。新技能与装备：
>
> 1:
> 技能或装备，会扔给目标一把古灵精怪枪。
> 古灵精怪枪：提供1攻击力，1生命值，1攻击速度。携带者每次普攻会使自己受到1%最大生命值真实伤害。
>
> 2:
> 小木斧（1费）：
> 为携带者提供20最大生命值，10攻击力，2护甲，2魔抗。会有一个斧头召唤物（初始为木斧）登场，近战，普攻造成1ATK，攻速为0.8每秒。斧头召唤物拥有500+已收集的经验值最大生命值，30+0.05已收集的经验值攻击力，5护甲和5魔抗
> 被动1:这个装备不会进行升星，在卡池没有数量限制。 但这件装备将有个砍伐经验条随大轮重置，在商店里购买该装备会使经验条+15，每次进行一整把3条路的战斗后这件装备获得10经验值。斧头击杀或在3秒内参与击杀一个单位获得2经验值。 达到一定阈值后该装备会进化并清空进度条：
> 满80经验值进化为石斧，
> 满110经验值进化为铁斧，
> 满130经验值进化为金斧，
> 满160经验值进化为钻石斧，
> 此后积攒400经验值来完成一次最终进化。
>
> 每进化会获得新被动和属性，并且商店里这件装备的形态和售价会变化。
> 斧头装备拥有一个通用主动技能：
> 主动治疗140龟能：斧头召唤物回复5%最大生命值并为自己提供5%最大生命值护盾。
> 斧头召唤物拥有以下被动：
> 被动2（木斧解锁）:斧头召唤物每次普攻对有任何护盾的目标会窃取10%护盾值作为普通给自己。比如有特殊护盾也转普通护盾给自己。
> 被动3（石斧解锁）：该装备的价格加1费，这个被动给斧头召唤物提供50点最大生命值，5攻击力，3护甲和3魔抗。
> 召唤物每过9秒，普攻获得强化onhit来造成击飞和短暂击退，并造成0.5ATK额外物理伤害。
> 被动4（铁斧解锁）：该装备的价格加1费，这个被动给斧头召唤物提供50点最大生命值，5攻击力，3护甲和3魔抗。召唤物的每第二次普攻变为竖劈，额外造成5%目标最大生命值真实伤害并施加10层流血。
> 被动5（金斧解锁）：该装备的价格增加1费，这个被动给斧头召唤物提供50点最大生命值，5攻击力，3护甲和3魔抗。召唤物的每第一次普攻变成180度横扫，与竖劈交替进行。每次普攻命中会为召唤物提供一层持续5秒的效率层数，可无限叠加，每次叠加刷新时常。
> 一层效率提供4%攻击速度和2%移动速度。
> 被动6（钻石斧解锁）：该装备的价格增加1费，这个被动给斧头召唤物提供50点最大生命值，5攻击力，3护甲和3魔抗。 在释放治疗时，斧头召唤物会开始一段蓄力：在一个梯形预警范围里，每蓄力0.5秒使梯形区域的高增加100码，共蓄力4秒，蓄力期间获得70%减伤并且效率计时器中断，蓄力完毕后砸下斧头，高高击飞梯形里的所有敌人并眩晕3秒，每个敌人造成4ATK物理伤害。
> 在到达钻石斧后玩家攒满了400经验值后选择最终造物后经验值封顶。
>
> 最终造物1:亡灵之斧，该装备的价格增加1费，这个被动给斧头召唤物提供1200点最大生命值，5攻击力，10护甲和10魔抗。被动为召唤物会拥有一个300码的环，在环内的敌人每秒损失1%最大生命值魔法伤害，并每秒每存在一个敌人会使斧头召唤物回复0.3%最大生命值。斧头召唤物死亡后2.5秒会带着40%最大生命值重生。
>
> 最终造物2:炽天使，该装备的价格增加1费，这个被动给斧头召唤物提供50点最大生命值，20攻击力，3护甲和3魔抗，300码射程。被动为每次普攻附带8层灼烧层数。主动效果4秒里的猛砸将被替换为4秒内投掷10把斧头回旋镖，不再获得减伤，半径大小为300码，沿着目标所在的一条直线直直飞过，对接触的目标造成1ATK魔法伤害和8层灼烧层数。
>
> 最终造物3:全息斧，该装备的价格增加1费，这个被动给斧头召唤物提供400点最大生命值，5攻击力，3护甲和3魔抗，50%龟能充能速率。每次普攻将为血量最低的友方单位提供60护盾值和5龟能。主动4秒内的猛砸将被替换为将斧头插入地下4秒，转而获得30%减伤，期间释放全息法阵使600码内的友军每0.5秒回复100生命值和5龟能，并友军在范围内还获得30%攻击速度。
>
> 最终造物4；余烬，该装备的价格增加1费，这个被动给斧头召唤物提供150点最大生命值，80攻击力，80%攻击速度和20%移速。
> 斧头召唤物在普攻攻击命中目标时会给目标施加一层余烬种子，每层种子提供0.5%余烬处决线，无限叠加。召唤物会处决目标低于余烬斩杀线的敌人，通过这个被动处决一个单位会使召唤物获得150点龟能。主动技能的4秒猛砸将被替换为余烬之光，转而使其接下来4秒获得25%生命偷取，25%减伤和25%攻击速度，并免疫控制，这期间不会锁龟能，再次释放会提供一个新的余烬之光，不会打扰到当前的buff，独立的4秒。

---

## 调查

**动手前查过的，全部是实测不是推测。**

### 一、三件小改动的落点都确认了

| 需求 | 现状（实测） |
|---|---|
| 靶向器 | `p2eq_055`，费用 3，`img=equip/targeter.png`。**代码里的系统名是 `HookBombSystem`**（`scripts/systems/equip/hookbomb_system.gd`），文案里有 `{C:HookBombSystem.*}` 五处 |
| 白鲸气环 | `p2eq_063`，费用 3。三环引爆在 `scripts/systems/equip/eq_spirit_batch.gd:343` 的 `_eq_whale_ring`，`RING_TRIGGER := 3`，当前伤害是**纯定额 25/40/70 真实伤害** |
| ~~PFGA板~~ **FPGA板** | 用户打成了 `PFGA`，真名是 **FPGA板 `p2eq_040`**，费用 4。现效果是每 N 秒抽 2-bit 状态给自己上 buff，**与「给敌人塞装备」毫无关系** —— 这是**纯新增的一条效果**，不是改现有效果 |

### 二、装备召唤物**有**现成底座

`058 远古炮台` 是最贴的先例：登场时召唤、召唤物有自己的 maxHp/atk/攻速/射程、
普攻走自己的逻辑、还能「携带者存活时给召唤物加双抗」。另有骷髅、大熊（大熊那件还会换形态）。
⇒ **斧头召唤物本身不需要造新框架**，照 058 抄。

### 三、「大轮重置的累积值」**有**现成先例

`autoload/GameState.gd` 的 `season_id`（注释：**5 天一轮，切轮全重置**）就是需求说的「大轮」。
已经有两个同形状的字段：

- `chest_treasure_value`（随一大轮累积）／`chest_treasures_won`（本大轮已开）
- **093 香火石的刻痕池**（`GameState.gd:582`，注释明写「一大轮重置」）

⇒ 砍伐经验条**照香火石抄**，不用发明新机制。

### 四、这三样在仓库里**不存在**，是真缺口

| 缺口 | 证据 |
|---|---|
| **动态售价** | `ShopScene.gd:216` 是 `return maxi(1, int(edef.get("cost", 1))) * PRICE_MULT` —— 价格**直接读 json 的静态 `cost`**。「每进化 +1 费」需要一条运行时价格通道，现在没有 |
| **排除三合一升星** | 全仓 grep `不升星` / `no_star` / `MERGE_EXCLUDE` **零命中**。`_purchase_merge_star` 对所有装备一视同仁 |
| **商店里同一件装备显示不同形态** | 商店卡的名字与图标都来自 `edef`（DataRegistry 的静态定义），一个 id 只有一个形态 |

### 五、「卡池没有数量限制」**已经天然成立**，不用做

`phase2_equip.roll_shop` 是按 `cost` 分档**从定义表里随机抽**（`by_cost[tier][randi % size]`），
**根本没有「剩几份」的概念**。所以被动 1 的这半句是在描述现状，不是要开发的功能。
（写在这里是为了**不让它变成一条假工作量** —— 这轮已经栽过三次「照着旧账念，全是烂账」。）

---

## 出入

**需求与代码事实对不上的地方全部列出，不擅自替用户决定。**

1. **「PFGA板」实为「FPGA板」**。按 id `p2eq_040` 办，不新建装备。
2. **「古灵精怪枪」是「扔给目标」的装备** —— 本作装备是**局外背包**的东西，
   战斗中没有「给某个单位临时挂一件装备」的通路。实现上它会是一个**战斗内 buff/debuff**
   （内部按装备算属性），而不是真的进对方背包。名字仍叫「古灵精怪枪」。
3. **「1生命值／1攻击力／1攻击速度」**：本作 `atk`／`hp` 是绝对值、`aspd` 是「每秒几次」。
   字面 +1 攻速＝**每秒多一次攻击**，这在本作是极大的数（现有装备最高档也只有 +40%）。见未决点 ①。
4. **「每次普攻使自己受到 1%最大生命值真实伤害」** —— 真实伤害不吃任何减免（权威 §7.5）。
   它是**纯负面**，所以这把枪是「送给敌人的毒苹果」。与 FPGA 那条「给**对方**敌人」一致，逻辑自洽。
5. **「一整把 3 条路的战斗后 +10 经验」**：本作一场对局是**上路→下路→（可能的）决胜**，
   决胜不是必然发生。「3 条路」按**一场对局打完**理解（不论有没有走到决胜）。见未决点 ②。
6. **「进化会清空进度条」＋「满 160 进化钻石斧」＋「此后积攒 400」**：
   按字面，阈值是**每段独立**的（80→清零→再攒 110→清零…），不是累计到 160。已按此理解写验收。
7. **被动 3/4/5/6 每条都写「价格加 1 费」**，四条累加＝木斧 1 费→钻石斧 5 费→最终造物 6 费。
   **本作费用档只有 1~5**（`roll_cost_tier` 按 1-5 出货，`_cost_color` 也只认 1-5）。见未决点 ③。
8. **被动 5「每第一次普攻变横扫」＋被动 4「每第二次普攻变竖劈」** 合起来就是**横扫／竖劈交替**，
   与需求里「与竖劈交替进行」一致，不是三种攻击轮转。
9. **最终造物 4 的「余烬处决线」** 需求写「处决目标低于余烬斩杀线的敌人」，
   斩杀线＝层数 × 0.5% 最大生命，**没写上限**，按字面「无限叠加」。

---

## 方案

### ★分期交付（不是一次做完）

理由见「已知风险」。**每期各自走完整流水（门禁＋版本＋tag＋CI），一期结束再开下一期。**

| 期 | 内容 | 估计 |
|---|---|---|
| **一期** ✅ v0.19.300 | 055 改名病毒箭头＋新图标 · 063 三环伤害加 %最大生命真伤 | 半天 |
| **二期** ✅ v0.19.302 | 古灵精怪枪（战斗内负面装备）＋ FPGA板 登场塞枪 | 一天 |
| **三期** ✅ v0.19.303＋304 | 小木斧**骨架**：装备属性＋斧头召唤物＋通用主动（140 龟能治疗＋护盾）＋被动 2 窃盾＋立绘与四套动作 | 一到两天 |
| **四期** ✅ v0.19.305 | 经验条＋大轮重置＋四次进化＋动态售价＋排除升星＋商店换形态 | 两到三天 |
| **五期** | 被动 3/4/5/6（击飞、竖劈、横扫＋效率层、梯形蓄力猛砸） | 两到三天 |
| **六期** | 四个最终造物＋四选一入口 | 三到四天 |

### 一期：055 ／ 063

- 055：`name` → `病毒箭头`，新 32×32 图标（PixelLab，照仓库主流规格）。
  **效果一字不动**（需求只说改名重做图标）。
  ★`HookBombSystem` 这个**代码类名不改** —— 它是钩索炸弹的机制名，改它会波及五处文案占位符与门禁，
  且与「病毒箭头」的观感无关。若要连内部名一起改，见未决点 ④。
- 063：`_eq_whale_ring` 的引爆伤害 `25/40/70` → `25/40/70 + 目标当前 maxHp × 2/3/5%`，仍是**真实伤害**。
  两个常量收到一处（`RING_FLAT` / `RING_MAXHP_PCT`），照 036 温泉蛋刚踩过的教训办。

### 二期：古灵精怪枪 ＋ FPGA板

- 新建 `scripts/systems/equip/gremlin_gun.gd`：给单位挂 `gremlin_gun` 标记＋属性增量，
  并在 `_eq_on_hit` 的**普攻分支**里对**自己**结算 1% 最大生命真伤。
- FPGA板：`_eq_on_spawn` 里对**敌方**随机 1/2/3 个单位挂枪。
  ★「登场时」＝每条路开场各一次（本作 `_t` 跨路累加，按本场计时必须自己存 `t0`，CLAUDE.md §3.4）。

### ★斧头召唤物的形象（用户 2026-08-31 追加，原话照抄）

> 召唤物的形象你可以弄个新颜色的小将加手上一把斧头，但斧头的颜色要多变，就像不档一样

⇒ 拆成三条可验的：

1. **底子＝现有的小将换个新颜色**（用户**点名**可以在小将上改色 ——
   这是「素材不复用除非点名」那条铁律的**例外授权**，仅限本项，别外扩）。
2. **手上加一把斧头**。
3. **斧头的颜色随进化档位变**（「就像不档一样」＝像不同档位一样）。
   ⇒ 一共 **9 种斧头配色**：木／石／铁／金／钻石 ＋ 四个最终造物。
   ★斧头是**独立图层**、只换斧头的色，身体不跟着换 —— 否则 9 套全身立绘的量。

★这条落在**三期**（召唤物登场）与**四期**（进化换形态），一期不做。

### ★出货概率：改成【独立概率 + 斧头羁绊】（用户 2026-08-31 追加设计）

原来的做法（跟着费用档走）**会让进化反过来惩罚玩家**：经验主要靠"买它"（+15，另两条只有 +10 / +2），
而它越进化越贵、越贵在低档越难买到 —— 等于自己掐断自己的经验来源。实测：

| 大轮档 | 1费(木) | 2费(石) | 3费(铁) | 4费(金) | 5费(钻) |
|---|---|---|---|---|---|
| 档1 | **4.17%** | 0% | 0% | 0% | 0% |
| 档5 | 2.00% | 1.48% | 0.95% | 0.05% | 0% |
| 档10 | 0.42% | 0.71% | 0.95% | 1.50% | **1.79%** |

⇒ 用户拍板改成**独立出货概率**，规则（原话）：

> 干脆改为独立的出货概率，但得仔细想想怎么弄，为木斧专门设一个羁绊，木斧在未出现的时候会是1费概率，
> 因为用户如果不想玩这个流派，那等级起来后木斧概率会越来越低，但买了木斧并装备木斧，
> 激活斧头羁绊后，概率将为3%加玩家激活斧头羁绊时在这大轮游戏的局数*0.1%，最终概率最高为10%

拆成可实现的五条：

1. **没买过**：仍按 1 费档走（它本来就是 1 费装备 ⇒ **这条天然成立，不用做**）。
   等级起来后 1 费概率自然衰减 —— 这正是用户要的"不想玩这流派就越来越少见"。
2. **买了并装备、激活【斧头羁绊】之后**：改走独立概率
   `P = 3% + 本大轮激活羁绊后的局数 × 0.1%`，**封顶 10%**。
3. **经验封顶之后**（选完最终造物）：**彻底不再出货** —— 再买没有任何用。
4. **卖掉**：羁绊失效 ⇒ 概率回落到 1 费档；但**经验与档位不回退**（它们是赛季级的）。
5. **斧头羁绊**是第 12 个类型，且**只有一件装备** ⇒ 要开特例：**持有并装备 1 件即激活**
   （本作羁绊惯例是"装满 N 件同类型"）。⚠ 类型有**四张平行表**（TYPES / TYPE_EMOJI /
   TYPE_NAME / TIER_DESCS），`type_tables_audit` 盯着键集一致，加类型时最容易只改一张。

**★实测的两个数（用来判断节奏，不是拍脑袋）**：商店 **10 格**；
一个大轮从 1 档打到 10 档要 **188 局**（每场 +2 赛季经验，阈值表 `LEVEL_XP_THRESHOLDS`）。
⇒ `3% → 10%` 需要 **70 局**，约走完大轮的 1/3 就封顶，节奏合适。

### ★召唤物美术：三轮才做对（用户 2026-08-31）

| 轮 | 做法 | 用户判定 |
|---|---|---|
| 1 | Pro 生成"琥珀伐木工"（**没把小将当参考图传**，只写进了文字） | ❌ 不是小将，是另一个角色 |
| 2 | img2img（强度 190/110/70） | ❌ 几乎没变，还是绿的、还是三叉戟 |
| 2' | 手工：小将整体色相位移 ＋ 贴我自己的斧头 | ❌ 糊成一团棕，斧头不突出 |
| 3 | **Pro ＋ 把小将当真参考图**（保剪影/圆盔/背后触须/正面视角），石板蓝甲＋铜边 | ✅「这个可以」 |

**★教训（memory 里记过，我又犯了）**：说"参考 X"就要**真把 X 当参考图传进去**，
不能写进文字描述了事。前两轮白花的钱就是这么来的。

**★还有一个我没锁的东西：朝向。** 16 个候选里斧头一会儿在左手一会儿在右手 ——
用户当场看出来了。游戏的约定在 `scripts/scenes/battle/battle_render.gd:373`：
**立绘默认朝左，游戏靠 `flip_h` 自动镜像**。⇒ 素材必须**统一朝向**，斧头固定在同一侧。
也因此**只需要 south 一个方向的动画**（公告板 + 两向翻转），不必出 8 方向。

### ★★提示词写错的方向：为了保住道具，把动作写死了（用户 2026-08-31 指出）

用户原话：「你提示词怎么做的，动作很烂啊，砍击整个身体不动吗？」——**说得对，是我写坏的。**

我先踩了「模板动画把斧头弄丢」那个坑，于是矫枉过正，提示词里写满了
**"torso stays level"「手臂和斧头锁死在同一位置」** 这类约束。
结果：斧头是保住了，但**整个人像木头，只有斧头在动** —— 砍击没有重心转移、没有跟随。

**★正确的写法是描述【动作本身】，不是描述【什么不许动】：**

| 写坏的 | 该怎么写 |
|---|---|
| 「躯干保持水平」「手臂锁死」 | 预备后仰蓄力 → 躯干旋转前压、屈膝 → 斧头抡过膝盖 → 回弹直起 |
| 只说结果（"挥砍"） | 说**重心怎么走**（leans back and coils → snaps forward → recoils） |
| 8 帧塞一个重动作 | 重动作给 10 帧，让预备和回弹有地方放 |

并且明写一句 **"The body moves as much as the axe"** —— 把"别把身体写死"直接说出来。

**★"斧刃最小像素"这条自动判据仍然有效**，但它只管"道具在不在"，
**管不了"动作好不好"**。后者只能看图。两条都要，不能拿前者代替后者。

### ★★判据漏了一半，于是我一直在从坏样本里挑（用户 2026-08-31：「你给我你的提示词」）

用户要看提示词，我把 11 条原文全列出来，自己看的时候发现：
**动作提示词里从头到尾没提过「护目镜」**。我据此猜"漂掉是因为没提"——
**量了一遍，猜错了**：

| 轮 | 提没提护目镜 | 护目镜丢帧 | 斧头 |
|---|---|---|---|
| 走路① | 没提 | **0/9 完美** | ❌ 最小 15 |
| 走路② | **提了** | 0/9 | ✅ 但动作僵 |
| 走路③（我选的） | 没提 | ❌ **4/9** | ✅ |
| 技能① | 没提 | **0/9 完美** | ❌ 最小 4 |
| 技能③（我选的） | 没提 | ❌ 5/9 | ✅ |

⇒ **护目镜和斧头一样是随机的**，跟提示词无关。

**真正的错在判据**：我只用「斧刃最小像素」一个条件挑，
挑出来的正好是「斧头好、护目镜坏」的那些 —— **单条件筛选会系统性地选出另一维的坏样本**。

把 11 条全部拿双条件（斧刃 ≥23 且 护目镜 0 丢帧）重判：**只有 1 条全过**。
⇒ 单次通过率约 **1/11**，我前面掷的次数**远远不够**，而且从一开始就在用半个判据挑。

**★规则**：挑选随机产物时，**判据要覆盖全部要保住的特征**，
少一维就等于在那一维上随机取样 —— 而且是**从已经按另一维筛过的样本里**取，会更差。
（同族 [[fb-judge-must-fit-the-shape]]：判据宽一格造假 bug，**窄一格放过真 bug**。）

★另一个被量翻的猜想：**锁调色板修不了护目镜**。
我以为护目镜是"被画成了别的颜色"，把每帧映射回参考立绘的 38 色调色板 ——
丢失的帧锁完仍是 0。因为模型**根本没画**护目镜，那几帧头部就是一片纯甲色。
调色板只能纠正"画错色"，纠正不了"没画"。

### ★★这轮最贵的教训：一次坏结果不等于路走不通，先重掷（用户 2026-08-31 点破）

v3 自定义动画**一次只花 1 个生成额度**（工具文档原话「cheap re-roll for small sprites」）。
而 32 px 那版的攻击**本来就是好的**——有举斧、有白色挥砍弧、斧刃全程 28~42 px。
同样的提示词换到 64 px 变差，那是**掷到了坏结果**，不是方法不成立。

**我做了什么**：因为一次坏结果，
① 去写「torso stays level / 手臂锁死」这类约束 ⇒ 把动作写僵（用户：「砍击整个身体不动吗」）
② 又去搞 **25 额度**的「起止姿势插值」，结果生成的终止姿**缩放对不上**，废掉
③ 然后打算改方案（"动画只出 4 帧，力度交给引擎 VFX"）

用户一句「你这听上去压根行不通」把我拉回来——**对的做法是重掷**：1 额度一次，
连掷几条、逐条量、挑最好的。比上面任何一条都便宜、也直接。

**⇒ 规则**：随机生成的东西，**先重掷 N 次再改方法**。
改方法之前要能回答"我掷了几次"——只掷过一次就改方法，是把随机性当成系统性。
（同族 memory [[fb-no-n1-comparison-on-flaky-process]]：会随机波动的东西上，N=1 的对比无效。）

⚠ 一个实现细节：**同一条 `action_description` 不会重复排队**（返回 "already queued or complete"）。
重掷要把措辞改一改（动作意图不变），否则以为在掷、其实什么都没发生。

### ★动画做了几轮，量到的东西（2026-08-31）

用两条**自动判据**逐帧量，比肉眼稳：

| 判据 | 怎么量 | 抓什么 |
|---|---|---|
| **斧刃最小像素** | 每帧取斧刃连通块的像素数，取最小 | 「斧头丢了」＋「斧头被身体挡住」 |
| **身体重心/头顶起伏** | 排除斧刃像素后算身体重心 x 与最高点 y 的幅度 | 「整个人像木头」 |

（基准：立绘上斧刃 47 px。最小值 <23 判不合格。）

| 轮 | 待机 | 走路 | 攻击 | 技能释放 |
|---|---|---|---|---|
| 模板动画 | — | — | — | — |
| ↑ 结果 | **斧头全丢**（掉到 0） | 同左 | — | — |
| v3 第 1 版 | 最小 43 ✅ 但**护目镜在闪色** | 最小 15 ❌ 斧头飘 | 最小 47 ✅ | 最小 4 ❌ 没举过顶 |
| v3 第 2 版（加了"锁死"约束） | — | 排队中 | 最小 4 ❌ 身体只动 3.2px | 最小 4 ❌ 仍没举过顶 |

**★三次都没做对，说明「靠一句 action_description 让它自由发挥」这条路不够。**
下一步换 **`end_frame` 插值**：我自己把「砍到底」那一帧当立绘生成出来，
让工具在「举斧」→「砍到底」之间插值 —— 动作的**起止由我定**，不再赌模型。
（`animate_character(mode='v3', custom_start_frame_url=..., end_frame_url=...)`，单方向限定。）

### ★四条动画的接线现状（诚实记账，2026-09-01）

| 动作 | 素材 | 接线 |
|---|---|---|
| 待机 | `eq-axe-idle.png` | ✅ `_EQ_BODY_SPR["axe"]` |
| 攻击 | `eq-axe-attack.png` | ✅ `ACTION_ATTACK["_summon_axe"]`（`_play_action` 播完回 idle） |
| 走路 | `eq-axe-walk.png` | ✅ `ACTION_RUN["_summon_axe"]`（2026-09-01 接上） |
| 技能释放 | `eq-axe-cast.png` | ✅ `ACTION_ELITE["axe_cast"]` ＋ `AxeSystem.play_cast()`（2026-09-01 接上） |

★★**上面这两行原来写的是 ❌，理由是「引擎没有 walk 通道」—— 那句话是错的**（2026-09-01 用户纠正：
「怎么可以没通道啊，小将动画就有攻击待机走路等」）。
`ACTION_RUN` 一直都在，忍者／幽灵／三种小将／亡魂／训龟大师全在用它，`battle_render._update_run_anim`
按「0.1 秒时间窗测速 > 48px/s」自动切走路、停下回 idle —— **斧头只是从没登记**。
我当时只查了 `_play_action`（它确实只认 attack/hurt/death），就下了「引擎没有」的结论，
于是那张走路表在盘上躺着、引擎里零引用，而它追着敌人跑时一直是**站姿滑行**。

技能释放同理：不必走 `_manual_anim`。键登记进 `ACTION_ELITE`（那张表名叫 ELITE 是历史包袱，
实际是「非标准一次性动作」总表），`_play_action` 的不可打断闸认的就是它 ⇒ 施法播到一半不会被普攻打断。
★不能复用 `_elite_anim`：它开头 `if not u.get("is_elite")` 直接 return，斧头走那条**永远播不出来且不报错**。

⇒ 教训不是「当时该顺手接上」，而是**「引擎没有 X」这种结论只查了一个函数就不算数** ——
要 grep 全仓找同类消费者（`grep -rn "ACTION_" scripts/scenes/battle/` 一眼就能看见 `ACTION_RUN`）。

★**受伤/死亡：用户明确说不要**（2026-09-01「我说不要死亡受伤啊」）。
我先误读成"缺口"、用 pro 各生成了一条（40 额度），已作废、不接线不入库。

### ★用户 2026-09-01 问「一大轮开始时有多少用例与斧头有关」——**当时 0 条**

四个字段（`axe_exp_bar` / `axe_exp_total` / `axe_stage` / `axe_final`）在 `GameState` 里加了、
大轮重置与存档往返也写了，**却一条断言都没有**。典型的"写进去了没人验"。已补 4 条：

- 分母：重置**前**四个字段确实非零（否则下一条恒真）
- **大轮开始 → 四个字段全部归零**（走真入口 `start_new_season()`，不是手工赋值）
- 存档字典**两侧**的键都在（存了没人读 ＝ 重启就丢）

★补的过程里我自己的判据又错一次：第一版想「真存盘再读回来」，当场红 ——
根因是 `GameState.save()` 在 `test_mode` 下**直接 return**（防污染真存档，设计是对的）。
**差点当成产品 bug 去查。** ⇒ 判据改成量源码两侧的键。
（同族：判据要挑对工具，测试环境里被关掉的东西验不了。）

### ★图鉴实拍看出的两个文案缺口（四期补）

图鉴渲染正确（名字/1 费/类型「斧头」/属性/四段效果/羁绊「装满 1 件即激活」，羁绊数 11→12）。
但描述里**缺两件玩家需要知道的事**：

1. **砍伐经验怎么攒**（买 +15 ／ 打完一场 +10 ／ 击杀或 3 秒内助攻 +2）——
   现在只说"最大生命 ＝ 500 ＋ 已收集的砍伐经验"，玩家不知道经验从哪来。
2. **随大轮重置** —— 影响决策（不知道攒的东西会清零）。

**没有提前写进文案** —— 这两条是四期才实装的机制，先写就是承诺一个还不存在的东西，
而且 `tooltip_number_audit` 要求文案数值必须有代码事实源。记为四期验收项。

### ★真战斗实拍：三个盲区验掉两个（2026-09-01）

用户问「有任何 bug 吗」之后我开了真战斗（`EQDEMO_EQUIP=p2eq_096 EQDEMO_ATTACKER=1` ＋ 连拍）。

**第一次跑：场上只有两只绿龟，斧头根本没出现。** 差点当成召唤失败去查 ——
根因是我加了 `SHIP=1`，**那个开关关掉 demo 劫持，把装备演示台一起关了**
（CLAUDE.md §4 写着「冒烟测试必须带 SHIP=1」，我把它当成了通用开关）。去掉就正常了。

**去掉后实拍结论：**

| 盲区 | 结果 |
|---|---|
| ① 真战斗里从没跑过 | ✅ **跑通** —— 斧头召唤出来、追着敌人走、打出伤害、「+64 盾」飘字证明**偷盾生效** |
| ② 帧宽 80 没验过 | ✅ **对的** —— 单帧显示是一个完整小人，不是"一排小斧头人"（竹叶龟踩过的那个坑） |
| ③ 走路/技能没接线 | ❌ 仍在 —— 实拍里它**走路时也是站姿**（全程 idle 帧） |

### ★还没被门禁盖住的三个盲区（说清楚，不假装守得住）

1. **斧头召唤物在真战斗里一次都没跑过。** 274 条门禁全是静态检查（素材格式、属性数值），
   没有一条真打一场看它动。演出接错（攻击动画不在攻击时播）**抓不到**。
2. **帧宽 80 是算出来的，没实拍验过。** 填错会显示成"一排小斧头人"（竹叶龟踩过）。
3. **走路/技能两张在盘上零引用。** 见上表。

### ★★★动画必须用 `mode="pro"`，不是 v3（2026-08-31 实测，别再试 v3）

用户拿一张骑士挥剑的 8 帧参考图指出「哪有你这么敷衍的」——那张里每帧都是**完全不同的姿势**。
我在 v3 上掷了 **十几次** 都做不出来，因为 **v3 做的是「从一张参考帧做局部形变」**，
它天然只能让手臂/道具动，**换不了整个身体的姿势**。这不是运气问题，重掷解决不了：
四条攻击的躯干重心幅度全是 1.0~1.6 px，包括我特意写"leans back and coils / knees bending /
The body moves as much as the axe"那条（3.2 px）。

**`mode="pro"` 是另一套引擎，一次就到位：**

| | 斧刃最小 | 护目镜丢帧 | 姿势变化(剪影逐帧差) |
|---|---|---|---|
| v3 姿势最好的 atk_d | 33 | 3 | 313 |
| v3 最稳的 s_atk（提示词就写 `axe attack`） | 43 | 1 | 163 |
| **pro** | **47** | **0** | **909** |

pro 出来的四帧：待机持斧 → 双手抡到头顶/身体压低/触须后甩 → 劈下(**横贯全身的大白弧刀光 + 地面扬尘**)
→ 收回。用户参考图里要的东西全在。

**代价**：画布 80px 时 pro **固定只出 4 帧**（≤64px 才给 16 帧），且 **20 额度/次**（v3 是 1）。
⇒ 但 4 帧好姿势远胜 9 帧站桩，而且 v3 那十几次加起来也不便宜。

**★规则：动作类动画一律 pro。** v3 只适合"原地小幅度"（呼吸、飘浮）。
判断依据不是"哪个便宜"，而是**这个动作要不要换姿势**。

### ★★动画的两条硬事实（实测，别再试）

**① 模板动画会把手上的道具弄丢。**
`animate_character(template_animation_id="breathing-idle" / "walking-8-frames")` 出来的帧里
**斧头没了** —— 模板是拿骨架重摆姿势，握着的东西不跟着走。
逐帧量斧刃连通块：原立绘 36 px → 转向后 36 px（**转向那步不丢**）→ 待机帧0 掉到 0。
⇒ **四个动作全部走 `mode="v3"` 自定义**，它从参考帧出发、不走骨架。
实测 v3 的攻击九帧斧刃 28~42 px **全程在**，而且挥砍轨迹（白色弧线）也对。

**② 尺寸定在 64。**
32 px 太小：并排看背后触须没了、铜边几乎看不见、斧头缩水（斧刃 47→36 px）。
80 px 是用户通过的那张。最后用户拍板 **64**（2026-08-31「还是64像素的吧」）。★实测 `create_character(size=64)` 出来的画布是 **80×80** —— 工具会给动画留约 25% 余量，角色本体才是 64。

**③ 只出 `south` 一个方向。**
`battle_render.gd:373`「立绘默认朝左，靠 `flip_h` 镜像」⇒ 公告板两向，不必出 8 方向。

**④ 打包格式照仓库既有约定**（`_EQ_BODY_SPR` + `verify_eq_body_sprites`）：
**16 帧乒乓排帧**（0..8 再 7..1）让循环首尾闭合 —— 手枪(323/1600)与直升机(774/4096)
都因为直接首尾相接而**每轮跳一下**，这条有门禁盯着。

### ★斧头换色实测（用户问「怎么改变斧头颜色呢」）

拿 Pro 出的候选量了一遍：整张只有 **38 种颜色**（真调色板像素画），
斧刃是冷灰、只占 **71 个像素**，与蓝甲/铜边完全分得开。

⇒ 把"哪些像素属于斧刃"存成**掩膜**（按区域，不按颜色），换档只重绘掩膜内的点。
实测九档换完，**掩膜外被改动的像素 = 0 个**。

**★为什么不能按颜色换**：旧小将本体有 **2144 种颜色**（带抗锯齿），
而九档刃色阶共 81 个 RGB —— 撞上只是时间问题，一撞就是"换成金斧连龟身也变金"。
`tools/axe_palette.py` 现在两条都有：`recolor()` 按颜色（给单体图标用，色数少够用）、
`recolor_masked()` 按掩膜（给召唤物用）。两者在干净基底上**可见像素逐点一致**（验过 9/9 档）。

### ★斧头召唤物要哪些动画（2026-08-31 补 —— 用户问「召唤物有哪些动画有制定在方案书里面吗」，答案是**原来没有**）

需求原文里每一条被动都点名了一个**不同的动作**，但从没被列成清单。逐条抽出来：

| # | 动画 | 出处（需求原文） | 落在哪期 |
|---|---|---|---|
| 1 | **待机** ⭐用户点名 | 召唤物要站在场上 | 三期 |
| 2 | **走路** ⭐用户点名 | 「近战」⇒ 要走向目标 | 三期 |
| 3 | **攻击** ⭐用户点名 | 「普攻造成 1ATK，攻速为 0.8 每秒」 | 三期 |
| 4 | 死亡 | 亡灵之斧要「死亡后 2.5 秒重生」⇒ 先得有死。**用户 2026-08-31 点名的是「待机/走路/攻击/技能释放」四条**，死亡没点到 ⇒ 挪到六期跟重生一起做 | 六期 |
| 5 | **技能释放** ⭐用户点名 | 「主动治疗 140 龟能」 | 三期 |
| 6 | **强化挥砍**（击飞＋击退） | 被动 3「每过 9 秒，普攻获得强化 onhit」 | 五期 |
| 7 | **竖劈** | 被动 4「每第二次普攻变为竖劈」 | 五期 |
| 8 | **180° 横扫** | 被动 5「每第一次普攻变成 180 度横扫，与竖劈交替」 | 五期 |
| 9 | **蓄力**（4 秒，姿态要能看出在蓄） | 被动 6「开始一段蓄力……共蓄力 4 秒」 | 五期 |
| 10 | **猛砸落斧** | 被动 6「蓄力完毕后砸下斧头」 | 五期 |
| 11 | **重生** | 最终造物 1「死亡后 2.5 秒会带着 40%最大生命值重生」 | 六期 |
| 12 | **远程投掷**（回旋镖出手） | 最终造物 2「4 秒内投掷 10 把斧头回旋镖」 | 六期 |
| 13 | **插地**（斧头插进地面，持续 4 秒） | 最终造物 3「将斧头插入地下 4 秒」 | 六期 |
| 14 | **处决** | 最终造物 4「处决目标低于余烬斩杀线的敌人」 | 六期 |

**不算角色动画、算特效**（走 `battle_vfx` / 现有 VFX 库，不用 PixelLab 出帧）：
亡灵之斧的 300 码环、全息斧的全息法阵光环、余烬之光的自身 buff 光、余烬种子层数标记。

### ★风险 6：「斧头独立换色」与「逐帧动画」互相打架

用户定的是**斧头单独一个图层、只换斧头的颜色**（否则 9 个档位＝9 套全身立绘）。
但**一旦做成逐帧动画，斧头在每一帧里的位置、角度、透视都不同** ——
PixelLab 出的是**合成好的整帧**，不会替你把斧头分层。

三条路，各有代价：

| 方案 | 代价 |
|---|---|
| **A. 身体一套帧 + 斧头单独一张贴图，运行时按骨骼点跟随** | 要给每一帧记一个「手部锚点＋旋转角」表。挥砍时斧头是主体，靠锚点跟随很容易穿帮 |
| **B. 每个档位各出一套完整动画** | 14 个动作 × 9 个档位＝**126 套**，量级完全不现实 |
| **C. 只出一套动画，斧头部分用调色板替换（palette swap）** | 需要斧头用**专属色段**、且这些颜色不出现在身体上。出帧时要盯住这一条，PixelLab 不保证 |

✅ **已拍板：C**（用户 2026-08-31「C」）。一套动画 ＋ 斧头专属色段做调色板替换。
这是像素游戏里换装的标准做法，也只有它能让 14 个动作 × 9 个档位落地。

**★C 对出帧的硬约束（出图时必须盯住，事后补不回来）**：

1. **斧头的颜色不许出现在身体上**。一旦身体某个像素用了斧头色段里的颜色，
   换档位时它会跟着变 —— 表现为「换成金斧，龟的护腕也变金了」。
   ⇒ 出帧后要有一条检查：**斧头包围盒之外，斧头色段一个像素都不许出现**。
2. **9 个档位的色段必须等长**（同样几个索引），否则映射表对不齐。
3. PixelLab **不保证**这一点，它出的是合成好的整帧 ⇒ 需要一个后处理：
   量出斧头区域用到的颜色 → 检查它们在身体区域的出现次数 → 不为 0 就重出或改色。

⇒ 三期要连这条后处理脚本一起做（记在下面验收清单里），
**不能等到六期发现「换色把龟也染了」再回头返工**。

★另外要说清：**14 个动作不是一期能做完的量**，它们本来就分散在三／五／六期。
三期只需要 1～5 这五个（待机／移动／普攻／死亡／施法）。

### 三～六期：小木斧

骨架照 **058 远古炮台**：`_eq_on_spawn` 造召唤物、召唤物走自己的 tick。
经验条照 **093 香火石**：`GameState` 上一个赛季级字段＋大轮重置钩子。
动态售价、排除升星、商店换形态**三条都是新通路**，各自要新门禁。

---

## 已知风险与未决点

### ★风险 0：这不是「改三个数」的需求，工时量级是**十天上下**

上面分期表加起来 **9.5～14 天**。前面几份方案书是半天一份，这份不是。
**不写这条就等于默许「明天早上能看到」。** 要压缩只能砍范围（例如先不做六期），不能靠赶。

### ★风险 1：四个最终造物里有三个要**改主动技能的形态**，这是最贵的部分

- 炽天使：4 秒内投 10 把回旋镖（直线飞行、接触结算）
- 全息斧：插地 4 秒＋600 码光环持续治疗／给龟能／加攻速
- 余烬：可**叠加的独立 4 秒 buff**（「再次释放不打扰当前 buff」＝每次一个独立计时器，不是刷新）

每一个都是一套演出＋一套结算。而 CLAUDE.md §3.5 的血泪教训是
**数值结算绝不能埋在 tween 链末尾**（无头 CI 推不动，本地永远复现不出来）。
⇒ 每一个都要拆成「纯几何／纯数值函数 ＋ 演出调它」两半，门禁只打数值那半。

### ★风险 2：「梯形预警范围」是本作没有的形状

现有范围判定只有圆（半径）与直线带（中线两侧各 N 码）。
梯形（且**高每 0.5 秒 +100 码**、共 4 秒＝高从 0 长到 800 码）要新写一个
「点在梯形内」的判定＋一套会长大的地面预警演出。**这是新几何，不是调参数。**

### ★风险 3：`aspd` 的单位

古灵精怪枪写「1攻击速度」。本作 `aspd` 是**每秒攻击次数**（058 炮台 `TURRET_ASPD` ＝ 0.5／秒），
所以字面 +1 ＝ 攻速翻倍还多。**这个数如果按字面做，这把「毒苹果」会变成大礼包。** 见未决点 ①。

### ★风险 4：真实伤害的自伤会不会打死携带者

「每次普攻自己受 1% 最大生命真实伤害」—— 真实伤害**不吃任何减免**，
一个 1.0 攻速的单位 100 秒就把自己打死。需求给的就是这个（毒苹果），
但要确认**它能不能真把携带者打死**（还是留 1 血）。见未决点 ⑤。

### ★风险 5（已解决）：斧头召唤物的属性公式与「已收集经验值」耦合

`500 + exp` 最大生命、`30 + 0.05×exp` 攻击力 —— 而 exp **每次进化会清零**。
那么公式里的 `exp` 是**当前进度条**还是**历史累计**？
按字面「已收集的经验值」＋「进化清空进度条」，两者给出**完全不同**的强度曲线
（清零版：钻石斧上限 500+400；累计版：500+880 且一直涨）。

✅ **已拍板：历史累计**（用户 2026-08-31）。

⇒ **这意味着要存两个字段，不是一个**：

| 字段 | 什么时候变 | 谁读它 |
|---|---|---|
| `axe_exp_bar`（砍伐进度条） | 攒到阈值 → **清零** ／ 大轮 → 清零 | 进化判定 |
| `axe_exp_total`（历史累计） | **只增不减** ／ 大轮 → 清零 | 斧头召唤物的 maxHp／atk 公式 |

★两个字段必须**同源写入**（一次加经验同时写两边），否则必漂。
门禁要有一条「进化之后 `axe_exp_bar` 归零而 `axe_exp_total` 不变」——
这正是「一个数存两份」那类坑的标准形状，**只验其中一个等于没验**。

### 未决点（要你拍板）

| # | 问题 | 我的建议 |
|---|---|---|
| ① | ~~古灵精怪枪的「1攻击速度」~~ | ✅ **已拍板：+1%**（用户 2026-08-31「是1%」） |
| ② | ~~「一整把 3 条路的战斗」的定义~~ | ✅ **我按建议自拍：打完一整场对局**（不论有没有走到决胜）。理由：决胜不是必然发生，按「必须打满 3 条路」会让大多数对局拿不到经验 |
| ③ | ~~费用会涨到 6 费~~ | ✅ **已拍板：封顶 5 费**（用户 2026-08-31「按你说的」）⇒ 钻石斧就是 5 费，最终造物**不再 +1** |
| ④ | ~~055 内部类名改不改~~ | ✅ **我按建议自拍：只改显示名**，`HookBombSystem` 保留。理由：它是钩索炸弹的机制名，与「病毒箭头」的观感无关，改它要动五处文案占位符 |
| ⑤ | ~~自伤真实伤害能不能把携带者打死~~ | ✅ **已拍板：能打死**（用户 2026-08-31「能打死」） |
| ⑥ | ~~斧头属性公式里的「已收集的经验值」~~ | ✅ **已拍板：历史累计**（用户 2026-08-31「历史累计」）⇒ 进度条与累计值是**两个字段**：进度条进化时清零、累计值只增不减（大轮重置时两个都归零） |
| ⑦ | ~~最终造物怎么选~~ | ✅ **已拍板：商店里出一张「四选一」卡**（用户 2026-08-31「按你说的」） |
| ⑧ | ~~买第二把小木斧会怎样~~ | ⚠ **我定错了，2026-08-31 由用户纠正**。原来写的是「买 N 把＝场上 N 个同形态斧头」——用户：「我记得我说过买了第一把木斧后，后面的木斧购买都会化作经验值吧，那为什么会出现两个友军带两个斧头的情况？」**正确的是：玩家只能拥有一把**，之后每次购买 = +15 经验、不进背包。（需求原话本来就只说「购买该装备会使经验条+15」，配上「不升星」，第二把本来就只能是经验；是我把它读成了「还能再拿一把」。） |
| ⑨ | ~~已有的小木斧跟不跟着进化~~ | ✅ **已拍板：跟着进化**。否则手里会同时存在木斧与钻石斧两种形态，而经验条只有一条，说不清谁是谁 |
| ⑩ | ~~最终造物选完能不能改~~ | ✅ **已拍板：不能，本大轮锁定**（大轮重置时连同经验一起清） |
| ⑪ | ~~斧头怎么随档位换色~~ | ✅ **已拍板：C 调色板替换**（用户 2026-08-31「C」）—— 只出**一套**动画，斧头用**专属色段**，档位之间只换那几个颜色索引 |
| ⑫ | ~~「3%～10%」的口径~~ | ✅ **已拍板：整个货架至少出一个**（用户 2026-08-31）。⇒ 换算到每格 ≈ 0.3%～1.05%（10 格，`1-(1-p)^10 = P` 反解）。**实现时必须按这个反解式算每格概率**，不能把 3% 直接当每格用 —— 那是 10 倍的差 |

---

## 验收清单

**一期**

- [x] 055 游戏内显示为「病毒箭头」（图鉴实拍已补，v0.19.300 与换图后的 v0.19.301 各拍一次）（门禁 tests/verify_eq055_063.gd：名字与图鉴渲染）
- [x] 055 新图标是专属图、32×32、放进真控件渲出来看过（六个候选并排渲出，用户选 F）（门禁 tests/verify_eq055_063.gd：图在盘上 + 尺寸；并入 tools/data_integrity.py 的图标覆盖）
- [x] 055 **效果文案一字未动**（门禁 tools/text_prose_guard.py：文案的文字部分逐字未动）
- [x] 063 三环引爆 ＝ `25/40/70 + 目标最大生命×2/3/5%`，**真实伤害**（不吃护甲／魔抗）。常量落点 scripts/systems/equip/eq_spirit_batch.gd（门禁 tests/verify_eq055_063.gd）
- [x] 063 判据**拿两种最大生命各量一次**（否则定额与百分比两种实现同解，把百分比删掉照样绿）。常量落点 scripts/systems/equip/eq_spirit_batch.gd（门禁 tests/verify_eq055_063.gd）
- [x] 每条门禁都反向验证过会红（四条变异全红，第四条第一版是假的、当场被抓）（工具 tools/mutate_verify.py，逐字节还原 + git 第二只眼）

**二期**

- [x] 古灵精怪枪：+1 攻击力／+1 生命／**+1% 攻击速度**（不是 +1 次/秒）。常量落点 scripts/systems/equip/gremlin_gun.gd（门禁 tests/verify_gremlin_gun.gd）
- [x] 携带者**每次普攻**自己掉 1% 最大生命，**真实伤害**（判据要量「不吃减伤」）。常量落点 scripts/systems/equip/gremlin_gun.gd（门禁 tests/verify_gremlin_gun.gd：量「不吃减伤」）
- [x] FPGA板 登场时给**对方**随机 1/2/3 个敌人各一把（分母：真的挑中了 N 个不同敌人）。常量落点 scripts/systems/equip/equip_system.gd（门禁 tests/verify_gremlin_gun.gd）
- [x] FPGA板 **原有效果一条没丢**（新增不是替换）（门禁 tests/verify_gremlin_gun.gd）
- [x] 换路时「登场」要重新触发一次，且不吃 `_t` 跨路累加的亏（CLAUDE.md §3.4）—— 走 `_eq_apply_all_stats` 的 pending 模式，与 058/032 同一条路，每条路开战各跑一次；没有用到 `_t`（门禁 tests/verify_gremlin_gun.gd）

**三期（骨架）**

- [x] 召唤物动画：待机／走路／攻击／技能释放 四条，**四条各自登记在哪张表里都被门禁盯着**
  （门禁 tests/verify_summon_art.gd ④：待机→`_EQ_BODY_SPR`／走路→`ACTION_RUN`／攻击→`ACTION_ATTACK`／技能释放→`ACTION_ELITE`，
  每条都配了「表解析到 N 字」的分母；三条拆掉各自反向验证过会红）。
  ★这个勾**第一次打的时候是假的**：我打它是因为生成了四张精灵表，不是因为它们真的被引擎读到 ——
  当时 walk 与 cast **在盘上躺着、引擎里零引用**，而且那四张同时有三个错（朝向反了／只有龟一半高／6 帧只播 5 帧），
  全在这个勾底下躺着，直到用户喊「方向」。
  ★我还写下过一句**错的结论**「引擎没有 walk 通道」—— `ACTION_RUN` 一直都在（忍者／幽灵／三种小将／亡魂／训龟大师都在用），
  只是斧头没登记。用户 2026-09-01：「怎么可以没通道啊，小将动画就有攻击待机走路等」。**他是对的。**
- [x] **没有死亡、没有受伤动画**（用户 2026-08-31 与 09-01 两次点名）
  （门禁 tests/verify_summon_art.gd ④：`ACTION_HURT` 与 `ACTION_DEATH` 里一个 axe 键都不许有，
  配「表解析到 N 字」的分母；偷偷加一条死亡动画的变异当场红。
  另 tests/verify_axe_anim_runtime.gd：**走真入口** `_play_action(hurt)` / `(death)` 调下去，贴图纹丝不动）
- [x] 四条动作**真的会播**（不只是登记在表里）
  （门禁 tests/verify_axe_anim_runtime.gd：真战斗场 + 真召唤 + 真推着它跑，量 `u["anim_sd"]["tex"]` 的资源路径；
  走路推 33ms 就切且停下会换回 idle；施法期间普攻打断不了。三个变异全部当场红）
- [x] **调色板替换可用**：斧刃色阶与柄色相撞 = 0（tools/axe_palette.py，两条变异都验过会红）
- [x] 档位数与斧头色段**等长且一一对应**（档位表落点 scripts/gamedata/axe_evolution.gd，不在方案书里手抄份数）（门禁 tools/axe_palette.py 自检 + tests/verify_axe.gd）

- [x] 小木斧 1 费，给携带者 +20生命／+10攻击／+2护甲／+2魔抗（门禁 tests/verify_axe.gd）
- [x] 斧头召唤物登场：近战、1ATK、0.8 攻速、5 护甲／5 魔抗（门禁 tests/verify_axe.gd）
- [ ] ⏳ 召唤物属性随**历史累计** exp 变（✅ 公式已验）；**进化后进度条归零而累计值不变**要等四期真接上攒经验才验得了
- [x] 主动：140 龟能 → 召唤物回 5%最大生命 ＋ 5%最大生命护盾。常量落点 scripts/systems/equip/axe_system.gd（门禁 tests/verify_axe.gd）
- [x] 被动 2：普攻窃取目标 10% 护盾**转成普通护盾**给自己（特殊护盾也要能转，判据要拿特殊护盾试）。常量落点 scripts/systems/equip/axe_system.gd（门禁 tests/verify_axe.gd：拿特殊护盾试，两边都量）

**四期（进化与商店）**

- [x] 经验条：买 +15（`ShopScene._on_buy`）／打完一场 +10（`GameState.axe_on_match_end`，挂在 `_settle_season` 的**有赛季**分支里，demo/教程不白送）／斧头击杀或 3 秒内助攻 +2（`AxeSystem.on_death`）。（门禁 tests/verify_axe_evolution.gd）
  ★击杀与助攻**必须分两路**：on-hit 那一整块被 `u["alive"]` 挡着，**致命的那一下根本走不到** ⇒ 只靠触碰时间戳会漏掉「斧头自己打死的」那一半。门禁四种情形各量一次（亲手杀／3 秒内碰过／超时／没碰过）
- [x] 图鉴文案补上「经验怎么攒」「四个阈值」「只能拥有一把」「随大轮重置」。
  ★**撑破了商店详情框**（414px vs 246px，成了全表最长），是我自己造的真回归：`verify_shop_layout ⑥` 当场红。
  修法是压缩而不是放宽判据 —— 7 段并成 4 段（每段都浪费半行，合并是最大杠杆）+ 砍虚词与重复（「一把」「每秒」「给自己」、第二次出现的「砍伐」），
  **机制一条没丢、数值占位符一个没删**（丢掉 `ACTIVE_SHIELD_PCT` 就等于把它写死成「等量」）。414→345→276→**一屏放得下**
- [x] 大轮重置：五个字段（bar/total/stage/final/**syn_matches**）在 `reset_save` 与 `start_new_season` **两处**都清。（门禁 tests/verify_axe_evolution.gd）
  判据配了分母（先证明调用前确实非零，否则「归零」是恒真式），两个重置函数**各自**反向验证过会红
- [x] 四次进化阈值 80/110/130/160，每次清空进度条。落点 `AxeEvolution.advance()`（纯函数，门禁直接调）。（门禁 tests/verify_axe_evolution.gd）
  ★「只验 bar 归零」是假门禁 —— 配了「同一次进化 total 仍是 85」这条，把 total 也清零的实现当场红
- [x] 钻石斧后攒 400 → `AxeEvolution.final_ready()`。三条分母：差 1 点不 ready／没到钻石斧不 ready／已选过就本大轮锁定。（门禁 tests/verify_axe_evolution.gd）
  ⏳ 四选一的**商店卡**属六期（造物效果还没做，先弹卡等于给一个按不下去的按钮）
- [x] **玩家真的看得见进度条、真的选得到最终造物**（机制做完 ≠ 玩得到）
  （门禁 tests/verify_axe_shop_codex.gd ④：真商店重绘后从屏幕上抓「砍伐经验 40/80」这行字、抓四个按钮，
  并**真按下去**验最终造物定了；配了「没攒够点不动」「选完锁定」「经验封顶」三条 + 各自的分母；五个变异全红）
- [x] **不参与三合一升星**（EquipPool.NO_STAR；判据走**真合成入口** try_merge_all，并配了"换成 058 必须合得出 2★"的分母）（门禁 tests/verify_axe.gd）
- [x] 商店形态/售价/图标随进化变：`ShopScene._deco()` 返回**浅拷贝**，四个读点统一走它。（门禁 tests/verify_axe_evolution.gd）
  ★关键判据是「**没弄脏 DataRegistry 里的原件**」—— 改原件的实现前三条照样全绿，但会把落盘的货架冻在掷货那一刻。反向验证已证明这条会红
- [x] 商店卡片／商店详情／图鉴详情**真的画出**了对应形态的名字与图标，且商店与图鉴**叫同一个名字**
  （门禁 tests/verify_axe_shop_codex.gd：真场景真重绘 + 遍历节点树抓 Label 文本与 TextureRect **像素指纹**；三个变异全红）
- [x] 费用封顶 5 费：五档 1-5 + 四造物都是 5，逐项扫过（门禁 tests/verify_axe_evolution.gd）
- [x] 只能拥有一把：走**真 `_on_buy`**（实例化了 ShopScene），量三边 —— 背包不涨／经验涨了／**同一个 `_on_buy` 买 058 背包确实会涨**（这条分母证明「不涨」不是恒真式）（门禁 tests/verify_axe_evolution.gd）
- [x] 斧头羁绊：`tiers=[1]` 一件即激活；四张平行表键集一致；096 在 `p2eq-types.json` 里真映射到「斧头」。（门禁 tests/verify_axe_evolution.gd）
  `GameState.axe_synergy_active()` **直接问 `synergy_rows()`**，不自己数一遍装备（手抄的副本必然落后）
- [x] 独立出货概率：3% + 局数×0.1%，封顶 10%，**整货架口径**。（门禁 tests/verify_axe_evolution.gd：回代 + 蒙特卡洛）
  ★每格由 `slot_prob` 反解（`q = 1-(1-P)^(1/n)`），门禁**回代** `1-(1-q)^n == P` 查 6 组 m×n —— 写成 `P/n` 当场红；
  再加 4000 次蒙特卡洛量实测频率。激活时**先把 096 从常规池剔除**，否则两条路叠加、实际概率高于标称
- [x] 选完最终造物 → `shelf_mode` 返回 `off`，从池里剔掉不再出货（门禁 tests/verify_axe_evolution.gd）
- [x] 卖掉后：羁绊失效 + 概率回落常规 1 费档 + **经验与档位不回退**（赛季级字段，不跟着装备走）+ 无限件卖了不退张（分母：同一函数退 058 会涨）。（门禁 tests/verify_axe_evolution.gd）
  ⚠ 没驱动卖出 UI（`_sell_selected` 要一个带 `_sel_bench` 的 host），量的是卖出**之后的状态后果**与池子那一侧的真函数

**五期（被动 3～6）**

- [ ] ❌ 召唤物动画 6～10（强化挥砍／竖劈／180°横扫／蓄力／猛砸落斧）—— **机制已实装但共用攻击那套动画**，专属动作未做（PixelLab pro，五张）

- [x] 被动 3：每 9 秒一次强化 on-hit（击飞＋短暂击退＋0.5ATK 额外物理）。落点 `AxePassives.smash_on_hit`／常量在 `AxeEvolution`。（门禁 tests/verify_axe_passives.gd）
  ★存「下一次可用的时刻」而不是「已过多少秒」——后者要自己累加 delta，漏一帧就永远差一点且看不出来
- [x] 被动 4：每第 2 次普攻竖劈（+5%目标最大生命真伤 ＋10 层流血）。（门禁 tests/verify_axe_passives.gd）
  ★「真实伤害」的判据**第一版是假的**：我拿 500 护甲当杠杆，而这条路（`_apply_damage`）**根本不查护甲** ——
  护甲只在 `_resolve_dmg`／`_phys_after_armor`／`_dot_after_resist` 里算。`bucket=="tru"` 真正跳过的是 `flat_dr` 与 `damage_reduction`，判据改落到那两个上才红
- [x] 被动 5：180° 横扫与竖劈**交替**（两条共用一个普攻计数器，奇数横扫偶数竖劈）；效率层 5 秒、无限叠、每叠刷新整条时长。（门禁 tests/verify_axe_passives.gd）
  ★判据是「连打 6 下把每一下是什么列出来比」——只验「第 2 下是竖劈」看不出交替本身
- [x] 被动 6：治疗进 4 秒蓄力（梯形高每 0.5 秒 +100 码 → 满蓄 800 码、70% 减伤、效率计时中断），砸下击飞＋眩晕 3 秒＋4ATK。（门禁 tests/verify_axe_passives.gd）
  ★**梯形是本作没有过的形状**（原有只有圆与直线带）⇒ 写成纯几何 `AxeEvolution.in_trapezoid()`，门禁直接喂坐标验，不打真战斗去撞。
  ★「效率计时中断」那条**第一版是假的**：门禁直接调 `hold_eff` 循环，从没证明 `tick_charge` 会调它 ——
  把 `tick_charge` 里那句删掉，门禁一条都不红。改成走真 `tick_charge` 才红。
  ★梯形的两条底边宽度需求没给，我自拍 300／900（未决点 ⑬），满蓄是 300 宽 800 长张口 900 的扇形块
  ⚠ 蓄力中不再放主动 —— 否则 140 龟能一满就把蓄力重开，永远砸不下去
- [x] 四条被动各 +50生命／+5攻击／+3护甲／+3魔抗且**可叠加**（判据拿 `minion_hp/atk` 的差值验 ×4，不看常量本身）（门禁 tests/verify_axe_passives.gd）

### ★用户 2026-09-01 抓「方向」——三个错一起暴露，全是我该验没验的

用户只说了两个字「方向！！！」，追问后是：「左右没反吗，大小呢，你这样截图一点用都没有啊，
你这个调试场不够干净你怎么看啊」。**他是对的** —— 我拿全场战斗截图眯眼看就下了「召唤物正常」的结论，
而 memory 里白纸黑字写着 [[fb-clean-vfx-stage-not-squint]]（验特效要建干净调试场）与
[[fb-acceptance-checklist]]（交窗前查朝向/单帧立绘/大小）。

量出来三个错（不是看出来的）：

| 问题 | 量法与数值 | 修成 |
|---|---|---|
| **左右反了** | 四套动画的护目镜横向重心全在身体中心**右侧** +2.3~4.0px ⇒ 素材朝右；而 `ART_FACES_RIGHT` 是 `["hiding","headless","mech","__trainer__"]`，**没有 axe** ⇒ 引擎按「原图朝左」翻转 | 登记进表 |
| **只有龟的一半高** | 世界身高 = 内容 61px × pixel_size = **0.708 m**；28 只龟中位 **1.40 m** ⇒ **51%** | `col_size` 26→48 ⇒ **1.31 m（94%）** |
| **6 帧只播 5 帧** | 根因是 `drop_last=true`，而三张表**末帧都有内容**（手铳 577／急救塔 2090／斧头 2506 个不透明像素） | 关掉 drop_last |

★第三条我一开始**归错因**（怪那个写死的 `"frames": 16`）。反向验证打脸：那个数被
`mini(declared, frame_total)` 钳着，单改它**一条都不红**、行为没变。真凶是 drop_last。
这个事实写进了门禁注释，免得下一个人修错地方。

★★**为什么记了却做不到，以及这次做了什么不一样的**：memory 是一段描述，
没有任何东西强制它执行 —— 它靠我在正确的时刻想起来。焊进 `run-tests.sh` 的不靠记忆。
⇒ 新增 `tests/verify_summon_art.gd`（14 条，进门禁）：

- 判据落在**素材像素与代码常量**上，**不落在截图**上（人眼看会判反，量像素不会）
- 朝向两个方向都卡：素材朝右必须登记、朝左必须不登记
- 尺寸判据落在**龟身高的真实分布**上（28 只实测），不是我拍的一个数
- 帧数判据**走真 `_resolve_summon_sprite()`**——第一版我写的是源码子串匹配，
  反向验证证明它是假的（源码换了行就匹配不上，一条都不红）
- **通用**：以后任何召唤物进 `_EQ_BODY_SPR` 都自动被这三条盖住

**六期（最终造物）**

- [ ] ❌ 召唤物动画 11～14（重生／远程投掷／插地／处决）

- [ ] ❌ 亡灵之斧：300 码环每秒 1%最大生命魔法伤害／每敌每秒回 0.3%／死后 2.5 秒带 40% 复活。常量落点 scripts/systems/equip/axe_final_forms.gd
- [ ] ❌ 炽天使：射程 300、普攻 8 层灼烧、主动改 10 把回旋镖（**结算不许埋在 tween 末尾**）。常量落点 scripts/systems/equip/axe_final_forms.gd
- [ ] ❌ 全息斧：普攻给最低血友军 60 盾＋5 龟能、主动插地 30%减伤 ＋ 600 码光环。常量落点 scripts/systems/equip/axe_final_forms.gd
- [ ] ❌ 余烬：种子层每层 +0.5% 处决线、处决 +150 龟能、主动是**独立叠加**的 4 秒 buff（不是刷新）。常量落点 scripts/systems/equip/axe_final_forms.gd
- [ ] ❌ 四选一的入口＝**商店里的一张卡**；选完**本大轮锁定**不能改选

**全期共通**

- [ ] ❌ `bash run-tests.sh` 全绿
- [ ] ❌ 版本号五处 ＋ tag ＋ CHANGELOG ＋ 路线图 ＋ CI 绿
- [x] 未决点 ①②④⑤⑥ 已定（①+1% ／ ⑤能打死 ／ ⑥历史累计 由用户拍板；②④ 我按建议自拍）（门禁 tests/verify_gremlin_gun.gd 与 tests/verify_axe_evolution.gd 各自焊住了这些决定）
- [x] 未决点 ③⑦⑨⑩ 已定（用户 2026-08-31「按你说的」）（门禁 tests/verify_axe_evolution.gd：费用封顶 5 / 本大轮锁定）
- [x] 未决点 ⑧ **已由用户纠正**：只能拥有一把，后续购买化作经验（我原来定反了）（门禁 tests/verify_axe_evolution.gd：只能拥有一把，三边都量）
- [x] 未决点 ⑫ 已定：**整个货架至少出一个**（用户 2026-08-31）；每格概率由 `1-(1-p)^格数 = P` 反解，格数读 scripts/gamedata/phase2_config.gd（门禁 tests/verify_axe_evolution.gd：反解回代 + 蒙特卡洛）
- [x] 未决点 ⑪ 已定：**C 调色板替换**（用户 2026-08-31）（门禁 tools/axe_palette.py 自检：9 档色阶两两不撞、都不与柄色相撞）

---

## 版本留痕

| 版本 | 日期 | 内容 |
|---|---|---|
| 0.19.300 | 08-31 | **一期**：055 靶向器→病毒箭头（新图·效果不动）· 063 三环引爆加 %目标最大生命 |
| 0.19.301 | 08-31 | 055 换图（F→J）：自查时判定 F 读成狼牙棒，重做 |
| 0.19.302 | 08-31 | **二期**：古灵精怪枪 ＋ FPGA板登场塞枪；055 图标再换（用户选 B） |
| 0.19.303 | 08-31 | **三期骨架**：新装备 096 小木斧 ＋ 斧头召唤物 ＋ 主动 ＋ 被动1/2 ＋ 九档配色 |
| 0.19.304 | 09-01 | **三期美术**：召唤物立绘 ＋ 四套动作（pro 模式）＋ 九档图标归一 |
| 0.19.305 | 09-01 | **四期 进化与商店**：砍伐经验（买／场／击杀助攻）＋ 四次进化 ＋ 商店动态形态售价 ＋ 只能拥有一把 ＋ 斧头羁绊独立出货概率 ＋ 大轮重置 ＋ 图鉴文案 |
| 0.19.306 | 09-01 | **五期 被动3~6** ＋ 用户抓「方向」: 斧头朝向/尺寸/帧数三修 |
| 0.19.307 | 09-01 | 顺手修一条偶发红的门禁 verify_copy_rules（与小木斧无关，同批推） |
| 0.19.308 | 09-01 | plans_lint 规则⑦「打勾必须点名门禁」＋ 本份还清 39 条欠账（那条假的勾降级成 ⏳） |
| 0.19.309 | 09-01 | 走路/技能释放两条通道接上（"引擎没有 walk 通道"是我写错的）＋ 四条动作逐条焊进门禁 |
| 0.19.310 | 09-01 | 动作**运行时**验证：真战斗里量引擎拿的是哪张贴图（"登记在表里 ≠ 真的播出来"） |
| 0.19.311 | 09-01 | 商店/图鉴**渲染**验证：抓屏幕上的文本与像素；修「同一件装备两个名字」 |
| 0.19.312 | 09-01 | **进度条 UI ＋ 最终造物四选一入口**（机制全绿但玩家看不见/选不了）＋ 经验封顶 |

## 实施回填

### 一期（v0.19.300）已交付

| 项 | 结果 |
|---|---|
| 055 | `靶向器` → **`病毒箭头`**；图 `targeter.png` → `virus-arrow.png`（32×32，PixelLab 六选一，用户选 **F**：病毒球＋白羽尾）；**效果文案一字未动**，`HookBombSystem` 类名保留 |
| 063 | 三环引爆 `25/40/70` → `25/40/70 + 目标 maxHp × 2/3/5%`，仍是真实伤害；常量 `RING_FLAT` / `RING_MAXHP_PCT` 收在一处 |

门禁 `tests/verify_eq055_063.gd` 九条全绿，反向验证四条全红
（删掉百分比那半／百分比写错一档／真伤改物理／两环就引爆）。

### ★判据分两层，这是这轮最重要的设计

**精确值判据落在纯函数 `_ring_boom_dmg(tgt, si)` 上**，端到端那层只验对倍率不敏感的性质。
理由是端到端要穿过一整条伤害管线的全局乘数；拿它对精确值，等于把所有增伤机制都焊进这条判据，
以后任何人改增伤都会误伤这里。（CLAUDE.md §3.5 同一条道理：要验的数值不该只存在于结算链中间。）

### 二期（v0.19.302）已交付

| 项 | 结果 |
|---|---|
| 古灵精怪枪 | 新文件 `scripts/systems/equip/gremlin_gun.gd`。+1 攻击／+1 生命／**+1%** 攻速；每次**普攻**自伤 `1% 当前最大生命`，**真实伤害且能打死自己** |
| FPGA板 040 | 新增一条「登场时向随机 1/2/3 名敌人各塞一把」。**原有的 2-bit 四态一条没动** |

门禁 `tests/verify_gremlin_gun.gd` **24 条**全绿；反向验证 **7 条**全红。

### ★三个把它写成假门禁的陷阱，都堵上了

1. **「+1% 攻速」不能只判「aspd_perm 变大了」** —— `+1`（每秒多打一次）也会变大，
   而那正是未决点 ① 要防的错。判据量**差值恰好等于 0.01**。
   反向验证把它改成字面 `1.0`，三条断言同时红。
2. **「真实伤害」要拿产品真读的字段** —— `def` / `flat_dr` / `damage_reduction`。
   （一期我喂 `armor` / `dr_mult`，全仓没人读，那条断言当场被反向验证抓成假的。）
3. **「给对方」不能只数「有几个人拿到枪」** —— 全给自己人也照样是那个数。
   判据拆成三条：给的把数对 ／ **己方拿到 0 把** ／ **没有敌人拿到 2 把**（不重复）。
   反向验证「把枪塞给自己人」「允许重复」各让三条红。

### ★两个实现上的选择，都是踩过的坑决定的

- **自伤走 `_apply_damage(u, dmg, col, null, "tru", is_self=true)`** 那条**无来源**路，
  不走 `_apply_damage_from(自己, 自己, ...)` —— 后者会把自己算成攻击者，
  去触发一圈 on-hit（反伤／吸血／法力）。CLAUDE.md §3.3：两条伤害路各自扣盾扣血，选错一条
  就会出现只在某类伤害下的诡异行为。
  （⚠ 我第一版把 `true` 传在第 4 位，那一位是 `src` 不是 bool。）
- **「登场时」走 pending 模式**（`_gremlin_pending` → 首帧消费），与 058 炮台／032 骷髅同一条路。
  开战 setup 期间敌方单位还没全进 `battle._units`，当场挑人会挑不到人。
  它也**不读 `_t`** ⇒ 天然不吃「`_t` 跨路累加」那个亏（CLAUDE.md §3.4）。

### ★这轮踩的两个坑

| 坑 | 怎么发现的 |
|---|---|
| **heredoc 里的 `\n` 变成真换行，撑断了 json** | `json.loads` 报 `Invalid control character`。**memory 里记过这条**（[[fb-heredoc-backslash-n-corrupts-json]]），还是又踩了 ⇒ 写字面反斜杠一律用 `chr(92)` |
| 文案引用了 `GremlinGun.*`，而实现 040 的类不是它 | `text_const_owner_audit` 报「跨主体引用」。这**是**正当的（040 塞出去的本来就是别的东西），按它要求的方式加进 `EXEMPT` 并写明理由，而不是把常量在 FPGA 里再存一份 |

### 三期（v0.19.303）代码骨架已交付；召唤物美术还没做

| 项 | 结果 |
|---|---|
| 新装备 096 小木斧 | 1 费 · **遗物**（与 058 炮台同类）· +20生命/+10攻击/+2甲/+2抗 · 图标 `wood-axe.png` |
| 斧头召唤物 | 近战 · 1×ATK · 0.8 攻速 · 双抗 5 · 血 `500+累计经验` · 攻 `30+0.05×累计经验` |
| 通用主动 | 召唤物自己攒 140 龟能 → 回 5% 最大生命 ＋ 给自己 5% 最大生命护盾，龟能清零 |
| 被动 2 | 普攻窃取目标 10% 护盾**转普通护盾**给自己（圣光那种"特殊盾"也照偷） |
| 被动 1 | **不升星**（`EquipPool.NO_STAR`）＋ **卡池无限**（`EquipPool.UNLIMITED`） |
| 九档配色 | `tools/axe_palette.py` 一键出木/石/铁/金/钻＋四个最终造物，**柄一个像素不动** |

门禁 `tests/verify_axe.gd` **33 条**全绿；反向验证 **8 条**全红。

### ★★纠正一条我在「调查」里写错的事实

我原来写「卡池没有数量限制**已经天然成立**，不用做」——**错的**。
商店出货有**两层**：`phase2_equip.roll_shop` 那层确实是无限有放回，
但 `ShopScene` 先用 `EquipPool.available()` 按**私人池剩余张数**过滤过一遍
（`GameState.equip_pool`，每件按费用有 31/26/19/11/10 张，买走扣、卖出退、赛季补满）。
**我只看了第一层就下了结论。** 所以「无数量限制」是一条**真要做的功能**，已实现并焊了门禁。

### ★这份门禁里四条最容易写假的，各自怎么堵的

| 要验的 | 写假的写法 | 实际判据 |
|---|---|---|
| 不升星 | 断言"名单里有它" ＝ 数我自己插的标记 | 喂三件 1★ 进**真合成入口** `try_merge_all`，看它合不合；**再配一条分母**：换成 058 同样三件必须合出 2★（否则"没合"可能是合成入口整个坏了） |
| 卡池无限 | 断言 `left()` 返回大数 ＝ 恒真式 | **真买 20 次**看张数掉不掉；分母：换成 058 买 3 次后归 0 且第 4 次买不到 |
| 偷盾 | 只验自己涨了盾 ⇒ 凭空造盾也绿 | **两边都量**：目标掉多少 ＝ 自己涨多少；再加"没盾的偷到 0"「不是斧头的偷不到」两条分母 |
| 历史累计 | 只验一个字段 | 公式喂 0 与 200 两个经验值各量一次；`GameState` 里**存两个字段**（进度条/累计），一次加经验必须同写两边 |

### ★调色板替换（未决点 ⑪ 的 C）已经跑通，不是纸上谈兵

`tools/axe_palette.py` 既是生成器也是门禁：

- 基底是 PixelLab Pro 一次出的 64 个候选里挑的 #19（竖柄＋单侧扇形刃，**刃与柄天然分色**）。
- 刃的 9 个颜色排成一条色阶，每个档位只换这 9 个索引 ⇒ **柄一个像素不动**。
- 焊了两条硬约束，**都反向验证过会红**：
  ① 任何档位的刃色不许与柄色相撞（撞了＝换成金斧连柄也变金）
  ② 档位两两之间最亮级色差 ≥ 40（否则玩家看不出进化）

### ★这轮我自己的五个错

| 错 | 怎么发现的 |
|---|---|
| **方案书里断言「卡池无限已成立」** | 加装备时读到 `GameState.equip_pool` 才发现有第二层过滤。只看一层就下结论 |
| 前四次生成的斧头图标**全是锤子/镐** | 放进真控件看出来的。改成**竖柄姿势**＋换 Pro 模型才拿到真斧头 |
| **`\n` 被多转义一次**，游戏里显示成字面的 `\n` | 图鉴实拍看出来的。今天**第二次**栽在这个字符上（上次是 heredoc 把它变成真换行撑断 json） |
| 不升星的装备**还显示着「数值分档 ★1/★2/★3」** | 图鉴实拍看出来的 —— 它永远只有 1★，摆三档图例是对玩家说谎 |
| 改图鉴时**猜了变量名** `it`，真名是 `eq` | 详情面板整个空了。报错就写着 `Identifier "it" not declared`，我没先去看函数签名 |

### ★图标最终定在候选 B（v0.19.302）

用户 2026-08-31「箭头选B吧」。B 在真框里的读感：杆**穿过**病毒球、右上露出箭尖，
所以剪影不是 F 那种「球顶在杆头」的狼牙棒；病毒感是六个候选里最强的。

### ★图标返工一次（v0.19.301）—— 用户定的工作方式：「搞完你自己每个部分看图验证，不行的修改或重做，一直迭代」

v0.19.300 上线的那张（候选 F）**我自己看图判定不合格**：放进真图鉴框按实际渲染尺寸看，
剪影是「带刺球 + 短柄」＝**狼牙棒**，白羽尾还长在杆中间，箭镞完全没有。
重出五张、全部**贴进真框**比，选 J（白羽尾在左下 · 长杆 · 右上紫绿感染箭镞）。

**★教训：候选图必须放进真控件按真尺寸看。**
孤立地看 32×32 缩略图（甚至放大 8 倍的对照条）判不出剪影读感 ——
我在缩略图上把 F 选成「唯一同时读出病毒和箭的」，一进框就露馅。
实测详情页图标渲染尺寸是 **66px**（32×32 素材放大 2.06 倍），
以后挑图标一律先合成进这个框再比。

### ★这轮我自己的四个错

| 错 | 怎么发现的 |
|---|---|
| 判据成员名写成 `_equip_sys._spirit`，真名是 `_spirit_sys` | 六次调用全抛「Invalid access to property」，伤害量到 0。**和 036 那轮同一个形状**（我把 `battle._equip_tick_sys` 写成 `_equip_sys._tick`） |
| **「是真实伤害」那条判成了恒真式** | 上面那个错让伤害全是 0，而 `absf(0-0) < 1.01` 判 PASS。⇒ 补 `soft > 1.0` 这一半。**判据要自带分母** |
| 拿端到端量精确值，同一组参数跑出 444／666 两个数 | 探针打出来：`src.id=="basic"` 触发**小龟·不屈**（按目标稀有度增伤 ×1.2），再叠 25% 概率暴击 ×1.5。**这不是回归，是测试自己不稳** |
| **喂的减伤字段名全是我编的** | 反向验证把 `raw=true` 改成 `false`，「是真实伤害」那条**一条都没红**。产品读的是 `def`／`flat_dr`／`damage_reduction`，我喂的 `armor`／`dr_mult` 全仓没人读。⇒ 判据没错，被测对象根本没被布置成那样 |

### 顺带纠正一个我之前说错的事实

我早先说「32×32 是装备图标的主流」——**错的**。实测 `assets/sprites/equip/` 里
**64×64 有 69 张、32×32 只有 19 张**，而那 19 张里 16 张是宝箱战利品、4 张是今天新做的。
新图仍按 32×32 做，是因为用户点名要 32、且今天已过的四张都是 32，跟它们保持一致。

## 决策记录

| 时间 | 谁 | 定了什么 |
|---|---|---|
| 2026-08-31 | 用户 | 提出本批需求（原文见上） |
| 2026-08-31 | 我 | 查明「PFGA板」实为 `FPGA板 p2eq_040`；「卡池无数量限制」现状已成立、不算工作量 |
| 2026-08-31 | 我 | 分六期交付，估计 9.5～14 天 |
| 2026-08-31 | 用户 | **⑥ 历史累计 · ① +1% · ⑤ 能打死** |
| 2026-08-31 | 我 | ②「一整把」＝打完一场对局 · ④ 055 只改显示名（按建议自拍，用户未反对即生效） |
| 2026-08-31 | 我 | 摸底时新冒出 ⑧⑨⑩ 三个未决点（多份小木斧／已有的跟不跟着进化／最终造物能否改选） |
| 2026-08-31 | 用户 | 「按你说的」⇒ **③封顶5费 · ⑦商店四选一卡 · ⑧共享同一形态 · ⑨已有的跟着进化 · ⑩本大轮锁定**。至此 ①～⑩ 全部有主 |
| 2026-08-31 | 我 | 开工一期（055 改名＋新图标 · 063 三环加 %最大生命真伤） |
| 2026-08-31 | 用户 | 055 新图标六选一 → 选 **F**（病毒球＋白羽尾） |
| 2026-08-31 | 用户 | 追加：斧头召唤物＝小将换新色＋手持斧头，斧头颜色随档位变（落在三／四期） |
| 2026-08-31 | 版本 | 一期 = v0.19.300 |
| 2026-08-31 | 用户 | 问「召唤物有哪些动画有制定在方案书里面吗」⇒ **原来没有**，已补 14 条动画清单＋风险 6＋未决点 ⑪ |
| 2026-08-31 | 用户 | ⑪ 选 **C 调色板替换** ⇒ 至此 ①～⑪ 全部有主，三期美术可开工 |
| 2026-08-31 | 用户 | 定工作方式：「搞完你自己每个部分看图验证，不行的修改或重做，一直迭代」 |
| 2026-08-31 | 我 | 自查判定 055 图标 F 不合格 → 重做选 J（v0.19.301） |
| 2026-08-31 | 用户 | 055 图标最终选 **B**（紫色病毒球串在箭杆上）——「箭头选B吧」 |
| 2026-08-31 | 版本 | 二期 = v0.19.302 |
| 2026-08-31 | 我 | **纠正**：方案书「调查五」写的「卡池无数量限制已成立」是错的，实为真功能，已实现 |
| 2026-08-31 | 版本 | 三期骨架 = v0.19.303（召唤物美术与 5 个动画未做） |
