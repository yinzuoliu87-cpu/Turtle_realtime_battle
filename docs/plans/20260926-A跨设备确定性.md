# 20260926 — A 阶段：跨设备确定性（周日按原稿做完的前提）

> 状态：**实施中**（尺子已进门禁 · `d252aef4`；CI 的跨平台对比结果待回填）
> 上游：`docs/design/斗龟场大轮赛制方案v2-用户原稿.md` §五.1 / §五.3

---

## 1. 需求原文

用户原稿 §五.1，逐字：

> **确定性模拟是全部前提**：同种子 + 同双方阵容摆位 ⇒ 必然同结果。一切随机（暴击、索敌、
> 技能判定）走同一种子驱动的 PRNG；禁止裸调 `Math.random()`；**禁用依赖帧率/浮点平台差异的
> 逻辑**（定点数或固定步长模拟）。…此项**排期在赛制上线之前**。

§五.3：

> **结算权在服务端**：结算一次、存档一份（种子 + 双方阵容包 + 结果摘要）；客户端只做重放渲染。

用户 2026-09-26：「**你还要我花多少时间，我方案书都摆给你了？**」
⇒ 原稿已经定死了做法，不再开选项。按 A → B → C 做。

---

## 2. 调查

### 2.1 现在的周日为什么会「两边都赢」

服务端**一场都不打**：只有一张 `finals_results` 表等客户端来报。
A 和 B 各自在自己机器上跑的是**两场不同的对局**：

| | A 机上 | B 机上 |
|---|---|---|
| 左队（活的） | A 的真阵容 | B 的真阵容 |
| 右队 | B 的**快照** | A 的**快照** |
| 种子 | A 这台的 | B 这台的 |

`finals_report` 是 `on conflict … do nothing`（先报的算），后报的那份**静默丢掉**且仍返回
`ok: true`；而那个客户端的 `finals_settle(won)` **一个字都不看服务端答复**，已经发过币、
加过胜场、播过胜利屏。⇒ 不是竞态，是「服务端结算 + 重放」整块没做时的必然结果。

### 2.2 已经有的部分

| | 状态 |
|---|---|
| 一切随机走种子化 PRNG | ✅ 基本做到：sim 路径用 `_battle_rng`；剩下的裸 `randf` 都在演出侧（飘字抖动/火花） |
| 固定步长 | ✅ `_deterministic` 模式每帧恰跑一步 `SIM_DT`；tween 由 sim 步喂（`custom_step`） |
| 同种子**同进程**两遍逐位一致 | ✅ `verify_determinism_b` 41 条，9 个场景逐步指纹 |
| 同种子**跨进程 / 跨平台**一致 | ❓ **本方案要量的就是这个** |
| 服务端结算 + 重放存档 | ❌ 一行没做（B 阶段） |

### 2.3 ★为什么必须实测，静态扫答不了

IEEE-754 **只**保证 `+ − × ÷ sqrt` 逐位一致（它们要求正确舍入）。
**不保证** `sin / cos / tan / atan2 / pow / exp / log` —— 那几个是各家 libm 各自的近似实现，
差一个 ulp 就够让一局分叉。

全仓 sim 路径上确实在用（2026-09-26 扫，已排除 `*_vfx.gd`）：

| 文件 | 处数 |
|---|---|
| `RealtimeBattle3DScene.gd` | 31 |
| `star_system.gd` | 20 |
| `hookbomb_system.gd` | 17 |
| `chest_system.gd` / `headless_system.gd` / `signal_wave_system.gd` | 11 / 10 / 10 |
| `phoenix` / `hiding` / `equip_system` | 9 / 9 / 9 |

★**但原始计数虚高**：逐条看下去，绝大多数是住在 system 文件里的**演出代码**
（`wisp.position = battle._world_pos(center + Vector2(cos(a), sin(a)) * r, h)` 这种），
不进指纹。真正喂到 **sim 状态**（`u["pos"]` / 伤害 / 索敌）的只有这几处：

| 位置 | 干什么 |
|---|---|
| `rocket_system.gd:93` | `uu["pos"] = base - aim * (5.0 * sin(t * PI))` —— 直接写单位坐标 |
| `star_system.gd:755` | `oc["pos"] = c + Vector2(cos(ang), sin(ang)) * rr` —— 环绕体坐标 |
| `RealtimeBattle3DScene.gd:5758` | `u["pos"] = u_start - dir * (18.0 * sin(p * PI))` —— 攻击后仰 |
| `RealtimeBattle3DScene.gd:6510` | `var to = u["pos"] + Vector2(cos(ang), sin(ang)) * SPIKE_FLY` —— 弹道终点 |
| `cyber_system.gd:152` | `var cand = u["pos"] + Vector2(cos(aa), sin(aa)) * 200.0` |
| `crystal_system.gd:318` | `atan2` 吃两只单位的坐标 |
| `shell_system.gd:440` / `hiding_system.gd:542` | 起点/落点，待逐条确认是否进 sim |

⇒ **数数只能列出嫌疑，答不了「差多远」。** 只有把同一份输入在两个平台上各跑一次对一下才知道。

---

## 3. 与现状 / 上游的出入

| # | 出入 | 处理 |
|---|---|---|
| 1 | 原稿要「定点数或固定步长」二选一；现状是**固定步长**（没有定点数） | 固定步长已做到且有门禁。定点数是重写全部数值逻辑，代价远大于收益 —— **先量，量出不一致再决定要不要局部定点化** |
| 2 | 原稿说这项「排期在赛制上线之前」；而周日**已经上线了**（`PHASE_MODE_LIVE[FINALS] = true`，v0.19.446 翻的） | 顺序确实反了。本方案是补回来。不回退上线（10 人测试期需要它跑着），但「两边都赢」在 B 做完前一直存在 —— 已在 CHANGELOG 与路线图登记 |
| 3 | 原稿 §五.2 还要求「**同步撮合**：点匹配 → 锁定 → 超 20 秒降级快照 → 共享种子结算一次」 | 那是 B/C 的事（在线撮合）。A 只管「同输入同输出」这一条 |

---

## 4. 方案

**A 分两步，这次只做第一步（尺子）。**

### A1 尺子（本次）
1. 场景表抽到 `tests/_det_scenarios.gd`，**全仓唯一一份**，两个门禁共用。
2. 新门禁 `verify_determinism_cross`：9 个场景的逐步指纹各压成一个 SHA-256，
   与钉在 `tests/golden/determinism_cross.json` 的金标逐字比。
3. 金标在本地（Windows / MSVC libm）生成 ⇒ **CI（ubuntu / glibc）每次 push 都在替我做
   两平台对比**，不用手工两边跑。

### A2 修（等 A1 的结果）
- CI 绿 ⇒ Windows↔Linux 逐位一致。剩下的缺口只有 **ARM**（见 §6.1），B 阶段可以开工。
- CI 红 ⇒ 门禁会报第一个分叉步（`TURTLE_DET_DUMP=1` 导出逐步指纹离线 diff），
  顺着 §2.3 那张表逐个换成确定性实现。

---

## 5. 验收清单

| # | 子需求 | 判据落在哪 | 状态 |
|---|---|---|---|
| A1-1 | 场景表只有一份，两个门禁共用 | 两边都断言「表里正好 9 个场景」 | ✅ |
| A1-2 | 同进程复跑摘要逐字相同 | `verify_determinism_cross` ① | ✅ |
| A1-3 | **同机跨进程**两次 9/9 逐字相同 | 手工跑两个进程比过（这条不成立，金标全是噪声） | ✅ |
| A1-4 | 9 个场景出 9 个**不同**摘要 | ② | ✅ |
| A1-5 | 换种子摘要必须变 | ③ | ✅ |
| A1-6 | 每个场景都真的在推进（不同指纹 > 1 且累计承伤 > 0） | 每场景一条分母 | ✅ |
| A1-7 | 周锚点钉死（不跟着星期几变） | `PIN_WEEK_ANCHOR` 常量，两个门禁共用 | ✅ |
| A1-8 | `verify_determinism_b` 覆盖面没缩 | 抽表前后 PASS 数 40 → 41（多的是表的分母断言） | ✅ |
| A1-9 | 全套门禁 | `GATE_RC=0` · ALL PASS (389/389) | ✅ |
| **A1-10** | **CI（Linux/glibc）跑同一份金标** | CI 门禁绿/红 —— 这就是 A 的答案 | 🔲 跑中 |

---

## 6. 已知风险与未决点

### 6.1 已知风险

| 风险 | 缓解 |
|---|---|
| **ARM 完全没验** —— 玩家跑 iOS(ARM64)，而 CI 只出 IPA、不跑测试 | 本门禁证明的是 Windows/MSVC ↔ Linux/glibc。**在 ARM 验过之前不许声称跨设备确定。** 两条路：① 加 macOS/ARM runner 跑同一份金标 ② 做一个「真机跑一次并把摘要回传」的一次性工具。②更省，但要装机一次 |
| 只覆盖这 9 个场景 | 场景外的技能/装备没被量过。加场景的成本很低（表里加一条），但每加一条金标要重生成 |
| 金标会因**任何**玩法改动而变 | 这是它的本意（数值改了摘要就该变），但它会让「改平衡」多一步「重生成金标」。⇒ 门禁失败时打印整段可贴的 JSON |
| Godot 版本升级会让金标失效 | 同上，属于预期。升级时重生成并在提交信息里写清 |

### 6.2 未决点

| # | 问题 | 我的处理 |
|---|---|---|
| U1 | ARM 怎么验（§6.1 两条路） | **等 A1-10 的结果再定**：Windows↔Linux 就不一致的话，ARM 更不用谈，先修那个 |
| U2 | 若不一致，是「局部换确定性实现」还是「整体定点化」 | 先看分叉在哪几处。§2.3 那张表只有 7 处喂进 sim ⇒ 大概率局部换就够 |
