# LOL 塞恩（Sion）Q · Decimating Smash — 视觉参考

> 用户 2026-09-03:「给我去搜lol塞恩机甲皮肤的Q，逐帧看」

## 来源

- 视频：`Mecha Zero Sion Skin Spotlight - League of Legends`（YouTube `V4YEUY_yiT0`，227 秒）
- Q 出现在 **115.2~117.5 秒**（屏幕上有 `Decimating Smash` 字样）
- 抽帧：30fps（`ffmpeg -ss 114.8 -t 2.4 -vf fps=30`）

★ **下载要点**（下次别再踩）：
- 必须走代理（`HTTPS_PROXY`），绕过会超时
- 必须强制 **H.264**（`format='best[vcodec^=avc1]'`）—— 默认给 AV1，本机 ffmpeg 解码失败，
  227 秒的片子只抽出 54 帧还不报错
- 找技能段的办法：先 1fps 抽全片，算**相邻帧灰度差**排序，变化最大的时刻就是技能爆发

## 逐帧量到的（这才是参考的价值，不是"看着像"）

| 项 | 参考 |
|---|---|
| **预警区填充** | **零** —— 地面石板缝、草、掉落物全部清晰可见 |
| 边线 | 一条约 4px 的青色**发光线**（中间亮、边缘渐隐） |
| 边线宽度比 | 4px / 200px 区域长 ≈ **2%** |
| 边线形状 | 长条形，**末端是圆弧**不是直角 |
| 蓄力进度指示 | 边线上有**流动的光点**从角色往远端跑 |
| 区域随蓄力 | **变长**（不变宽） |
| 命中 | 中心白光 + **六边形蜂窝网格**炸开覆盖整个预警区 |
| 余韵 | 网格渐隐（橙→白灰）+ 目标击飞 |

## 照它改了什么（斧头钻石斧·被动6 蓄力猛砸）

| | 改前 | 改后 |
|---|---|---|
| 预警区填充 | 0.22 琥珀（实拍读成"一块板"，敌人站上去像贴纸） | **0.07 上限**（黑场留极淡底噪，否则暗处边界断开） |
| 边线宽度 | 14 码 / 800 = 1.75% | **8 码 / 800 = 1%**（参考是发光线，我是硬边实心带，同比例下显粗一倍 ⇒ 收细补视觉重量） |

## 还没做的（记在这，别忘）

1. **边线上流动的光点** —— 参考用它表达蓄力进度；我现在靠"每 0.5 秒长一格 + 闪一下"
2. **命中改成六边形网格** —— 我现在是一张贴地陨石坑素材，**还有方块边缘**的毛病
   （素材内容铺满整张 128×128 画布，贴地后能看到直角边）

---

## 第二个样本：原版塞恩「Q Decimating Smash」专门演示（2026-09-03 补）

来源 YouTube `di3qPeiLa1I`，8 秒，225 帧 @30fps，抽帧在 `C:/tmp/sionref/qonly/`。

**这是两个样本里唯一一次能看清全程的 Q。** 逐帧目视定位（三种颜色判据全失效，见下）：

| 归档图 | 帧 | 时间 | 画面 |
|---|---|---|---|
| — | f88 | 2.933s | 干净，无任何预警 |
| `orig-onset-f90-flash.png` | **f90** | **3.000s** | **脚下炸出红色亮点 + 一圈外环，最亮** ← 蓄力起手 |
| `orig-onset-f92-stretch.png` | f92 | 3.067s | 亮点被**横向拉成一道条**（顺 Q 方向），亮度略降 |
| `orig-onset-f94-shrink.png` | f94 | 3.133s | 收缩回小亮点，更暗 |
| — | f96 | 3.200s | 起手闪消失，预警区这才刚显形（极淡） |
| `orig-charge-f110-peak.png` | f110 | 3.667s | 预警区**最深最广** |
| — | f114~f118 | 3.80~3.93s | 反而**回落变淡**（脉动，非单调递增） |
| `orig-release-f120-blade.png` | **f120** | **4.000s** | **释放**：整片深红扇形斧刃 + 472 伤害铺开 + 击飞 |

**蓄力时长 = f90 → f120 = 整 30 帧 = 1.000 秒。**

### 判据踩坑（下次别重走）

| 判据 | 结果 |
|---|---|
| 青色（机甲皮肤 Q 色） | 这个视频基线 **0** —— 原版 Q 是**红色**不是青色 |
| 红色 | 基线 **1750** —— 这张地图本身是**橙红泥土地**，判据抓的是地面 |
| 帧间差分 | 只定位到 4.0~4.7s 一大段，分不出蓄力起点 |
| **目视逐帧接触印相** | ✅ 唯一可靠 |

⇒ **颜色判据依赖「背景不含这个颜色」这个前提。前提不成立时，人眼看接触印相比任何自动判据都可靠。**

### 满蓄样本：**没有**

塞恩 Q 最长蓄 2 秒；两个样本分别是 **0.40 秒**（机甲 spotlight）和 **1.000 秒**（本样本），都是半蓄。
再下两个视频都失败（`duration<400` 过滤 / HTTP 403）。
判定不阻塞：蓄力全程形态在这 1 秒里已录全，满蓄只是更久更长；我们的 4 秒是玩法数值，本就不照抄时长。
