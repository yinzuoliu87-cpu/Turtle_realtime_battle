# B 阶段 —— 战斗确定性加固

日期：2026-09-16　|　状态：**已拍板**

> 母方案书 [20260916-大轮赛制v2周赛制.md](20260916-大轮赛制v2周赛制.md) §4.2 与附录 B 的展开。
> 按 U8，本篇与 [A 阶段](20260916b-A阶段离线周赛骨架.md) **并行**推进。**一行产品代码还没动。**
>
> **本篇与 A 篇的分工不同**：A 篇的增量是「穷举改动面」，因为母方案书低估了；
> 而附录 B **已经是逐行穷举**（60 处、24 文件、最难的 10 处各有行号）。
> 所以本篇的增量是**先把尺子做出来**，把「缺口有多大」从估计变成实测数。

---

## 1. 需求原文

母方案书 §4.2 与 D7 拍板：

> **D7 服务端复算** = 两步走：先做「客户端算 + 服务端账目校验与抽检」，**架构上预留复算接口**；
> **B 阶段确定性加固完成后再决定要不要真开那台机器**。

⇒ **B 阶段是那个决定的前提**。没有 B，「服务端复算」这条路永远打不开；
有了 B，才谈得上 C（本地录像）与 D（服务端结算），因为它们要的正是「同种子同输入 → 同结果」。

---

## 2. 调查

### 2.1 目标形态仓库里已经有了

不是从零造：`_sim_step`（`RealtimeBattle3DScene.gd:2236`）驱动的固定步长
（`SIM_DT = 1/60`，`:811`）已经是全项目的模拟节拍，配套设施齐全 ——
`_pending_shots`（`:701`，`battle_ballistics.gd` 推进）、
`equip_tick_system.tick_delayed / schedule`（`:628` / `:669`）、
各系统自己的 `tick()`、走战斗时钟的 `_wait_sim(secs)`（`:1715`）。

**哑铃早就这么搬过**（`RealtimeBattle3DScene.gd:3523-3528` 有注释），B 阶段是把剩下的按同一条路搬完。

### 2.2 缺口的两个口径（别混为一谈）

| 口径 | 数字 | 说明 |
|---|---|---|
| **会影响战斗结算的** | **60 处**（48 tween 回调/`tween_method` + 12 `process_frame` 协程），24 个文件，其中 **45 处直接改血/盾/控制/状态** | 母方案书附录 B 的逐行清单，抽样复核过。**这才是 B 阶段的工作面** |
| 全仓机械 grep | `tween_callback`/`tween_method` 602 处、`process_frame` 34 处、涉及 57 个文件 | 含 UI / 菜单 / 商店 / 图鉴，**绝大多数与战斗结算无关**。拿这个数当工作量会吓死人，也会让人去改根本不用改的地方 |

⚠ 记在这里是因为我自己差点用错口径：**报缺口之前先说清是哪一个**。

集中区：`RealtimeBattle3DScene.gd` 9 处、`hiding_system.gd` 7 处、`star_system.gd` 5 处。

### 2.3 裸随机：棘轮已经收紧（2026-09-16 本篇动的唯一一处）

`tools/rng_discipline.py` 的存量台账 `tools/rng_budget.json` 原本写 `max_bare_rng: 336`，
而实际只剩 **319** 处 —— 工具自己在输出里喊「已还 17 处，请把 336 改到 319」。
**这 17 个空位等于给新的裸随机留了后门**，而 B 阶段的全部目的就是确定性。

⇒ 已收紧到 319。反向验证：往 `timestop_system.gd` 塞一处 `randf()` ⇒
`[FAIL] 裸全局 RNG 增加了 (320 > 319)` 当场红；还原后 diff 0 行、工具复绿。

⚠ 但**「不许新增」不等于「同种子同结果」**：现存 319 处多数是视觉抖动（idle bob 相位 / 飘字偏移 / 粒子位置），
冻结不动无害；真正改结果的约十来处（例 `dice_system.gd:198`）要在 B3 顺路改走 `_battle_rng`。
`rng_discipline.py` **守不住这件事**，只有 B1 的指纹尺子守得住。

### 2.4 最难搬的 10 处（母方案书逐行看过，本篇不重复抄，只标出实施顺序）

`RealtimeBattle3DScene.gd:4303-4310`（小熊按立绘帧号结算）、
`star_system.gd:362-391`（伤害写在 1.16 秒 `tween_method` 里、每渲染帧跑一次）、
`equip_system.gd:945-969`（扫描式命中用 `get_process_delta_time()` 推进）、
`hiding_system.gd:366-388`（0.833 秒踩背协程，可被打断、同时改攻守双方 `pos`）、
`RealtimeBattle3DScene.gd:5605-5613`、`two_head_system.gd:104-106`、`cyber_system.gd:291`、
`pirate_system.gd:299→309→318`（三段链式 tween，每段读上一段改过的坐标）、
`RealtimeBattle3DScene.gd:6365`（`_sk_dmg` 多段波，**多技能共用通道**）、`ninja_system.gd:129`。

---

## 3. 与现状的出入

1. **确定性不是「没做」，是「只在窄条件下验过」**：`TURTLE_SEED` 那条路已经存在且在用
   （det 模式每帧恰 1 个 `SIM_DT` 步，`:2207`），今天查 059 沙漏时多种子复现完全一致。
   但验过的是**同一进程内、同一台机器、1v1**；从没验过「满阵容满装备 + 跨机器」。
2. **`_sim_step` 与演出用的是两条时钟，这是有意的**：演出走未钳制真实 delta（`rd`），
   sim 走固定步长。B 阶段**不是要把演出也变确定性**，而是把**结算**从演出时钟上摘下来。
   摘错方向会让手感变差却换不来确定性。
3. **今天刚踩过这条线**：059 沙漏定格比设定长 1~2%，根因正是「顿帧那几帧倒计时不走」——
   `if frozen: … elif in_ts:` 二选一。这类「时间口径不一致」就是 B 阶段要清的同一类东西
   （已记在母方案书 U2，**数值不擅自改**，等用户拍板）。

---

## 4. 方案

### B1　先把尺子做出来（**不动产品代码**）

把 `tests/verify_battle_determinism.gd` 从 1v1 扩到「3 统领 + 3 小将、每只带装备、打到分出胜负」，
指纹取每 N 步的血量与坐标摘要。

**先跑一遍证明它现在就会红** —— 这一步的产出是「缺口有多大」的**实测数**，不是猜。
⚠ 顺序不能反：先改代码再做尺子，等于永远不知道自己修好了多少（memory `fb-verify-check-can-fail`）。

**B1 的验收不是「绿」，是「红得可复现」**：同一份代码连跑 3 次，指纹分歧出现在同一批位置。
若分歧位置每次都不同，说明尺子本身不稳（采样点挂在演出上了），要先修尺子。

### B2　12 处 `process_frame` 协程 → 走 sim 队列

弹道进 `_pending_shots`，定时进 `equip_tick_system.schedule`，等待用 `_wait_sim`。
**一批一跑门禁**，每批跑完记一次 B1 的指纹分歧数 —— 这个数应当单调下降。

### B3　48 处 tween 回调 → 结算与演出分家

数值结算抽成能同步调用的函数，tween 只留演出（CLAUDE.md §3.5 海盗钩索的办法）。
**先做附录 B 里「按固定节拍排队」那批**（`ice_system.gd:312-314` 每 0.5 秒一跳、
`lightning_system.gd:143-145` 每 0.075 秒一道雷等 16 处），最机械、风险最低。

顺路把**改结果的裸随机**（约十来处）改走 `_battle_rng`。

### B4　四处难点单独做

小熊按立绘帧号 / 星星 `tween_method` 逐帧染 / 宽刃帧 delta 扫描 / 踩背 0.833 秒协程。
每一处都要**重新表达成「按 sim 步推进」**，不是把 delta 换个来源就算完。

### B5　种子接线

现在只有环境变量能定种子（`battle_world_builder.gd:371-376`）⇒ 改成也能从 `GameState.battle_seed` 进来，
并把种子存进战绩记录（**这一条同时是 C1 的前置**）。

### B6　跨机器验证

CI 加一个 job，用同一组种子跑同一批对局，指纹与本机逐字比对。
**这是「确定性从没端到端验证」真正被关掉的那一刻**，也是 D7 第二步的开关。

---

## 5. 已知风险与未决点

| # | 事项 | 状态 |
|---|---|---|
| **B-R1** | **跨平台浮点**：iOS/ARM 与 Linux/x86 的浮点与 `NavigationServer` 可能算出不同结果。这是 B6 才会暴露的问题，**可能根本修不动**（真修不动就只能退回 D7 第一步永久方案） | ❓ B6 之前无法预判 |
| **B-R2** | 60 处改动**会动手感**。tween 只留演出这件事本身不改数值，但「结算时刻」从渲染帧挪到 sim 步，快机器上会有毫秒级差异 | ⏳ 每批做完要人工看一眼演出 |
| **B-R3** | `_sk_dmg`（`RealtimeBattle3DScene.gd:6365`）是多技能共用通道，改它牵一发动全身，还牵扯随机选靶顺序 | ❌ 放最后做，单独一批 |
| **B-R4** | 本机 CPU 有确诊硬件故障，B 阶段要反复跑全套门禁 + 指纹对比，是最危险的负载 | ⏳ 小批次 + 批间留间隔 + 断点续跑 |
| **B-R5** | 顿帧让时停定格多 1~2%（母方案书 U2）这类「时间口径不一致」还有多少处，没有普查过 | ❓ B1 的尺子做好后顺带扫 |

---

## 6. 验收清单

- [x] B1 尺子已做成**独立门禁** `tests/verify_determinism_b.gd`（40 条断言 / 9 个场景 / 16680 帧 / 本机 123 秒，`run-tests.sh` 的 `frames_for` 已登记 40000 帧）。**没有去改 `verify_battle_determinism.gd`**：那份比的是「终局指纹」，本篇要的是「逐 sim 步指纹序列」，形状不同，合并会把两条判据都弄钝。
- [x] B1 实测数已产出并回填（见 §8）：全部修复撤掉时 ② 379/600、③ 370/600、④ 116/600 步分叉；「红得可复现」由 12 次单点变异佐证（每次红在同一批位置、同一首分叉步）—— 证明它的门禁是 `tests/verify_determinism_b.gd`（`verify_determinism_b`，已进 `bash run-tests.sh`）。
- [ ] ❌ B2 母方案书附录 B 点名的 `process_frame` 协程那批搬完，每批后 B1 的指纹分歧数单调下降
- [ ] ❌ B3 附录 B 点名的 tween 回调那批做到结算与演出分家（先做其中「按固定节拍排队」的子批，最机械）
- [ ] ❌ B3 改结果的裸随机全部改走 `_battle_rng` —— 在 `tools/rng_discipline.py` 里加「战斗路径零容忍」一类；反向验证：塞回一处 `randf()` 必红
- [ ] ❌ B4 四处难点各自重新表达成按 sim 步推进
- [ ] ❌ B5 种子可从 `GameState.battle_seed` 进来并存进战绩记录
- [ ] ❌ B6 CI job：同种子同批对局，指纹与本机逐字相同
- [ ] ❌ `bash run-tests.sh` 全绿（只认 `GATE_RC=`）
- [x] `rng_budget.json` 棘轮由 336 收紧到 319 —— `tools/rng_discipline.py`（进门禁）；反向验证：塞一处 `randf()` ⇒ `(320 > 319)` 当场红，还原后 diff 0 行
- [ ] ❓ B-R1 跨平台浮点能不能过，B6 之前无法预判
- [ ] ⏳ B-R2 每批做完人工看演出有没有变味

---

## 8. 第一轮落地（2026-09-20）—— 尺子 + 三族根因

> 本节每个数都是**探针/门禁打出来的**，不是估的。代码已改、门禁已绿、**未提交**（提交权在主会话）。

### 8.1 尺子：`tests/verify_determinism_b.gd`（进门禁）

同种子跑两遍，**逐 sim 步**比全场快照（每只单位 `hp / pos / alive / shield / gold / energy / prism_color / crit`），
报「分叉步数 / 比对步数」与「首个分叉步」。配四类分母断言：比对步数 == 要求帧数、指纹必须随步变化、
本局真的建过演出 tween、换种子必须得到不同的指纹序列。

**撤掉全部修复时的缺口（实测 rc=1）：**

| 场景 | 分叉步数 / 总步数 | 首个分叉步 |
|---|---|---|
| ② 3v3 裸装（海盗/忍者/骰子/闪电） | 379 / 600 | 86 |
| ③ 3v3 满装备（每只 3 件 3★） | 370 / 600 | 84 |
| ④ 双头/藏身/宽刃 | 116 / 600 | 350 |

### 8.2 族 1 · 演出 tween 走未钳制真实 delta（一处收口，覆盖 515 个 `_reg_tween()` 注册点）

探针：`_apply_damage_from` 入口打 `get_stack()` + `_sim_step_n`，同种子两遍逐行 diff。
命中：海盗登场轰击的伤害挂在 `_pirate_cannonball` 的 `tween_callback` 末尾
（`pirate_system.gd:208` → `battle_spawn.gd:658`）⇒ **同一发炮弹 A 跑落在 sim 步 93、B 跑落在 92**，
之后 376/600 步指纹不同。

修：新增 `RealtimeBattle3DScene._step_sim_tweens()`，det 模式下 `_reg_tween()` 建出来的 tween 一律 `pause()`，
改由 `_sim_step` 末尾按 `SIM_DT` `custom_step` 喂。**只在 `_deterministic` 为真时生效**，交互游玩一字不动（B-R2 零风险）。
反向验证 MUT-C（把 `pause()` 与 `custom_step` 一起撤掉、`_frame_sim_dt` 保留）：② 7/600、③ 370/600、④ 214/600 当场红。

### 8.3 族 2 · 协程按 `get_process_delta_time()` 推位移（13 处）

探针：逐步指纹首个分叉点 —— 忍者冲刺落点 **x = 638.98 vs 639.01**，之后 324/600 步不同。
修：新增 `battle._frame_sim_dt`（本帧 sim 推进量 = 本帧步数 × `SIM_DT`），13 处全部改读它：
`ninja_system:131`、`two_head_system:106`、`equip_system:948/1303`、`hiding_system:107/345/376`、
`RealtimeBattle3DScene` 六处协程。
反向验证：MUT-D（只改回忍者一行）② 327/600 红；MUT-F（藏身三处）⑥ 698/800 红；MUT-G（阔剑/千刃）⑤ 4/1400 红。

### 8.4 族 3 · `_juice_rng` / 裸 RNG 被用在【对局结果】上（7 处，全部反向验证过）

`battle_world_builder.gd:762` **无条件** `battle._juice_rng.randomize()` —— 设了 `TURTLE_SEED` 也照样随机。
而它的声明注释写的是「震屏/粒子专用（演出·永不种子化）」。实测有 7 处在决定对局结果：

| 站点 | 决定什么 | 撤回后分叉 |
|---|---|---|
| `battle_spawn.gd:609` 彩虹棱镜开局定色 | 普攻附什么效果 | 368/1000（首分叉步 **0**） |
| `fortune_system.gd:89`（裸 `randi_range`） | 掷几个金币 | 854/1000 |
| `dice_system.gd:185`（裸 `randi_range`） | 稳定骰子掷几段＝打几下 | 1563/1800 |
| `dice_system.gd:198`（裸 `randf_range`，§2.3 点名的那处） | 命运骰暴击点数 | 9/1000 + 16/1800 |
| `_barrage_bolt` | 闪电龟雷暴挑谁挨雷 | 858/1000 |
| `_tick_cyber_drones` 选靶 | 无人机打谁 | 840/1000 |
| 无人机 `fire_t`（裸 `randf`，两处） | 伤害落在哪一步 | 660/1000 |

`tools/rng_discipline.py` 守不住其中走 `_juice_rng` 的那一半 —— 它只认「裸 RNG」，`_juice_rng.randi()` 在它眼里合规。
棘轮同步收紧 **319 → 314**（还了 5 处），反向验证：塞回一处 ⇒ `(315 > 314)` 当场红。

### 8.6 副作用：把随机路由对了，反而让另一条门禁红了（已换形状，不是放松）

`verify_timestop_global_freeze`（「时停期间全场系统状态一个字段都不许变」）在本轮之后红：
`_battle_rng.state` 动了。逐站点二分（7 次单点变异、每次都复核还原）定位到**唯一**一处：
本用例的携带者是财神龟，它在测量窗口里放了一次骰子（`_sk_fortune_dice`）——
**时停文案本来就写死「active 携带者照常施法」，而施法要掷骰**。
以前不红只是因为那一掷走的是**裸全局 `randi_range`**（引擎全局状态，本用例根本扫不到）；
路由到 `_battle_rng` 之后才落进扫描面。**红的不是「时停漏了什么」，是「原来有一处漏在扫描面之外」。**

⚠ 第一版我把 `_battle_rng` 加进了那条门禁的 `ALLOW` —— 主会话否掉了，理由正确：
那是**放松判据**，加完之后「时停里偷偷掷骰」的真 bug 也不会红。
改成**换形状**：让携带者在窗口内本就不该掷骰（摘掉它的 `active_skills`），`_battle_rng` 留在禁止集里。
配两条分母：⑥ 控制组真放一次骰子 → 金币 +8（证明「金币增量」确实等价于「施过法」，不是恒真式）；
⑦ 窗口内金币增量 == 0（证明确实一次都没放）。反向验证：不摘 `active_skills` ⇒ ⑦ 报「+13 金」、
主判据报 `_battle_rng.state` 当场红。

**已知缺口**：这条门禁因此不再覆盖「携带者边施法边冻结」那一路。要覆盖它，判据得写成
「`_battle_rng` 变的次数 == 窗口内施法次数」，两边都要有分母 —— 另案。

### 8.5 这一轮**没有**做到的（照实写）

1. **只有 det 模式的 tween 被搬上 sim 钟**。玩家实际打的那一局仍不是逐字节可复现的；
   可复现的是 det 模式下的重算/重放 —— 那正是 D7 第二步要的口径。推到交互模式＝演出从 144Hz 降到 60Hz，属 B-R2。
2. **运行时判据只抓得到「够长」的站点**：双头灵能炮弹射程仅 400 码，改回真实 delta 两遍也慢得一样
   ⇒ 逐步指纹不分叉（实测单独变异它 30/30 全绿）。补救是第 ⑧ 条**源码棘轮**
   （战斗路径 `get_process_delta_time()` 必须 0 处，扫 108 文件 / 75969 行；反向验证：写回一处当场红）。
3. **16 处裸 `create_tween()`**（不经 `_reg_tween`）仍走真实钟：时停演出 5、HUD 4、飘字 3、换路过场 4。
   逐个读过调用点没有一处结算伤害，但**没有门禁守住「以后不会有人在裸 tween 里结算」**。
4. **`_sk_dmg_wave` 的 `randomAoe` 目前零生产者**（全仓只有读它的那一行），所以那一处虽然改了、判据却覆盖不到。
5. `_juice_rng` 本身仍不种子化 ⇒ 纯演出在两遍之间仍不同（有意保留；指纹只记对局字段）。
6. **B5 / B6 没做**：种子还是只能从 `TURTLE_SEED` 进来；跨机器（Windows vs Linux CI）指纹逐字比对没建。
   B-R1 跨平台浮点仍无法预判。

---

## 7. 决策记录

| 日期 | 决策 | 依据 |
|---|---|---|
| 2026-09-16 | 按 U8 把母方案书 §4.2 拆成本篇，与 A 阶段并行 | 用户选项回答：「A 与 B 并行」 |
| 2026-09-16 | B 是 D7 第二步的前提：确定性加固完成后才决定要不要开复算机器 | 用户选项回答：「两步走」 |
| 2026-09-16 | 先做尺子再改代码，B1 的验收是「红得可复现」而不是「绿」 | memory `fb-verify-check-can-fail`：报「没问题」前先证明检查会 FAIL |
| 2026-09-16 | `rng_budget.json` 336 → 319 | 工具自己在输出里要求；17 个空位是给新裸随机留的后门，与 B 的目的直接冲突。已反向验证 |
| 2026-09-16 | 顿帧让时停多 1~2% 这条**不在 B 阶段擅自改** | 数值/手感不擅自改；已记母方案书 U2 等用户拍板 |
| 2026-09-20 | 第一轮落地随 **v0.19.417** 发版（与赛季自然周换轮、地面碎料层同一版） | 本篇 §8；三件事改到同一个文件拆不成三个自洽提交，而发从没构建过的版本号会破坏「测试者报 bug 能说清哪一版」这个唯一价值 |
| 2026-09-20 | `verify_timestop_global_freeze` **不加** `_battle_rng` 到 ALLOW，改成摘掉携带者 active | 主会话否掉第一版：加 ALLOW 是**放松判据**（那条门禁从此看不见时停期间任何随机消费），memory `fb-my-invented-rules-become-the-wall`「改判据要换形状不是放松」 |
