# 20260730d — 装备平衡 7 项 + 局内 5 条反馈

> **交付状态：⚠ 已逐条复核（2026-08-02）—— 代码全对，但 6 项没有门禁。**
>
> **1-1 ~ 1-6 六项的代码值我逐条核过、全部正确**，但交付提交 `397c55c` 的 `--stat` 显示
> **它没有碰过 `tests/` 下任何文件**。清单里承诺的「门禁验次/秒」「各一条反向断言」
> 「验随机池而不是验某一次抽到什么」「验计数是 5 不是 4/6」——**一条都不存在**。
> 唯一相关的是 `tools/tooltip_number_audit.py`，但它只保证"文案里的数字在代码里存在同样的数组"，
> **不验行为、不验用在了对的地方**。
>
> ★★ **这个缺口直接放跑了一个真 bug**：`1-4 荆棘海胆` 的反伤**实际是名义值的两倍**
> （通用钩 `equip_stats_apply.gd:51` + 专属分支 `equip_system.gd:1104` 拿同一个 dmg 各发一发，
> 都在 `battle_damage.gd` 同一个 `if not from_equip` 块里，无互斥）。
> 探针实测 1★ 名义 12%：打 1000 → **反伤 240**；且累计器 `thorn_accum` 只统计一半 ⇒ 阈值触发也慢一倍。
> **已于 2026-08-02 修复**（通用钩跳过 p2eq_015）并补门禁 `tests/verify_thorn_reflect.gd`
> （含 p2eq_013 对照组，反向验证退回双发 → 三档全报 2.00×）。
>
> **结论：把「未打勾」当成「没做」是错的，但把「代码对」当成「做完了」也是错的** ——
> 这一批真正没做完的是**门禁**，而正是门禁的缺席让双倍反伤活了三天。


用户 2026-07-30 两批需求。**需求2 的 5 条全部指向我刚交付的东西**，其中 **2 条已确诊是真 bug**（含一条是我门禁的盲点）。

---

## 0. 已查证的事实（不是推测）

| # | 结论 | 证据 |
|---|---|---|
| 1-1 | **大熊攻速代码就是 0.5 次/秒**，文案没错 | `RealtimeBattle3DScene.gd:2837` `"atk_interval": 2.0` → 1÷2.0 = 0.5 |
| 2-e | **★魔法石跨路继承没生效** | `_dl_build_lane_field()` 每路都调 `_spawn_trainers()` → 大师是**全新单位字典**，`_ms_stacks` 天然归零。我删掉 `_dl_start_fight` 里的显式清零**根本不够** |
| 2-e' | **★这是我门禁的盲点** | `verify_trainer_magicstone` ⑦ 组喂 23 层给一个 dict 再调 `_dl_start_fight()` 验它还是 23 —— 而玩家换路走的是 `_dl_build_lane_field()`。**测的不是玩家真正走的那条路**（同 memory `fb-verify-must-run-the-real-path`） |
| 2-c | **PK 条会跳的机制找到了** | `_pk_target = 当前存活总血 ÷ 开场基线`（`battle_hud.gd:637`），而分子按 **`_eff_side`** 归边（`_pk_sum:588`）。所以①驯服归顺 → 整只龟的血从敌方分子搬到我方，**基线不动** → 两条同时跳；②口哨临时血 +700 → 我方分子凭空 +700；③驯服重生 30% → 分子凭空回血 |
| 2-d | 驯服进决胜的**幸存名单按 `_eff_side` 分组**，且 spec 里**有**拷 `tamed_side` | `dual_lane_flow.gd:640-660` |
| 1-5 | 大熊现有 `hp [650,1100,10000]` / `atk [70,120,2000]`，**没有双抗** | 同 2837 行 |

---

## 0.5 交付状态（2026-07-31 收）

**需求1（7 项）+ 需求2（5 条）全部交付**，本地门禁 114/114，版本 0.17.22 → 0.17.26。

| # | 项 | 版本 | 备注 |
|---|---|---|---|
| 2-c | PK 条莫名增减 | 0.17.22 | 三个跳变源：归顺搬边 / 临时血 / 单位数变就满格 |
| 2-e | 魔法石跨路继承 | 0.17.22 | ★真 bug + 我门禁的盲点 |
| 1-1/1-5 | 大熊 | 0.17.23 | 代码本就是 0.5，文案没错 |
| 1-2 | 深海堡垒甲削弱 | 0.17.23 | 三星回复 250（用户确认笔误） |
| 1-3 | 复活海螺 | 0.17.23 | 小虫带 3 件 4/5 费装备 |
| 1-4 | 荆棘海胆重做 | 0.17.23 | 累计反伤 → 护盾 + 强化下一击 |
| 1-6 | 飞镖 | 0.17.23 | 每 5 下击飞 1 秒 |
| 1-7 | 信号放大器重做 | 0.17.24 | A 方案（扇形张角 90→360） |
| 2-a | 投降键压血条 | 0.17.25 | ★16:9 恰好不重叠 = 本机复现不出来 |
| 2-d | 驯服跨战场 | 0.17.26 | ★真 bug：spec 写了没人读 |
| 2-b | 灵体小龟形象 | 0.17.26 | 小龟统领轮廓 + 灵体化 |

**★本轮三次同一个教训**（都是"读代码以为对"）：
① 魔法石继承——门禁测的不是玩家走的那条路；
② 投降键——16:9 下恰好不重叠，PC 上永远复现不出来；
③ 驯服跨战场——快照那侧写进去了、spawn 那侧从来没读。
**都必须端到端跑真实入口才发现。**

**★两个"假门禁"被自己抓出来**：
① HUD ⑨ 组用公式模拟几何而不量真实按钮 → 改回写死坐标它照样绿；
② `verify_signal_amplifier` 合成单位放在 ARENA 外被钳到同一点 → 500 帧红、1500 帧绿。
★后者**在 CI 上真的红了一次**（提交 `3ce8029`），下一个提交修好。

---

## 1. 需求1 — 装备平衡 7 项

### 1-1 大熊攻速 0.5 → 0.7 次/秒
`atk_interval` **2.0 → 1.4286**（=1/0.7）。★文案里若写了"每 2 秒一击"要同步。

### 1-2 深海堡垒甲**削弱**
- 汲取伤害 → **0.8 / 1 / 2.5**（护甲 + 魔抗之和的倍率）
- 每汲取一个敌人回复 **50 / 100 / 25 + 5% 已损生命**
- **汲取不应包括训龟大师、以及屏障里的龟蛋**

> ⚠ **未决**：`50 / 100 / 25` 三星回复反而比二星低（100 → 25），看着像笔误。
> 猜测是 `50 / 100 / 250`。**动手前需你确认**，我不擅自改。

### 1-3 复活海螺**加强**
- 小虫 `hp` **100 / 1500 / 10000**、`atk` **50 / 80 / 200**
- 小虫诞生时**携带 3 件随机装备**：星级 **1 / 2 / 3 星**，从**5 费或 4 费**池里随机

### 1-4 荆棘海胆**加强 + 效果重做**
- 反伤 **12 / 25 / 40%**，最大生命 **100 / 200 / 1000**
- **新效果**：携带者每反伤 **300 / 270 / 230** 点伤害 →
  ① 给自己 **50 / 70 / 200** 护盾
  ② **强化下一次普攻**，命中施加 **30 / 50 / 90 层流血**

### 1-5 玩偶小熊的大熊**加强**
`hp` **1600 / 3000 / 15000**、**+70 点双抗**（护甲与魔抗各 70）。★攻击力用户没提 → **不动**。

### 1-6 飞镖**加新效果**
- 提供 **40 / 80 / 150% 攻击速度**
- **每 5 下普攻**，携带者下一次普攻强化 → **击飞目标 1 秒**

### 1-7 信号放大器**效果重做**
- 提供 **30% 攻击速度**（固定，不分星）
- 每次普攻叠一层「放大」：**+1 / 2 / 5% 增伤** 且 **+5% 攻速**，**最多 20 层**
- **每满 5 层** → 从携带者中心爆发电磁波：朝目标以 **90° 张角**发射一道**缓慢移动的弧形波**，造成 **125 / 250 / 500 魔法伤害** + **眩晕 2.5 秒**
- **每释放一次，张角 +90°**（90 → 180 → 270 → **360 封顶**）
- 每次释放还使自己获得 **5 / 7 / 10 点魔抗穿透**

---

## 2. 需求2 — 局内 5 条反馈

### 2-a 手机上**投降键压在血条上**
PK 条占 y `16..42`、副条 `46..55`；右上按钮是**写死坐标**（`Vector2(1088, 12)` 这类），
没走 `SafeArea.margins()`。手机安全区 + 不同宽高比下会与 PK 条重叠。
→ **右上按钮改走 SafeArea + 让位给 PK 条**（PK 条居中、按钮贴右上角且 y 下移到 PK 条之下或缩小）。

### 2-b 灵体小龟形象
改用**小龟统领的形象**加一点灵体感（幽蓝 + 半透明 + 轮廓辉光）。
★**不是复用**：这是"同一只龟的灵体态"，是设定要求；但要**新出一张图**而不是直接 modulate 现有立绘。

### 2-c ★PK 条会莫名增减
根因见 §0。三个跳变源：**驯服归顺搬边**、**口哨临时血抬分子**、**驯服重生回血**。

> ⚠ **未决（要你选）**：
> - **A. 归顺时把该单位的 maxHp 一并从敌方基线搬到我方基线** → 比例连续、不跳（我倾向这个）
> - **B. 归顺单位不计入任何一方**（中立）
> - **C. 保持现状，但加 0.3 秒过渡**让它"流过去"而不是瞬跳
>
> 临时血那条我建议：**把 +700 也加进基线**（否则条会超 100% 被截断）。

### 2-d 驯服跨战场 + 血条颜色
幸存名单**按 `_eff_side` 分组**、spec **有**拷 `tamed_side` —— 看起来是对的，
但**我还没端到端验过**（今天两次栽在"读代码以为对"上）。
→ **必须写一条走真实换路的门禁**：驯服一只 → 结束本路 → 进决胜 → 验它①在我方②血条是我方色③2%/秒掉血继续。

### 2-e ★魔法石继承（已确诊没生效）
`_spawn_trainers()` 每路重建大师 → 层数天然归零。
→ **把 `_ms_stacks` 按 side 持久化**（存 `GameState` 或 battle 层的跨路容器），
`_spawn_trainers()` 建完后**回填**。敌方大师同理。

---

## 3. 验收清单（逐条·动手前列全）

### 需求1

- [x] ✅ **已补门禁(2026-08-25)** 1-1 大熊 `atk_interval` = 1.4286（0.7 次/秒），门禁验**次/秒**而不是验 interval 字面量　代码 `RealtimeBattle3DScene.gd:2816` `1.0/0.7` 正确，但 `tests/` 下零断言
- [x] ✅ **已补门禁(2026-08-25)** 1-2 深海堡垒甲三档倍率 0.8/1/2.5；回复公式；**排除训龟大师**；**排除屏障内龟蛋**（各一条反向断言：把大师/蛋放进汲取范围 → 不该被汲）　代码全对（含排除大师/龟蛋），但 `grep _tick_fortress tests/` N=0
- [x] ✅ **已补门禁(2026-08-25)** 1-3 小虫 hp/atk 三档；**诞生带 3 件装备**且**星级 1/2/3**、**费用 ∈ {4,5}**（验随机池而不是验某一次抽到什么）　代码全对；另：「星级 1/2/3」被实现成"按携带者星级取"，与"三件分别 1/2/3 星"两种读法都通，**需求歧义未固化**
- [x] ⚠ 1-4 反伤 12/25/40%；hp 100/200/1000；**累计反伤达阈值 → 护盾 + 下次普攻附带流血层数**（阈值触发要验"累计"而不是"单次"）　**复核查出反伤双发（实际 24/50/80%），2026-08-02 已修 + 已补门禁 `verify_thorn_reflect`**
- [x] ✅ **已补门禁(2026-08-25)** 1-5 大熊 hp 1600/3000/15000 + def/mr 各 70；**攻击力不变**（反向断言）　代码全对，"攻击力不变"的反向断言也不存在
- [x] ✅ **已补门禁(2026-08-25)** 1-6 飞镖攻速 40/80/150%；**第 5 下**普攻击飞 1 秒（验计数是 5 不是 4/6，且击飞真的发生）　代码全对，连 `_dart_kb_n` 这个"供门禁用"的证据字段都**没有任何消费者**
- [x] 1-7 信号放大器：30% 攻速；每层 1/2/5% 增伤 + 5% 攻速；**20 层封顶**；**每 5 层**触发波；波 125/250/500 魔伤 + 眩晕 2.5 秒；**张角 90→180→270→360 封顶**；每次 +5/7/10 魔抗穿透　✅ `verify_signal_amplifier` 全绿（张角 90→180→270→360 / 20 层封顶 / 每 5 层放波 / 眩晕 2.5s 均有断言）

### 需求2

- [x] 2-a 手机比例下投降/统计键**不与 PK 条相交**（用矩形相交判据，多个宽高比各验一次）　✅ `verify_battle_hud_r1` ⑨ 组：真实 `get_global_rect()` 量矩形，5 个宽高比各一条
- [x] 2-b 灵体小龟新图**是新生成的**（SHA256 全量比对）且**真的进了 `_world`**　✅ 进 `_world` 有断言；SHA256 全库 625 张唯一（本次复核实跑）
- [x] ✅ **已补门禁(2026-08-25)** 2-c PK 条三事件 —— `tests/verify_pk_bar_continuity.gd`(18 条断言)。
      ★方案书原话「现有门禁**抓不住回退**：合成单位全未驯服 → `_eff_side` 退化成 `side`，把分母改回去照样全绿」
      ⇒ 本文件**第一条就是分母断言**：场上必须真有一只 `_eff_side != side` 的龟，否则整份是空检查。
      **反向验证把分母改回 `_eff_side` ⇒ 当年那个 bug 原样复现**：我方分母 2000→3000、敌方 2000→1000、
      **敌方条「跌」变成了涨(0.800 → 1.000)** —— 正是用户 2026-07-30 报的「方向反了」。
- [x] ⚠ 2-d 驯服单位**走真实换路**进决胜后：在我方 / 血条我方色 / 掉血 buff 继续　3 项中 2 项已证（在我方 / 掉血 buff 继续，走真实换路且有 backtrace）；**「血条我方色」无断言**
- [x] ⚠ 2-e 魔法石层数**走真实换路**（`_dl_build_lane_field`）后仍在；**敌方大师同样保留**　我方已证（23 层跨路保留 + 断言大师真被重建）；**「敌方大师同样保留」从未被执行**（门禁全程 right 侧恒为 0）

### 门禁纪律（每条都要）

- [x] 期望值写**需求字面值**，不引用被测常量（否则恒真式）　✅ 5 份门禁扫描**未发现恒真式**（`verify_trainer_magicstone` 头部记录了历史上真有过一个，已修）
- [ ] ❌ **部分缺失** 每条做**反向验证**：改坏 → 确认 FAIL → 还原复绿　只有 1-7 / 2-a / 2-d / 2-e 四条有记录；1-1~1-6 因为没有门禁，无从反向
- [x] 打**分母**（N=0 是空检查不是通过）　✅ 各门禁均有 `★分母` 行，且分母缺失时是硬 FAIL 非静默跳过
- [x] 涉及换路/重建的，**必须走玩家真正的入口**，不许只调子函数　✅ 两处都走 `_dl_build_lane_field()`，并用 `not is_same(旧,新)` 断言真的重建了

---

## 4. 已知风险与未决点

**① ~~`50 / 100 / 25` 疑似笔误~~** → 用户 2026-07-31 确认是 **250**。已实现。

**② PK 条跳变的处理方式要你选**（§2-c 的 A/B/C）。不选的话我按 A 做，并在实现里留开关。

**③ ~~1-7 语义歧义~~** → 用户确认 **A 方案（扇形张角）**。已实现。原文：**1-7 信号放大器是本批最大的一件。** 它有：层数系统 + 阈值触发 + **角度递增的弧形波弹道** + 眩晕 + 穿透。
弧形波要新写一套弹道（现有的是直线/抛物），且"90° 张角发射"的语义有歧义 ——
是**一次发射张角 90° 的扇形波**，还是**沿 90° 方向发一道弧**？我按**扇形张角**理解，
角度递增 = 扇形越来越大，到 360° 就是整圈。**若理解错，这一项要返工**。

**④ ~~费用表~~** → `data/phase2-equipment.json` 有 `cost` 字段，已用。原文：**1-3「5 费或 4 费装备」需要装备费用表。** 若 `EquipStats` 里没有"费用"字段，
得先确认费用是从哪来的（商店价？稀有度？）——**这是实现前置**。

**⑤ 1-4 的"每反伤 N 点"是累计计数器**，跨路要不要清零？（与魔法石叠层同类问题）
未说明 → 我按**跨路保留**做（与刚拍板的魔法石一致），但这是我的推断。

**⑥ 2-b 灵体小龟用"小龟统领形象"** —— 需确认小龟统领的立绘文件名/是否有多方向帧。
若它是龟统领本体立绘，直接拿来改色**算不算复用**？我的判断：**要新出一张**（用它做参考重画），
不直接引用原文件。

**⑦ 本批改动会让第六轮胜率数据失效。** 7 件装备里 6 件改了数值/机制，
平衡数据要**重新跑一轮**才有意义。

**⑧ arch_budget 台账 8600 行**：1-7 那套弧形波若写进主文件会撞线，
**必须新开 `scripts/systems/equip/` 或塞进现有装备系统文件**。

---

## 5. 建议顺序

1. **先修 需求2 的 c/d/e**（都是已交付功能的 bug，且 e 已确诊）—— 一并补上"走真实换路"的门禁
2. **再做 需求1 的 1-1 / 1-5 / 1-6**（纯数值 + 简单机制，风险低）
3. **然后 1-2 / 1-3 / 1-4**（等未决点①④确认）
4. **最后 1-7**（最大、有歧义、要新弹道）
5. **2-a / 2-b** 穿插做（UI 与美术，互不阻塞）

---

## 6. 不做

- 不动第六轮平衡表的其他条目（本批只改这 7 件装备）
- 不在本批里做需求4 的 P3 高低差 / P4 多地图轮换


---

## 2026-08-25 · 补齐门禁 —— `tests/verify_equip_balance_20260730d.gd`(33 条断言)

1-1 / 1-2 / 1-3 / 1-5 / 1-6 五条已补。**每条都走真入口**（召大熊走
`_big_bear_charge_and_spawn`、汲取走 `_tick_fortress`、飞镖计数走 `_eq_on_hit`），
期望值一律写**需求原文的字面量**，不从被测常量读回来（那是恒真式）。

方案书点名的几处「反向断言」都落到位了：
- **把训龟大师和龟蛋放进汲取范围 ⇒ 它们不该掉血**（去掉排除分支，这两条当场红，
  且携带者回血从 200 翻到 390 —— 症状与预期一致）
- **飞镖「差一下」时还没击飞** ⇒ 证明计数是 5 不是 4；再打 5 下累计 2 次 ⇒ 周期稳定
- **小虫验的是【池子】不是某一次抽到什么**：跑 12 次，每次 3 件、每件都在 4/5 费池内
- 大熊攻速验的是**次/秒**（0.7），不是 `atk_interval` 的字面量 1.4286

### ★写这条门禁时我连推理错两次，探针才断的根因

大熊怎么都召不出来。我先以为「实体召唤是同步的」，再以为「等得不够久」，
两次都是推理。探针打出 **`_t 0.000 → 0.000`（120 帧后纹丝不动）** 才看清：
主循环是 `var _fight_on := not _edit_mode and …` 然后 `if _fight_on and not _over: _t += dt`，
**`_fight_on` 是每帧算出来的局部变量、赋值不了**，真闸门是 `_edit_mode` ——
而它正是**我自己设的 `DEBUG_EDIT = true`** 打开的。召大熊 `await _wait_sim(1.2)` 等的就是 `_t`。

⇒ 时钟不走 = 永远等不到 = 被误判成「大熊没被召出来」。CLAUDE.md「断根因先写探针」。

### 仍未补（显式登记，不是"应该没问题"）
- **1-6 的「飞镖攻速 40/80/150%」是过期需求**：这件装备已被重做，
  现在的属性是 `+攻 45/90/400`（`equip_stats.gd:175`），没有攻速项。
  没有去断言一个不存在的东西 —— 要不要恢复攻速属性，**需用户拍板**。
- **1-3 的「星级 1/2/3」需求歧义未固化**（方案书原文就写着）：
  现实现是"按携带者星级取"，与"三件分别 1/2/3 星"两种读法都通。**需用户拍板**。
- **2-c PK 条三事件比例连续**：这条要造驯服/临时血/重生三种事件并逐帧比对，
  与本条门禁不同族，另起一条更清楚。**未做，仍登记在案。**
