# 战斗场景美术重做 —— 对标 30 款同类型好游戏

状态: **实施中**（P0-1 已落地并达标 · P0-2 试过被回退 · P0-3/P1 未做） · 2026-09-18

---

## 需求原文

用户 2026-09-18，逐条钉住：

1. 「别，问题是现在的游戏地图也很差啊」← 否掉「渲一帧战斗场景当主菜单背景」
2. 「你明白意思吗」「**现在战斗内的背景很烂**」← 本方案书要解决的
3. 「额，有100个吗，**是好游戏吗，别拿低劣游戏敷衍我**」← 否掉上一轮 151 个样本
4. 「写方案书，然后来做，你自己推进」

⚠ 否定式指令单独列（最容易丢，memory `fb-pin-user-words-dont-drift`）：
- ❌ 不许拿低劣/老旧/不同类型的游戏充样本
- ❌ 不许拿现在的战斗场景当主菜单背景（它本身要先修）
- ❌ 素材不复用，新内容一律新素材（memory `fb-no-asset-reuse-unless-told`，用户 09-17「尽量不要复用好吗」）

---

## 调查

### 一、上一轮样本为什么作废（量过的）

上一轮 151 个来自 Game UI Database 的 Main Menus 分类，按排序抓前几页，**没按质量筛**。成分：

| 成分 | 数量 | 占比 |
|---|---|---|
| 老主机（SNES/PS1/3DS/DS 时代） | 48 | 32% |
| VR 小品 | 19 | 13% |
| 小体量独立/手游 | 57 | 38% |
| 其他（含现代作品） | 27 | 18% |

**82% 不可比**，而剩下 27 个多数是单机叙事（Xenoblade/Ys/Yakuza/A Plague Tale）。用户的质疑成立，这批作废。

### 二、这一轮的样本（两条硬标准：好游戏 + 同类型 + 看战斗场景）

| 批次 | 数量 | 来源 | 名单 |
|---|---|---|---|
| Steam 游戏内截图 | 20 款 / 100 张 | Steam 官方 `appdetails` | Hades、Hades II、Slay the Spire、Balatro、Darkest Dungeon、Inscryption、Monster Train、Wildfrost、Slice & Dice、Dicey Dungeons、Cult of the Lamb、Loop Hero、Vampire Survivors、Brotato、Peglin、Dave the Diver、Marvel Snap、Super Auto Pets、Backpack Battles、Dota Underlords |
| 手游官方截图 | 10 款 / 40 张 | App Store 公开接口，**1600×900 全尺寸横屏** | **云顶之弈、金铲铲之战**、炉石传说、荒野乱斗、部落冲突、明日方舟、原神、崩坏星穹铁道、Marvel Snap、符文之地 |

抓法：App Store 的 `screenshotUrls` 尾部 `/<W>x<H>bb.jpg` 是**缩略尺寸**，不替换只能拿到 360×270
（第一轮就栽在这，裁出来 312×260 什么都看不见）；换成长边 1600 才是全尺寸。
皇室战争与 Rush Royale 只发竖屏宣传图，被宽高比 >1.3 的筛子筛掉。

### 三、从这批数出来的战场五条规律（每条可指到具体画面）

**① 地面绝不是平色。** 四种做法都见过，没有一个用纯色：
云顶之弈=磨损土地+草簇+半透明贴花；荒野乱斗=斜向条纹；Vampire Survivors=菱形暗纹地砖；
Brotato=密集低对比碎石点。

**② 场地边界要有体积。** 云顶的石砌矮墙看得见**顶面+侧面+投影**；荒野乱斗的箱子同理。

**③ 场外环境有逻辑关系。** 云顶是瀑布→河→松树→石桥→火盆，彼此位置说得通，不是撒的。

**④ 一个主光源，而且落到地上。** Hades II 的绿光柱在地面有对应光斑、物件有投影、画面边缘压暗。

**⑤ 配色只留两个主色**，强调色只给 UI 和特效。

两条更省力的替代路子（不需要复杂材质）：
- **Super Auto Pets**：天空/远山/树丛/草地/前土 **五条水平色带**就造出纵深，零复杂材质。
- **Cult of the Lamb / Peglin**：**成片有方向的高草丛框住画面边缘**。

### 四、现有实现的事实（读代码 + 量实拍，不是看法）

- 地图是 **58×34 tilemap**（`data/maps/arena.json`，tile 38.4 码），5 种地块：
  grass 37% / void 26% / water 21% / stone 13% / sand 3%。
- 地砖**有**细节贴图（`tile-silt/water/stone/sand.png`，64×64 灰度）+ `TILE_COLS` 着色 + 水/地两套 shader。
- 分层其实**是齐的**：地面 → 边框装饰带 → 中景地标(`MID_OBJS` 13 件) → 远景(backdrop/terrain/fish/bubbles) → 光束 → 气泡。
- ★`height` 数组 **1972 格全是 0.0**，`_tilemap_from_data` 写死 `hh = 0.0`。
- ★边框装饰是**均匀随机散布**：120 步网格 × 30% 概率 × ±26 抖动 × 最小间距 70。
  2026-07-23 按用户反馈调过稀疏度（0.58→0.30、步长 80→120），但**没有改变"随机散布"这个本质**。

**★★量实拍（`battle_bg.png` 去掉两侧面板与顶部血条后的场地区域，147875 个采样点）：**

| 分位 | p5 | p25 | p50 | p75 | p95 |
|---|---|---|---|---|---|
| 明度(0~255) | 23 | 32 | **50** | **135** | 187 |

**明度直方图是双峰的，中间是空的。** 一半画面挤在明度 23~50 的极暗窄带，然后直接跳到 135+。
前 10 主色里 7 个明度 16~47、2 个明度 149~165，**中间调（60~130）一个都没有**。
⇒ 问题不是"颜色种类多在打架"，是**只有两极、缺中间过渡**。这条很关键：
它意味着**不用推翻用户硬锁的暗调**，要补的是中间明度。

---

## 出入（本项目 vs 五条规律）

| # | 规律 | 现状 | 判定 |
|---|---|---|---|
| ① | 地面不是平色 | 有 64×64 细颗粒贴图，但在游戏内被拉开后几乎看不见 | ⚠ 有但无效 |
| ② | 边界有体积 | `height` 全 0，边界是一条锯齿几何边 | ❌ |
| ③ | 场外环境有逻辑 | 均匀随机散布；且**按老地图撒的、跟新构图从没对齐过** | ❌ |
| ④ | 主光源落地 | 有光束但地面无对应亮斑，全场**零投影** | ❌ |
| ⑤ | 配色两主色 | 明度双峰、中间空 | ❌（但**病因与我最初判断不同**，见调查四） |

★③④ 不是新发现：`20260731-重画一板地图.md` §6 已登记为未决点 2 和 4，原文写着
「底部那几株珊瑚现在长在潟湖里」。本方案书是去把那两条**关掉**，不是重新发现它们。

---

## 方案

按「收益 ÷ 风险」排。P0 四条**都不碰** `height`、navmesh、玩法与硬锁调色板。

### P0-1　补中间明度（对应规律⑤ · 最高性价比）✅ 已落地 2026-09-18

**实际落地的是两行**，而且病因与本节原先设想的不同（原以为要在调色板内部重分配明度）：

1. `ground_land.gdshader`：`c = base_col.rgb * det` → `c = base_col.rgb * mix(0.80, 1.30, det)`。
   实测 det 均值 silt 0.541 / stone 0.643 / sand 0.949 —— 纹理在**单向压暗**，
   占地面 37% 的 grass 被从明度 36 压到 **19**（近黑），四种地块里只有 water(105)
   落在中间调区间。这是中间调只有 11% 的直接根因。
2. `battle_world_builder.gd`：装饰 `modulate` (0.50,0.68,0.92) → (0.88,0.94,1.00)。

★两处都**没有动 TILE_COLS**，硬锁调色板一个值没改。

以下是本节原先的设想，保留作对照：


明度只有 23~50 和 135+ 两档。在**硬锁调色板内部**把地面拉开到三档：
- grass `#1a2340`(明度36) 保持为最暗底
- 启用调色板里**已经写着却没用起来**的亮部 `#263056`(明度49) 做中景地面
- stone `#3a3f5c`(明度65) 提到 ~90 一档（仍在暗调内，只是不再和 grass 糊在一起）
- water 高光 `#3fe9e0` 压一点，别让 p95 顶到 187

判据：改完重量一次直方图，**p50 与 p75 之间不许再有 85 的空档**。

### P0-2　装饰从「随机散布」改成「成片成带」（对应规律③）

不换素材、不加素材，只改 `_build_tilemap_decor` 的排布算法：
- 用**聚落**取代均匀随机：先撒 5~7 个种子点，每个种子周围按衰减概率长 3~8 株同类
- 同一聚落**用同一种/同一族**装饰（现在是每株独立随机 11 选 1，所以满屏杂）
- 顺带关掉未决点 4：装饰位置与新构图对齐，**不许再长在潟湖里**（判据见验收）

### P0-3　给所有贴地物件与单位加投影（对应规律④）

一个暗色椭圆贴地 sprite，跟随物件。**不碰 `height`、不碰贴地 VFX 的 y 基准**——
投影自己贴在 y≈0.02，与那 40+ 处 VFX 无关。

### P0-4　画面边缘压暗（对应规律④）

Environment 调整或一层全屏渐变。中心亮、四角暗，视线自动收到战场上。

### P1-5　场地边界的体积（对应规律②）

在 arena 边沿加一圈**新生成**的矮墙 billboard（顶面+侧面+投影画进贴图里），**伪造**体积。
★不用 `height` —— 那条有明确前置条件（见风险 R2）。

**2026-09-18 按 R15 标定填实的设计：**

| 项 | 值 | 依据 |
|---|---|---|
| 目标屏幕高 | **≈31 px** | 参考 0.81 × 龟屏幕高中位 38 px |
| 世界高（**billboard**） | **1.11 米** | 31 ÷ 27.78（公告板朝相机，**不吃**俯角压缩） |
| 世界高（若改真实竖直几何） | 1.75 米 | 31 ÷ 17.55（竖直面**吃** cos50.8° 压缩） |
| 落点 | 139 个边界格（非 void 且四邻有 void，越界当 void） | `_probe_pxm` ⑤ |
| 真可见 | 49 格；**42 格在画面上 1/3**（远端），下 1/3 仅 7 格 | 同上 |
| 贴图原生尺寸 | **64×32**（由 128×64 **整数降半**得到） | 渲染高 31 px，1:1 才不打烂像素网格 |

★★**两个必须分清的世界高**：同样是屏幕 31 px，billboard 要 1.11 米、真实竖直面要 1.75 米
（差 1.58 倍）。我第一次写 R15 时只写了 1.75，那是**真实几何**的值，而方案定的是 billboard ——
两者混用会把墙做高一半。

★贴图必须**整数缩放**：实测把 128×64 非整数缩到 31 px 高，砖层厚薄不一、边缘发毛；
整数降半到 64×32 干净。（`vfx_discipline` A 条管的是 tween 动画式缩放，静态尺寸不受它管 ——
但**画面问题是真的**，判据管不到不等于可以不管。）

★★**结构照 R15 实测做，不照我原先的想象**：亮竖面（比场内地面亮 23%~40%）+ **上下各一条
2~4 px 暗缝**（顶沿投影 + 落地接触影）。「读出体积」靠亮面被两条暗线夹住，不是一块暗面。

### P1-6　地砖图案加粗（对应规律①）

现有 64×64 细颗粒在游戏内看不见。**新生成**一套更大更明显的图案地砖
（参照 Vampire Survivors 的菱形暗纹 / 荒野乱斗的斜条纹这种"大而低对比"的做法）。

---

## 已知风险与未决点

- **R1（硬锁·不许动）**：调色板是**用户 2026-07-13 拍板的硬锁**
  （`docs/design/场景地图方案.md` §4，暗蓝紫深海夜，理由是"暗底让技能特效跳出来"）。
  P0-1 只在这张表**内部**重新分配明度，**不新增颜色、不提亮整体**。
  ⚠ 我最初凭眼睛判断的是"四组颜色打架"，量完直方图才发现病因是"缺中间调"——
  这两个结论的**药方完全相反**（前者要减色，后者要加过渡）。所以照量的来，别照第一印象。
- **R2（有前置条件·本轮不做）**：恢复 `height` 会让 **40+ 处贴地 VFX 掉到地板下**
  （2026-07-15 因此拍平）。要做必须先统一贴地 VFX 的 y 基准，那是独立一轮的工作。
- **R3（老未决点）**：装饰位置仍**硬编码在 `battle_world_builder.gd`**，`arena.json` 里没有装饰字段。
  P0-2 只改排布算法与对齐，**不做数据化**（数据化是 `20260731` §6 未决点 4，要另外拍板）。
- **R4（素材铁律）**：P1-5 的矮墙与 P1-6 的地砖**必须新生成**，
  不许拿 `assets/sprites/map/` 现有件改。用户 09-17「尽量不要复用好吗」。
- **R5（口径风险）**：五条规律来自 30 款同类型好游戏，其中真正与本项目同构（自走棋/棋盘战场）的是
  云顶、金铲铲、Super Auto Pets、Dota Underlords、Backpack Battles 五款。
  其余是"俯视战斗/卡牌战场"的近亲。**别把它当成"所有好游戏都这样"。**
- **R6（验证口径）**：改完必须**重拍实拍并重量直方图**，不许靠眼睛说"好多了"
  （memory `fb-my-thresholds-degrade-good-assets`：我拍的阈值会把好素材改坏）。
- **R9（试过并回退·2026-09-18）**：P0-2 装饰聚落化**做了一轮、效果是负的、已回退**。
  实测三次: ①「跳过 water 格」把 37 株里的 32 株拦掉, 只剩 5 株, 色数 86→78
  （错在那圈青绿水**环绕在场地外围**而种子正撒在 ARENA 外; 也错在海藻珊瑚长水里本就合理,
  老账抱怨的是它们浮在**明亮水面**上像贴纸, 真正不该放的只有 void）;
  ②改成只跳 void 后 22 株, 仍被自己的分母断言判为「株数过少」;
  ③补到 31 株, 色数仍是 78 回不去。**最后看实拍才定性**: 聚落把装饰推到了左右边缘与角落
  （右侧那丛被 UI 面板遮掉大半）, 画面下方中央空了一片 —— 种子撒在椭圆环上, 正好排除了
  玩家最常看的下方中央。⇒ 回退, 只保留同批改动里对的那一处(装饰环境色偏)。
  下一轮要做, 种子分布得**贴着场地边缘均匀铺开并避开左右各 185px 的 UI 遮挡区**。
  为它加的 `tile_type_at()` 已随回退一并删除（零调用者不留）。
- **R10（缺口·本轮没解决）**：`tools/battle_scene_audit.py` **进不了门禁** ——
  它要量一张实拍图，而门禁是 headless、截不了图。于是"画面达标"这件事目前**只有人工跑过**，
  改坏了不会有人报。可行解有两条：① 门禁里用 `--audio-driver Dummy` 起带渲染的窗口再截图
  （VFXLAB/评审台就是这么干的，但重、且要串行）；② 把判据从"量截图"改成"量场景里的材质/颜色参数"
  （轻，但就变成了"量我自己的配置"而不是量屏幕，正是 memory `fb-gate-must-measure-requirement-not-my-hook` 说的假判据）。
  **没想清楚之前不硬挂**。
- **R12（新发现·2026-09-18·★门禁替我抓到的）**：换地砖那轮我**差点交一个自相矛盾的改动**。
  `verify_tile_texture` 红 6 条（3 张图 × 2 判据）：
  · ①**不是纯灰度**（silt 2927 个非灰度像素，容差 0.004）—— 管线是 `albedo_color × 贴图`，
    彩色贴图逐通道相乘会把用户 2026-07-13 锁死的调色板搅偏。生成/下载来的图默认带轻微色偏
    （实测通道最大差只有 2~8/255，肉眼完全看不出，**只有判据看得出**）。
  · ②**最亮点没到 255**（stone 只有 0.902）⇒ 石台白白暗掉 9.8% ——
    **而这一轮的全部目的就是「提亮地面」**。判据不拦，我就会交一个把自己抵消掉的改动。
  ★修法是**改素材不是放宽判据**：转真灰度 + 线性拉伸到最亮=255（stone ×1.11）。
    注意不能"把几个像素钉死成 255"——那是糊弄判据，结构一点没改善。
  ★这条也暴露了**判据的理由会烂**：②的注释写着「贴图只做减法」，而 v0.19.403 我自己
    把 `c = base*det` 换成了 `mix(0.80,1.30,det)`，det=1 现在是 1.30 倍基色，那句话已经不成立。
    判据仍然对（够不到上端 ⇒ 整片地面被系统性压暗），但**理由必须跟着改**，否则下一个人
    照旧注释推断会得出错的结论。同族见 memory [[fb-no-such-channel-needs-repo-wide-grep]]。
- **R15（标定完成·2026-09-18·P1-5 的数值依据）**：「场地边界该多厚」不再靠拍脑袋。
  **参考侧**（31 张同构候选里只有 **TFT 一款**同时具备「场内地面→边缘构件→场外材质」三段结构并且
  没被宣传框裁掉 —— 其余因 key art / 无可量边界 / 被立绘遮挡全部剔除，**样本量 1，不是 5，如实记**）：
  竖面 ÷ 角色屏幕高 = **0.81**（敏感度扫描 0.76~0.87）。必须做透视校正，不校正会得到 0.43 或 0.60（差一倍）；
  同一堵墙在近/远两端各出现一次 ⇒ 可解出地平线，两张图独立求解互差 **1.4%**（交叉验证）。
  **本项目侧**（`tests/_probe_pxm.gd` 实测，不是推导）：@1280×720 竖直 **17.54 px/米**、
  横向(公告板) **27.78 px/米**、竖/横 = 0.6317 = cos(俯角) ⇒ **俯角 50.8°**，与
  `battle_world_builder.gd:370` 注释写的「≈51°」对得上 —— **尺子先和已知答案对上才敢用**。
  龟是 `BILLBOARD_ENABLED`（朝相机，**不吃俯角压缩**），单帧归一到 `TARGET_BODY_H = 2.0 米`，
  再乘非透明包围盒占比 ⇒ **龟屏幕高中位 38.0 px**（18/28 只可量，10 只贴图结构不同取不到）。
  ⇒ 目标**屏幕高 ≈ 31 px**。换成世界米要看用哪种做法：**真实竖直几何 1.75 米**（吃 cos50.8° 压缩）／
  **朝相机的 billboard 1.11 米**（不吃压缩）——★**差 1.58 倍，混用就会把墙做高一半**。
  方案定的是 billboard ⇒ 取 **1.11 米**。（作为对比，现在 `TILE_THICK = 0.15` 只有 2.63 px。）
  ★★**结论不是「把地砖板加厚」**：加到 0.5 米也才 8.8 px，参考做法是在边界**另立一圈挡土墙构件**，
  高度接近一个角色。
  ★★**还推翻了我自己的一个判断**：我原以为边界是「受光顶沿 + 一段暗的垂直侧面」。实测相反 ——
  **竖面是全场最亮的**（L\* 比场内地面 +23%~+40%），真正暗的是夹住它的**两条 2~4 px 细缝**
  （顶沿投影 + 落地接触影）。「读出体积」靠的是**亮面被两条暗线夹住**，不是一块暗面。
  而且比明暗更强的信号是**彩度**：构件 C\* 12~25 vs 地面 C\* 35~42，构件是明显去饱和的另一种材质；
  场外只比场内暗 2.5%~8.6%，差异主要也在彩度/色相。
  ★**不可搬的**：顶沿占竖面 7.5% 这个比例是水平面、依赖相机俯角（∝1/tanθ），TFT 是长焦浅俯角，
  要按本项目 50.8° 重推。竖面高度比是深度无关的，可以搬。
  ★**动手前先问了「这堵墙有人看得见吗」**（`_probe_pxm` ⑤）——否则就是「写了没人读」的另一个形状：
  地图 58×34、非 void 1451 格、**边界格 139 格**（非 void 且四邻有 void，越界当 void）；
  用真相机投影后 **75 格（54%）落在 1280×720 画面内，49 格（35%）不被左右各 185px 的 UI 栏遮挡**。
  ⇒ 三分之一的边界是真能看见的，P1-5 值得做，但**不是一圈都可见** —— 别按"整圈"估工。
  ★★这一步我先做错过一次：无头视口是 **1280×1280 正方**（`--resolution` 在 headless 下改不动它，实测无效），
  我试图手推「1280×1280 → 1280×720」的换算并写成了中心裁切 —— 错的，Godot 的 `fov` 是**竖直** FOV，
  换宽高比时横竖两向换算不同。那一版报「139 格里只有 1 格在屏内」，**差点让我判定「造出来也没人看得见」
  而放弃整件事**。现在改成直接用真实视口，方形视口当场报废而不是给一个假数。
- **R17（未决·2026-09-18·P1-5 做完后剩下的）**：边界墙**仍是阶梯状**。
  根因不在墙上，在**场地形状本身**：岛的边界在 tile 分辨率（38.4px/格）下就是锯齿的，
  逐行发卡必然阶梯化。第一版四个方向全画时更糟（实拍读成「一层层错开互相重叠的砖带」，当场否掉），
  现在只画朝北一圈 ⇒ **从四层叠成一层，但阶梯本身还在**。
  ★要根治得让边界走一条**平滑轮廓**（marching squares 取等值线，或直接把 `arena.json` 的岛形
  改成边缘规整的形状）——那是独立一轮的活，且后者会动地图数据，**要用户拍板**。
  ★顺带：这条也说明「战斗场景不够好」不止是贴图和光照的问题，**场地的形状设计**本身也在其中。
- **R16（登记·待查·2026-09-18）**：龟立绘的**填帧率**从 42.2%(`headless`) 到 95.3%(`ghost`)，
  极差 53 pp ⇒ 屏幕高 23.4~53.0 px，**最高的是最矮的 2.26 倍**，而 `battle_spawn.gd:351`
  按帧高归一的本意是「齐高」，`RealtimeBattle3DScene.gd:1723` 更明写「本体必须填满整帧，
  否则会被压成细条」（训龟大师初版与精英小将**各栽过一次**）。
  ★**但不当 bug 报**：填得最少的三只正好是 `headless`(无头龟)/`hiding`(缩头龟)/`shell`(龟壳龟) ——
  **本来就该矮**，很可能是角色设定不是缺陷。而且 28 只里 10 只取不到（贴图结构不同），分母不全。
  要下结论得先分清「设定该矮」与「画歪了」，没分清之前不动。
- **R13（新发现·2026-09-18·★这条可能是「战斗背景很烂」的成因之一）**：
  「暗深海夜」是 2026-07-13 的**一整套**设计，但它被**劈成了两半挂在两个开关上**：
  地面那半边靠 `const MAP_V2 := true` 常开，灯光/天空那半边留在 `TILEMAP` env 后面
  （`battle_world_builder.gd` 的 `_build_environment`，两处）⇒ **正式对局两个月来跑的
  一直是「暗地面 + 亮灯光」的混搭**，没有任何一处文档写过这件事。
  ★看上去是个该补的漏接，**同种子 A/B 实测结论相反**：
  | | 中间调 | 有效色数 | `battle_scene_audit` |
  |---|---|---|---|
  | `TILEMAP` 未设（正式对局现状） | **29.3%** | **96** | 2/2 ✅ |
  | `TILEMAP=1`（配套设计） | **13.1%** | **76** | **0/2 ❌** |
  两版 `|Δ明度|>8` 占 64.1%，而**同码重跑的噪声底只有 26.4%** ⇒ 真效应不是抖动。
  ★根因：陆地 shader 吃光（`ground_land` 没写 `unshaded`，它自己的注释还专门说明了为什么），
  而水是 `unshaded` 不吃 ⇒ 一压灯只压暗陆地、青水纹丝不动 ⇒ 构图被反转成
  **「发光轮廓框着一个黑洞」**—— 正是 `ground_water.gdshader:9-10` 自己警告过的失败模式。
  ★13.1% / 76 几乎就是 v0.19.403 提亮之前的那组数（11.1% / 73）⇒ **这套灯光本身就是病灶**，
  它关着是走运不是缺陷。
  ★**未拍板不擅自删**（这是用户 2026-07-13 那天的设计意图）。已在两处分支上方写下完整实测，
  拆掉「有人来补漏接」这个陷阱。**要不要连灯光一起重做，需要用户拍板。**
- **R14（登记·未做·2026-09-18）**：`tools/zero_caller_audit.py` 的
  `ROOTS = ["scripts/systems", "scripts/scenes/battle"]` ⇒ 只覆盖 `scripts/` 下 **111/174 = 64%**，
  **8599 行的主文件 `RealtimeBattle3DScene.gd` 根本不在受检范围里**。
  文件头写的理由是「纯数据表与场景脚本另论」，但主文件不是数据表、它是核心 sim。
  ⇒ R11 那 135 行里有 71 行在主文件，就算它真的零调用者，那条门禁也一个字不会说。
  ★试算时撞到第二层原因：同一个审计器在 111 个文件上是**秒级**，扩到 174 个文件跑了 **8 分钟还没完**
  （已主动掐掉，没让它拖住门禁）⇒ **当初收窄 `ROOTS` 很可能有性能原因，不只是文件头写的「数据表另论」**。
  所以 R14 不是「把 ROOTS 改宽一行」那么简单，得先把它的复杂度降下来（现在是
  「每个函数 × 全仓源码」的朴素扫法）。存量债的数字**还没量到**，别当成已知。
- **R11（已清·2026-09-18）**：`_build_arena_ring()` 是**有调用者却到不了**的死代码 ——
  `_build_ground()` 里 `if MAP_V2 or TILEMAP or MAPEDIT: _build_tilemap_ground(); return` 在第 16 行，
  而 `_build_arena_ring(half_arena)` 在第 35 行，`MAP_V2` 常开 ⇒ 永远执行不到。
  ★`tools/zero_caller_audit.py` 抓不到它（它**有**调用者），这是"写了没人读"的一个新形状。
  而它正是 P1-5「场地边界」该落的位置，下一轮从这里改。
  ★**已清**：连根删掉 135 行（`_build_ground` 里 40 行旧 PlaneMesh 地面 + 主文件 71 行的
  `_make_ground_material` + `_build_arena_ring` + `_make_arena_ring_texture` +
  `ARENA_RING_COLOR/A`、`GROUND_NEAR/FAR`、`CAUSTIC_SPEED`），主文件 8677 → **8599**。
  验证不是"编译过了"而是**实拍 A/B**：删前 vs 删后 `|Δ明度|>8` 占 23.6%，
  而**同码重跑的噪声底是 26.4%** ⇒ 差异落在噪声之下 ⇒ 画面确实没变。
  ★配套加了门禁 `tools/const_branch_audit.py`（已进 run-tests）：专抓
  **恒真常量分支 + return 吞掉同函数后续代码**这一个形状。判据收得很紧 ——
  全仓 6 个 `const := true/false` 里另外 5 个是刻意的 A/B 开关，它一个都不报；
  把形状塞回去当场红。自检带阳性/阴性夹具，分母（182 个 .gd / 6 个常量 / 7607 条 if）全部打印。
  ★★还有一层教训：`ground_common.gdshaderinc:60` 的注释**上一轮就已经写下了这个事实**
  （「那条路径自 MAP_V2 常开后【一行都跑不到】, 等于白写」）—— 当时只把焦散公式搬走，
  **没把尸体清掉**。「发现了却只顺手取走有用的一块」才是这类死代码能活两个月的真正原因。
- **R7（遗留）**：主菜单那一轮的改动仍未提交、两个版式门禁必然红；
  `assets/sprites/menu/arena/` 下三件罗马竞技场素材是**方向错误的产物**，等定夺后删。
  见 `20260917-主菜单版式重做.md`。

---

## 验收清单

每条都要能量出来或指着实拍说"是/否"，不许"大致上"：

判据与阈值的**唯一事实源**是 `tools/battle_scene_audit.py`（方案书不存第二份数字）。

★下面前四条**已经手动验过并达标**，但仍写成未勾 —— `plans_lint` 要求"打勾必须点名哪条门禁证明了它"，
而 `battle_scene_audit.py` **需要渲染截图才能跑**（headless 截不了图，memory `fb-test-window-right-middle`），
所以它进不了 `run-tests.sh`。**没有门禁守着的东西不算完成**，这是规矩不是形式。
要转成打勾，得先解决"门禁里怎么拿到一张战场实拍"（见 R10）。

- [ ] ⏳ 中间调占比达标 —— 手动 `python tools/battle_scene_audit.py <实拍>`；**已达标但无门禁**
- [ ] ⏳ 有效色数达标 —— 同上；**已达标但无门禁**
- [ ] ⏳ 1280×720 实拍并与改前并排逐条对照 —— **已做**（`battle_ba2.png`），但属人工核对
- [ ] ⏳ 量尺本身拿**已知好样本**验过 —— **已做**（对照结果与阈值都在 `tools/battle_scene_audit.py` 文件头的标定表里，这里不存第二份）；仍属人工
- [ ] ⏳ 装饰**聚落化** —— 试过一轮**被回退**（见风险 R9），留给下一轮
- [ ] ⏳ 每个贴地装饰与单位**都有投影**（数量比 1:1，打印分母）—— P0-3 未做
- [x] 地砖图案加粗（P1-6）—— 三张**新生成**已替换（v0.19.404）；由 `tools/battle_scene_audit.py` 复量仍 2/2
- [x] 场地边界的体积（P1-5）—— **v0.19.405 已做**，由门禁 `tests/verify_edge_wall.gd` 焊住（12 条）：
  远端一圈 billboard 墙卡，世界高 1.11 米，素材 `wall-edge.png` 新生成。
  ★门禁**不是恒真式**：它**独立地从 `arena.json` 重算**一遍「朝北的连续边界段有几段」，
  再和产品真建出来的墙卡数 / 总宽对账（16 段 / 53.45 米），两边任一改坏都会红。
  变异：少画一段⇒红 2 条、段宽窄一格⇒红 1 条、墙高改半⇒红 4 条，还原后复跑干净。
  ★★**第一版这条门禁自己就是恒真式**：判据写的是 `size.y == BWB.WALL_H_M`，
  而测试也读同一个常量 ⇒ 把常量改成 0.55 时**一条都没红**。已改成钉在标定字面值 1.11 上，
  并补一条「产品常量 == 标定值」。（同族 memory：门禁自己喂那个字段再去测它。）
  标定见 R15、逐帧研究见 `docs/design/20260918-场地边界墙标定.md`。
  ★**阶梯问题没解决**，见 R17 —— 不当作完事。
- [ ] ⏳ 新素材**零复用**：`git status` 显示 `??` 不是 `M`
- [ ] ⏳ `bash run-tests.sh` 只认 `GATE_RC=0` 与 `ALL PASS (N/N)`
- [ ] ⏳ 新判据焊进门禁且**每条反向验证过会红**

---

## 决策记录

| 时间 | 事项 | 结论 |
|---|---|---|
| 2026-07-13 | 战场色调 | ✅ 用户拍板：暗蓝紫深海夜 · **硬锁调色板**（本轮不动） |
| 2026-07-15 | `height` 拍平 | ✅ 因 40+ 处贴地 VFX 掉地板下而拍平（本轮不恢复） |
| 2026-07-23 | 装饰太密 | ✅ 用户反馈后调稀疏（但没改"随机散布"本质 → 本轮改） |
| 2026-09-18 | 上一轮 151 个样本 | ❌ 用户否：「是好游戏吗，别拿低劣游戏敷衍我」→ 作废重取 |
| 2026-09-18 | 拿战斗场景当主菜单背景 | ❌ 用户否：「现在的游戏地图也很差啊」 |
| 2026-09-18 | 新样本 30 款同类型好游戏 | ✅ 已完成，五条规律见调查三 |
| 2026-09-18 | 配色病因 | ✅ 量直方图定为「明度双峰·缺中间调」，**推翻**我最初"四组颜色打架"的判断 |
| 2026-09-18 | 本方案书 | ✅ 用户：「写方案书，然后来做，你自己推进」 |
| 2026-09-18 | 我凭眼睛列的 8 条"病" | ❌ 拿 7 款已知好游戏标定后**证伪 3 条**（四角压暗/暗部占比/局部对比），只剩 2 条站得住 |
| 2026-09-18 | P0-1 补中间调 | ✅ 落地两行，两条判据 0/2 → **2/2 达标** |
| 2026-09-18 | P0-2 装饰聚落化 | ❌ 三次尝试均为负收益，**已回退**（见 R9） |
| 2026-09-18 | 本轮发版 | ✅ **v0.19.403** —— 地面 shader 绕基色调制 + 装饰环境色偏放松，随主菜单版式那轮一起出；两条判据 0/2 → 2/2 |
| 2026-09-18 | P1-6 地砖图案加粗 | ✅ **v0.19.404** —— 三张地砖全部**新生成**替换。现有 `tile-silt` 动态范围只有 **0.035**（整张图灰度在 0.51~0.55 之间，等于纯色），这是"地面死平"比 `* det` 压暗更底层的原因；新图动态范围 ×28.6 / ×7.1 / ×10.6，det 均值 0.535/0.566/0.609（不整体偏亮偏暗）。★门禁 `verify_tile_texture` 当场红 6 条，见 R12 |
