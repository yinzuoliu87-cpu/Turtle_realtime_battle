# 方案书 · RealtimeBattle3DScene 核心引擎拆分（12585 → 目标 3000）

> **交付状态（2026-08-02 复核）**：本方案书的实现【已交付】—— 最后一次改动本文件的提交
> `d287d56`（2026-07-26）就是它的落地提交：docs(校准+盖章): CLAUDE.md 版本/路径校准 + 8份本轮改过的文档加维护头 + 立文档头约定
> ⚠ 但下面验收清单里仍有 **7 条没打勾**，那是**当时忘了回填**，不是没做。
> 我（2026-08-02 整理时）**没有逐条复验**，所以不替它打勾 —— 打勾必须有证据，
> 盲打比空着更坏（空着只是没记，打了就是假记录）。要用这份清单前请自行复核。


> 📅 2026-07-26 · 维护：LuckyZ（Claude 协助）

2026-07-26。承接"大厂规范化"收尾：全项目唯一实质不规范项＝这一个 12585 行文件（全项目 32%·arch_budget 台账"欠 9585 行"）。前面已抽出角色技能/装备/大师/音效/调试场/双路/信息面板/评审台，剩下的是**核心引擎 + 一次性世界构建 + 视觉层**混在一起。

## 事实：12585 行的真实构成（459 函数·按语义归类）

| 簇 | 约行数 | 热路径? | 拆分性 |
|---|---|---|---|
| **世界/场景构建** `_build_viewport…_build_navmesh` | ~1000 | ❌ 开局一次 | ★★★ 最干净 |
| **视觉/特效** `_float/_juice/_impact/_play/_render/_cam` | ~700 | 部分(render) | ★★☆ |
| **伤害/治疗/状态** `_apply_damage*/_heal/_dot/_buff/_grant_shield` | ~800 | ✅ 每帧 | ★☆☆ 确定性敏感 |
| **弹道/sim step** `_fire*/_step*/_pending_shots` | ~450 | ✅ 每帧 | ★☆☆ |
| **生成/生命周期** `_make_unit/_spawn*` | ~1400 | 开局+召唤 | ★☆☆ 核心 |
| **目标选择** `is_*/_nearest_*/_pick_*` | ~300 | ✅ 每帧 | ★☆☆ 谓词内聚 |
| **HUD/触控/编辑UI** `_build_stats_panel/_build_edit_palette/joystick/disc` | ~800 | ❌ | ★★☆ |
| **该留**：`_do_skill` 派发·`_sk_basic/_sk_dmg` 共享基建·`_process` 主循环·`_resolve` 胜负·`_recalc_stats` | ~剩余 | ✅ | 不拆(拆＝碎化) |

## 验收清单（分层·低风险→高风险·每层独立 commit+83扫描+CI，视觉层加 F5）

- [ ] **刀1 世界构建** `battle_world_builder.gd`：viewport/tilemap(ground/decor/ambient)/camera/environment/ground/arena_ring/map_props/decorations/far_backdrop+terrain+fish+bubbles/lightshafts/bubbles/navmesh。**预计 −1000 行**。验：smoke 能建场景 0 错 + **F5 战场画面不变**。
- [ ] **刀2 视觉特效** `battle_vfx.gd`：`_float*/_juice*/_impact*/_play*` 特效生成。**−600 行**。验：smoke + **F5 打击感/飘字/特效在**。
- [ ] **刀3 渲染插值+相机** `battle_render.gd`：`_render_step`(定长步渲染插值)/`_cam_*`。**−200 行**。⚠热路径。验：**F5 移动平滑无抖 + 相机拖拽**。
- [ ] **刀4 目标选择** `battle_targeting.gd`：`is_*` 谓词/`_nearest_*/_pick_*`。**−300 行**。验：determinism 逐字节 + stress。
- [ ] **刀5 伤害/治疗/状态** `battle_damage.gd`：`_apply_damage`/`_apply_damage_from`(两条路都搬·§3.3)/`_heal`/`_dot`/`_buff`/`_grant_shield`。**−800 行**。⚠最敏感。验：determinism + stress + **F5 真打一局**。
- [ ] **刀6 弹道** `battle_ballistics.gd`：`_fire*`/sim `_step*`/`_pending_shots`。**−450 行**。验：determinism + stress。
- [ ] **（可选）刀7 生成** `battle_spawn.gd`：`_make_unit`/`_spawn*`。**−1400 行**。⚠改动面最大。

刀1-6 累计约 **−3350 行 → ~9200**；到 3000 目标必须含刀7（生成）甚至再拆主循环，边际风险陡增。

## 已知风险与未决点

**风险**
1. **热路径性能**：刀3-6 每帧调用穿过 RefCounted 边界（`battle._x_sys._fn()` 多一层）。GDScript 方法调用有开销，双路 STRESS 每秒数百单位×多效果 → 需压测确认帧时不劣化。
2. **确定性**：伤害/弹道/目标是逐字节可复现的根。搬动若改了求值顺序/RNG 取用次序 → determinism 门禁红。每刀后必跑 `verify_*_determinism`。
3. **§3.3 两条伤害路**：`_apply_damage` 与 `_apply_damage_from` 各自扣盾扣血，刀5 必须两条一起搬，只搬一条＝诡异 bug。
4. **§3.2 单位字典**：跨系统传单位字典正常（传引用），但**绝不能**在新系统里拿它当 dict key / 用 `==`。
5. **共享状态**：`_t` 全局时钟(§3.4)、`_units`、`_pending_shots` 等被多簇读写 → 留 battle、各系统走 `battle._t`/`battle._units`。
6. **`$NodePath`/`create_tween`/`await`**：RefCounted 不能用（坑11）→ 世界构建里大量 `$`/add_child/tween → 走 `battle.get_node`/`battle.create_tween`（extractor 已处理 add_child，$ 要手转或排除）。
7. **F5 覆盖不了的都靠压测**：determinism + STRESS + soak 是热路径的主网；F5 只验"看起来对"。

**未决点（要你拍板）**
- **U1 拆到哪一层？** ①仅刀1-2(安全·−1600·到~11000) ②刀1-4(+目标·−1900·到~10600) ③刀1-6(到~9200) ④一路到 3000(含生成+可能拆主循环·风险最高)。
- **U2 每刀都 F5 还是攒着一起 F5？** 视觉刀(1/2/3/5)建议逐刀 F5；纯逻辑刀(4/6)靠 determinism+stress 自动网。
- **U3 性能门槛**：是否要我在刀3前先加一个"双路 STRESS 帧时基线"探针，拆后对比不劣化才算过？（建议要——热路径拆分没有性能回归网＝盲拆）

## 执行纪律（每刀）
双网(check-only + 函数对账·battle 注入版) → reimport → 全上下文加载 → 全 83 扫描 → determinism/stress(热路径刀) → commit → push → CI 亲验 → F5(视觉刀)。arch_budget 台账每刀下调冻结值。参见 memory [[project-god-file-decomposition]]。

---

## 实施结果（2026-07-26 收官）

8 刀全部 CI 绿，主文件 **25272 → 8476 行（−66%）**，tag `v0.15.10`。

| 刀 | 落点 | 行 | 验证要点 |
|---|---|---|---|
| 1 世界构建 | battle_world_builder.gd | 715 | F5 战场画面不变 |
| 2 视觉特效 | battle_vfx.gd | 532 | F5 打击感/飘字 |
| 3 渲染/动画 | battle_render.gd | 393 | F5 平滑 + 首个热路径·性能无回归 |
| 4 目标选择 | battle_targeting.gd | 141 | determinism 两测 ✅（无RNG） |
| 5 伤害/结算 | battle_damage.gd | 452 | §3.3 两伤害路 + determinism 三测 ✅ |
| 6 弹道 | battle_ballistics.gd | 443 | determinism ✅ |
| 7 生成/生命周期 | battle_spawn.gd | 764 | determinism ✅（spawn RNG 保序） |
| 8 UI/HUD | battle_hud.gd | 768 | F5 面板/横幅 |

**未决点结局**：U1 用户拍板"一路到3000"，但实施证明 **3000 不可干净达成**——剩余 ≈4500 行是每帧耦合的核心 sim 循环（process/sim_step/tick_unit/do_skill/移动/状态），碎化它得不偿失；用户遂拍板**停在 ~8500**。U3 性能基线立了，且证明**热路径 RefCounted 边界实测可忽略（<0.2%）**——"碎化"是可维护性顾虑而非实测性能问题；期间一次 +7% 是整场压测热降频（stash 背对背隔离证伪）。

**风险全部命中且已处理**：确定性（每刀 determinism 保住）、§3.3 两伤害路（一起搬）、热路径性能（背对背对比）、内建/关键字误前缀（BI 补齐）、审计 gap（tooltip 纳入 battle_*.gd + push 前跑审计器）、内部类耦合（_build_pause_panel 排除）。顺带 F5 逮到并修了历史遗留崩溃 `battle.floorf`（财宝/赛博技能·v0.15.10）。
