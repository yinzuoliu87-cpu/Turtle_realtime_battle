# 20260820 · 阵容上传后端（让别人打到我的阵容）

状态：已拍板

## 需求原文

> 用户 2026-08-20:「对了，还有新的方案书，就是比如我把安装包下到了我的苹果手机或什么其他手机上，
> 我在里面创了我自己的阵容怎么存入后端，让其他人在其他手机上有可能打到我的阵容？」
>
> 用户 2026-08-19:「后端你选一个吧」

## 调查（实测, 每条带行号）

### ★最关键的一条: 现在根本没有后端

| 事实 | 证据 |
|---|---|
| **全仓零行网络代码** | `grep -rn "HTTPRequest\|HTTPClient\|http://\|https://" scripts/ autoload/ --include=*.gd` → 空 |
| `scripts/net/backend.gd` 叫 backend, 实际只读写本地文件 | `POOL_PATH := "user://ghost_pool.json"`（backend.gd:18）|
| 上传函数只是"写本地 json" | `upload_ghost()` = load_pool → pool_add → save_pool（backend.gd:353-357）|
| 唯一的"共享内容"是打包进安装包的策划队 | `SEED_PATH := "res://data/ghost_seed.json"`, 10 支（backend.gd:19）|

⇒ **你在手机上创的阵容从来没离开过那台手机。别人打到的"对手"只有两种:**
**① 安装包里自带的 10 支策划队 ② 本机 `make_bot()` 现编的 bot**（backend.gd:103）。

### 已经做好的那一半（这是好消息）

阵容**序列化**早就写完了, 而且很完整 —— `build_ghost_snapshot()`（backend.gd:359-407）已经存:
龟阵容 `leaders` / 分路 `lane_assign` / 每只龟的装备 `equipped` / 等级 `pet_levels` /
**小将配置与小将的装备** `minions` / 赛季场次 / **宝箱战利品进度**（schema 2, 2026-08-15 加）。
匹配逻辑也在: 分档 `bracket_for_battles()`、按档找对手 `find_opponent()`、
确定性对局随机数 `make_match_rng()`。

⇒ **缺的只有"把这个 Dictionary 送出去/取回来"这一层**, 不是从零做联机。

### 隐私: 快照里没有个人信息

`profile` 是 `{"name": "玩家阵容", "avatar": <第一只龟的id>, "id": <ghost_id>}`
（RealtimeBattle3DScene.gd:7794）—— **`name` 是写死的字面量**, 不是玩家输入。
`ghost_id = "g_<赛季号>_<三只龟id排序拼接>"`（backend.gd:346-349）。
⇒ 上传内容 **零个人信息**, 隐私政策可以写得非常短。
（注: 一旦有了真服务器, 服务端日志会留 IP —— 那属于要披露的项, 但仍是最简档。）

## 出入（需求描述 ↔ 代码事实）

| 用户以为 | 实际 | 影响 |
|---|---|---|
| "存入后端" | 存进了本机文件 | 需求不是"接一下", 是**从零加网络层** |
| 别人有可能打到我的阵容 | 目前不可能 | 但序列化/匹配都现成, 工作量集中在传输与存储 |
| —— | 本地池同时是**离线缓存**的天然形态 | 可以做成"离线照玩, 联网才同步", 不必强联网 |

## 方案

### 架构: 本地池 = 缓存, 服务器 = 交换所（改动最小）

```
打完一局 → build_ghost_snapshot() ──┬─→ 本地 pool（现状, 不动）
                                    └─→ POST /ghost   （新增, 失败静默忽略）
进入匹配 → find_opponent(本地池) ←──── GET /ghosts?bracket=N （新增, 拉回来并入本地池）
```

**为什么这样**: 现在的游戏**完全不依赖网络**也能玩(有策划队+bot)。
把网络做成"锦上添花的同步"而不是"必须联通的依赖", 断网/被墙/服务器挂掉都不影响开局 ——
这对一个测试者在国内、要靠 Sideloadly 装包的项目是刚需。

### 服务端只需要两个接口

| 接口 | 作用 | 数据量 |
|---|---|---|
| `POST /ghost` | 收一份快照, 按 `bracket` 分桶存, 每桶封顶(照抄 `BUCKET_CAP := 50`) | 一份约 1~3 KB |
| `GET /ghosts?bracket=N&limit=20` | 随机返回该档若干份 | 几十 KB |

没有账号、没有登录、没有实时对战 —— **异步 PvP 只需要这两个**。

### 选型（用户让我选一个）

**我的推荐: 先用「腾讯云开发 CloudBase 云函数 + 云数据库」。**

| 候选 | 优点 | 致命问题 |
|---|---|---|
| **腾讯云开发 CloudBase** ✅推荐 | 国内节点、延迟低; **默认域名 `*.service.tcloudbase.com` 开通即用、免备案**(已核实); 免费额度够这个量级 | 需要**实名认证**; 想绑自己的域名才要 ICP 备案 |
| Supabase | 免服务端代码(自动 REST)、免费额度大、上手最快 | 海外节点, **国内访问不稳定**, 测试者可能连不上 |
| Firebase | 生态成熟 | **中国大陆被墙**, 直接出局 |
| 自己租 VPS | 完全可控 | 要备案(国内)或忍受延迟(海外) + 运维成本, 对单人项目不划算 |

**决策逻辑**: 你和测试者都在国内 ⇒ 海外后端会让一半人连不上, 而"连不上"在这个设计里
表现为"永远只能打策划队", **和没做一样**。所以国内节点优先。

### 落地步骤（每步都可独立验证）

1. 在 `scripts/net/` 新增 `remote_pool.gd`: 两个 `HTTPRequest` 封装 + 超时 + 失败静默
2. `upload_ghost()` 末尾追加一次 POST（**不改现有本地逻辑**, 失败不影响存档）
3. 进匹配前拉一次 GET, 结果并入本地池（`pool_add` 现成, 幂等）
4. 门禁: 新增 `verify_remote_pool_offline` —— **断网时必须一切照旧**
   （判据: 把 URL 指到不可达地址, 断言开局仍能找到对手且不卡顿）
5. 服务端: 一个云函数 + 一张表, 逻辑照抄 `pool_add`/`pool_find` 的分桶规则

## 已知风险与未决点

**风险 1 · 备案这条已核实, 但免费额度没核实。**
免备案已确认(官方文档: 默认域名 `*.service.tcloudbase.com` 开通即用, 只有自定义域名要 ICP 备案 ——
https://docs.cloudbase.net/run/develop/access/client )。
**仍未核实**: 免费额度具体多少、超额后是静默降级还是扣费。开通前必须先看计费页并设消费上限。

**风险 2 · 作弊。** 客户端直接 POST 任意快照 ⇒ 有人可以伪造一支"全 SSS 满级满装备"的队。
这个量级的项目**先不做防作弊**(没有排行奖励, 伤害有限), 但要在服务端加最基本的
字段校验(龟 id 在白名单、装备数 ≤ 上限、等级 ≤ 10), 免得一条脏数据让所有人开局崩。

**风险 3 · 池子污染没有回滚手段。** 现在 `_drop_stale_schema` 靠 `SCHEMA_VER` 整批丢弃
（backend.gd:206）。远端也必须存 `schema_ver`, 否则升 schema 时**只能清库**。

**风险 4 · 成本失控。** 免费额度用完会静默降级还是扣钱? 上线前设消费上限。

**~~未决点 1 · 实名认证~~ —— 已拍板(用户 2026-08-25):「先不管」。**
  ⇒ 本方案书按**不做实名**推进; 若将来上架国内渠道要求实名, 再单开一份。
**~~未决点 2 · 阵容带不带名字~~ —— 已拍板(用户 2026-08-25):「要带但先不做」。**
  ⇒ **数据结构里预留昵称字段**(schema 先带上, 免得以后改 schema_ver),
     但 V1 不开放玩家自填、显示仍用写死的"玩家阵容" —— 隐私政策与敏感词审核等到真开放那天再做。
  一旦允许玩家自填昵称, 隐私政策与内容审核(脏话/敏感词)都要跟上 —— **建议先不做**。
**~~未决点 3 · 上传时机~~ —— 已拍板两次, 最终口径:【打赢才传】。**
  · 用户 2026-08-25:「就是要打完一局就传」⇒ 当时记成"维持现状"。
  · **2026-08-27 查代码发现「现状」其实是「赢了才传」**(那行在 `if won:` 里,
    完整条件 = 赛季模式 + 非表演赛 + 赢) —— 与"打完一局就传"字面不一致, 摆给用户。
  · **用户 2026-08-27 确认:「要打赢才传」** ⇒ 维持现状, 代码不动。
  ⚠ 我当时提议改成每局都传(理由: 池子空、只传赢的长得慢), **被否掉了**。
    别再提第二次 —— 池子里只放赢过的阵容是有意的取舍(对手偏强但值得打)。
  ⇒ `RealtimeBattle3DScene.gd` 那条不动, 现场已加长注释挡住"顺手优化"。
  要不要改成"只传赢的局"或"每赛季只传一次最终阵容"? 影响池子质量与流量。

## 验收

**实现于 2026-08-26。** 客户端整层 `scripts/net/remote_pool.gd` + 门禁 `verify_remote_pool.gd`(21 条断言)
+ 服务端规则对账 `tools/server_rule_sync.py`(进 run-tests)。服务端实现 `server/cloudbase/index.js`
**已写好但从未部署**（要腾讯云环境 + 实名，用户 2026-08-25「先不管」）—— 下面逐条标清了哪半边没验。

- [x] ✅ V1 断网启动游戏, 能正常开局并找到对手（**离线不退化**是硬指标）
      判据不是"没报错", 是**量墙钟**: 把后端指到不可达的 `127.0.0.1:9`（discard 端口，**立刻被拒**
      而不是超时 ⇒ CI 上不飘），走**真的 `HTTPRequest`** 而不是假传输，然后量 `find_opponent`
      的耗时 —— 实测 **9 ms ≪ 超时 6000 ms**，且照常返回对手。
      结构上的保证: 拉取是 `pull_async`，**给的是下一局的池子**，本局那行是 no-op。
- [ ] ⏳ V2 A 手机打完一局 → B 手机在同一 bracket 能匹配到 A 的阵容（**这是需求原话**）
      **2026-08-27: 除"两台真手机"外全部验完。**
      · 客户端 ✅(`verify_remote_pool.gd` 23 条) · 服务端逻辑 ✅ · **云上真部署 ✅**
      · 对着真云端实测: A 传上去 → B 拉下来拿到同一份、字段逐个一致、同 id 重传 4 次仍只有 1 条。
      ⇒ **只剩把包装到两台手机上各打一局**。地址已填进 `project.godot`,
        出包即可验 —— 这条留着直到真手机上跑通。
- [x] ✅ V3 服务端拒绝伪造快照(龟 id 不在白名单 / 装备超上限 / 等级 > 10) —— **客户端侧完整验**
      `snapshot_valid()` 四类各一条 + **等级 == 上限必须放行**（挡 off-by-one）
      + **分母断言在最前**: 合法快照必须被接受，否则下面全是恒真式。
      白名单取自产品自己的 `turtle_stats.STATS` / `EquipStats.STATS`，不手抄。
      ★为什么客户端也要拦（方案书原文只写了服务端）: 服务端拦的是"别塞脏数据"，
      客户端拦的是"**塞进来了我也不能崩**" —— 我控制不了服务器什么时候被绕过。
- [x] ✅ **V3-服务端【逻辑】已验(2026-08-27)** —— `server/cloudbase/index.js` 的 `reject()`
      ★"没部署"挡住的只是**部署**，挡不住**逻辑**：云函数无非 `exports.main(event) → {statusCode, body}`。
      新增 `server/local_host.js` 把 `@cloudbase/node-sdk` 换成内存库、HTTP 请求翻译成 event，
      **真的 index.js 原样跑起来**（宿主里一行 index.js 的逻辑都没复制，否则就是自己验自己）。
      `tools/backend_smoke.py` 13 条检查全过，六类伪造逐条被拒且报文准确：
      龟 id 不在白名单 / 装备 id 不在白名单 / 单只装备 4 件超上限 3 / 等级 11 超上限 10
      / schema_ver 不匹配 / bracket 越界。已焊成门禁 `tools/server_logic_gate.py`（进 run-tests）。
      反向验证：拆掉服务端等级校验 ⇒ 当场红（`HTTP 200 {'ok': True}`）。
      ★★过程里踩到一个**环境**坑，记在这里免得下次再查一遍：
      本机有 `HTTP_PROXY=http://127.0.0.1:18081`，**它会改写 POST 的 body**
      （开头多一个 CRLF CRLF、末尾截掉 4 字节）⇒ JSON 全部解析失败 ⇒ 服务端把**所有**
      快照包括合法的判成非法。表现极具迷惑性：六条"伪造被拒"全绿而**全是恒真式**，
      **是第一条分母断言（合法的必须被接受）当场揪出来的**。curl 与 urllib 都读这两个
      环境变量所以现象一致，一度让我以为是 Node 的问题；裸 socket 直连才定位到代理。
- [x] ✅ **V3-服务端云上部署已完成并验过(2026-08-27)**
      部署在 **Deno Deploy**(用户当天注册, 免实名/免信用卡): `https://turtle-ghost.yinzuoliu87-cpu.deno.net`
      入口 `server/deno/main.ts`, 存储 Deno KV, 构建 26.1 秒全绿(Install/Build 都正确跳过)。
      **对着真云端跑 `tools/backend_smoke.py` —— 13 条全过**, 六类伪造逐条被拒且报文准确。
      ⚠ 中途一坑: 本机 `HTTP_PROXY` 会改写 POST body ⇒ 冒烟工具改为**默认绕开系统代理**
      (`USE_PROXY=1` 可反悔)。验收器要量的是服务端按不按规则办事, 中间多一个改写请求的东西就量不到了。
- [x] ✅ V4 `schema_ver` 不匹配的远端快照被丢弃, 不进本地池
      喂 2 份（1 旧 1 新）→ 收 2 拒 1 入 1，三个数一起断言（只断"拒了"看不出有没有误杀新的）。
- [x] ✅ V5 上传/下载失败时不弹错、不卡顿、不影响存档
      真让 POST 和 GET 各失败一次，然后 **逐字节比对** `user://save.json` 与 `ghost_pool.json`。

## 决策记录

| 日期 | 决策 | 理由 |
|---|---|---|
| 2026-08-20 | 网络做成"可选同步"而非"必需依赖" | 现游戏离线可玩; 国内网络环境不可靠, 强依赖=一半人打不开 |
| 2026-08-20 | 推荐腾讯云开发(国内节点) | 用户与测试者都在国内; 海外后端连不上=等于没做 |
| 2026-08-20 | 先不做玩家昵称 | 一旦可自填就要做内容审核 + 隐私政策变复杂; 当前零 PII 是巨大优势 |
| 待定 | 是否做实名认证 | 国内后端前置条件, 用户决定 |
