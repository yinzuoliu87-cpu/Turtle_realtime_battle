# 实测 8 条（用户 2026-08-12 大规模测试反馈）

日期：2026-08-12　|　状态：**已完成**（10/10 · v0.19.133~138 · 全套门禁 185/185）

---

## 1. 需求原文（用户逐字）

> 1:法器装备过于强势：初始法器在未激活羁绊时为200法力值 / 2法器为180法力值 /
>   5法器为150法力值 / 8法器为120法力值 / 10法器为80
> 2:5费的直升机为什么压根没有召唤
> 3:小手枪增加限制：受到的所有治疗都将为1点
> 4:为什么有些召唤物入炮台和触手等在还没开打就可以去攻击了？
> 5:为什么我发现在上半场有枪的炮台而在下半场有触手，但不应该啊，羁绊的效果是3个战场共享的啊
> 6:为什么很多技能还有召唤物触手都会锁训龟大师，不应该啊，训龟大师只是个监视者可以吃aoe
>   但像闪电连锁等不可以锁从而造成平衡失衡啊
> 7:5费蛋糕礼盒的特殊护盾条我压根没看到
> 8:5费定海针不应该原地跳，修改为朝着敌人最密集的地方跳，但跳的时候移动的落点为250码内，
>   需要正确的显示预警区，即落点的多少范围而不是起跳的

---

## 2. 逐条调查（带行号证据）

| # | 涉及 | 现状（证据） |
|---|---|---|
| 1 | 法器满值 | `staff_synergy_system.gd:43` `MANA_FULL := [100, 80, 60, 50]`（按档位 1~4），档 0 靠 `clampi(tier-1,0,3)` 落到索引 0 ⇒ **档 0 与档 1 同值 100**。法器阈值 `phase2_types.gd:50` = `[2,5,8,10]` —— **与用户给的 2/5/8/10 完全对应** |
| 2 | `p2eq_080` 打捞旋翼机（5 费） | 文案「**登场时**召唤一架…直升机」。要查登场钩子有没有真的建出单位 |
| 3 | 小手枪（`p2eq_077` 生成物） | 它已有「受到的伤害降为 2 点」（`_dmg_cap_val`，2026-08-12 已加"有护盾就不封顶"）。**治疗没有任何限制** |
| 4 | 召唤物开打前可攻击 | 要查开局倒计时/战斗开始闸对召唤物是否生效 |
| 5 | 羁绊跨路 | ★**与我 2026-08-12 v0.19.130 的改动直接冲突**：我那次把 `_by_side` 改成**每路按场上单位重算**；用户要的是**三个战场共享同一份**。商店/背包算的是**全阵容**（`GameState.team_p2_equips_for_synergy()` = 3 统领 + 双路小将），而战斗算的是 `_calc_tiers(side)` = **只数这一路场上的**。⇒ 真正的分歧不是"清不清"，是**口径**：战斗应当按全阵容算 |
| 6 | 训龟大师被锁定 | 已有标准排除的地方（memory `fb-hand-rolled-copies-drift` 记着"赛博漏 is_trainer 锁大师"）；要穷举**还有哪些选靶没排除它** |
| 7 | `p2eq_072` 终极护盾条 | 该护盾走特殊护盾余额（`_spec`），血条上应有专属段。要查 UI 侧有没有接 |
| 8 | `p2eq_090` 定海针 | `eq_arcane_batch.gd` `_pestle_leap()` 起跳后**原地砸**；预警区（若有）画在**起跳点** |

---

## 3. 需求 ↔ 代码的出入

- **第 5 条是口径分歧，不是 bug 的简单修复。** 用户的表述「羁绊效果是 3 个战场共享的」
  ⇒ 战斗侧应改成**按全阵容算**（与商店/背包一致），而不是按每路场上单位算。
  这会**推翻 v0.19.130 的方向**（那次修的是"下路沿用上路的值"，方向上让两路各算各的）。
  正确终点：三路都用同一份（全阵容口径），于是"沿用"与"重算"都不再是问题。
- **第 1 条给的是 5 个数、而档位只有 4 档 + 未激活**，正好一一对应（未激活/2/5/8/10）。
  ⇒ 满值表要从"按档位索引 1~4"改成**按档位索引 0~4**（档 0 单列 200，不再与档 1 同值）。

---

## 4. 方案（逐条）

1. **法器满值** → `MANA_FULL_BY_TIER := [200, 180, 150, 120, 80]`（下标 = 档位 0~4）。
   `mana_full()` 改成直接按档位取，不再 `clampi(tier-1,…)`。043 的"上限 +50/25/0%"倍率照旧叠在上面。
2. **直升机** → 查 `on_spawn` 链路，补上真正的召唤。
3. **小手枪治疗封顶 1** → 与伤害封顶同一个位置思路：治疗的**共用收口**上加一道带值闸
   （不要在每个治疗源各写一遍）。
4. **开打前不许动手** → 查战斗开始闸，召唤物一并纳入。
5. **羁绊三战场共享** → 战斗侧 `_calc_tiers` 改成读**全阵容装备**（与 `team_p2_equips_for_synergy` 同口径）。
6. **训龟大师只吃 AoE** → 穷举所有"选一个目标"的入口，统一走标准排除；AoE/范围结算不排除。
7. **终极护盾条** → 接进血条的特殊护盾段（与圣盾段同一套）。
8. **定海针** → 落点 = 敌人最密处（250 码内），预警圈画在**落点**且半径 = 真实伤害半径。

---

## 5. 已知风险与未决点

| # | 风险 | 状态 |
|---|---|---|
| R1 | 第 5 条改口径后，**决胜路**（幸存者对决）的羁绊按全阵容算是否合理？阵亡的龟装备还算不算？ | **未决**：先按"全阵容恒定"实现（用户原话"3 个战场共享"），若不合意再收 |
| R2 | 第 3 条治疗封顶会不会误伤别的单位 | **待验**：闸要按单位字段开，且只有小手枪写这个字段 |
| R3 | 第 6 条"哪些算 AoE、哪些算锁定"边界模糊 | **未决**：按"选单一目标 = 排除；范围/穿透 = 不排除"实现，逐条列出改了哪些 |
| R4 | 第 8 条"敌人最密处"与已有的类似算法（080 航线选最优）可能重复 | **待查**：先搜现成的密度选点函数，别造第二个 |
| R5 | 第 1 条大幅削弱法器（100→200 未激活）会连带影响 088/089/090 三件"只有法力条"的装备 | **已知**：这正是用户要的（法器过强） |

---

## 6. 验收标准

每条都要：**门禁断言 + 反向验证（故意改坏 ⇒ 必须红）**。逐条写在实施回填里。

---

## 7. 决策记录

| 项 | 决定 | 谁定的 |
|---|---|---|
| 8 条的优先级 | 先做"数值/规则明确"的（1、3、7、6），再做"要查因"的（2、4），最后做"要设计"的（5、8） | 我定 |
| 第 5 条的终点 | **战斗按全阵容算**，三路同一份 | 用户（「羁绊的效果是3个战场共享的」） |

---

## 8. 实施回填（2026-08-13 收尾 · 10/10 完成）

| # | 需求 | 版本 | 根因 / 关键实现 |
|---|---|---|---|
| 1 | 法器满值 200/180/150/120/80 | v0.19.133 | 档位阈值本就是 [2,5,8,10]，与用户给的五个数一一对应；满值表由「按档位 1~4」改成「按档位 0~4」 |
| 2 | 直升机没召唤 | v0.19.134 | **换路顺序反了**：先生成召唤物、后跑批④撤场 ⇒ 刚生成就被清。直升机不是单位 ⇒ 节点被拔；小手枪/炮台是单位 ⇒ 人还在但驱动没了 |
| 3 | 小手枪治疗封顶 1 | v0.19.133 | 接 `_heal()` 共用收口，且在**所有乘算之后**（先放前面被 heal_amp 放大成 3，门禁抓到） |
| 4 | 摆位期就攻击 | v0.19.137 | `_sim_step` 无条件跑，`place` 闸只包住单位 tick；羁绊/召唤物/大师全在闸外。闸**不看 `_over`**（在途效果不该被掐断） |
| 5 | 羁绊三战场共享 | v0.19.138 | 是**口径**不是清不清：战斗数「这一路场上」、商店数「全阵容」⇒ 改全阵容、与商店同一个函数。**推翻 v0.19.130 的方向** |
| 6 | 大师被闪电连锁锁定 | v0.19.134 | 单体指向要走 §PICK-TARGET 闸；炮台/触手连敌表都是手写的 ⇒ 给标准层加**按阵营重载** |
| 7 | 礼盒护盾条看不见 | v0.19.133 | **满血时段宽恒为 0**（自画 ColorRect 拿 maxHp 当分母），而这盾正是开局满血时给 ⇒ 改走 HpBar（barMax 会撑开） |
| 8 | 定海针落点/预警/半空阵亡 | v0.19.136 | 落点复用 `densest_point` 钳 250 码；预警钉落点；半空阵亡「演出善终、效果作废」；演出半径改成**引用**伤害常量 |
| 9 | 特效数字残留 | v0.19.135 | 兜底扫**只扫 `_world`**，飘字挂 `_ui_layer` ⇒ 从没被扫过。用**显式标记**不用快照（快照会误伤 HUD，实测 13→0） |
| 10 | 结算按钮点不到（阻断） | v0.19.133 | 数据表每单位一行且无上限 ⇒ 卡片超视口。数据区封顶滚动 + 按钮移出滚动区 |

### 与方案的偏差

- **第 5 条推翻了 v0.19.130**：本方案书 §3 已标出这条冲突，实施时确认「正确终点是三路本来就该一样」。
- **第 4 条的闸不看 `_over`**：方案里没写这一层；门禁（③L3 预警圈收不掉）抓出来后才补，理由是本仓库靶向器那段注释早写过同样的道理。

### 新发现的坑

1. `set_deferred("x", <算式>)` 只延迟**赋值**、不延迟求值。
2. `ScrollContainer` 默认给竖滚动条**预留宽度** ⇒ 结算卡左移 5px，「居中」门禁红。
3. 开着 `DEBUG_EDIT` 的门禁在第 4 条那道闸之后要显式 `_edit_mode = false` 才推得动战斗。
4. **门禁替我抓到两次回归**（第 4 条把 `_over` 写进闸、第 9 条用快照判常驻），都不是我看出来的。
