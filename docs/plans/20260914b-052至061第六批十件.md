# 052~061 第六批逐件体检（10 件并行）

状态：实施中（随 **v0.19.379** 发出）

---

## 需求原文

- 用户 2026-09-14：「主线的话现在是 6 件并行对吧，**改成 10 件并行**」
- 用户 2026-09-14：「主线你就不要停，**一直做直到所有装备全搞完**」
- 用户 2026-09-14：「我一直说主线不要停，**我有空我就会来验收一个**」
- 铁律（不变）：数值不擅自改 / 素材不复用除非点名 / 本地 commit 不 push /
  演出必须就是效果本身 / 共享原语被否就换原语

本批：052 左轮 / 053 霰弹贝古 / 054 瞄准镜 / 055 病毒箭头 / 056 飞镖 /
057 狙击长管 / 058 远古炮台 / 059 沙漏 / 060 磷光水母伞 / 061 钻孔螺。

---

## 调查

### 一、接线体检：这一批比前几批干净

逐条查了"写了没人读 / 读了没人写 / 两条钟 / 手抄副本"四族，**结论多数是健康的**：

| 件 | 查了什么 | 结论 |
|---|---|---|
| 052 | 弹匣从哪来、谁扣、谁加、封不封顶 | 初始值由 `equip_stats_apply` 写（按路重置）、`mini(6, …)` 封顶、0 发不开火 —— 健康 |
| 055 | 「累计造成 400 伤害」这个计数器 | 读的是 `_st_dealt`（伤害面板那个累计量），两条伤害路径都在记 —— 健康 |
| 056 | 「靶子」标记谁挂谁读 | `battle_damage.gd:568` 击飞时挂、`battle_render.gd:352` 到期移除 —— 健康 |
| 058 | 炮台的两条挂钩 + **HP_MULT 陷阱** | `_turret_aspd_mult` 有真读者（攻速算式两处）；**炮台血没有多乘 HP_MULT**（`_spawn_summon` 里是 `"hp": hp`）—— 健康 |
| 060 | `"stat": "dodge"` 是不是没人读的字段 | **是 buff 的 stat 名**，`_recalc_stats` 会把它累加进 `dodge_bonus`（还带 `DODGE_CAP` 封顶）—— 健康 |
| 055/056 | 结算有没有埋在 tween 里 | `hookbomb_system` 自己就写着「中间那段等待走 `_pending_shots`（sim 驱动·无头稳），不挂 tween」—— 健康 |

**058 那条值得单独记**：它旁边 032 骷髅的注释里记着一次同样的事故
（「旧写法 `[19,21,25]×3` = 57/63/75，属于 CLAUDE.md §3.1 那类"多乘一次"的旧账」），
而炮台是同一个函数、同一种写法 —— 查下来没问题，但这正是**必须查**的理由。
门禁里那条断言同时守着这个陷阱（反向验证：给它乘个 3 就当场红）。

### 二、057 狙击：三条真缺陷

| # | 缺陷 | 证据 |
|---|---|---|
| ① | **瞄准线指着 A、子弹可能打 B** | `_eq_sniper_charge_then_fire` 选 `low` 并画线，1 秒后 `_eq_sniper` **重新选一遍**最低血 |
| ② | **瞄准线终点是蓄力那一帧算死的** | `_sniper_charge_fx` 画一条 `dur=1.0` 的静态线，目标在这 1 秒里会走 |
| ③ | **同一个选靶写了两份** | 蓄力用 `CombatMath.hp_frac`、开火用裸 `o["hp"] / o["maxHp"]`（连 maxHp=0 都不防） |

①最要紧：文案写的是「**锁定**生命百分比最低的敌人并蓄力 1 秒，随后开枪」——
锁定就是锁定。②是「链最容易断在位置上」那一族。③是「手抄的副本必然落后」。

### 三、061 的台子配错：把要看的东西关掉了

原有的 `p2eq_061` 台子配的是 `"ui": true`，而 `ui` 会**把飘字连同整层 UI 一起关掉**。
而 061「满 20 层破损后这段伤害转**真实伤害**」的读法**正是飘字变色**（蓝 → 白）——
配 `ui` 等于把要验的那一下关了。这是第四批已经栽过的同一个坑。
而且 `dur` 只有 12 秒、不加攻速，每击叠 2 层根本攒不到 20 层，**那一下一帧都拍不到**。

---

## 出入（与原设想的差异）

1. **原以为"十件台子全是 0"** —— 错的。我用 `"eq": "p2eq_060"` 去数，而台子可以
   **只用键名注册**（`get_case` 自己把键名当 eq）。060/061 本来就有台子，
   我还照着错结论插了重复键，把 `vfxlab_cases.gd` 写到无法解析。
   **这是本轮第五次尺子不匹配形状。**
2. **原以为这一批会像前几批一样有一堆接线缺陷** —— 实际接线很干净，
   缺的是**覆盖**（零门禁 / 台子配错）。所以本批的产出重心从"修"转向"焊判据"。

---

## 方案

### A. 057 三条一起修（不动数值）
- 锁定目标带到开火那一刻（`locked` 参数），只有它死了/不可选才重选；
- 瞄准线每 `SNIPER_AIM_STEP`(0.2) 秒重画一次，每次**现读当前坐标**，排在游戏钟上；
- 选靶收口到 `_sniper_pick_lowest` 一处。

### B. 新门禁 `verify_equip_052_061`（59 条）
判据全部量**产品自己的账**，期望值写死在门禁自己那儿。几条值得记的判法：

- **053 弹珠数**：弹珠没有数组容器（走 `_ballistics._shotgun_pellet` 的回调），数不出来
  ⇒ 把敌人放到 **60 码近处**让整个扇面都打在同一个人身上，**总伤害 ÷ 单颗 = 颗数**。
- **054 必中**：带镜 20 发**全中**（必中是确定性的）；**反证**不带镜 40 发只中 11 发。
  没有那条反证，闪避根本没生效也照样绿。
- **060 半径**：伞内拿到减伤与闪避 **+ 伞外一点都没拿到**。
  没有后半条，半径写成无穷大也照样绿。
- **057 锁定**：蓄力期间把**另一条线上**的敌人压成全场最低血，枪必须仍打锁定的那个。
  配一条分母断言「两者不在同一条直线上」——否则打谁都会把两个都扫到，判据分辨不出来。

### C. 台子
补 052/053/055/056/058/059 六个；订正 061（`ui` → `bars`、12 → 24 秒、攻速 ×2、敌人 40 万血）。
**054 故意不配台子**：与 047 同类，纯属性件（「造成的伤害无视闪避」），一个节点都不建。

---

## 已知风险与未决点

- **风险①**：059 沙漏的时停整段**还没有门禁**。它牵动全场 tick，判据要构造"时停期间只有
  携带者能动"的证据，成本高。台子已建（`dur` 34 秒，拍点压在解除前后 30.80/31.20 ——
  解除那一下是最容易出问题的地方），但**目前只有实拍没有断言**。
- **风险②**：056 飞镖的完整因果链（普攻 ×5 → 强化击飞 → 挂靶子 → 下周期射镖 → 靶子消失）
  只有台子，**没有门禁**。它跨多个周期，判据要推很长的游戏钟。
- **风险③（我量不到）**：061 的破损裂纹覆盖层"随层数越来越密"只能实拍看，门禁量不了。
- **未决①（全仓性，不属于本批）**：玩家读「X% 最大生命值」会对着血条算，
  而本仓口径是 `maxHp / HP_MULT × pct` ⇒ 实际是屏幕数字的 1/3。
  039 竹箭 / 盾羁绊冲击波 / 020 哑铃 / 047 重击锤都是这个口径。
  要改是**统一改文案口径**，属另一件事。

---

## 验收

- [x] 052/053/054/055/057/058/060/061 的判定链有断言 —— 门禁 `verify_equip_052_061`
- [x] 逐条反向验证会红 —— 逐条改坏产品代码后跑 `verify_equip_052_061`，11 条变异各红在各自那条判据上，还原后复跑全绿
- [x] 057 的「锁定」名副其实 —— 门禁 `verify_equip_052_061` 判据⑦（含"两者不在同一条线上"的分母）
- [x] 058 的 HP_MULT 陷阱被守住 —— 门禁 `verify_equip_052_061` 判据④（反向验证：给召唤血乘个 3 当场红）
- [x] 台子配置合法 —— 门禁 `verify_vfxlab_case_config`
- [x] 架构预算达标 —— 门禁 `tools/arch_budget.py`（狙击与霰弹的纯演出搬进 `battle_vfx`，3011 → 3000）
- [x] `bash run-tests.sh` 全绿 —— 判据只认日志里的 `GATE_RC=0`
- [x] 版本号五处 + tag + CHANGELOG + 路线图 —— 门禁 `tests/verify_version.gd` + `tools/docs_authority_lint.py`
- ⏳ 059 沙漏的时停门禁（见风险①）
- ⏳ 056 飞镖因果链的门禁（见风险②）

---

## 决策记录

- **2026-09-14 用户定**：批次从 6 件改为 **10 件并行**；主线不停，用户有空来验收一件。
- **2026-09-14 我定**：054 不配台子（与 047 同类，纯属性件）。写进台子表的注释里，
  免得下一个人以为是漏了。
- **2026-09-14 我定**：061 的台子从 `ui` 改 `bars`。依据是 `equip_readouts.gd` 表头那条
  规矩与第四批的实拍教训 —— 飘字是这件的核心读数。
