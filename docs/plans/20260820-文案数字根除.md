# 20260820 · 文案数字与代码分歧的根除

状态：实施中


> 用户 2026-08-19/20:「很离谱为什么有这么多分歧？到底网上怎么处理」「怎么根除」「逐条看不行吗」

---

## 需求原文

> 用户 2026-08-19/20:「很离谱为什么有这么多分歧？到底网上怎么处理」「怎么根除」「根除了吗」
> 「你这里不搞完，我根本不敢把新需求给你」「逐条看不行吗」

## 调查(实测数据, 不是推理)

### 根因

同一个事实在**五层**各写一遍, 而**没有任何一层是从另一层生成的**:

| 层 | 例子 | 谁维护 |
|---|---|---|
| 代码常量/字面量 | `tgt["maxHp"] * 0.22` | 改数值的人 |
| 代码注释 | `## 引爆 22%` | 靠自觉 |
| JSON 结构化字段 | `cd: 6` `energyCost: 100` | 配置的人 |
| 玩家文案 ×3 | brief / desc / detail | 写文案的人 |
| 文档 | docs/design/*.md | 谁想起来谁改 |

改一处不会让其余四处报错 ⇒ **分歧是默认结果, 一致才是意外**。

实测分母(`tools/number_coverage_audit.py`): 玩家文案 576 段 · 数字 2913 个。
其中占位符 456(15.7%) · 有审计器逐个对代码验的 1005(34.5%) · **没人验 1452(49.8%)**。

## 出入(需求描述 ↔ 代码事实)

用户说的是「文案有分歧」, 但**逐条查下来分成三类, 修法完全不同**:

| 类 | 实例 | 该改哪边 |
|---|---|---|
| **文案落后于代码** | 熔岩龟整只停在 2026-08-14 削弱之前(五段250%/0.67灼烧/12%回血) | 改文案 |
| **两句话互相打架** | 财神梭哈: 首发盾与变身金盾的 ×1/×5 被写反 | 改文案(以代码为准) |
| **★代码自己没生效** | 幽灵龟 2026-07-28 的削弱改在了零调用者的死表上, 玩家从没吃到 | **要用户拍板**(动它=改平衡) |

第三类不是文案问题, 逐条核对才能发现 —— 这是「逐条看」相对「机械转引用」的额外价值。

## 方案

### 业界怎么处理(三种, 本项目全部要)

| 做法 | 本项目对应 | 状态 |
|---|---|---|
| **单一事实源 + 生成** 数值只存一处, 文案从它渲染 | 占位符 `{N:}` `{P:}` + 新增 `{C:Class.CONST}` 直接引用代码常量 | 机制已通 |
| **一致性 CI** 两处都手写, 但机器逐条对账 | tooltip_number_audit / text_claim / text_formula / basestats / tri_audit / brief_detail | 已覆盖 1005 个 |
| **金样本(golden/approval)** 把玩家看到的成品钉死, 任何改动必须显式重新祝福 | `tools/text_golden.py` + `tests/golden/text_snapshot.txt`(483 段) | 已在门禁 |

★ 三者的分工: 生成**消灭**分歧 · CI **发现**分歧 · 金样本保证**改造过程本身**不误伤玩家可见内容。

## 3. 验收清单(逐条可判定)

- [x] A1 `{C:Class.CONST}` 与 `{C:Class.CONST%}` 可用, 图鉴/商店/战斗三处共用一份渲染
- [x] A2 金样本覆盖全部 483 段玩家文案, 进门禁
- [x] A3 `number_coverage` 棘轮**只降不升**(当前基线 1452)
- [x] A4 拿水晶龟走通一遍完整流水并验证「玩家看到的字一字未变」
- [ ] B1 28 只龟的文案数字**逐条**对代码核对一遍, 产出「真错了的」清单
- [ ] B2 138 段装备文案的写死数字**逐条**核对(三档值已由 tooltip_number_audit 覆盖, 不重复)
- [ ] B3 B1/B2 查出的每一条: 修 + 门禁反向验证会红 + 提交
- [ ] B4 每修一条, `number_coverage` 棘轮跟着收紧

## 4. 已知风险与未决点

**风险 1 · 「全部转成引用」是按周算的, 今晚做不完。**
110 个条目 / 1452 个数, 水晶龟 5 个数花了十几分钟。
⇒ 今晚的目标定为 **B1/B2「找出真错的并修掉」**, 而不是 B「全转成引用」。
两者价值不同: 转引用是**未来不会再错**, 核对是**现在没有错**。先保证后者。

**风险 2 · 自动把字面量转成常量引用不可行(已实测否掉)。**
按数值匹配: 装备侧 20% "可映射", 但全是垃圾(`equip_system.gd` 里含全部装备 id,
任何值为 6 的常量都能匹配上暴君之牙); 龟侧 13 条命中里 2 条是**同值不同义**
(寒冰的 20 是伤害百分比, `ICICLE_MAX` 是叠层上限)。⇒ **只能逐条人工判语义**。

**风险 3 · 分母本身有污染(已发现一例, 方向安全)。**
`FPGA板` 文案里的 `00/01/10/11` 是 2-bit 状态标签不是数值, 却被计入「没人验的数字」。
方向是**高估**(棘轮更严), 故不修; 但报数时要知道 1452 里含这类噪声。

**风险 4 · agent 会报错东西。**
6 路并行对账的结论**不能直接采信**: 要求每条带 `file:line` + 两个具体数值,
「找不到代码」必须单独归类不许算成不一致 —— 但仍需主会话逐条复核后才动手改。

**未决点 1 · 088 涨潮碑的数值没有用户原话**, 历史记录里查不到设计意图, 需用户拍板。
**未决点 2 · 剩余 1452 个数字的转引用工作要不要做、按什么优先级做**, 需用户定。
我的建议: 按「玩家最常看 + 最容易改」排序, 每次改到某只龟/某件装备时**顺手把它那几个转掉**,
而不是专门开一个大工程 —— 棘轮保证它只降不升, 时间会把它磨到 0。

## 实施台账（每批回填 · 用户 2026-08-22:「查过的都记录好」）

> **基线只降不升**：`tools/number_coverage_audit.py` 的 `BASELINE`，进 `run-tests.sh` 门禁。
> 起点 1722（2026-08-22 当天开工前）。每批的做法与踩过的坑见对应 commit。

| 批 | 转了哪些段 | 基线 | 这一批查出的**真问题** |
|---|---|---|---|
| 1 | 龟壳·气场觉醒 / 熔岩龟·熔岩之心 | 1722→1675 | 三个工具各抄一份常量正则且**没锚行尾** ⇒ 推导式常量被截成半截，`pet_code_scope` 把「减速 20%」读成「加速 ×1.0」。抽出 `tools/derived_consts.py` 统一 |
| 2 | 海盗龟四技能 / 双头龟·双生 | 1675→1633 | **海盗船 detail 末尾灰字写「射程 300」，brief 与代码都是 110** —— 同一条技能两份文案自己打架 |
| 3 | 赛博龟·浮游炮 / 糖果龟三技能 | 1633→1578 | 首次跌破 50% |
| 4 | FPGA板 / 寒冰·冰霜 / 熔岩·熔岩爆发 | 1578→1540 | 装备评审表自称「由 json 生成」，改 json 后漂了 ⇒ 跑生成器重生成，不手改 |
| 5 | 水晶·壁垒 / 无头·万千触须 / 忍者·忍术 | 1540→1489 | `grep ninjaInstinct` 全仓 **0 命中**，差点报「文案吹牛」——实际落点在 `battle_spawn.gd` 一行裸赋值 |
| 6 | 小龟·不屈 / 钻石两技能 / 龟壳·暗影重复块 | 1489→1424 | ①钻石滚球被动函数头注释写「100 码」而代码 07-12 已是 200 ②龟壳 detail 末尾**重复粘着一份 brief 的旧字面量**，第一批漏了 |
| 7 | 石头·嘲讽 / 赌神·多重打击 / 猎人·精准射击 | 1424→1389 | — |
| 8 | 竹叶·生长 / 无头·灵魂打击 / 彩虹·反射+强化棱镜 / 寒冰·冰锥 | 1389→1348 | 竹叶生长是**两套值**（选没选「竹击」），原本挤在主场景一行三元表达式里 |
| 9 | 打捞旋翼机 / 电鳗双管铳 | 1348→1336 | 装备**三元组**已由 `tooltip_number_audit` 覆盖=②类；③类是**标量**。080 的常量代码里早就齐了、只差文案引用 |
| 10 | 小龟·龟派气波 / 双头·精神干扰·吸收 / 双头·融合 | 1336→1302 | `text_formula_audit` **分母塌了**(→94)并自己报「判据可能失效」——教它先展开 `{C:}` 再比, 分母回到 **125**(比转换前还高)。另: r6 一条断言同时查「加」与「减」两半, 我只改了"加", 差点造出它正在防的分歧 |
| 11 | 凤凰·烫伤 / 闪电·连锁 / 线条·画龙点睛 | 1302→1281 | `verify_text_render` **当场抓到我**: `line_system.gd` 常量块锚点没匹配上(它的 `var battle` 前另有 const), 常量没插进去但代码与文案的引用已经写好 ⇒ 编译会炸、文案会露 `{C:}`。门禁在提交前就红了 |

| 12 | 信号放大器 / 手半剑 | 1281→1265 | 084 的扇形半角在 `BladeEqVfx` 里存的是**弧度字面量**(1.0471975512), 而文案写"120 度" ⇒ 改成存**全角度数**、半角由 `deg_to_rad(× 0.5)` 推导。占位符占比首次过 30% |

| 13 | 玩偶小熊 / 温泉蛋 | 1265→1250 | 温泉蛋的孵化进度**五个来源散在四个文件**(周期 tick 在 `equip_system`、敌我死亡在主场景、造成/承受伤害在 `battle_damage`)，文案把五个数抄在一句话里 |

| 14 | 小龟·过肩摔 / 寒冰·团队护盾 / 熔岩·地裂 / 猎人·狩猎弹幕 / 星际·虫洞 | 1250→1208 | ★**基线一度不降反升(1250→1208 中间报过 1208>1198)**: 我先按预估写了基线, 门禁当场红并打印「新加的数字要么用占位符, 要么补覆盖」—— 这条 ratchet 的方向判断做得对, 按实测值改回即可 |

| 15 | 线条·墨迹/墨水炸弹 / 凤凰·灼烧 | 1208→1187 | 墨迹的"每层 5% 真伤"落在 `battle_damage` 的伤害管线里、而上限 7/10 在 `line_system`, 文案把两处写在同一句 |

| 16 | 宝箱·财宝风暴/藏宝图 / 财神·梭哈 | 1187→1172 | 开箱阈值与逐箱回血都是**数组**, `{C:}` 直接渲染成 "3000/6000/10000/18000/30000" 与 "8/8/11/11/15" —— 文案原样, 一个数都不用手写 |

| 17 | 双头·技一 / 彩虹·棱镜 / 凤凰·强化涅槃 / 赌神·命运之轮 | 1172→1133 | ★命运之轮四花色的八个数在代码里**写了两遍**(抽到时 / 复活后套用) —— 提成常量正好把这份重复消掉, 替换报告里那四行显示 `OK 2` 就是证据 |

| 18 | 骰子两技能 / 熔岩·岩浆涌动+重击 / 水晶球 / 香火石 | 1133→1100 | ★**审计器自己有个误报**: FPGA 板的 `00/01/10/11` 是 2-bit **状态名**不是数值, 被当成"没人验的数字"。加了一条 `LABEL_NUM`(判据是**紧跟等号**, 不是裸数字)把它们摘掉。⚠ 修这条时我用**非 raw 字符串**写正则, `` 被 Python 解释成**退格符 0x08** 写进文件 ⇒ 正则永远匹配不上, 而 `grep` 看不出来(退格符不显示)。查了三轮才发现, 靠的是 `print(repr(pattern))` |

| 19 | 钻石·坚不可摧 / 财神·聚宝盆 / 赌神·赌注 | 1100→1074 | ★`codex_text_lint` 抓到一个**只有转换后才会出现的新问题**: 原文写「化为**七段**」(中文数字), 换成占位符后渲染成「化为7段」⇒ 数字贴着汉字, 违反排版惯例。补空格。**转换不是纯机械替换** —— 中文数字换成阿拉伯数字会改变排版 |

| 20 | 缩头乌龟防御/强化随从 + 霰弹贝古/银色箭袋/连发弩机 | 1074→1043 | 五段常规转换 |
| 21 | 竹叶竹刺阵 / 猎人隐蔽 / 磷光水母伞 / 珊瑚糖糕 | 1043→1017 | 竹刺阵的半径**写了两遍**(预警圈一次、伤害判定一次), 合成一份 |
| 22 | 财神梭哈+招财进宝 / 石头岩石护盾 | 1017→997 | ★这一批**抓到根除动作自己制造的毛病**, 见下 |

### ★★第 22 批：根除本身在制造它要消灭的毛病

给财神加 `LOWHP_TRIGGER := 0.20` 并把文案指向它。**所有门禁全绿** —— 它确实渲染成 20%。
但产品真正读的是【早就存在】的 `LOWHP_PCT`（`battle_damage.gd:445`），值也是 0.20。
**同一个数存了两份，文案指着没人读的那份。** 谁以后改 `LOWHP_PCT` 而没改副本，文案就开始说谎，
而现有门禁一条都发现不了 —— 它们只验"占位符能渲染成数字"，不问"这个数字是不是活的"。

⇒ 建 `tools/text_const_orphan_audit.py`（已进门禁）：每个 `{C:Class.CONST}` 指的常量，
在声明行以外必须至少还有一处 `.gd` 引用。零引用 = 它只为文案而活 = 天然会漂。

**一开门就是 60 个孤儿（分母 460）**，不是个案。查下来分四类：

| 类 | 数量 | 真相 | 处置 |
|---|---|---|---|
| 加了常量但**没接线** | 44 | 我第 18 批起只做了"加常量 + 改文案"两步, 漏了第三步"改代码"。`_lava_flame_strike` 里仍是字面量 `1.3`/`0.08`/`0.20` | 逐个接线(A/B/C/D 四批·每条 old 串要求文件内**恰好一次**) |
| **同一个数存了两份** | 4 | `LOWHP_TRIGGER`↔`LOWHP_PCT` / `FLAME_ARC_DEG`(全角70)↔`PHX_CONE_HALF_DEG`(半角35) / `SCALD_DEBUFF_SEC`↔通用 `BUFF_SEC` / `EGG_IV`↔全局 `EQ_TICK` | 删副本, 文案改指真源。凤凰那个把主场景的半角删掉、事实源移回技能自己家并存**语义值**(全角), 半角改推导 |
| 推导常量 | 4 | `BURN_LIFE = BURN_TICKS * BURN_TICK_SEC` 这类 | 审计器自动豁免(结构上不可能漂) |
| **输出不是输入** | 8 | `*_KNOCK_SEC` 滞空秒数是 `_knockback(dist, vy_mult)` 抛物线**算出来的结果**, 源码里没有"0.82"这个量可引用; `SMASH_KNOCK_DIST` 同理(真值是 `push_mult=9.25`) | 族豁免 + 第二只眼: 该文件必须真的在调击飞, 否则报"豁免不成立" |

反向验证：把 `_lava_flame_strike` 的 `STRIKE_ATK_COEF` 改回 `1.3` → `FAIL x1`，还原 → `ALL OK`。

**中途差点误报一个 bug**：`p2eq_036` 温泉蛋不在任何周期表里，我以为"每周期 +5"永远不触发。
查下去发现那个 case 在 `_eq_tick`（**每帧**函数，自带 `EQ_TICK` 闸门），不是周期分发器 —— 没有 bug。
教训还是那条：**先读清楚被测对象在哪个调用链上，再下结论。**

| 23 | **泡泡龟整只**(被动+四技) + 熔岩烈焰暴走 | 997→966 | 泡泡龟的 `bubble_system.gd` **一个常量都没有**, 数值散在**三个文件**: 被动在主场景 `_tick_unit`、束缚削甲在 `battle_damage`、技能在自己家 |
| 24 | 忍者手里剑+忍者足 / 溺者的浮囊 / 深海气压罐 / 毒蛾茧 | 966→943 | 新门禁抓到我又造了两份副本(见下) |
| 25 | 天使祝福+平等 / 骰子孤注一掷 / 彩虹全色风暴 | 943→917 | 「对目标打击4 段」汉字贴数字, 补空格 |

### 新门禁上线后立刻抓到的（它建对了的证据）

`text_const_orphan_audit` 在第 23~24 批里响了 **3 次, 全是真的**:

| 抓到 | 真相 | 处置 |
|---|---|---|
| `BubbleSystem.FOAM_STORE_PCT` | 文案说「储存受到伤害的 100%」, 而代码里这个 100% 是**隐式**的(直接 `+ d`, 没有系数) | 改成显式 `* FOAM_STORE_PCT` —— 那个 100% 现在才是真能调的 |
| `EqPotionBatch.CAN_LASER_RANGE` / `CAN_LASER_SEC` | **又造了两份副本**: 真源在 `potion_eq_vfx.gd` 的 `BEAM_RANGE`/`BEAM_SEC`(伤害 tick 读的就是 `Vfx.BEAM_SEC`) | 删副本, 文案改指真源 |

累计「同一个数存两份」已发现 **6 处**。没有这条门禁, 这三处都会照常全绿混过去。

同批还差点错两次, 各被另一条判据挡住:
· 毒蛾茧的类名是 `VenomDroneSystem` 不是我以为的 `EqVenomDrone` —— **渲染门禁**挡住(占位符解析不出来)。
· `_eq_grant_energy(u, 5.0)` 有两处(065 与 068 巧合同值) —— **脚本自带的"必须唯一命中"**挡住。
  ⇒ 这两条各自都值钱: 一条管"占位符能不能解析", 一条管"我改的是不是那一处"。

| 26 | 闪电龟被动(雷电) + 雷盾 + 闪电连锁递减 | 917→901 | ★电击标记上限 8 散在**四个文件五处**, 全是裸字面量; 文案手写的 0.54/0.324 是 `0.9×0.6` 与 `0.9×0.6²` —— 改成**推导常量**, 不存第二份 |
| 27 | 寒冰寒域(被动) / 猎人猎杀(被动) | 901→886 | 寒域三个 `0.7` 是减速后的**倍率**, 文案说的是"降低 30%" ⇒ 存**语义值**(降幅), 倍率现推 |
| 28 | 宽刃弯刀 / 连发弩 / 香火石简述 | 886→871 | 月光斩是 500~800 码的**环形带**不是整块扇形, 释放点自选偏移的 650 改成 `(R_IN+R_OUT)/2` 推导 |

第 27 批新门禁又抓到一个, 而且是**文案与代码的真分歧**:
`HunterSystem.EXEC_SCAN_IV` 报孤儿 ⇒ 去查"每 0.1 秒扫描"在代码里是什么, 发现节流确实是 0.1s,
但**斩杀线 0.24/0.14 在主场景又写了一遍**(`hunter_system` 里早有 `EXEC_MARKED`/`EXEC_BASE`)。
⇒ 第 **7** 处「同一个数存两份」。孤儿报警把我领到了一处它自己都没在查的重复。

### 这一路上建立的手法（后来者照做）

1. **顺序**：跑审计器挑最大段 → dump 文案到文件用 Read 看（控制台 GBK，直接 print 中文会崩）
   → 找实现提常量 → 文案改 `{C:类名.常量}` → **brief 与 detail/desc 两份都要转** → 验 → 降基线 → 全套门禁 → 提交。
2. **`{C:X%}` 只 ×100 不带 % 号**，文案要写 `{C:X%}%`。
3. **`{N:expr}` 不认代码常量**（只认 `build_vars` 的变量），写错了会**原样吐回、不留花括号**。
4. **存语义值不存实现值**：减速存 20% 不存移速 ×0.8；锥角存全角不存半角 —— 否则文案又得手写一个数。
5. **能推导就推导**：`BARRAGE_LIFE = (跳数−1)×间隔`、`RAMPAGE_HASTE_MULT = 1+比例`。
6. **平衡门禁必然红**（已 8 次）：它们断言的是"源码里含某个字面串"，提常量后当场失败但**需求没变** ⇒
   改成比**常量的值** `is_equal_approx(X.CONST, WANT)`；文案侧改成先 `SkillText.render_consts` 再比。
7. **新增 `class_name` 文件后必须跑一次 `--import`**，否则它不在全局类表里，
   **连引用它的那个类**的 `{C:}` 也一起静默失效。

### 新增的门禁

- **`tests/verify_text_render.gd`**（第 6 批建）：在**引擎里**渲染全部 405 段（龟 brief/desc/detail + 装备四个字段），
  断言一个花括号都不许剩、且不许残留大写常量名样式的裸标识符。
  ★它管的那层 Python 审计器**结构上看不到**：`{C:}` 有两条独立解析路径
  （审计器自己正则解析 `.gd` / 游戏走 `get_global_class_list()`），两条随时会不一致。

## 决策记录

| 日期 | 决策 | 理由 |
|---|---|---|
| 2026-08-20 | 今晚目标定为「找出真错的并修掉」, 不是「1452 个数全转引用」 | 后者按周算; 两者价值不同(转引用=未来不会错, 核对=现在没有错), 先保证后者 |
| 2026-08-20 | 给 `RealtimeBattle3DScene.gd` 加 `class_name` | 让文案能 `{C:RealtimeBattle3DScene.BUFF_SEC}` 引用全局战斗常量; 否则每个时长都要手抄 |
| 2026-08-20 | `number_coverage` 基线 1452 → 1734(**上调**) | 修正我自己的分类错误: `{N:0.5*ATK}` 里的 0.5 是手写系数、照样会漂, 原来被算进"不可能错" |
| 2026-08-20 | 幽灵龟按**活代码**(0.4物+0.9真)改文案, 不动平衡 | 事实源是代码; 那次削弱要不要真落地是设计决策, 需用户拍板 |
| 待定 | 哑铃/竹制弓箭「X% 自身最大生命」实发是 1/3(除了 HP_MULT) | 是改文案还是去掉除法, 需用户拍板 |
