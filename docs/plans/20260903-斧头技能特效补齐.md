# 斧头技能特效补齐 + 龟能条 + 机器人 AI 自杀参数

> **状态：已完成**（v0.19.321 → v0.19.322 → **v0.19.323** · 2026-09-03）
>
> · v0.19.321 — 特效从零补齐（§1~§8）
> · **v0.19.322** — 参考逐帧复核后的第二、三轮修正（§9~§10）：填充回来了 + 起手闪
> 未决点见 §5，实施回填见 §8。

---

## 1. 需求原文

用户 2026-09-03 逐字：

> 斧头不是随着被动有各种技能呢？都开窗口看看

> 所以是钻石以及4个最终造物什么特效都没有是吧

> 那你这太失败了啊，也就是说快照全部要重新跑因为你装了蠢ai，以及斧头特效要全部补齐且不能敷衍

> 为什么我没看到龟能条

> 那你自己全部做完，不要停，我早上来验收，给你10小时

★ 更早的相关铁律（本次全程适用，逐字）：

> 不要拿图片贴图敷衍我，我要动画像素特效  ——2026-08-29

> 素材不复用除非点名  ——2026-08-03

---

## 2. 调查结论（每条带 `文件:行号`）

### 2.1 龟能条：条被 hide、填充又不读它

| 证据 | 事实 |
|---|---|
| `RealtimeBattle3DScene.gd:2149-2157` | `_make_status_bar` 里确实建了龟能条（血条下方 3px `#48c9ff`），初始宽度 0 |
| `battle_spawn.gd:853` | `bar["en"].visible = false` —— **通用召唤体的龟能条被统一隐藏** |
| `axe_system.gd` 全文 | **零处** unhide —— 斧头是通用召唤体那条路 |
| `battle_render.gd:481-494` | 填充只遍历 `active_skills` 查 `skill_cd`，**不读召唤物自己的 `energy`** |
| `axe_system.gd:92-93` | 而斧头确实自带 `ax["maxEnergy"]=140` / `ax["energy"]=0` |

⇒ 两个 bug 叠在一起：既没有条、也没有人画。

★ `battle_spawn.gd:736` 的注释写着「A方案随从带自己技能→龟能条恢复显示（通用召唤体在
`_spawn_summon` 里 hide·用户 **2026-07-17**"随从我都没看到技能龟能"）」—— **同一个问题
用户两个月前提过**，当时只对随从点对点修了一次。

### 2.2 八张招式动作帧：素材在盘上，零调用者

| 证据 | 事实 |
|---|---|
| `ls assets/sprites/vfx/eq-axe-*` | 12 张，全部 480×80 = 6 帧 × 80²，每帧非空 |
| `RealtimeBattle3DScene.gd:423/460/490/1897` | 只登记了 4 张（idle/walk/attack/cast） |
| `grep '"axe_smash"' scripts/` 等 8 个 | **各 0 处** |

⇒ 石斧强化砸 / 铁斧竖劈 / 金斧横扫 / 钻石斧蓄力猛砸 / 炽天使甩镖 / 全息插地 / 余烬处决，
斧头全是**站着不动就把伤害结算了**。

### 2.3 场景特效：`axe_passives.gd` 建视觉的调用是 0 处

| 文件 | 建视觉的调用 |
|---|---|
| `axe_system.gd` | **0** |
| `axe_passives.gd`（被动 3/4/5/6） | **0** |
| `axe_final_forms.gd`（四造物） | 11（亡灵环/吸血线/复活、炽天使回旋镖、余烬火种/处决/余光、全息法阵/脉冲） |

### 2.4 AI 的自杀参数

探针跑在旧池 12732 条快照（800 只机器人跑满 6366 场）上：

```
xp ≤ 1 的 6 个流派   最高只升到 5~7 级  ⇒ 装备槽 8~12   达档8率 0.40%
xp ≥ 2 的 8 个流派   能升到 10 级       ⇒ 装备槽 18     达档8率 1.61%
```

根因 `phase2_config.gd:55` — `team_equip_cap(level) = (level-1) × 2`。

| 流派 | 达档8率 | 平均持有 | 4~5 费占比 |
|---|---|---|---|
| `hi_cost`（高费流） | **0%** | 10.7 件 | **7.8%** |
| `random`（对照组） | 1.28% | 19.0 件 | 24.7% |
| `snowball`（xp:0） | 0% | 6.9 件 | 0.9% |
| `line_top`（唯一能打顶档的） | 0.13% | 13.7 件 | 9.2% |

⇒ 一个"高费流"买的高费**比随机瞎买还低 17 个百分点**。

---

## 3. 需求 ↔ 代码的出入

| # | 我原以为 / 用户看到的 | 代码事实 |
|---|---|---|
| ① | 「4 个最终造物什么特效都没有」 | **造物的特效是有的**（11 处建视觉）。我第一次 grep 用 `_vfx\.` 而文件里变量名是 `vfx`（无下划线），**漏掉了全部 11 处**，据此给用户报了错误结论，已当场更正。真正为 0 的是**四条被动 + 主动**。 |
| ② | 用户说「钻石以及四造物没特效」 | 钻石斧（被动 6 蓄力猛砸）确实**零视觉**；四造物有视觉但**我只给他开了 5 个材质档窗口，造物窗口一个都没开** —— 他看不到不是因为没做。 |
| ③ | 「快照要重跑因为装了蠢 AI」 | 属实，但根因不是"策略选得笨"，是 `xp≤1` 这个**结构性自杀参数**（不买经验就没有装备容量）。 |
| ④ | 验收报告里「零羁绊 33.6% > 20%」判 FAIL | **判据本身错了**：分母混进 4800 条低档快照（档0 装备 0 件、档1 才 3.43 件，物理上凑不出 3 件同类型）。按档位切开：档0 100% → 档4 17% → **档8 0.0%**，AI 的羁绊行为完全正常。 |

---

## 4. 方案（穷举清单）

| # | 改哪里 | 为什么 |
|---|---|---|
| 1 | `battle_render.gd` 龟能条填充 | 让「显不显示」与「填多少」由**同一个事实**（`is_summon && maxEnergy>0`）决定；判据用 `is_summon` 把 demo 假人挡在外（`battle_spawn.gd:197` 给它们也写 `maxEnergy` 并灌满） |
| 2 | `ACTION_ELITE` 加 8 个斧头条目 | fps 由真实节拍倒推：普攻 `1/MINION_ASPD=1.25s`×60% ⇒ 8fps；炽天使 `4s÷10 把` ⇒ 15fps；charge/plant 是持续状态 ⇒ 循环 |
| 3 | `axe_system.play_action()` 单一出口 | 八处不各抄一遍 `_resolve_action + _set_anim_sheet` |
| 4 | `axe_passives.swing_kind()` 纯函数 | 「这一下是竖劈还是横扫」收在一处，结算侧与演出侧读同一个答案（否则是第三份副本） |
| 5 | 新建 `axe_passive_vfx.gd` + 5 张素材 | 被动 3/4/5/6 + 主动的场景特效 |
| 6 | `axe_final_forms.gd` 三处接动作帧 | throw / plant / execute |
| 7 | `battle_vfx_lab.gd` 支持 `axe_stage`/`axe_final` | 九档逐档能开窗口 |
| 8 | `vfxlab_cases.gd` 加 9 个 case | 每档的 `dur`/拍点按各自被动的真实触发节奏算 |
| 9 | `_cohort.gd` 的 `MIN_XP_BUYS` + 跑前守卫 | 配了自杀权重直接拒绝开跑 |
| 10 | `ACTION_ELITE` 整表搬到 `gamedata/` | 主文件超 `arch_budget`，按 CLAUDE.md §5 落位表挪走而不是抬台账 |

**「不敷衍」的操作定义** —— 每个演出的形状必须由机制决定：

| 演出 | 形状的依据 |
|---|---|
| 蓄力梯形 | **就是 `AE.in_trapezoid()` 的判定区**，四角逐码对齐 |
| 横扫弧 | 直径 = `atk_range × 2` = `sweep_targets` 的判定直径 |
| 猛砸落地 | 取 `TRAPEZOID_NEAR_W`，**范围由梯形表达**，它只负责"砸得重" |
| 强化砸 | 冲击环，尺寸对应 `SMASH_KNOCK_PUSH`（只打一个人，不做成大 AOE） |
| 主动治疗 | 绿光柱（治疗）+ 金底环（护盾），对应 `_heal` + `_grant_shield` |

★ 梯形半宽按**满蓄高 800** 插值、不是按当前高 —— 与判定同一个公式。跟着当前高插值会
让梯形长大时"变胖"，演出就比判定宽。

---

## 5. 已知风险与未决点

### 未决（两条）

**① ~~七个档位还没逐档实拍~~ → 已补齐（见 §8.4）**
九档全部实拍验收过了。补拍过程里又抓到两个真问题（`cleave` 素材偏暗、四个 case 敌人
太远），已修并各自写进 §8.3。

**①b 竖劈素材形状不对（用户 2026-09-03 当场指出）**
> 建议竖劈重生成，这个月牙太弯了

量出来的证据：`eq096-cleave.png` 逐帧包围盒 **104×109 / 101×110 / 98×106**，长宽比
**0.92~0.95（近正方形）**，弧身中线 **−155.9°** —— 它是一张**横着的弯月牙**，
而竖劈是「从上往下劈」，应该是**窄而高、接近直线**的斩击轨迹。
★这是我选帧时没验形状: 我按"材质最足"挑了 [1]，只量了亮度和帧数，
**没量它是不是竖向的** —— 判据缺了"形状符不符合这一招"这一维。
⇒ 重新生成中（描述改成强调 vertical / near-straight / narrow tall，避开 crescent）。

**② 流派验收判据「零羁绊 ≤ 20%」本身有问题**
见 §3 出入④。新池跑完若**只红这一条**，是判据的问题不是 AI 的问题。
⇒ 但要**先量再改**，不许因为红了就放宽。建议改成「档 ≥4 的零羁绊率」或按档位分层。

### 已堵

| 风险 | 处置 |
|---|---|
| 调试台写 `GameState` 污染真存档 | 台子里统一置 `test_mode`（`battle_vfx_lab.gd` pre_spawn 最前面）。已写进 memory `fb-debug-stage-writes-real-save` |
| 一次普攻连播两张帧表互相顶掉 | 优先级 = 稀有度倒序（强化砸 9 秒才一次 → 优先），门禁④守着 |
| 梯形在异常路径上留在地上 | 收在 `_end_charge`（砸下/斧头死/被打断都经过它），门禁⑦守着 |
| 第二次蓄力播不出动画 | `_axe_charge_anim` 在砸下那一帧也要清（只在 `cr<0` 清会漏） |

### 未验

**③ `cleave` / `smash` / `heal` 三个演出的尺寸没有硬判据**
它们分别是单体真伤 / 单体 / 自身，**不存在对应的判定几何可以对账**，尺寸是按观感定的
（`atk_range×1.1` / 150 码 / 130 码）。将来改尺寸不会有门禁红。

**④ 五张素材是 PixelLab best-of-4 选出来的**
只验了"9 帧全非空"，没做逐帧质量核对（色数/前缘/材质）。
`tools/assetgen/build_eq096_vfx.py` 可重跑。

---

## 6. 验收标准（以及怎么证明它会 FAIL）

新增 `tests/verify_axe_vfx.gd`（**29 条**）：

| 组 | 判据 |
|---|---|
| ① | 八格蓄力高度的梯形四角，往里 2 码必在判定内、往外 2 码必在判定外（**两侧各验** —— 只验一侧则宽一格和窄一格都发现不了） |
| ② | 半宽按满蓄高插值（h=100 时 187.5，不是按当前高的 450） |
| ③ | 招式帧走**真入口** `on_hit`，读产品自己的 `_axe_last_action` |
| ④ | 强化砸与竖劈撞在同一拳 → 播稀有的那个 |
| ②b | 五张素材：在盘上 / 是多帧（单帧 = 敷衍）/ 真建节点并挂进 `_world` / **分母**：`_world` 子节点 141→146 |
| ⑤b | 横扫直径 140 码 = 判定直径 140 码 |
| ⑦ | 斧头死在蓄力中 → 梯形被收掉且节点真的 free（不是只清字段） |

**反向验证（各变异一次，全部逐字节还原）：**

| 变异 | 结果 |
|---|---|
| 半宽改成按当前高插值 | FAIL ×32 |
| 去掉强化砸优先级 | FAIL ×1（精确命中④） |
| `_end_charge` 不收梯形 | FAIL ×1（精确命中⑦） |
| `_cohort` 任一条 `xp` 改回 1 | 跑前守卫拒绝开跑并点名 |

全套门禁：**285 项**。

---

## 7. 决策记录

| 决策 | 谁定的 | 内容 |
|---|---|---|
| 快照全部重跑 | 用户 | 「快照全部要重新跑因为你装了蠢ai」—— 已归档旧池到 `tools/autoplay/_bad_ai_20260902/`，用新 seed 20260903 重跑 |
| 特效不许敷衍 | 用户 | 「斧头特效要全部补齐且不能敷衍」⇒ 定出 §4 那张"形状由机制决定"的表作为操作定义 |
| 素材必须是动画 | 用户（2026-08-29） | 「我要动画像素特效」⇒ 五张全部 9 帧，门禁②b 把"单帧 = 敷衍"焊死 |
| 素材不复用 | 用户（2026-08-03） | 五张为斧头新生成，没用剑的 eq084 |
| 不抬 `arch_budget` | 我 | 主文件超预算时把 `ACTION_ELITE` 整表按落位表搬走（8949 → 8925，比冻结值还少 3 行），而不是改台账 |
| 「零羁绊 ≤20%」判据存疑 | 我提出，**待用户拍板** | 见 §5 未决②。我没有擅自放宽它 |

---

## 8. 实施回填

### 8.1 实际改了什么

| 文件 | 改动 |
|---|---|
| `scripts/scenes/battle/axe_passive_vfx.gd` | **新建** 400+ 行 —— 梯形预警（含边框）+ 五个演出 |
| `scripts/gamedata/action_elite.gd` | **新建** —— 从上帝文件搬出的动作总表（14 项） |
| `assets/sprites/vfx/eq096-{cleave,sweep,smash,slam,heal}.png` | **新建** 9 帧 × 128² |
| `tools/assetgen/build_eq096_vfx.py` | **新建** —— 素材下载组装 |
| `tests/verify_axe_vfx.gd` + `.tscn` | **新建** 29 条 |
| `battle_render.gd` | 龟能条：显示与填充合到同一个事实 |
| `battle_vfx_lab.gd` | 斧头档位支持 + **台子绝不写玩家存档** |
| `vfxlab_cases.gd` | 斧头九档 case（含 `enemy_hp` / 拍点对准真实触发时刻） |
| `axe_system.gd` | `play_action()` 单一出口 + on_hit/tick 接演出 |
| `axe_passives.gd` | `swing_kind()` + 梯形三处接线（建/推进/收） |
| `axe_final_forms.gd` | `_play()` + 三处造物动作帧 |
| `tests/_cohort.gd` | `MIN_XP_BUYS=2` + `check_suicide_weights()` + `_ready` 拦截 |
| `tests/verify_summon_art.gd` | 表判据从**源码 grep** 改成读**真常量**（`get_script_constant_map`） |
| `tests/verify_axe_anim_runtime.gd` | 修不稳定：先钉 `no_move` 再轮询等待（墙钟） |

### 8.2 与方案的偏差

- 方案里没写「`ACTION_ELITE` 要搬家」—— 是加完 8 条后 `arch_budget` 红了才决定的。
- 方案里没写「`verify_summon_art` 要改判据」—— 搬表之后它从源码 grep 不到，**这正是
  CLAUDE.md §2 警告过的坑**（「函数外迁到新文件后审计器找不到 = 误报」），顺手改成读真常量。

### 8.3 新发现的坑（比结论值钱）

**① 连猜三次根因，最后靠染色法一次定位**
梯形**边框看不见**。我依次猜：抬高 y → 设 `render_priority` → 换对比色，**三次全错**。
染成纯品红重拍、数出 **0 个品红像素** ⇒ 确定是"根本没渲染"而不是"不显眼"。
真因：边框是**四条独立的带**，法线由 `Vector2(-d.y, d.x)` 算，四条边方向不同 ⇒
**有些带绕向朝下，被 `CULL_BACK` 整条剔掉**；填充是绕向一致的四边形所以可见。
⇒ memory `fb-probe-before-claiming-rootcause`。

**② 染色判据本身也错过一次**
给横扫染品红后只数到 48 像素，我差点判成"渲染有问题"。真相：**`modulate` 是相乘不是
替换** —— 金色素材 (1.0,0.8,0.3) × 品红 (1,0,1) = (1,0,**0.3**)，蓝分量通不过 `B>170`。
横扫一直在正常渲染。

**③ 三次"台子没配对"被读成"效果没做"**
- 横扫拍点落在斧头**走路**的四秒里 ⇒ 一片空白
- 不给假人加血 ⇒ 斧头 t=0.63 秒把三个假人清空，**整场只触发一次横扫**
- 贴地朝向用 `axis=AXIS_Y + rotation.y` 怎么转都不跟攻击方向走 ⇒ 改调 blade 那份
  **已验收**的 `ground_basis`（头注写着「符号不靠推理」），`aim` 从素材逐帧量出来 = **-20.9°**

**④ VFXLAB 污染了真存档（门禁替我抓到）**
`GameState.save()` 的守卫 `test_mode` **只在 headless 时自动置位**，而 VFXLAB 必须渲染
⇒ 九个窗口把存档改成 `axe_stage:4 / axe_final:"holo"`。表现是 `verify_axe` 当场红：
「斧头最大生命 = 500 期望 · 实测 1500」（钻石斧身板）。
恢复靠**未被污染的** `axe_exp_bar = 51 < 80`（石斧阈值）反推档位必为 0。

**⑤ 「看不见」当成「太细」，把参数往错方向推**
边框宽度 14 → 22 → 14 绕了一圈。看不见的真因是绕向被剔掉，跟粗细无关；修好后 22 码明显
过粗。⇒ memory `fb-my-thresholds-degrade-good-assets`：先确认它到底渲没渲染，再谈阈值。

**⑥ 我给用户报过一个错误结论并当场更正**
第一次 grep 用 `_vfx\.` 而文件里变量名是 `vfx`，据此报「四造物只有 2 处建视觉」——
实际是 **11 处**。用正确模式重查后立刻更正。

### 8.4 九档实拍验收结果

| 档 | 实拍确认到的 |
|---|---|
| 木斧 | 主动治疗：绿光柱 + 金底环 +「+25 盾」飘字 |
| 石斧 | 强化猛砸：蛛网裂纹贴地冲击环 + 敌人被击飞 |
| 铁斧 | 竖劈：刀光 + 45000 真伤（5% 敌人最大生命） |
| 金斧 | 横扫：贴地金弧（被 52° 俯视压扁），以斧头为心 |
| 钻石斧 | 梯形逐格长出（带亮边）→ 砸下时闪一下 + 裂地冲击 + 45000 |
| 亡灵 | 绿环 + 三路 5143 吸血 |
| 炽天使 | 回旋镖飞行 + 灼烧层 40×4 |
| 全息 | 友军「+60 盾」金环 +「+100」治疗 |
| 余烬 | 火焰弧 + 处决 |

★补拍时改的台子配置（都是"台子没配对"被读成"效果没做"）：
`stone/iron/四造物` 的 `enemy_dist` 从 220~240 拉近到 90~100（斧头 `atk_range` 只有 70，
拍点全落在走路上）；`iron` 改用 `aspd: 4.0` 让招式密到任何时刻都在演；
所有 case 加 `enemy_hp: 900000`（不加的话斧头 t=0.63 秒把假人清空，整场只触发一次）。

---

## 9. 参考逐帧复核（2026-09-03 第二轮·**原版塞恩** Q 专门演示）

> ## ⛔ §9 与 §10 量的是**原版塞恩**，不是用户点名的机甲皮肤
>
> 用户点名的是「lol塞恩**机甲皮肤**的Q」，而这两节量的是原版（红色 Q）。
> 据此得出的两条结论**已在 §11 被推翻**：
>
> | §9/§10 的结论 | 实际（机甲皮肤，见 §11） |
> |---|---|
> | 蓄力 1.042 秒 | **1.94 秒**（那才是满蓄） |
> | 有「起手闪」（脚下炸亮点再横拉） | 机甲**没有**，它是**线从角色向外延伸** |
> | 预警区把地面**压暗到 75%** | 机甲是**六边形网格纹理**（亮度 103%、纹理对比度 +46%） |
>
> 这两节留着，是为了记住「参考拿错了会导出什么样的错结论」——
> 三种颜色判据失效那段（§9.2）仍然成立，与拿错段落无关。

用户 2026-09-03:「你用了多少张，我要你搞400张对比，我们的是4秒不知道和参考的是不是一样」
「参考是 115.2 → 116.4 秒 = 约 1.2 秒？你凭什么推的」「满蓄呢，素材在哪」

### 9.1 两个参考样本的实测时长（**逐帧数出来的，不是估的**）

| 样本 | 位置 | 蓄力起 | 释放 | **时长** |
|---|---|---|---|---|
| 机甲皮肤 spotlight | `C:/tmp/sionref/allq/` | 帧 118 (115.93s) 青线跳变 | 帧 130 (116.33s) 橙色爆发 | **0.40 秒** |
| **原版专门演示** | `C:/tmp/sionref/qonly/` | **f90 (3.000s)** 脚下亮点 | **f120 (4.000s)** 红扇斧刃 | **1.000 秒** |

★我一度目测报「约 1.2 秒」，用户当场质疑「你凭什么推的」——**逐帧重量后是 0.40 秒，差 3 倍**。
教训同 memory [[fb-match-reference-by-measured-curve]]：**照参考要量成曲线，不许目测**。

### 9.2 三种颜色判据全部失效 → 最后靠目视逐帧

| 判据 | 结果 |
|---|---|
| 青色（机甲皮肤 Q 色） | 原版视频基线 **0** —— 原版 Q 是**红色**，不是青色 |
| 红色（改判据后） | 基线 **1750** —— 这张地图本身是**橙红泥土地**，判据抓到地面 |
| 帧间差分 | 只能定位到 4.0~4.7 秒这一大段，分不出蓄力起点 |
| **目视逐帧接触印相** | ✅ 成功 —— 41 张 / 21 张两级放大，起止一目了然 |

⇒ **通用教训：颜色判据依赖"背景不含这个颜色"这个前提；前提不成立时，人眼看接触印相比任何自动判据都可靠。**

### 9.3 参考里有、我们没有的三项（逐帧对比结论）

| # | 参考的样子 | 我们的现状 | 处置 |
|---|---|---|---|
| ① | **起手闪** f90 亮点+外环 → f92 横拉成条 → f94 收回 → f96 消失（0.20 秒） | 前 0.2 秒**画面上什么都没有**，梯形慢慢长出来 | ✅ **已做** `_charge_onset_flash()`，逐段照参考的三段形变；门禁 ⑤c/⑤d |
| ② | 预警区浓度**非单调**：f96 极淡 → f110 最浓 → f114~f118 回落 | 我们是单调递增 + 2Hz 呼吸 | 判定为**已被呼吸覆盖**，不硬改（参考的回落也可能是脉动的一个相位） |
| ③ | 释放瞬间一整片**深红扇形斧刃**刷满预警区 + 击飞 | slam 贴图 + hex_burst（六边形网格，橙 alpha 0.88） | 形态不同但**我们是梯形+龟不是塞恩**（用户 09-03:「梯形还是按我们的来因为参考的是长方形，我们是梯形」）⇒ 不照抄形状 |

### 9.4 已知缺口（诚实登记）

- **满蓄样本没拿到**。塞恩 Q 最长蓄力 2 秒，两个视频里最长的一次是 **1.000 秒**（半蓄）。
  再下两个视频都失败（一个被 `duration<400` 过滤、一个 HTTP 403）。
  ⇒ 判定**不阻塞**：蓄力全程形态在这 1 秒里已录全，满蓄只是**更久更长**，逐进度形态一致；
  而我们的 4 秒是玩法数值（蓄越久伤害越高），本来就不照抄时长。
- **真 bloom 没做**。假 bloom（实心圆盘叠加）实拍是一层灰罩，比不做更差，已删。
  真 bloom 需要 emissive 材质 + Environment glow，属于全局渲染设置改动，超出本方案范围。

### 9.5 素材归档位置

| 内容 | 路径 |
|---|---|
| 机甲 spotlight 原视频 | `C:/tmp/sionref/sion.mp4` |
| 机甲 Q 段 2268 帧 | `C:/tmp/sionref/allq/` |
| **原版专门演示 225 帧** | `C:/tmp/sionref/qonly/` ← 9.1 那次完整 Q 在 f90~f126 |
| 逐帧接触印相 | `C:/tmp/sionref/qonly_zoom.png`（f60~f140 步长 2）、`qonly_charge.png`（f86~f126 放大） |
| **项目内存档** | `docs/plans/ref/sion-q/`（3 张关键帧 PNG + 实测数据 README） |

---

## 10. 第三轮：填充回来了（推翻我自己上一轮的判断）

### 10.1 我上一轮写错了什么

上一轮我在 `charge_field` 的注释里写下：

> 照参考直接不要它 —— 塞恩 Q 的预警区本来就是零填充，只有边线。这也是用户要的"一模一样抄"。

**这句话是错的。** 它是拿机甲皮肤那个糊样本目测出来的（memory [[fb-clean-vfx-stage-not-squint]]：眯眼看会判反）。
原版专门演示视频量下来，预警区的填充**不但存在，而且是区分内外的主力**：

```
f88(预警前) 区内 RGB 60.4 / 58.8 / 20.2
f110(最浓)  区内 RGB 51.2 / 38.4 / 15.7
净叠加            −9.3 / −20.5 / −4.6      区内平均亮度 46.5 → 35.1 = 压暗到 75%
★分母: 同两帧的【区外】方块差值 +0.1/+0.1/+0.1 ⇒ 不是全局亮度变化
反推填充色 ≈ RGB(34, 0, 7) @ α 0.35 —— 一层很暗的红
```

### 10.2 不照抄数值，照抄对比度

参考是**暗红压在亮草地上**；我们是**青色主题 + 黑地板**。同一个"暗红 @0.35"在我们这儿什么都看不见。
⇒ 取**同等对比度**的青色叠加：`COL_FIELD_FILL = Color(0.24, 0.72, 0.78, 0.20)`。

★**边线不动**。两个参考样本在这里不一致：机甲皮肤有明确的亮青边线，原版靠色块、边线很弱。
用户点名的是**机甲皮肤**（「lol塞恩机甲皮肤的Q」），所以边线按机甲皮肤保留，原版只用来证明「填充存在」这一条。

### 10.3 同一个坑踩了第二次：写了没人读 / 被下游覆盖

填充恢复后实拍**仍然只有梯形刚建出来那一帧是青的**，之后全变回近乎透明。根因：

| 覆盖点 | 症状 |
|---|---|
| `charge_update`（上一轮已修） | 每长一格重设 `Fill.mesh` ⇒ 我调五次 alpha 毫无反应 |
| **`_tick_field_pulse`（这一轮才发现）** | **每帧**把 Fill 材质重设成 `COL_SLAM` 琥珀 α0.02~0.07 ⇒ 把 `charge_field` 设的 0.20 青覆盖掉 |

两处是同一形状 —— memory [[fb-write-without-reader-and-fake-gates]] / [[fb-read-a-field-nobody-writes]] 那一类。
⇒ 现在 `_tick_field_pulse` 在 `COL_FIELD_FILL` 的基准 alpha 上呼吸（`0.55 + 0.45k`，钳到 0.05~0.42），不再换色。

### 10.4 顺带记一条：多打一个右括号，游戏照跑，梯形整个消失

改 `charge_clear` 时我在 tween 的 lambda 里多打了一个 `)`。**GDScript 解析失败 ⇒ 整个 `axe_passive_vfx.gd` 加载不了 ⇒ 所有 `vfx.xxx()` 调用静默失败**，
游戏正常启动、VFXLAB 正常拍完 12 张图，**图上梯形完全不见**。
差点被读成"填充改动把梯形搞没了"。这正是 memory [[fb-block-delete-eats-functions]]：鸭子类型下 `vfx.xxx()` 编译期不检查。

★而我当时用 `--headless --path . --quit-after 3` 做的"编译检查"是**假的** —— 主菜单场景根本不加载战斗脚本
（memory [[fb-gate-subject-never-constructed]]）。**改完 VFX 脚本必须跑 `verify_axe_vfx` 这种真入口**，不是 boot 一下主菜单。

### 10.5 起手闪：30 码 → 55 码

首次实拍在 zoom 2.0 下**完全找不到** 30 码的亮点，与"根本没做"无法区分。
定 55 的依据不是"调大点"，是横拉那一段：`55 × 2 × 3.2 = 352 码 ≈ 梯形近端宽 300 码`
⇒ 观感正是参考 f92 的"一道横条横跨预警区近端"。颜色也从 `COL_EDGE_CORE` 换成超白 `COL_ONSET(1.35,1.45,1.42)` —— 与边线同色会被边线淹没。

### 10.6 并排复核结果（40 张 / 20 个进度点）

`_cmp_sheet.png`：左参考右我们，按**进度 0~100%** 对齐（参考 1.00 秒 vs 我们 4.00 秒，不按秒对齐）。

| | 一致 | 差异 |
|---|---|---|
| 危险区随时间**变浓** | ✅ | |
| 区内有色 / 区外无色的**结构** | ✅ | |
| 释放时铺满 | ✅ | |
| 区域**长度** | | 参考一开始就是完整长方形；我们逐格长大 —— **设计如此**（需求：每 0.5 秒 +100 码高），用户 09-03 也定了「梯形还是按我们的来」 |
| 边线强度 | | 我们更强 —— 照机甲皮肤，见 §10.2 |


---

## 11. 第四轮：参考段落一直拿错（用户指出后重做）

### 11.1 错在哪

用户 2026-09-03：「这是机甲皮肤？？你别撒谎行吗」→「1分59到2分02吧」→「你搞这么久连图都不知道从哪来啊」

**我前三轮量的都不是用户点名的那一次。**

| 我量过的 | 是什么 | 结果 |
|---|---|---|
| `sion.mp4` 115.9 秒 | 机甲皮肤，但只是**半蓄**（0.40 秒） | 边线色对，时长不对 |
| `q_only.mp4` 全片 | **原版塞恩**（红色 Q），根本不是机甲皮肤 | 据它加了「起手闪」和「纯色填充」——**两条都是错的** |
| **`sion.mp4` 1:59~2:02** ← 用户指的 | **机甲皮肤的满蓄 Q** | 这才是参考 |

★「起手闪」是原版塞恩特有的（脚下炸亮点再横拉），机甲皮肤**没有这一下**，它是**线从角色向外延伸**。
★「预警区把地面压暗 75%」也是从原版量的；机甲皮肤是**六边形网格纹理**。

### 11.2 正确参考的实测（原生 29.97fps，108 帧在 `C:/tmp/sionref/m2/`）

| 时间 | 画面 |
|---|---|
| 119.97s | 干净 |
| **120.00s** | 地上出现一条细白线 ← 蓄力起点 |
| 120.03~120.90s | 线向外延伸变长，青色，橙黄亮点沿线跑向末端 |
| 120.94~121.17s | 线折返回来合成**封闭长条轮廓** |
| 121.20~121.44s | 轮廓完整；区内浮出**淡的六边形网格纹理** |
| 121.47~121.90s | 轮廓**从青 → 黄 → 橙** |
| **121.94s** | 轮廓变**红**，释放 |
| 121.97~122.24s | 橙白六边形网格大爆发 |

**蓄力 = 1.94 秒**（塞恩 Q 上限 2 秒 ⇒ 这就是用户问的「满蓄」）。

分群计数（青 150~215° / 黄橙 12~70°，亮度>0.42 且饱和>0.34）：

```
进度      青占比   黄橙占比
0~46%     16→37%   63→54%    线在延伸，黄橙火花沿线跑
48%       66%      27%       轮廓合拢，青色接管（突跳）
48~76%    62→89%   27→6%     纯青
84%       48%      48%       黄橙杀回来
100%      —        —         红群从 ~300 突增到 2199
```

★量法坑：第一版取「色相的环形向量平均」，在 84~98% 报出 99~123°（黄绿），
而我明明排除了 70~150° —— 那是**黄群(50°)和青群(180°)平均到中间**的假值。
双峰分布不能取平均，要分群计数。同族 memory [[fb-judge-must-fit-the-shape]]。

### 11.3 做了什么

| # | 改动 | 依据 |
|---|---|---|
| ① | 新增纯函数 `edge_color_at(p, layer)`：p<0.76 恒 180°；≥0.76 线性降到 0° | 上表的色相曲线。**故意经过绿区**（参考 121.50s 是青绿、121.60s 是黄绿） |
| ② | 撤掉 `_charge_onset_flash` 与 `COL_ONSET` | 机甲皮肤没有起手闪 |
| ③ | 纯色填充 → `_hex_mesh(h, full=true)` 六边形网格 | 参考区内亮度只有 **103%**、纹理对比度 **+46%** ⇒ 是加纹理不是改亮度 |
| ④ | 台子加 `VFXLAB_REALMAP=1` | 黑场地面亮度 ~10，同样叠加算出来 364%，读成"一块板" —— **判断"特效与地面的关系"必须有地面** |
| ⑤ | 台子加 `VFXLAB_NO_PLACE=1` | 同时开四个窗口比四个造物时，每个都摆"当前屏正中"会全叠在一起 |
| ⑥ | 展示窗口口径：副屏 → **当前这块屏** | 用户 09-03「现在可以用当前这个屏幕了」（第三次变更） |

★ `edge_color_at(0.0, 2)` = `from_hsv(180°, 0.27, 0.99)` ≈ `#b7fdfd`，与实测芯色 `#b7fdf8` 几乎重合
⇒ 沿用的那组 S/V/A 是对的，只换了色相。

### 11.4 门禁（`verify_axe_vfx` 37 条）

旧 ⑤c/⑤d（起手闪）**随实现一起删**——它们守的是错东西。新判据：

| # | 判据 | 说明 |
|---|---|---|
| ⑤c | p=0.00 与 p=0.50 都是青（180°±12°） | 纯函数逐点验 |
| ⑤d | p=1.00 是红（<12° 或 >348°） | |
| ⑤e | p=0.92 落在青红之间且与两端都拉开 >25° | **卡"真的在渐变"**——`return 青 if p<0.99 else 红` 的阶跃实现会过 ⑤c⑤d，在这条红 |
| ⑤f | **真实节点**上色相随蓄力变了 >60°（实测 180° → 38°） | 证明曲线接上了，不是只有纯函数好看 |
| ⑤g | `Fill` 有六边形网格 mesh | |
| ★ | 每条配分母断言 | |

⚠ 写这条门禁时踩到：`tick_charge(ax, delta)` 的 `delta` **只管效率计时**，蓄力进度走的是
`charge_elapsed() = battle._t − _axe_charge_t0`。测试不推 `battle._t` 的话 `charge_height()` 恒为 0
⇒ `charge_update` 第一行 `h <= 0.0` 就 return ⇒ `_tick_field_pulse` 一次都跑不到，
读到的全是建场那一刻的初值 = **恒真式**。是反向验证「没红」才发现的。

---

## 12. 数值调参（用户 2026-09-03 给的数）

| 常量 | 旧 → 新 |
|---|---|
| `EXP_ON_BUY` / `EXP_ON_MATCH` / `EXP_ON_KILL` | 15/10/2 → **20/15/5** |
| `MINION_HP_PER_EXP` / `MINION_ATK_PER_EXP` | 1.0/0.05 → **5.0/0.3** |
| `MINION_DEF` / `MINION_MR` | 5/5 → **12/12** |
| `SHELF_P_BASE` / `_PER_MATCH` / `_CAP` | 3%/0.1pp/10% → **6%/0.5pp/18%** |

**量级（动手后立刻算给用户，不等他问）**：400 经验（造物门槛）时斧头
从 **900 血 / 50 攻** → **2500 血 / 150 攻**（2.8× / 3.0×）；攒满从约 16 局降到约 11 局；
商店概率到顶从 70 局降到 **24 局**。

### 连带失效的两条门禁（不是回归，是判据跟着数走）

| 门禁 | 原判据 | 为什么失效 | 改法 |
|---|---|---|---|
| `verify_axe_evolution` | 「★分母：封顶前（第 60 局）还没到顶（9%）」 | 新参数 **24 局就到顶**，第 60 局必然已封顶 ⇒ 这条**分母恒假** | 改用第 12 局（6+6=12%） |
| `verify_axe` | 「200 点经验 → 血 700 / 攻 40」写死 | 成长系数变了 | 期望值改成**由常量现算**；这条要守的是「吃的是历史累计」与「随经验单调增」，具体数字交给 ★分母 那条 |

★另一份可反复查的数值台账（94 个常量 · 带文件行号 · 可搜索）：
用 `get_script_constant_map()` 从代码真读生成，不是手抄源码。
