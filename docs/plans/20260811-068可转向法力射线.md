# 068 深海气压罐 —— 可转向的法力射线（真 3D · 扫射结算）

**日期**：2026-08-11
**由来**：用户「气压罐也没有人眼看过特效，我手机上看感觉不满意」→
逐帧分析 LoL 圣光维克兹 R（生命形态解体射线）→
用户定调：「**本来就应该是可以转向的射线，射线碰到谁就开始造成伤害，射线离开就停止造成伤害**」，
且「**必须做 3D，不要拿图片敷衍，也不要拿简单程序图形糊弄**」，「**参考里面人家做了命中特效的**」。

---

## 〇、参考数据（全部实测，不是描述）

参考视频：[Arclight Vel'Koz Skin Spotlight](https://www.youtube.com/watch?v=DVg7SleafEs)（SkinSpotlights，60fps／1280×720）
本机 `yt_dlp` + `imageio_ffmpeg` 可下载抽帧 —— **这是本项目第一次真的做到逐帧参考**
（上一次「参考《虐杀原型2》」拿不到视频，那三个节奏数是我自己拍的，至今挂在「待拍板」）。

权威 wiki 只给了引导 2.6s／射程 1555／宽度 175，**转向速率没有公开数值** ⇒ 只能量。

### ① 时间包络（t=95.13～97.63s，共 2.5s）

| 阶段 | 时长 | 相对强度 |
|---|---|---|
| 起手闪 | 2 帧 | 尖峰 |
| 蓄势 | ~0.65s | 0.20～0.22 低位持平 |
| **爬升** | ~0.85s | 0.25 → 0.85 |
| 持续 | ~0.90s | 0.67～0.85 起伏 |
| **峰值** | **结束前最后一帧** | **1.00** |
| **切断** | **1 帧** | 0.83 → 0.24 |
| 余辉 | ~0.5s | 衰减到底 |

> ★**最亮的一瞬是结束前那一帧，然后一帧切断。**
> 我们现在的 `beam_current` 是**开场即满、指数衰减** —— 形状完全反过来，
> 这比颜色和粗细都更致命：它读成「闪了一下」，而不是「越打越狠」。

### ② 横截面（差分抠出光束；底帧已验证：两张开火前的帧互差仅 46 个残差像素 ⇒ 相机静止）

| | 饱和度 | 颜色 |
|---|---|---|
| 外缘 | **0.50～0.65** | R175 G140 B75 浓琥珀金 |
| 中层 | 0.40～0.55 | 渐亮 |
| **白芯** | **0.00～0.09** | 纯白，**只占整束宽度 15～20%** |

从外到内是**平滑斜坡**，不是硬分带。沿程峰值大致持平（各站位都顶到 255），
**没有明显的沿程衰减** —— 而我们现在写的是 `exp(-1.1x)`。

我们现状：整条 `Color(0.62,0.80,1.0)` 一个色、饱和度 0.38、alpha 硬分三带（0/1/0）
⇒ 没有芯晕分化 ⇒ 一坨均匀淡蓝，读成雾。

### ③ 转向速率

第一次施放玩家只是慢慢划（~22°/s），**不是上限**。第二次甩得狠：

| 时段 | 角度 | 速率 | 束身点数 |
|---|---|---|---|
| 116.30 → 116.47s | 23.6° → 11.7° | **72 °/s** | 1.4 万（完整） |
| 116.00 → 116.10s | 0.3° → 19.0° | 187 °/s | 6000 ⇒ **光束还没成形，不算数** |

**屏幕角上限 ≈ 72 °/s。** 换到世界角要过地面压缩 `tan(θ屏)=k·tan(θ世界)`，
k 取 0.55 ⇒ 倍率 ≈1.6 ⇒ **世界转向速率 ≈ 110～120 °/s**。

> ⚠ **k=0.55 是我的估计，不是量出来的**（见 §五 未决点 2）。
> 但有一条好性质：**角速度是无量纲的**，不随场地尺度变 ⇒ 参考值可以 1:1 搬过来，
> 不需要知道 LoL 的码与我们的码怎么换算。

---

## 一、要做成什么

一道**可转向**的法力射线：指着一个目标，目标死了就转向新目标，
**转的过程中扫过谁就烧谁，离开就停**。

---

## 二、验收清单（逐条列全，别挤牙膏）

1. 射线**能转向**：当前目标死亡/失效 ⇒ 自动改指新目标（最远的活敌），按角速率上限逼近，**不瞬移**
2. 射线**跟踪活着的目标**：目标位移时方向持续跟随（同样受角速率上限约束 ⇒ 快速绕后能甩脱）
3. **扫到即伤、离开即停**：伤害按「本 tick 内被射线照到的时长比例」结算，不是「tick 结束瞬间在不在线上」
4. **不漏人**：转向时 tick 之间跨过的敌人必须结算（见 §三·B 的扫掠区间）
5. **数值契约不变**：一个全程被照的目标，3 秒仍吃满充能值的 100/120/600%
6. 演出是**真 3D 几何**：芯／晕两层独立网格，转向时整体旋转
7. 横截面照实测：**白芯占 15～20%**，外缘浓琥珀金（饱和度 0.55～0.65），中间平滑过渡
8. 时间曲线照实测：**蓄势→爬升→持续→末端峰值→一帧切断→余辉**
9. **伤害与亮度仍走同一条曲线**（现状这个设计是对的，要保住）
10. **枪口星芒**
11. **命中特效**（用户点名）：射线照到谁，谁身上就持续有爆点，离开即停
12. 换路／战斗结束时**全部清干净**（068 的头顶条就是死在这条上）

---

## 三、怎么实现

### A. 转向

```
每帧:
  若 target 无效/已死 → target = 最远的活敌
  desired = (target.pos - origin).angle()
  cur = move_toward_angle(cur, desired, TURN_RATE * delta)
```

`TURN_RATE = 110 °/s`（实测换算值）。**伤害判定用 `cur`（射线真实朝向），不是用 target 的位置** ——
否则"扫过谁烧谁"就成了假的。

### B. ★不漏人：按【角度扫掠区间】结算，不是按当前直线

`BEAM_TICK = 0.25s`，110°/s ⇒ **一个 tick 转 27.5°**。
在 500 码处是 240 码弧长，而射线全宽只有 116 码 ⇒ **照直做会整个漏掉敌人**。
（与「逐帧机制门禁必须逐帧喂」「合成单位被钳到同一点」同族：粗粒度采样看不见缝隙。）

解法 —— 每个 tick 用**角度区间求交**，是闭式解不是采样：

```
θ0 = tick 开始时的朝向, θ1 = tick 结束时的朝向
对每个敌人:
    d = |敌人 - 原点|;  超出射程 → 跳过
    α = atan((HALF_W + 敌人半径) / d)      # 敌人占的角半宽
    β = 敌人方位角
    若 |θ1-θ0| < ε:  f = 1 if |β-θ0| ≤ α else 0        # 不转 ⇒ 退化成现状
    否则:            f = |[β-α, β+α] ∩ [θ0, θ1]| / |θ1-θ0|
    伤害 = 本 tick 伤害 × f
```

★两条好性质：
- **不转时 f=1，与现有行为逐字一致** ⇒ 天然的回归保护（门禁可以断言这一点）
- f ≤ 1 自动成立（交集不可能超过扫掠区间本身）

### C. 为什么契约不变

`_beam_hit` **现在就是「带内每个敌人各吃全额」**（不是平分）。
所以「3 秒共造成充能值的 100/120/600%」本来就是**对单个持续被照的目标**说的。
改成扫射后这条不变；变的只是**被扫过的敌人按被照时长拿一部分**。

⇒ **不需要改任何数值契约**，也不需要重新平衡星级倍率。

### D. 真 3D 几何（不是贴图、不是"简单程序图形"）

- **两层同轴套管**（不是平铺地面的四边形）：
  - 内层：窄白芯，半径 = 外层的 15～20%（实测值）
  - 外层：浓琥珀金晕，饱和度按实测 0.55～0.65
  - 各自 N 段径向细分 ⇒ 是**真的扫掠体**，从任何角度看都有体积
- **`CULL_BACK` 只画正面**：实心闭合曲面 + 加色混合 + 双面 ⇒ 中心必然加爆成白
  （v0.19.90「白球家族」那轮的结论，四件同一个病）
- **亮度靠 alpha 给，不靠把颜色洗白**（同上）
- 沿程**不做强衰减**（实测大致持平），只在射程末端轻微收束
- 网格**建一次**、每帧只转节点 ⇒ 转向不重建，热路径零分配

### E. 时间曲线

把 §〇① 的实测包络做成**相对包络表**（等比拉伸 2.5s → 3s），
`beam_current` = 表（瞬时强度），`beam_frac` = **表的归一化积分**（累计伤害比例）。
两者是导数关系 ⇒ **画面最亮那一瞬也正是打得最狠那一瞬**，现状这个性质保住。

### F. 枪口与命中（用户点名）

- **枪口**：白热点 + 放射星芒，强度跟着同一条包络
- **命中**：每个**当前被照到**的敌人身上持续一个爆点（星芒 + 白芯），**离开即停**
  —— 不是一次性的爆炸，这是"扫射"的关键读数

---

## 四、放哪（CLAUDE.md §5：不在 `_sim_step` 调用链上的不进主文件）

| 东西 | 去哪 |
|---|---|
| 转向状态机 + 扫掠结算 | `scripts/systems/equip/eq_potion_batch.gd`（068 已经在这儿） |
| 3D 几何 + 枪口 + 命中爆点 | `scripts/scenes/battle/potion_eq_vfx.gd` |
| 实测包络表 | 同上（纯数据常量） |

---

## 五、已知风险与未决点

1. **实战总伤害多半会下降。** 契约（对单目标）不变，但扫射把伤害摊到被照时长上，
   而旧版是"一条固定直线上的所有人全程吃满"。**这是玩法削弱，量级未测。**
   要不要补一轮胜率验证（既有双 worktree 全循环赛流程）？—— **等你拍板**
2. **地面压缩因子 k=0.55 是估计值，不是实测。** 转向速率 110°/s 依赖它。
   要真量，得用"光束末端在地面上画的是圆弧"去拟椭圆解出 k —— 更贵，且末端常常出画面。
   **要不要花这个成本？** 我的意见：先用 110°/s，手感不对再调 —— 角速度是可感知量，
   你在真机上一眼能判断"太慢/太快"，比我把 k 量准更快。
3. **命中爆点数量 × 性能未测。** 一次扫射可能同时照到多个敌人 ⇒ 多个爆点并存。
   路线图 §二·五 第 12 条「特效性能预算未测」本来就欠着。
4. **是否允许跟踪活着的目标**（验收清单第 2 条）。我按"跟踪"设计——理由是
   不跟踪的话，目标一走位射线就空放，"可转向"这个特性一大半就没用了。
   但这也意味着**它比旧版更容易打满**，与风险 1 部分抵消。**如果你要"只在目标死时才换靶"，说一声。**
5. **转向会让射线扫过友军所在的方位**。当前实现只对敌人结算，不会误伤 —— 但
   视觉上射线会从友军身上扫过去而没反应，可能读成"穿模"。要不要给友军一个被照的轻微反馈？

---

## 六、门禁（判据落在真实对象，不量替身）

| # | 断言 | 反向验证 |
|---|---|---|
| 1 | 不转向时，带内每个敌人吃满 —— **与改动前逐字一致** | 把 f 写死成 0.5 ⇒ 必须红 |
| 2 | 转向时，扫掠区间内的敌人**都**被结算（不漏人） | 把扫掠改回"只测当前直线" ⇒ 必须红 |
| 3 | 角速率**受钳制**：一帧内朝向变化 ≤ TURN_RATE·delta | 去掉钳制直接瞬移 ⇒ 必须红 |
| 4 | 目标死亡 ⇒ 换靶（读真实 target 字段，不是"我调用过换靶函数"） | 让换靶只写日志不改 target ⇒ 必须红 |
| 5 | 全程被照的目标，3 秒总伤害 = 充能值×倍率（**契约不变**） | 改倍率 ⇒ 必须红 |
| 6 | 白芯宽度／外晕宽度 ∈ [0.15, 0.20]（实测区间） | 芯改成和晕一样宽 ⇒ 必须红 |
| 7 | 时间曲线**末端最亮**：`current(0.95T) > current(0.1T)` | 换回指数衰减 ⇒ 必须红 |
| 8 | 亮度与伤害同源：`frac` 是 `current` 的归一化积分 | 让两者用不同的表 ⇒ 必须红 |
| 9 | 换路后射线／枪口／命中爆点节点数归零 | 去掉清理 ⇒ 必须红 |

★所有"数值对不对"的用例**不依赖任何动画 tween 跑完**（CLAUDE.md §3.5）：
扫掠结算是闭式解，直接同步喂 delta 即可判定。

---

## 七、交接（2026-08-11 收尾 · 给下一个会话）

### 已完成并提交（v0.19.93 · 本地 commit `def97e6`，**尚未 push**）

门禁 **182/182 全绿**。反向验证 12 条逐条确认会红，产品代码逐字节还原已核对。

- 可转向（110°/s，目标死改指最远活敌，不瞬移）
- 扫到即伤离开即停（角区间求交闭式解；不转向时 f≡1 与改动前逐字一致）
- 真 3D 五层同轴套壳（差分叠加，实拍闭环校准过色相）
- 时间包络照实测（末端最亮 + 一帧切断）
- 充能条进装备图标框（`can_pct` 归一化镜像）
- 顺手修：头顶白线拆除、药水一路换路没被清、两张数据表搬出主文件

### ★用户验收后的反馈（2026-08-11，**这是下一步的输入**）

> 「转向速度还好，**射线和爆点我觉得离参考有很大差距**，而且**为啥飘字，应该是给一个特殊颜色护盾条，这是特殊护盾**」
> 「**标准是像素级 1:1 视频参考**」

### 欠债三条（按优先级）

**① 法力护盾接进血条**（最小、最确定，先做这个）
`hp_bar.gd` 已有三种独立颜色的特殊盾段的现成写法：
`_holyShieldVal`（白黄亮）/ `_hidingShellVal`（壳青绿）/ `urchin_sh_left`（海胆紫）。
法力护盾**本来就该是第四段**。现在用的是一行飘字 —— 飘完就没了，
玩家不知道自己还有多少法力盾、还剩几秒。**照那三个的写法加第四段，读
`battle._spec.val(u, CAN_MANA_KEY)`，删掉 `_float_text("法力护盾")`。**
> 这跟「充能条没人看得见」是同一个错误的第二次犯 —— 有现成机制没用（[[fb-hand-rolled-copies-drift]]）。

**② 爆点整个重做**（差距最大）
现状：`_star_mesh(7)` + `_disc_mesh(22)`，**7 根等长三角 + 一个圆盘，平躺地面**。
参考（`r_0273.png` 已裁剪研究过）：几十根**长短粗细各异**的尖刺、**朝各个方向（含朝镜头）**
炸开成球状、中心一簇**硬边白色碎片核**、外裹柔光晕，三层有前后关系，**立起来**高度与角色相当。

**③ 射线束身补内容**
剖面数值对了但管子里是空的。参考有**多股细丝并行**（角度略有差异）、**沿束火花**、边缘辉光。

### 下一步从哪开始（用户已认标准，不必再问）

**从「量爆点」开始**，逐帧 profile 成表再驱动几何，**系数一个不手调**：
尖刺数量／长度分布／宽度分布／张角分布、白核尺寸占比与边缘锐度、
外晕半径包络与衰减、爆点随时间的膨胀-收缩-淡出曲线、束身细丝股数与角度离散、火花发射率/速度/寿命。
**先把表量出来给用户看数，认了再生成几何。**

### 还没验、别当成没问题的

- **跳数字**：`_apply_damage` 的 `dot_accum=true` → `_dot_accumulate` 是「不跳小飘字、
  累加进头顶按伤害类型桶的常驻数字」，射线**已经传了 true**。
  但我**只读了代码路径没实拍确认**：那个数字屏幕上显不显示、显示在哪、
  **射线离开目标后怎么收场**（定格飘走还是继续挂着）——**要实拍量，别推理**。
- **实战总伤害多半会降**（扫射摊薄），量级未测。
- **k=0.55**（地面压缩因子）是估计值；用户已验「转向速度还好」⇒ 暂不动。
- 命中爆点并存数量 × 性能预算未测。

### 素材与工具（别重造）

- 参考视频与抽好的帧在 scratchpad：`velkoz/spotlight.mp4`、`velkoz/r_full/`（93~102s，60fps）、
  `velkoz/r2/`（114~120s，第二次 R，玩家甩得狠，转向速率就是从这段量的）。
  **scratchpad 是会话级的，新会话可能已清 —— 没有就按 §〇 重下：**
  `yt_dlp` + `imageio_ffmpeg`（传 `ffmpeg_location`；只取视频轨免合流），
  视频 `https://www.youtube.com/watch?v=DVg7SleafEs`。
- 干净台：`VFXLAB=1 VFXLAB_CASE=p2eq_068`，拍摄时刻已改到能看出新包络；
  `VFXLAB_DUR=42` 可看两轮释放。开窗口给用户看：`--resolution 850x505 --position 0,514 --audio-driver Dummy`
  （**屏幕左下角 + 静音**，用户定的）。

---

## 八、爆点/细丝量测表（2026-08-11 · 欠债②③的输入，v0.19.95 已按此实装）

**口径**：底帧 r_0100 差分；黄色门 `dR>40 且 dG>30`（受击红描边 dG≈0、紫伤害数字都天然排除）；
束走廊按**笛卡尔垂距**排除（角扇区在近心处漏，束在 r=100px 已占 ±27°）；
爆点中心锚定用**敌方血条追踪**（y∈[195,285] 横带）+ 固定偏移到身体中心。
换算锚：参考束全宽 104px = 我们 116 码 ⇒ **1px ≈ 1.115 码**（角速度那次同款：无量纲/比例量可 1:1 搬）。

### 持续段（帧 222~240，束照着目标）

| 量 | 实测 | 实装常量 |
|---|---|---|
| 尖刺数（整圈外推） | 中位 15（逐帧 9~20） | `HIT_SPIKE_N=15` |
| 刺长 px→码 | p25 36 / p50 57 / p75 69 / max 81 px | `HIT_LEN_Q=[27,40,64,77,90]`码 |
| 刺角宽 | **双峰**：细针 4~12° 与宽瓣 35~55° 并存（p50=22°骗人） | needle 8° / lobe 42° |
| 刺形节拍 | 相邻帧(33ms)相关中位 **0.37** ⇒ ~2 帧一换 | `HIT_RESHUFFLE=0.04s` |
| 光晕剖面 | mean dL 在 r≤30px 平台，之后 e 折减尺度 ~13px，r≈80px 见底 | 平台 33 码/尺度 14.5/见底 90 |
| 颜色 | **全程琥珀** RGB(207,144,65)→(173,111,84)，sat 0.69→0.51，**白占比<4%** | `HIT_COL_IN/OUT`，无白芯 |
| 强度 | E 跟束包络同曲线起伏 ±15% | 爆点 alpha × env |
| 出现/消失 | 扫上 ≤0.2s（几何污染上界）；切断后 4 帧塌掉 | `HIT_IN=0.06 HIT_OUT=0.10` |

### 末端爆发（帧 271~276，共 ~0.1s）

E ×3、刺长 ×2.2（max 204px≈227 码）、**实心白核 r≈10px** + **白刺带 44~72px**（白色只在这里出现）。
⇒ `BURST_SEC=0.10 / BURST_LEN_MULT=2.2 / BURST_CORE_R=12码 / BURST_RING 49~80码 / BURST_OUT=0.07`。
★白刺带**不乘**刺长倍率（实拍核对时抓到的实装 bug，已修）。

### 束身细丝 & 火花（欠债③）

- 横截面亮脊 p50 = **4** 条（2~5 波动），位置沿束变化、逐帧重排 ⇒ `FIL_N=4`，0.08s 换排布。
- **离束火花实测为零**（perp>65px 无独立亮斑，2~80px blob 逐帧计数 p50=0）⇒ 不做飞散粒子。

### 量测口径的教训（v1/v2 都翻车，v3 才收敛）

1. **爆点中心不能从特效自身反推**（v1 白像素中位数落在束芯线/施法条白字上；v2 横向鼓包被
   环境噪声与出画束尾拽跑）——要锚定**目标本身**（血条横带追踪 + 固定偏移）。
2. **走廊排除要笛卡尔不要角扇区**（束不过极点时在极坐标里是"一条弦"，覆盖角随半径暴涨）。
3. **受击红描边会污染暖色差分**——黄色门（dR 且 dG）一并解决紫数字。
4. 束在持续期是**被玩家扫来扫去的**（dperp 轨迹 5↔160px）——"持续段"帧要按 dperp 选，
   不能拿时间窗一把抓。

### 遗留（诚实记录）

- 光晕三层壳 alpha [0.14/0.06/0.028] 是按实测剖面**拟合**的，没做实拍闭环校准（色相那次的口径）。
- 携带者**中途死亡**时束会冻在原地直到换路 clear_all（v0.19.93 起就有，非本轮引入）；
  末端爆发的自清也依赖场上还有活的 068 携带者在推 `advance`。都是小概率视觉残留，记账待修。
- 束终点与目标重合时（我们的 190 码近战距 vs 参考 1000+ 码），枪口星芒/束体/爆点三者叠加,
  中心会加色到偏白——参考里三者是分开的。要不要按目标距离压枪口亮度，等用户看过再定。

---

## 九、束身二轮量测 + shader/飘带（2026-08-11 · 用户「上 shader，直接做完一版」，v0.19.96 实装）

**用户对爆点已验收**（「命中特效可以了」），剩束身。二轮量测三个结论**与直觉全反**：

| 量 | 实测 | 结论 |
|---|---|---|
| 流速 | 55/55 帧对沿束亮纹互相关**最佳位移 = 0** | **束身不流动**——亮纹原地闪变，shader 不做 UV 滚动 |
| 闪变节拍 | lag 相关：17ms 0.99 → 67ms 0.92 → 267ms 0.50 | 去相关 ≈**270ms**（比爆点 40ms 慢一个量级，束是"稳"的）|
| 束沿 | 半宽起伏 std ±1px（2%） | **边缘光滑**——不做舔焰 |
| 调制幅度 | 沿束亮度 std/mean ±10.5% | shader `u_mod_amp` |
| 呼吸 | 束宽残差 ±6.0%，自相关首峰 4 帧（67ms） | shader `u_breath / u_breath_sec` |
| 飘带 | 3~4 条金色彗尾弧带绕身转，半径 60~95px（67~106码），端头星形闪点 | `RIBBON_*` 常量 |

**实装**：`assets/shaders/mana_beam.gdshader` 挂五层壳+细丝（顶点色不动=旧色相校准存活，
实拍闭环复核：换管线前后 R/G 1.132→1.130、G/B 1.649→1.654，零漂移）；时间只走 `u_t`
（set_progress 喂 t_acc，**禁 TIME**，门禁 ⑪ 扫源码焊死）；细丝轮换周期 0.08→0.27s（对齐实测去相关）。
飘带 3 条：弧 200~280°、半径差 ±18%、平面倾角 ±14°、公转 150°/s（**估计带**，同 k=0.55 性质：可感知量，真机不对再调）。

**实拍抓出来的三个实装 bug**（门禁全绿也看不见，只有拍）：
① 飘带横鳍挤出方向误写成**切线**（应为径向）⇒ 顶视零宽；② 每段只发一个三角形 ⇒ 宽底尖点交替＝虚线感；
③ `CULL_BACK` 把背对相机的半圈剔掉——平面条带必须双面（平面双面不翻倍，闭合曲面才翻倍）。

**门禁上的新教训**：`get_shader_parameter` 对没设过的参数返回 null，`float(null)` 是运行时错误 ⇒
**测试函数静默中止、剩余断言不跑、照样 ALL PASS**（断言计数 65→60 是唯一线索）。
回读一律 null 安全（null → -999 = 必红）。反向验证时 u_t 那条就是这样先假绿、修了才真红的。
