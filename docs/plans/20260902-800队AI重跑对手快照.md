# 800 队 AI 重跑对手快照 —— 并把「蠢 AI」换成有策略的 AI

状态: 已完成

## 需求原文

**★逐字存档**：[20260902-用户原话-800队快照.txt](20260902-用户原话-800队快照.txt)
（5760 字节 · sha1 `cf846355a7eb`）。

> 用户 2026-09-02:「必须一字不落地写好方案」「你懂一字不落吗」「不是简单地手册」
>
> ⇒ 「一字不落」指的是**把原话逐字存档**，不是我把需求复述成一份读起来很全的手册。
> 复述再详细也是我的话。**任何时候本篇与存档冲突，以存档为准。**

### 逐句口径清单（每一句原话 → 它要求什么 → 落在哪）

★这张表的用法：**动手前从头读一遍**。否定式指令最容易丢，单独标了「否定」。
（memory `fb-pin-user-words-dont-drift`：多轮迭代里我只记最新一条、前面的全丢，一小时内违反过他说过的 5 条）

| # | 原话（逐字） | 它要求什么 | 落在哪 | 状态 |
|---|---|---|---|---|
| 1 | 「然后是新的方案书」 | 这件事要先有方案书再动手 | 本篇 | ✅ |
| 2 | 「当前游戏里的所有对手快照要再次全部重新跑出来」 | **全部**重跑，不是增量 | §3 / §7.5 ② 备份后整池替换 | ⏳ 待跑 |
| 3 | 「我想要800支队伍通过ai来打」 | 规模 **800**，且是 **AI 互打**（不是合成数据） | §7.2 `--bots 800` | ⏳ 待跑 |
| 4 | 「还是以前的办法去记录快照」 | **沿用**旧管线，不另起一套 | `_cohort.gd` → `cohort_to_seed.py` → `ghost_seed.json` 三段没换 | ✅ |
| 5 | 「但这次我需要你优化ai策略」 | 策略要**优化**过 | §6 策略向量 + 五个决策点 | ✅ |
| 6 | **「别弄蠢ai」**（否定） | 不许再是那种只会按一个维度排序的 AI | §6：四种旧策略只作用在**一个**决策点，现在五个 | ✅ |
| 7 | 「要么那种玩四五费的高费流」 | 要有**高费流** | 锚点 `hi_cost` | ✅ |
| 8 | 「要么就是玩羁绊开9档19档」 | 要有**高羁绊流**（见 #10 的澄清） | 锚点 `line_top` / `dual_mid` | ✅ |
| 9 | 「要么就多多羁绊，很多个羁绊2档1档这样等等」 | 要有**多羁绊铺开流** | 锚点 `wide_syn` | ✅ |
| 10 | 「你可以搞多种策略」 | **多种**，不限于他点名的三类 | 14 个锚点 | ✅ |
| 11 | **「但别让蠢ai来行吗」**（否定·重复强调） | 同 #6，他说了两遍 | 门禁 `verify_bot_archetypes.gd` 24 条焊死 | ✅ |
| 12 | 「我的意思就是有些人会想玩高羁绊啊」 | 「9档19档」＝**高羁绊流**，不是字面 19 档 | 出入①已决 | ✅ |
| 13 | 「全部替换掉旧池」 | **整池替换**（不是合并） | 未决点⑥已决 + §7.5 ② 先备份 | ⏳ 待跑 |
| 14 | 「是啊，但只有6种流派也少了啊」 | 6 种**不够** | 从 6 扩到 **14 个锚点 + 随机扰动** | ✅ |
| 15 | 「够吧」 | 认可 14 个锚点 + 扰动的方案 | §6 | ✅ |
| 16 | 「多进程并行跑分片」 | 速度走 **P2**（不是 P1 FAST_SIM、不是 P3 降规模） | §5 + `tools/cohort_shard.py` | ✅ |
| 17 | 「先记录好方案」 | **先记录**，不是先开跑 | 本篇；开跑要等他喊 | ✅ |
| 18 | 「我到时候排别的任务」 | 我这边不要占着他的时间等 | — | ✅ |
| 19 | 「但随时可能喊你开始快照」 | 方案要能**被没有上下文的人直接执行** | §7 执行手册 | ✅ |
| 20 | 「所以必须一字不落地写好方案」 | 见 #21/#22 | 逐字存档 + 本表 | ✅ |
| 21 | 「你懂一字不落吗」 | 我第一版理解错了（当成"写详细手册"） | 改成**逐字存档原话** | ✅ |
| 22 | **「不是简单地手册」**（否定） | 光有手册不算数 | 手册留着（#19 要），但需求本体以存档为准 | ✅ |

**⇒ 22 句里还没兑现的只有 3 句（#2 #3 #13），全部是"要真的跑一次 800 队"。**
其余 19 句已落地并有门禁/实测支撑。

---

## 调查

### 现有管线（三段，全部还在跑）

| 段 | 文件 | 事实 |
|---|---|---|
| ① 模拟 | [tests/_cohort.gd](../../tests/_cohort.gd) | N 只机器人**真实队列互打**：每场推进两只，输光 8 命**直接出局不复活**。每打完一场把该机器人**当时的真实家当**存成快照 |
| ② 转换 | [tools/cohort_to_seed.py](../../tools/cohort_to_seed.py) | `tools/autoplay/cohort-snapshots.json` → `data/ghost_seed.json`。**不合成、不配平、不拟合**，原样搬运；自检不过关**拒写** |
| ③ 池子 | [data/ghost_seed.json](../../data/ghost_seed.json) | 当前 **184 条 / 9 档**（档0 20 条、档1 30 条、档2～7 各 20 条、档8 14 条） |

跑法（`_cohort.gd` 头注，实测有效）：

```
SHIP=1 DL_AUTOFIGHT=1 TURTLE_SEED=<seed> COHORT_BOTS=<n> \
<godot> --headless --audio-driver Dummy --path . res://tests/_cohort.tscn
```

环境变量：`COHORT_BOTS`（默认 32）、`COHORT_MAX_ROUNDS`（默认 120）、`COHORT_OUT`（默认 `res://tools/autoplay`）。
★不写玩家存档（`GameState.test_mode` + `backend.gd` 的 `save_pool` 守卫）。

### 现有 AI 到底「蠢」在哪 —— 逐条查过，不是我印象

`tests/_cohort.gd:31`：

```gdscript
const STRATEGIES := ["merge_first", "greedy_cost", "random", "synergy"]
```

四种策略**只作用在「货架 10 格按什么顺序买」这一个决策上**（`_cohort.gd` 的 `idxs.sort_custom`）：

| 策略 | 实际行为 | 蠢在哪 |
|---|---|---|
| `random` | 随机排序 | 纯噪声，不是玩家会有的行为 |
| `greedy_cost` | 按 `cost` 从高到低 | 只看单件费用，**不看羁绊、不看是否合成、不看龟的定位** |
| `merge_first` | 优先买"已有 1～2 张的同 id" | 只追三合一，**从不看类型** |
| `synergy` | 优先买"能推过下一档阈值"的（2026-08-12 才加） | 四种里唯一看羁绊的；但**只看下一档**，不认"这一路能不能走到顶档" |

**四种里没有任何一种做这三件事**：①按费用带成型（高费流）②认准一条羁绊线走到顶 ③故意铺宽拿多个 1 档。
⇒ 用户说的「蠢 AI」有具体所指，不是感觉。

### 羁绊档位的真实数据（决定 S6/S7 可不可行）

`scripts/gamedata/phase2_types.gd` 的 `TYPES` + `data/p2eq-types.json`：

| 类型 | 档位阈值 | 该类型**总件数** |
|---|---|---|
| 剑 / 食物 / 盾 / 药水 / 枪 / 弓箭 | `[3, 6, 9]` | 各 **9 件** |
| 奇械 / 灵物 / 法器 / 遗物 | `[2, 5, 8, 10]` | 各 **10 件** |
| 香火 | `[1]`（只有 1 档） | 1 件 |
| 斧头 | `[1]` | 1 件（096） |

**★关键事实**：计数按 **id 去重**（`Phase2Types.calc_active`，`_cohort.gd:456` 那段注释也写明"重复 id 不涨羁绊数"）。
而每个类型的件数**正好等于它的顶档**（9 或 10）⇒ **「开满顶档」＝把那一类的每一件都集齐，一件不缺**。
这决定了 S6 是一条**极难**的路线，不是随便买买就能到 —— 这一点必须在验收里量，不能假设它做得到。

---

## 需求 ↔ 代码的出入

| # | 需求那句 | 代码事实 | 处理 |
|---|---|---|---|
| ① | 「羁绊开 **9档19档**」 | 全表**没有 19 档**。最大档是 9（六个类型）与 10（四个类型） | 见未决点① |
| ② | 「800 支队伍」 | 现有池 184 条是 **32 只**机器人跑出来的；800 只的**耗时与稳定性都没有实测数据** | 见未决点②：先跑 64 只标定，再按线性外推报工时 |
| ③ | 「所有对手快照重新跑出来」 | `cohort_to_seed.py` 自检包含"档0 必须全裸"等硬条件，**不过关拒写** | 重跑后仍走同一套自检；不为了塞满 800 而放宽 |
| ④ | 「多种策略」 | 现有策略只影响**买装顺序**一个决策点；「高费流」还需要影响**刷新/攒钱/买经验**，那是另外的代码路径 | 见方案 §2：策略要能作用在**三个**决策点上，否则"高费流"只是"排序偏好"而已 |

---

## 方案

### §1 策略档案（Archetype）—— ⚠ **已被 §6 取代**（用户 2026-09-02:「只有6种流派也少了啊」）

> 下面这六种是我的**第一版**，用户当场否了。**以 §6 的 14 个锚点 + 策略向量为准。**
> 保留它只为记录当时的想法与被否的理由，**不要照它实现**。

每只机器人开局抽一个 archetype，整局**一以贯之**（现有实现是每场重抽顺序偏好，不构成"流派"）。

| # | 名称 | 对应原话 | 核心行为 |
|---|---|---|---|
| A1 | **高费流** | S5「四五费的高费流」 | 优先攒钱、少刷新；货架只在出现 4～5 费时出手；低费件只在"能三合一"时买 |
| A2 | **单线顶档** | S6「羁绊开9档/10档」 | 开局锁定一个类型，**只补该类型缺的 id**；其余只买能三合一的 |
| A3 | **双线中档** | S6/S7 之间 | 锁两个类型各推到中档（6 或 8） |
| A4 | **多羁绊铺开** | S7「很多个羁绊2档1档」 | 每次买**当前离最低档最近**的类型，横向铺满尽量多的 1～2 档 |
| A5 | **三合一优先** | —（保留现有 `merge_first` 当对照组） | 追星级 |
| A6 | **随机** | —（保留现有 `random` 当**下界基准**） | 噪声对照 |

★保留 A5/A6 是**故意的**：没有对照组就说不清"优化后的 AI 到底强了多少"。

### §2 策略要作用在三个决策点（不只是买装顺序）

| 决策点 | 现状 | 改成 |
|---|---|---|
| 货架排序 | 四种策略只改这里 | 按 archetype 的打分函数排序 |
| **买不买 / 攒不攒钱** | 现在是"有钱就买" | A1 高费流要能**跳过一整轮不买**，把钱留到出高费 |
| **刷不刷新** | `MAX_REFRESH = 2` 固定 | A2 单线顶档在"货架没有我要的类型"时才刷；A1 少刷 |

### §3 规模与产出

- `COHORT_BOTS=800`、`COHORT_MAX_ROUNDS` 按 §未决点② 实测定
- 产出仍是 `tools/autoplay/cohort-snapshots.json` → `cohort_to_seed.py --write` → `data/ghost_seed.json`
- 每条快照带上 `_strategy`（现有字段已有），便于事后按流派对账

### §4 工时标定（实测，不是估的）

跑法与产物同 §调查。标定条件：`COHORT_BOTS=8 COHORT_MAX_ROUNDS=40 TURTLE_SEED=20260902`，墙钟 600 秒。

| 量 | 实测 |
|---|---|
| 600 秒内跑完 | **4 轮 · 16 场** |
| 单场墙钟 | **≈ 37.5 秒** |
| 空场帧率（对照） | 146 fps；`Engine.max_fps` **本来就是 0**，没有帧率上限可解 |

外推到 800 只（每轮 400 场，单进程）：

| 轮数 | 场次 | 墙钟 |
|---|---|---|
| 20 轮 | 8000 场 | **3.5 天** |
| 40 轮 | 16000 场 | **6.9 天** |
| 60 轮 | 24000 场 | **10.4 天** |

★轮数**不随机器人数变化**（由"输光 8 命出局"决定，不由人数决定）⇒ 总场次 ≈ 人数/2 × 轮数，
  对人数是**线性**的。所以 800 只就是 32 只那次的 25 倍。

**⇒ 当前速度下 800 只不可行。** 三条可能的路（需要你选，见未决点⑧）：

| 路 | 说明 | 代价/风险 |
|---|---|---|
| P1 **实现 FAST_SIM** | `tests/_probe_fastsim.gd` 早就写好了验证（关/开跑同一场，指纹必须逐字相同），但 **`FAST_SIM` 在产品代码里从来没有实现过**（`git log -S` 全历史为空）⇒ 那个探针一直在比较两次**完全相同**的运行，断言恒真 | 提速倍数未知，要先量；确定性由现成探针守 |
| P2 **多进程并行** | 开 N 个 headless 分片跑，事后合并快照 | ★本机 CPU 有**确诊硬件故障**，CLAUDE.md 明写「多进程正是最容易触发的负载」 |
| P3 **降规模** | 比如 200 只 | 与原话 800 不符，要你点头 |

### §5 并行分片（用户 2026-09-02 拍板走 P2）—— 已实现并实测

工具：[tools/cohort_shard.py](../../tools/cohort_shard.py)（起 N 片 → 等 → 合并 → 逐条查 id 唯一）
`tests/_cohort.gd` 侧加了 `COHORT_SHARD`，改动四处，**非分片模式行为逐字不变**（片号 -1 走老分支）：

| 改处 | 为什么非改不可 |
|---|---|
| 每片种子 = `base + shard × 1000003` | 不改的话各片造出**一模一样**的机器人、打一模一样的场 —— 跑 N 片只是把同一份结果复制 N 遍 |
| `ghost_id` 带片号 | 原来是 `coh_<bot>_b<n>`，而 bot 编号**每片都从 0 开始** ⇒ 合并必撞（memory `fb-id-without-owner-dimension`：单机够用的 id 一接共享就塌） |
| `profile.id` 带片号 | 同上，它也是跨片会撞的标识 |
| 产物名带片号 | 否则同一个 `COHORT_OUT` 下各片互相覆盖 |
| 每片独立 `APPDATA` | Godot 在 Windows 从 `%APPDATA%` 解析 `user://`，共用会让 N 个进程抢同一个存档目录（门禁那边踩过） |

★合并这一步**逐条查 id 唯一性并在撞了时报错退出** —— 不是相信上面两条，是**验证**它们。

**实测吞吐**（本机 i9-14900HX：24 核 / 32 线程 / 32 GB）：

| 并行度 | 实测 | 效率 |
|---|---|---|
| 单进程（基线） | 600 秒 16 场 | — |
| 4 片 | 600 秒 48 场 | **3.0×**（75%） |
| 12 片 | 480 秒 114 场 | **8.9×**（74%） |

★效率从 4 片到 12 片**没有塌**（75% → 74%）。按 74% 外推：

| 并行度 | 800 只 × 40 轮 = 16000 场 |
|---|---|
| 12 片 | **18.8 小时** |
| 16 片 | **14.1 小时** |
| 20 片 | **11.3 小时** |

⇒ **一个通宵能跑完**。可续跑：哪片挂了 `--resume` 只补那片。

### §6 策略向量与 14 个流派（用户 2026-09-02:「只有6种流派也少了啊」→「够吧」）

**不做 6 个硬编码流派**，改成【命名锚点 + 随机扰动】：每只机器人抽一个锚点，再对数值项抖 ±35%
⇒ 800 支是 800 个样子，不是 14 个。（开关项如 `minion_first` 不抖 —— 二选一抖了就没意义。）

**★核心张力（这游戏里真实存在的分流，不是我编的）**：羁绊按 **id 去重** ⇒ 买第 2、3 张同 id
对羁绊**零贡献** ⇒ **「追三合一★3」与「追羁绊档位」是直接冲突的两条路**。门禁 ③ 专门焊死这一条。

| 锚点 | 覆盖 | 锚点 | 覆盖 |
|---|---|---|---|
| `hi_cost` 高费流 | 用户点名 | `low_flood` 低费海 | 与高费对立 |
| `line_top` 单线顶档 | 用户点名（高羁绊） | `star_rush` 三星速成 | 冲突轴的另一边 |
| `dual_mid` 双线中档 | 用户点名（高羁绊） | `fast_level` 速升级 | 经济向 |
| `wide_syn` 多羁绊铺开 | 用户点名 | `snowball` 滚雪球 | 与速升级对立 |
| `reroll_mad` 疯狂刷新 | | `no_reroll` 不刷新 | 与之对立 |
| `leader_all` 统领梭哈 | | `minion_up` 小将优先 | 与之对立 |
| `mix` 混合 | 最常见打法 | `random` 随机 | **下界对照组**（所有分布断言的分母） |

**策略作用在五个决策点**（★前四个过去全是写死的 ⇒ 对应流派在旧实现里根本不可能存在）：

| 决策点 | 原来 | 现在 |
|---|---|---|
| 买什么 | 四种策略只改这里 | 统一打分 `CohortSim.buy_score`（**纯函数**，门禁直接调） |
| 留多少币给装备 | 写死 `P2.AI_GEAR_RESERVE = 12` | 向量 `reserve` |
| 每次买几回经验 | 写死 `P2.AI_MAX_XP_BUYS_PER_VISIT = 3` | 向量 `xp` |
| 刷新几次 + 主动刷 | 写死"买不到才刷、最多 2 次" | 向量 `refresh` / `eager` |
| 装备先给统领还是小将 | 写死"先统领" | 向量 `minion_first`（`_equip` 拆成 `_fill_leaders`/`_fill_minions` 按序调） |

★向量里没有某项时**缺省仍是项目原口径**（行为不变）。

**门禁**：[tests/verify_bot_archetypes.gd](../../tests/verify_bot_archetypes.gd) —— 24 条全过。
判据是**排序关系**不是绝对分值（改权重数值不该红，改偏好顺序才该红），并且：

- 每条方向断言都配**反向那一边**（高费流 vs 低费海；锁剑 vs 锁盾）—— 只验一边等于没验
- 随机对照组必须**打分全平**（不平就不是对照组，后面所有分布对比全废）
- **源码守卫**：六个决策点逐个断言"真的在读向量" —— 没有这条的话，上面全是"表里写了不同的数"，
  而消费侧可能照样读写死常量（memory `fb-zero-caller-is-a-whole-class`：今天已经栽过两次）

反向验证：清掉高费流/三合一流/单线顶档/多羁绊 四个流派的偏好，**各自让门禁红**，产品代码逐字节还原。

## §7 执行手册（Runbook）

> **给"哪天用户喊一句开始、而我没有任何上下文"的自己。** 照着从上到下执行，不需要重新推导。
> 所有数字都标了出处；**别把这里的数字抄到别处**，改了以出处为准。

### 7.0 触发语与前置状态

触发语示例：「开始跑快照」「开跑 800 队」。

| # | 前置 | 怎么确认 | 状态 |
|---|---|---|---|
| P-A | 分布断言（落在**买入结果**上） | `tools/cohort_strategy_report.py` 存在；**拿旧池跑它必须 FAIL** | ✅ 已做（§10） |
| P-B | `cohort_to_seed.py` 按**流派分层抽样** | `select()` 里有 `by_arch` 两层轮转 | ✅ 已做（§10） |

**⇒ 两个前置都已完成（2026-09-02）。★★★而 7.2 那条命令 2026-09-03 就跑完了 —— 见文末「实施回填」。**
**这一整节（§7 开跑手册）现在是历史记录，不是待办。**

### 7.1 开跑前的检查（全绿才开跑）

```bash
cd /c/Users/Louis/Documents/GitHub/turtle-realtime-godot
git status --short                 # ① 工作区干净(跑起来之后不能再改仓库)
ps -W | grep -ci godot             # ② 期望 0(没有别的 Godot 在抢核, 也免得被误杀)
df -h /c | tail -1                 # ③ 留 2 GB 以上
python tools/cohort_shard.py --merge-only --shards 16 ; echo "rc=$? 期望 1"   # ④ 证明合并会拦
```

### 7.2 开跑（这一条就是全部）

```bash
cd /c/Users/Louis/Documents/GitHub/turtle-realtime-godot
nohup python tools/cohort_shard.py --bots 800 --shards 16 --max-rounds 120 --seed 20260902 \
  > /c/tmp/cohort800.log 2>&1 &
```

| 参数 | 值 | 出处 |
|---|---|---|
| `--bots` | **800** | 用户原话 |
| `--shards` | **16** | §5 实测 12 片效率 74% 且没塌；本机 24 核 / 32 线程 / 32 GB（16 个 headless 约 8 GB） |
| `--max-rounds` | **120** | `_cohort.gd` 原默认值。轮数由「输光 8 命出局」决定，给足上限让它自然打完 |
| `--seed` | 20260902 | 任意；**同一次重跑内不要中途换** |

**预期墙钟约 14 小时**（§5：37.5 秒/场 × 16000 场 ÷ 11.8）。这是外推值，用 7.3 核对。

### 7.3 跑起来之后怎么看进度

```bash
for i in $(seq 0 15); do printf "  片%-2d " $i; \
  grep -cE "^  第 *[0-9]+轮" tools/autoplay/shard-$i.log 2>/dev/null || echo 0; done
```

累计场次对照目标约 16000 场；偏离太多就查是不是并行度过高在互相抢核。

### 7.4 中断了怎么办（机器重启 / 蓝屏 / 手动停）

```bash
python tools/cohort_shard.py --bots 800 --shards 16 --max-rounds 120 --seed 20260902 --resume
```

只补没有产物的片。★本机 CPU 有确诊硬件故障（CLAUDE.md §2）⇒ **中途挂掉是预期内的**，不是意外。

### 7.5 跑完之后的完整流程（按顺序，一步都不能跳）

```bash
# ① 合并(逐条查 ghost_id 唯一; 撞了会 exit 1 —— 让它拦, 别绕过)
python tools/cohort_shard.py --merge-only --shards 16

# ② ★备份旧池(整池替换不可逆; 没有这一步就没有回滚)
mkdir -p docs/plans/attic
cp data/ghost_seed.json "docs/plans/attic/ghost_seed-旧池-$(date +%Y%m%d).json"

# ③ 先 dry-run 看自检(四条口径以该脚本头注为准)
python tools/cohort_to_seed.py

# ④ 自检全过才写入(不过关它会拒写 —— 这是设计好的, 别去放宽)
python tools/cohort_to_seed.py --write

# ⑤ ★流派与构筑验收(前置 P-A 的工具), 四条期望逐条对, 不达标 exit 1
python tools/cohort_strategy_report.py --pool tools/autoplay/cohort-snapshots.json

# ⑥ 池子相关门禁
bash run-tests.sh
```

### 7.6 已知失败形态与处置

| 现象 | 真因 | 处置 |
|---|---|---|
| 合并报「ghost_id 撞了 N 条」 | 片号没进 id | **不要绕过**；查 `_cohort.gd` 里 `_shard < 0` 那三处分支 |
| 合并报「产物结构不对」 | 产物是 `{_note, brackets}` 不是数组 | 以 `_cohort.gd` 的 `_report()` 为准 |
| 某片 rc≠0 且无产物 | 机器故障 / 被杀 | `--resume` 补那片 |
| 各片结果一模一样 | 种子没错开 | 查 `seed + i * 1000003` 两侧 |
| `cohort_to_seed.py` 自检不过 | 上游快照时机或档位判定有问题 | 按它报的那条查，**不要调 `PER_BRACKET` 去凑** |
| `cohort_strategy_report.py` 报某流派 0 条 | 那条流派活不下来 | **点名写进回填并说明原因**，不是删掉它 |

### 7.7 绝对不要做的事（每条都有出处）

1. **跑起来之后不要改仓库** —— 长跑期间改仓库＝结果作废（memory `fb-no-repo-edits-while-gate-runs`；本次会话已犯过一次）
2. **不要 `taskkill /IM` 杀 Godot** —— 会连门禁的 headless 一起杀（memory `fb-vfxlab-window-must-be-muted`）。要杀按 PID
3. **不要为了凑满条数放宽 `cohort_to_seed.py` 的自检** —— 那四条是池子质量的唯一保证
4. **不要跳过备份旧池** —— 替换不可逆
5. **不要把 `tools/autoplay/` 的产物提交进库** —— 已在 `.gitignore`

### 7.8 产出物清单

| 产物 | 位置 | 入库? |
|---|---|---|
| 各片原始快照 | `tools/autoplay/cohort-snapshots-s<0..15>.json` | 否 |
| 合并后快照 | `tools/autoplay/cohort-snapshots.json` | 否 |
| **新种子池** | `data/ghost_seed.json` | **是** |
| 旧池备份 | `docs/plans/attic/ghost_seed-旧池-<日期>.json` | 是 |
| 逐片日志 | `tools/autoplay/shard-<i>.log` | 否 |

---

## §8 调查与实验全过程（2026-09-02，按发生顺序）

> 用户 2026-09-02:「还有这次我们的所有对话和过程还有测试，专门调查快照的」
> ⇒ 这一节记**做过什么、量到什么、结论是什么**，包括**失败的尝试和我自己犯的错**。
> 目的：下一轮（可能没有上下文）不用重新试一遍。

### E1 摸清现有管线（只读调查）

| 查了什么 | 结论（带出处） |
|---|---|
| 快照怎么产生 | `tests/_cohort.gd`：N 只机器人**真实队列互打**，输光 8 命出局不复活，每场后存当时真实家当 |
| 怎么进池 | `tools/cohort_to_seed.py`：**不合成、不配平、不拟合**，自检不过关**拒写** |
| 现役池 | `data/ghost_seed.json` = **9 档 · 184 条**（档0 20 / 档1 30 / 档2~7 各 20 / 档8 14） |
| 池子是几只机器人跑出来的 | **32 只**（`_cohort.gd` 头注 + `COHORT_BOTS` 默认值） |

### E2 诊断「蠢 AI」蠢在哪（只读调查，逐条查过不是印象）

`tests/_cohort.gd:31` 的 `STRATEGIES = ["merge_first", "greedy_cost", "random", "synergy"]`，
四种**只作用在 `idxs.sort_custom`（货架排序）一个决策点**：

- `random` 随机排序；`greedy_cost` 只看单件 cost；`merge_first` 只追三合一从不看类型
- `synergy`（2026-08-12 才加）是唯一看羁绊的，但**只看下一档**，不认"这条线能不能走到顶"
- 四种里**没有任何一种**：按费用带成型 / 认准一条线走到顶 / 故意铺宽拿多个 1 档

而商店回合里另外**四个决策点全是写死的**（`_shop` / `_equip`）：
留币线 `P2.AI_GEAR_RESERVE=12`、买经验上限 `P2.AI_MAX_XP_BUYS_PER_VISIT=3`、
刷新 `MAX_REFRESH=2` 且只在"买不到"时刷、装备"先填满统领"。
**⇒ 用户说的「蠢 AI」有具体所指，不是感觉。**

### E3 羁绊档位的真实数据（决定「高羁绊流」可不可行）

- 6 类 `[3,6,9]`（剑/食物/盾/药水/枪/弓箭，各 **9 件**）· 4 类 `[2,5,8,10]`（奇械/灵物/法器/遗物，各 **10 件**）· 香火/斧头 `[1]`
- **计数按 id 去重**（`Phase2Types.calc_active`）
- ⇒ **每类件数正好等于它的顶档** ⇒「开满顶档」＝**那一类每一件都集齐、一件不缺**
- ⇒ **「追三合一★3」与「追羁绊档位」是直接冲突的两条路** —— 这条成了策略设计的主轴

### E4 标定跑（实测）

`COHORT_BOTS=8 COHORT_MAX_ROUNDS=40`，墙钟 600 秒 → **4 轮 · 16 场** ⇒ **单场 ≈ 37.5 秒**。
日志 `/c/tmp/cohort8.log`。外推：800 只 × 40 轮 = 16000 场 ⇒ 单进程 **6.9 天**。

★同时观察到日志里有 `Lambda capture at index 0 was freed`（在门禁的致命正则里）——
  **本次未追查**，记在这里免得下次当新问题查。

### E5 找加速点（两个发现）

| 试了什么 | 结果 |
|---|---|
| 无头帧率上限 | `Engine.max_fps` **本来就是 0**（无上限可解）；空场实测 **146 fps** ⇒ 瓶颈是**战斗仿真本身** |
| `Engine.time_scale` | det 模式（设了 `TURTLE_SEED`）每帧恰一个固定 SIM_DT 步，**不看 delta** ⇒ 调它无效（`_probe_fastsim.gd` 头注写明） |
| `FAST_SIM` | ★**产品代码里从来没有实现过**（`git log -S "FAST_SIM" --all -- scripts/` 为空）⇒ `tests/_probe_fastsim.gd` 一直在比较两次**完全相同**的运行，断言恒真 |

⇒ 给用户三条路 P1 实现 FAST_SIM / P2 多进程并行 / P3 降规模，用户选 **P2**。

### E6 并行分片实测（两次）

| 实验 | 配置 | 结果 |
|---|---|---|
| 基线 | 单进程 8 只 | 600 秒 **16 场** |
| 并行① | 4 片 × 6 只 | 600 秒 **48 场** ⇒ **3.0×**（效率 75%） |
| 并行② | 12 片 × 6 只 | 480 秒 **114 场** ⇒ **8.9×**（效率 74%） |

★效率从 4 片到 12 片**没塌** ⇒ 16 片外推 ~11.8× ⇒ 800 只约 **14 小时**。
本机 i9-14900HX：**24 核 / 32 线程 / 32 GB**（16 个 headless Godot 约 8 GB）。

### E7 分片实现时**先验才躲掉**的三个坑

1. **`ghost_id` 每片都从 0 开始** —— `coh_<bot>_b<n>`，16 片合并必撞。
   （memory `fb-id-without-owner-dimension`：单机够用的 id 一接共享就塌）
2. **每片种子必须不同** —— 否则各片造出一模一样的机器人，跑 16 片只是把同一份结果复制 16 遍。
3. **每片独立 `APPDATA`** —— Godot 在 Windows 从 `%APPDATA%` 解析 `user://`，共用会让 16 个进程抢同一个存档目录。

另加：`profile.id` 带片号、产物名带片号。**非分片模式（片号 -1）行为逐字不变**。

### E8 流派实现 + 门禁

- 14 个命名锚点 + 数值项 ±35% 扰动（开关项不抖）
- 策略作用在**五个**决策点（见 §6 表）
- 门禁 `tests/verify_bot_archetypes.gd` **24 条全过**；判据是**排序关系**不是绝对分值
- 反向验证：清掉 `hi_cost` / `star_rush` / `line_top` / `wide_syn` 四个流派的偏好 ⇒ **各自让门禁红**，产品代码逐字节还原
- 小批实跑（28 只 / 3 轮 / 42 场 / 墙钟 1855 秒）⇒ **84 条快照 · 12 个流派都出现了**

### E9 合并逻辑的 bug（真跑才发现）

我第一版 `merge()` 按**数组**写，实测合出来 **2 条** —— 那是在数 dict 的两个键。
真实产物是 `{"_note": str, "brackets": {"<档>": [快照…]}}`（`_cohort.gd` 的 `_report()`）。
**这种"我以为的格式"和"真实格式"的差，只有真跑一遍才看得见。**已按真实结构重写。

合并的 id 唯一性检查**正反两面都验过**：

- 反：喂两份**相同**的产物 ⇒ 抓到 **84 条**冲突并 exit 1
- 正：把其中一份的 id 改成分片格式 ⇒ **84+84=168 条、id 全唯一**、合并成功

### E10 我自己在这一段里犯的错（记下来免得再犯）

| 错 | 后果 | 已改成 |
|---|---|---|
| 「编译 OK」的检查**比实际窄** | `--headless --quit` **不加载 `tests/` 下的脚本**，两个语法错（`pick` 重名、`var n := 0` 被吃掉）都没抓到 | **真加载那个场景**来验 |
| 替换 `match` 块时把相邻的 `var n := 0` 一起吃掉 | 运行时才炸 | 块替换后逐行看 diff |
| 建 `tests/_probe_fps.gd` 时**同名覆盖**了 v0.17.33 起就存在的地砖网格密度探针 | 差点连它一起删掉 | `git checkout HEAD --` 已还原；memory `fb-check-file-exists-before-writing` |
| 门禁跑着的时候我把台账文件挪走做 A/B | 那一轮门禁结果作废（报"台账不存在"） | memory `fb-no-repo-edits-while-gate-runs`，本次又犯一次 |

---

## §9 池子本身的事实（回滚 / 校验 / 会触发哪些门禁）

### 9.1 一条快照里存了什么（18 个字段）

```
_strategy  bracket  equipped  ghost_id  is_bot  lane_assign  leaders  loadouts
minions    pet_levels  profile  schema_ver  season_eggs_killed  season_level
season_total_battles  trainer_skill  chest_treasures_won  chest_treasure_value
```

★**别抄这张表去写代码** —— 以 `data/ghost_seed.json` 里真实的一条为准。
★`_strategy` 是**离线对账用的元数据**，产品代码不读它（`scripts/net/backend.gd` 是 `ghost_seed` 的唯一消费者）。
  ⚠ 以后若给快照**加新字段**，`tools/asset_orphan_audit.py` 会把"没有消费者的 json 字段"报出来 ——
  那时要么给它接消费者，要么记进 `tools/asset_orphan_debt.json` 并写清理由。

### 9.2 回滚（新池不好怎么退回）

```bash
# 旧池备份在 §7.5 ② 那一步产生
cp "docs/plans/attic/ghost_seed-旧池-<日期>.json" data/ghost_seed.json
bash run-tests.sh
```

★所以 §7.5 ② 的备份**一步都不能跳** —— 没有它就没有回滚。

### 9.3 新池写入后会触发哪些门禁（跑完必须全绿）

| 门禁 | 它守什么 |
|---|---|
| `tests/verify_ghost_seed.gd` | 种子池结构/字段 |
| `tests/verify_bracket_gear.gd` | 每档件数不超槽位、档0 全裸 |
| `tools/data_integrity.py` | json 交叉引用 / 资源路径 |
| `tools/asset_orphan_audit.py` | 有没有**新增**的无消费者字段（见 9.1 的⚠） |
| `tests/verify_bot_archetypes.gd` | 流派本身（与池子无关，但同批改动） |

### 9.4 「新池是不是真的不一样」怎么证（风险⑦ 的具体做法）

光看条数变多不算数。三个量，跑完各算一遍写进「实施回填」：

| 量 | 怎么算 | 期望 |
|---|---|---|
| **流派覆盖** | 按 `_strategy` 分组计数 | 14 个锚点**每个都出现**；某个为 0 = 那条流派活不下来，要写明原因 |
| **羁绊深度分布** | 每条快照算"同时激活的类型数"与"最高档位" | 新池应显著高于旧池（旧池的机器人一件都不看羁绊） |
| **费用分布** | 每条快照的装备 cost 直方图 | `hi_cost` 组的 4~5 费占比应显著高于 `random` 组 —— 这就是前置 P-A |

### 9.5 旧池基线（**已算，2026-09-02**）—— 跑完拿这个当对照

存档：`docs/plans/attic/ghost_seed-旧池基线-20260902.json`（脚本口径见该文件的「来源」字段）

| 量 | 旧池实测（184 条 / 32 只机器人 / 装备件数均值 17.63） |
|---|---|
| 流派 | 只有旧四种：`merge_first` 50 · `random` 48 · `synergy` 46 · `greedy_cost` 40 |
| 费用分布 | 1费 2% · 2费 15% · **3费 49%** · 4费 18% · 5费 17% |
| 同时激活的羁绊类型数 | 均值 **1.42** · 最大 5 · **33% 的队伍一个羁绊都没有** |
| 最高档位 | 均值 **0.68** · 全池最大 **2** |

**★★全池 184 支没有一支打到过 3 档（顶档）。** 这是「蠢 AI」的**量化证据**，不是感觉 ——
也是新池的硬指标：新池若还是「33% 零羁绊、最高 2 档」，那这次重跑就白跑了。

**新池的期望（写死在这里，跑完逐条对）**：

| 量 | 期望 | 不达标说明什么 |
|---|---|---|
| 流派覆盖 | 14 个锚点**每个都出现** | 某个为 0 = 那条流派活不下来（要写明原因，不是删掉它） |
| 零羁绊占比 | **显著低于 33%** | 羁绊类流派没起作用 |
| 最高档位 | 至少出现 **3 档**（顶档）的队伍 | `line_top` 单线顶档流没走通（见风险③：顶档要集齐 9/10 件，可能真的到不了） |
| `hi_cost` 组 4~5 费占比 | **显著高于** `random` 组的 35% | 高费流没起作用（前置 P-A 要量的就是这个） |

★**旧池要先算一遍当基线**（现在就能算，不用等重跑）：否则跑完没有对照，说不清"变好了"还是只是"变了"。

---

---

## §10 两个前置（P-A / P-B）—— 已完成 2026-09-02

### P-B 按流派分层抽样（`tools/cohort_to_seed.py` 的 `select()`）

原来只有一层「按机器人轮转」。问题：**强势流派活得久 ⇒ 它在某一档的候选条数压倒性多**，
按机器人轮转救不回来（那些机器人全是同一个流派）⇒ **整档可能都是同一种打法**，
而这次重跑的全部意义就是"对手各不相同"。

现在两层：**① 按 `_strategy` 轮转 ② 层内再按机器人轮转**。

**证明它真的分层了**（实测，造一批「一个流派压倒性多」的候选）：

| 候选 | 挑出来 |
|---|---|
| 106 条，其中 `hi_cost` **占 85%**（来自 30 只不同机器人） | **5 个流派各 4 条** |

不分层的话整档会被 `hi_cost` 占满。
★老数据没有 `_strategy` 时全部落进 `(无)` 组 ⇒ 退化成原来的单层轮转，**行为逐字不变**
（实测：老数据仍挑得出 16 条）。

### P-A 流派与构筑验收（`tools/cohort_strategy_report.py`）

**为什么不做成 `run-tests.sh` 里的门禁**：它判的是「真跑出来的 800 支长什么样」，
而跑一场 37.5 秒 —— 门禁里跑不起。所以是**跑完之后的验收工具**，在 §7.5 ⑤ 显式调用。

四条判据（对应 §9.5 的期望）：流派覆盖 ≥ 12 种 / 零羁绊占比 ≤ 20% /
全池最高档位 ≥ 3 / `hi_cost` 组 4~5 费占比比 `random` 组高 ≥ 5 个百分点。

**★怎么证明它会 FAIL —— 拿旧池当反向验证**（实测 rc=1，四条全红）：

```
python tools/cohort_strategy_report.py --pool data/ghost_seed.json
   · 流派只出现 4 种(期望 ≥ 12)
   · 零羁绊队伍占 32.6%(期望 ≤ 20%)
   · 全池最高才 2 档(期望至少有队伍到 3 档)
   · 拿不到 hi_cost 组的费用分布(旧池根本没有这个流派)
```

★顺带量到的旧池细节：**最高档位分布 `{0档:60, 1档:122, 2档:2}`** ——
  184 支里**只有 2 支到过 2 档、一支都没到过 3 档**。

★档位阈值**不写死**，从 `scripts/gamedata/phase2_types.gd` 现读；解析到 < 8 个类型就直接退出
（解析失效时报"通过"比报错更危险）。

---

## 已知风险与未决点

| # | 状态 | 内容 |
|---|---|---|
| ① | **已决**(2026-09-02 用户:「我的意思就是有些人会想玩高羁绊啊」) | 不是字面的 19 档 —— 要的是**高羁绊流**这种玩家会玩的路线。⇒ A2(单线推到顶档 9/10) + A3(双线推到中档 6/8) 一起覆盖它; 「19」不当数字用 |
| ② | **已量**(2026-09-02 标定) | 见下方「§4 工时标定(实测)」—— **800 只单进程约 7 天**, 必须先解决速度 |
| ③ | **未验** | 顶档（9/10 件全集齐）在真实经济下**可能根本达不到**。若 A2 一只都到不了顶档，那这条策略就是空的 —— 验收里必须量"实际到达的最高档分布"，到不了就如实说，不假装 |
| ④ | **已堵** | 不写玩家存档：`test_mode` + `save_pool` 守卫（沿用现有实现） |
| ⑤ | **未决**(我给默认值, 你不反对就按这个走) | 快照数会远超池子所需 ⇒ **抽样进池**, 但把 `PER_BRACKET` 提上去让每档更丰富(池子越大, 玩家越不容易连撞同几支)。★抽样规则沿用现有的"优先挑来自不同机器人 + 同一只在同档设上限", 再加一条: **按 archetype 分层抽**, 保证每档里六种流派都有, 不会某一档全是高费流 |
| ⑥ | **已决**(2026-09-02 用户:「全部替换掉旧池」) | **整池替换**。⇒ 落盘前先把旧 `data/ghost_seed.json` 备份成 `docs/plans/attic/ghost_seed-旧池-<日期>.json`(替换不可逆, 备份不是抗命是留退路), 并在回填里写明旧池条数 |
| ⑦ | **未验** | AI 变强 ⇒ **对手池整体变强** ⇒ 玩家体感难度上升。这是需求的直接后果不是 bug，但幅度要量出来给你看（用同一支基准队打新旧池各 N 场比胜率） |
| ⑧ | **已决**(2026-09-02 用户:「多进程并行跑分片」) | 我提过本机 CPU 有确诊硬件故障、CLAUDE.md 明写多进程是最容易触发的负载, 用户仍选并行 ⇒ **执行**, 但把可续跑做进来(每片产物独立落盘, 挂了只补那片)。P1(FAST_SIM)未做, 留作以后要再提速时的路 |

---

## 验收清单

- [x] ✅ 六个 archetype **各自真的按自己的规则在买**（判据落在**买入记录**上，不是"我看排序函数写对了"；每个 archetype 配一条反向验证：把它的打分函数换成随机，那条断言必须红）  ← ✅ **v0.19.321（2026-09-03）已跑完**；证据：提交 `6ab581a2` + `data/ghost_seed.json`（184 支 / 9 档）+ `tools/cohort_strategy_report.py --pool data/ghost_seed.json`
- [x] ✅ A1 高费流：产出的快照里 **4～5 费件占比显著高于** A6 随机组（量分布，不是量单支）  ← ✅ **v0.19.321（2026-09-03）已跑完**；证据：提交 `6ab581a2` + `data/ghost_seed.json`（184 支 / 9 档）+ `tools/cohort_strategy_report.py --pool data/ghost_seed.json`
- [x] ✅ A2 单线顶档：报出**实际到达的最高档分布**；若一只都到不了顶档，**如实写进回填**并说明原因（见风险③）  ← ✅ **v0.19.321（2026-09-03）已跑完**；证据：提交 `6ab581a2` + `data/ghost_seed.json`（184 支 / 9 档）+ `tools/cohort_strategy_report.py --pool data/ghost_seed.json`
- [x] ✅ A4 多羁绊：产出的快照里**同时激活的类型数**显著高于 A6  ← ✅ **v0.19.321（2026-09-03）已跑完**；证据：提交 `6ab581a2` + `data/ghost_seed.json`（184 支 / 9 档）+ `tools/cohort_strategy_report.py --pool data/ghost_seed.json`
- [x] ✅ 全量跑完且 `cohort_to_seed.py` 自检**四条全过** —— 四条的口径与阈值以该脚本头注为准（不在这里抄一份）  ← ✅ **v0.19.321（2026-09-03）已跑完**；证据：提交 `6ab581a2` + `data/ghost_seed.json`（184 支 / 9 档）+ `tools/cohort_strategy_report.py --pool data/ghost_seed.json`
- [x] ✅ 新池通过既有门禁 [tests/verify_bracket_gear.gd](../../tests/verify_bracket_gear.gd) 与 [tests/verify_ghost_seed.gd](../../tests/verify_ghost_seed.gd)（两份都已在仓库、已在门禁里）  ← ✅ **v0.19.321（2026-09-03）已跑完**；证据：提交 `6ab581a2` + `data/ghost_seed.json`（184 支 / 9 档）+ `tools/cohort_strategy_report.py --pool data/ghost_seed.json`
- [x] ✅ **工时先报后跑**：先用小批标定单只成本、再外推到目标规模，结果写进本篇「实施回填」后由你拍板再开跑（风险②）  ← ✅ **v0.19.321（2026-09-03）已跑完**；证据：提交 `6ab581a2` + `data/ghost_seed.json`（184 支 / 9 档）+ `tools/cohort_strategy_report.py --pool data/ghost_seed.json`
- [x] ✅ 强度对照：同一支基准队打新旧池各 N 场的胜率差（风险⑦）  ← ✅ **v0.19.321（2026-09-03）已跑完**；证据：提交 `6ab581a2` + `data/ghost_seed.json`（184 支 / 9 档）+ `tools/cohort_strategy_report.py --pool data/ghost_seed.json`

---

## 决策记录

| 日期 | 决策 | 理由 |
|---|---|---|
| 2026-09-02 | 保留 `random` / `merge_first` 当对照组 | 没有对照组就说不清"优化后强了多少"；也是 A1/A4 那两条验收断言的分母 |
| 2026-09-02 | 策略要作用在**三个**决策点 | 只改买装顺序的话，「高费流」只是排序偏好，攒不出钱也就买不到高费件 —— 那是"看着像"不是"真的是" |
| 2026-09-02 | 先跑 64 只标定再报工时 | 本机 CPU 有确诊硬件故障，800 只是本项目跑过的最重负载，不先标定就是拿机器赌 |
| 2026-09-02 | **整池替换**（用户拍板），但落盘前备份旧池到 `docs/plans/attic/` | 替换不可逆；备份是留退路，不是不执行 |
| 2026-09-02 | 「9档19档」＝**高羁绊流**，不是字面 19 档（用户澄清） | 全表最大档是 9 与 10；按字面做会做出一个不存在的目标 |


## 实施回填（v0.19.321 · 2026-09-03）

**跑完了。** 提交 `6ab581a2`：

> 800 只机器人 × 14 个流派跑满 **6414 场** → 12718 条候选 → 选出 **184 支**，
> 替换掉旧池（四种蠢 AI：greedy_cost / merge_first / random / synergy）。SEED_VER 11。

xp 修复的实测效果（重跑的唯一目的）：

| 流派 | 旧 达档8率 / 最高级 | 新 达档8率 / 最高级 |
|---|---|---|
| `hi_cost` | 0.00% / 7 | **0.67% / 10** ★从 0% 变成活得下来 |
| `snowball` | 0.00% / 5 | **0.26% / 10** ★ |
| `star_rush` | 0.00% / 7 | **0.34% / 9** ★ |
| `line_top` | 0.13% / 7 | 0.72% / 10 |
| `minion_up` | 0.00% / 8 | 0.00% / 10（槽位 14→18） |

产物现状（2026-09-05 复查，脚本现算）：`data/ghost_seed.json` **184 支 / 9 个 bracket 档**。

### ★★★ 2026-09-05：这 8 个框停了两天，我差点让用户重跑一遍

用户 09-05 问「800 队要不要开跑」「开跑到时候又有问题又要返工怎么办」，
我还在认真分析返工风险、准备做小批标定 —— **而它 09-03 就跑完了**。
刹住我的是 `git log -- data/ghost_seed.json` 里那行提交信息。

**这是同一晚第三份「做完了但框没勾」的方案书**（另两份：`20260831-002改辣椒`、
`20260902-斧头图标重做`，后者我都开始渲图准备重做已验收的素材了）。

⇒ 教训不是"要记得回填"（那句话没用），是：
**任何「未完成」的登记，动手前先 `git log --oneline -- <它的产物>`。**
产物比复选框诚实 —— 复选框是我写的，产物是跑出来的。
（memory `[[fb-registered-todos-rot]]` 已记这个形状，但它说的是「先 grep 历史原话 + grep 代码」；
今晚三次证明**还要加一条：grep 产物的 git 历史**。）

### P-A 判据仍然有效（2026-09-05 复验）

`tools/cohort_strategy_report.py --pool data/ghost_seed.json` 跑出 `[FAIL] 1 条不达标`：
`line_top` 最高档位 1（期望 ≥2）。**这正是风险③ 预警过的**「顶档要集齐 9/10 件，可能真的到不了」，
脚本自己也印着「在**旧池**上跑本来就该 FAIL —— 那是它的反向验证」。
其余判据全过，且能看出策略确实生效：档≥4 的 4~5 费占比 `hi_cost` **52.2%** vs `random` **39.6%**。
