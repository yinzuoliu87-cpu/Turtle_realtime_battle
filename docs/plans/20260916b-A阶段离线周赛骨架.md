# A 阶段 —— 离线周赛骨架

日期：2026-09-16　|　状态：**已拍板**

> 母方案书 [20260916-大轮赛制v2周赛制.md](20260916-大轮赛制v2周赛制.md)（22 个未决点已全部拍板）§4.1 的展开。
> 母方案书写的是「改哪、门禁怎么写」；**本篇的增量是穷举** —— 历史教训是「方案写改 6 处，穷举后是 20 处」。
> 按 U8，本篇与 [B 阶段](20260916c-B阶段确定性加固.md) **并行**推进。**一行代码还没动。**

---

## 1. 需求原文

母方案书 §7 的拍板，与 A 阶段直接相关的部分：

- 赛程：周一休赛 / **周二~周五积分赛（8 命 + 24 场配额）** / 周六闯关赛 / 周日决赛日。
- 匹配硬条件 = **双方总场次相同**（不再按 9 档分档）；积分赛快照**不加时间窗**，按上传时刻倒序**优先选最新的**。
- 补发**不含**砍伐经验与糖果罐（只补币与经验）—— 用户：「不要补发砍伐经验和糖果罐」。
- 0 命 = 保持「淘汰锁定」，提示改成「等周六观赛」，**不再有表演赛**。
- 时区默认英国、**可配置**。
- 头衔四档：冠军 / 四强 / 进决赛日 / **积分赛满配额**（只有最后一档落在 A 阶段）。

A 阶段**不碰**：后端、周六周日的同步赛事、观赛、重放、训龟大师改造（那些在 D~F 与 §4.4）。

---

## 2. 调查

### 2.1 行号锚点复核（2026-09-16 逐条 `sed -n` 看过，全部对得上）

| 锚点 | 实际内容 |
|---|---|
| `phase2_config.gd:14` | `const SEASON_DURATION_SEC := 432000` |
| `GameState.gd:1081` | 保存区 `"season_start_ts": season_start_ts,` |
| `GameState.gd:1149` | 载入区 `season_id = int(data.get("season_id", 1))` |
| `GameState.gd:1300` | `reset_save` 区 `season_id = 1` |
| `GameState.gd:1641` | `func start_new_season() -> void:` |
| `RealtimeBattle3DScene.gd:7531` | `func _settle_season(won: bool) -> void:` |
| `RealtimeBattle3DScene.gd:7551` | `gs.season_total_battles += 1` |
| `RealtimeBattle3DScene.gd:7553` | `gs.axe_on_match_end()`（096 砍伐经验） |
| `RealtimeBattle3DScene.gd:7581` | `if not gs.match_history.is_empty():` |
| `MainMenuScene.gd:212` | `var eliminated := GameState.is_eliminated()` |
| `MainMenuScene.gd:235` | `var shop_locked := int(GameState.season_total_battles) <= 0 or eliminated` |
| `backend.gd:123` | `static func pool_find(pool, bracket, exclude_ids, rng):` |
| `backend.gd:207` | 排行榜区（`信任本地 season_eggs_killed, 不复算`） |
| `verify_mainmenu_layout.gd:209` | `★分母: 应为 4 行`（赛季面板行数） |

### 2.2 ★母方案书漏掉的 9 个门禁（本篇的主要产出）

母方案书附录 A 点名了 19 个「会跟着红」的门禁。按赛季 API 关键词
（`season_total_battles` / `is_eliminated` / `season_eggs_killed` / `pool_find` / `make_bot` /
`bracket` / `start_new_season` / `record_match`）重新 grep，**实际有 24 个 `tests/verify_*.gd` 引用它们**，
其中 **9 个母方案书一个字都没提**：

| 门禁 | 引用什么 | A 阶段会不会红 |
|---|---|---|
| `verify_self_match:55` | `Backend.pool_find(pool, 3, [], rng)` | **会** —— A6 要加「场次完全相同」过滤 |
| `verify_upload_flash:149-154` | 构造带 `bracket` / `season_total_battles` 的快照 | **会** —— A8 要往快照补三个排序字段 |
| `verify_battle_ui:218` | `gs2.hearts = 0` → `is_eliminated()` | **可能** —— A4 把淘汰拆成三种原因 |
| `verify_shop_layout:57` | `gs.season_total_battles = 3`（让商店开门） | **要复核** —— A4 若给商店锁加配额条件 |
| `verify_shop_merge_pips:36` | 同上 | 同上 |
| `verify_shop_persist:37,45` | 备份/还原 `season_total_battles` | 同上 |
| `verify_ui_consistency:550-551` | 同上 | 同上 |
| `verify_ui_layout:77-79` | 同上（注释明写商店在 `<=0` 时走上锁屏） | 同上 |
| `verify_incense_stone_real:314` | 数 `start_new_season` 里的重置处数 | **不会** —— 它数的是 `incense_charge = 0` 这个**具体字符串**，A2 加别的字段不影响。★我一开始判成"会精确爆掉"，核了原文才发现判错，记在这里免得下次再判一遍 |

### 2.3 `pool_find` / `pool_add` 的全部调用点（A6 要改签名，先数清）

`pool_find` 共 **10 处**：产品 1 处（`backend.gd:376`）、门禁 5 处
（`verify_ghost_seed:132`、`verify_remote_pool:119/134/142`、`verify_self_match:55`）、
探针 4 处（`_autoplay:146`、`_probe_fastsim:89`、`_probe_navleak:61`、`_probe_trainer_ai:42`）。

⇒ **新参数必须带默认值**（`exact_battles: int = -1` 表示不过滤），否则这 9 处调用一起炸。
⚠ 但带了默认值就意味着**老门禁全绿却一条新行为都没验到** —— 所以 A6 必须自带新门禁，
不能靠"现有门禁没红"当作没问题（memory `fb-gate-subject-never-constructed` 同族）。

`pool_add` 共 10 处：产品 4 处（`shot_diff.gd:63`、`backend.gd:309/431`、`remote_pool.gd:146`）、
门禁 6 处。A 阶段不改它的签名。

### 2.4 存档字段的「五处」长什么样

以 `season_total_battles` 为样板，新字段要照抄同一条线：

| # | 位置 | 写法 |
|---|---|---|
| ① 声明 | `GameState.gd:583` 那一批 | `var season_total_battles: int = 0    # 注释` |
| ② 保存 | `:1083` 附近 | `"season_total_battles": season_total_battles,` |
| ③ 载入 | `:1149` 那一批 | `season_total_battles = int(data.get("season_total_battles", 0))` |
| ④ `reset_save` | `:1300` 那一批 | `season_total_battles = 0` |
| ⑤ `start_new_season` | `:1641` 起 | `season_total_battles = 0` |

**漏任何一处都不会报错，只会在切轮或重启后悄悄漂。**

---

## 3. 与现状的出入

1. **配额这个概念现在完全不存在**。现在只有 `season_total_battles`（只增不减、跨大轮由 `start_new_season` 归零），
   没有「本周期可打几场」。A2 要新增的字段是真·新增，不是改语义。
2. **「按已打场数分 9 档」与「双方总场次必须相同」不是一回事**。现在 `pool_add` 按 9 档装桶，
   桶内随机取。A6 是在桶内再加一层精确过滤 —— **桶结构不用动、旧池不用迁移**（快照里本来就带原始场数，`backend.gd:482`）。
3. **排行榜现在按「碎蛋数」排**（`backend.gd:207` 的注释明写"信任本地 `season_eggs_killed`，不复算"），
   A8 要换成「胜场 → 余命 → 横扫」，而**快照里现在没有"横扫数"这个字段**，要新加。
4. **横扫（2-0）现在没人记**。判定所需的东西都在：`GameState.lane_results` 在结算这一刻还活着
   （写入点 `dual_lane_flow.gd:750`），`phase2_duallane.gd:39-47` 有「要不要打终极」的判定函数。
   但没有任何地方把"这局是不是 2-0"存下来。
5. **赛季面板现在恰好四行**，而且这个数字被门禁焊死（`verify_mainmenu_layout.gd:209`）。
   A5 加「剩余场次」那一行 ⇒ **这是要改的账**（把分母改成五并写清为什么），不是放松判据。
6. **时区现在没有任何概念**。`SEASON_DURATION_SEC = 432000`（5 天）是"每台设备各自滚动"，
   不是自然周、更没有收盘时刻。A1 要把赛程从"时长"改成"带时区的周历"，这是**语义变更**不是加常量。

---

## 4. 方案

### A1　常量落位　`scripts/gamedata/phase2_config.gd`

在「V2 赛季」区（`:14` 那一段）后面加一组：积分赛配额、补发地板（币 / 经验）、
晋级线、闯关线、赛程各阶段起止、**时区基准**、**收盘前封盘时长**。全部集中在这一个文件。

⚠ **2026-09-17 拍板改了两条**（母方案书第五轮）：
- **E2：时区写死 UTC，不跟英国夏令时** —— 收盘时刻全球固定。但 **UI 上的倒计时要按玩家本地时区显示**（E4 顺带解决）。
  ⇒ 常量存的是 UTC 时刻，**换算只发生在显示层**，别在赛程逻辑里做时区换算。
- **E3：收盘前 10 分钟不让开新局，闸设在点「开打」那一刻**（不是点匹配）——
  摆位不限时，闸设在「匹配」就盖不住。依据：纯战斗最长 225 秒 + 固定呈现 35 秒 ≈ 4.3 分钟（9457 场实测）。

⚠ 门禁**不要**断言「常量等于某个数」—— 那是把参数抄两份。常量的正确性由 A3~A7 的行为门禁间接守。

### A2　存档字段　`autoload/GameState.gd`

新增：积分赛已用场次 / 横扫数 / 补发已发场次（幂等用）/ 赛程阶段 / 自然周锚点 /
闯关战绩（胜、负）/ 是否已晋级。**每个都走 §2.4 的五处。**

门禁 `verify_week_season.gd`（A 阶段新建，现在还不存在）：
① 存档往返保值 ② `start_new_season()` 后全部归零。
反向验证：注释掉载入那一行 ⇒ ① 红；注释掉 `start_new_season` 那一行 ⇒ ② 红。

### A3　结算接线　`RealtimeBattle3DScene.gd:7531 _settle_season`

- `:7551` 旁边记积分赛配额消耗；**闯关赛 / 决赛日的场次不吃积分赛配额**，按赛程阶段分流。
- 横扫：在 `:7556` 附近判「两路都打完 + 不需要终极」⇒ 横扫数 +1，并写进 `record_match`（`:7581`）。
- 补发要用到的按场数成长在同一段：096 砍伐经验 `:7553`、糖果罐 `:7554`（**按拍板不补**，见 A7）。

门禁③直接调 `_settle_season(true/false)`（照抄 `verify_combat_sanity.gd:61-65` 的调法），量配额计数与横扫计数。
反向验证：把横扫条件改成恒真 ⇒ 打到终极的那局也被记成横扫 ⇒ ③ 红。
⚠ 门禁里的场数、命数**一律写死**，不要用常量算期望值。

### A4　开局拦截　`scripts/scenes/MainMenuScene.gd`

现在只有「0 命淘汰」一个闸（`:212` / `:235` / `:716-718`）。拆成三种原因各自一条提示：
命尽（文案改成「等周六观赛」）/ 配额已打满 / 当前不是能打的阶段。

门禁④照抄 `verify_season_elim.gd:24-45`（实例化主菜单 → 数 toast → 断言 `dual_active` 没被置 true）。
反向验证：去掉配额那一条 ⇒ 配额打满还能开打 ⇒ ④ 红。

⚠ **商店锁（`:235`）要不要也吃配额，必须显式决定**。现在它的条件是
`season_total_battles <= 0 or eliminated`，而 §2.2 有 5 个门禁靠"设 `season_total_battles = 3` 让商店开门"过日子。
改这一行就要同时复核那 5 个。

### A5　读数（界面）

主菜单 `:524-529` 赛季面板加「剩余场次」；结算屏 `battle_hud.gd:1395-1412` 数据块加同一读数；
副标题在配额打满 / 出局时各说各的。
⚠ `verify_mainmenu_layout.gd:209-210` 断言恰好四行 —— **改成五行并写清为什么**。

### A6　匹配　`scripts/net/backend.gd`

⚠ **2026-09-17 U11 拍板后，这一步的前提变了** —— 详见母方案书 §10.35 C7：
`player_ghost_id()`（`backend.gd:397-400`）**不含场次**，而 `pool_add`（`:69-73`）按 ghost_id 去重 ⇒
**同一玩家在池里只有最新一份快照**，场次比你低的人永远匹配不到你。
⇒ A6 要多做三件事：
1. **`ghost_id` 加上场次这一维**（`player_ghost_id` 改签名，调用点 `RealtimeBattle3DScene.gd:7562` 同步）；
2. **重定 `BUCKET_CAP`**（现在 50 是按「一人一份」设计的，加了场次维度后一个人最多占 24 格）；
3. **改掉 `backend.gd:69` 那条去重注释**（「同一玩家阵容跨场重传」在按档匹配下是对的，在新规则下是错的）——
   不改它，下一个人会照着注释把去重加回来。

`pool_find` 加参数（**必须带默认值**，见 §2.3），在桶内按「场数完全相同」过滤；
按 D10，同场次的多份快照**按上传时刻倒序取最新的**（新鲜度是排序不是过滤）。
回落顺序：精确同场次 → 同桶其它人 → 机器人，**每一场都记账**（这一场是精确 / 同桶 / 机器人），
让「有多少场是真同场次」变成可量的数。

门禁⑤：池里放场数不同的三份快照，我打过 N 场 ⇒ 必须选中场数 N 的那份；
再放两份同为 N 场但上传时刻不同的 ⇒ 必须选中较新的那份。
反向验证：去掉精确过滤 ⇒ 选中别人 ⇒ ⑤ 红；去掉倒序 ⇒ 选中旧的 ⇒ ⑤ 红。

### A7　配额补发　新函数放 `GameState`

触发：积分赛阶段结束且该玩家晋级；补发场数 = 配额 − 实打场数；每场补币与经验（地板值见 A1）。
**按 U6 拍板：不补 096 砍伐经验、不补糖果罐。**
幂等靠「补发已发场次」字段：只补差额，重复调用不再给。

门禁⑥：合成一份存档（打了 K 场、晋级），补发后币 / 经验等于「打满配额的最低供给」——**量真实字段，不套公式**。
门禁⑦：连调两次，第二次一分不加。反向验证：去掉幂等字段 ⇒ ⑦ 红。
门禁⑧：补发后 096 砍伐经验与糖果罐进度**没变**（把「不补」这条拍板焊死，否则将来顺手补上没人拦）。

### A8　终榜　`backend.gd:207-217` + `LeaderboardScene.gd:37/58/80`

排序键换成字典序「胜场 → 余命 → 横扫」；快照补这三个数（`:474-483`），旧快照缺字段当 0（不作废旧池）。
标题与表头文案一起改（现在写「本赛季击杀龟蛋数」）。

门禁⑨：构造同胜同命、横扫不同的三份快照，断言顺序。反向验证：比较器改回只比碎蛋数 ⇒ ⑨ 红。

### A9　收口

`bash run-tests.sh`（只认 `GATE_RC=` 与 `ALL PASS`，**与 commit 分两次调用**）→
把 §2.2 的 24 个旧门禁逐个改账 → 版本号五处 + tag + CHANGELOG + 路线图 → 本篇回填。
⚠ 门禁跑的时候不要改仓库。

---

## 5. 已知风险与未决点

| # | 事项 | 状态 |
|---|---|---|
| **A-R1** | 商店锁（`MainMenuScene.gd:235`）要不要吃配额还没定。改了会波及 §2.2 的 5 个 shop/UI 门禁 | ❓ 实现 A4 时必须显式决定并写进回填 |
| **A-R2** | 赛程从「每台设备滚动 5 天」改成「带时区的自然周」是**语义变更**，老存档的 `season_start_ts` 怎么迁移没定 | ❌ A1/A2 要处理 |
| **A-R3** | 「精确同场次」在离线小池子里可能极少命中 ⇒ 大部分场次回落到机器人。A6 的记账就是为了把这件事变成可量的数，但**多低算太低没有阈值** | ❓ 先量再定 |
| **A-R4** | 补发不含砍伐经验与糖果罐（U6 拍板）⇒ 这两样的持有者被补发后仍然吃亏。这是**有意的**，门禁⑧把它焊死 | ✅ 已拍板，非风险 |
| **A-R5** | 本机 CPU 有确诊硬件故障，反复跑门禁是高危负载 | ⏳ 小批次 + 批间留间隔 |

---

## 6. 验收清单

> ★★**2026-09-18 回填**（用户：「你这方案书有问题吧，很多都做过了你标记未做？」）：
> 下面 A1~A5 **早在 2026-09-17 就随 `34233f88` / `fd91eca4` / `4017a808` 三个提交落地了**，
> 账一直没回填，于是方案书连着一天多都在说"全部没做"。**这不是个别遗漏，是系统性的**——
> `plans_lint` 第 ④ 条只检查「未勾项有没有带分类标记」，**不检查那个标记是不是真的**，
> 所以一条 ❌ 可以在事情做完之后继续挂着而门禁永远绿。已登记成 A-R4 去补这个缺口。

- [ ] ⏳ A1 常量落位（含时区可配置）—— **代码已落地**：`scripts/gamedata/phase2_config.gd:32`
      `WEEK_CLOSE_HOUR_UTC`、`:58` `BUCKET_CAP_PLAYERS`。
      ★**故意不打勾**：全仓**没有任何一条门禁直接断言这两个常量**（`verify_season_elim` /
      `verify_week_season` 只是间接消费它们的行为）。按本项目规矩「给不出门禁就写成未勾」，
      所以留 ⏳ 而不是伪装成已验收。
- [x] A2 八个新字段各走五处 —— `autoload/GameState.gd:620-627` 声明八个；
      门禁 `tests/verify_week_season.gd`（**39 条**）守①存档往返②切轮归零，实测 ALL PASS
      （★原文写"七个"，实际是八个：`ranked_used / season_sweeps / backfill_paid / week_phase /
      week_anchor_ts / gauntlet_wins / gauntlet_losses / promoted`）
- [x] A3 配额计数与横扫计数 —— 由 `tests/verify_week_season.gd` **④⑤** 证明：
      「④积分赛打一场→配额+1 / 再一场→累计2 / ★闯关赛不动 / ★决赛日不动」
      「⑤★分母:这是真 2-0 / 2-0赢→横扫+1 / ★打到终极战场才赢→不算横扫」。
      产品侧 `RealtimeBattle3DScene.gd:7488` `gs.ranked_used += 1`、`:7500` `gs.season_sweeps += 1`
- [x] A4 三种拦截原因各一条提示 —— **三种都做齐了**（第三种于 2026-09-19 补上）。
      ① 淘汰 ② 配额打满 —— `MainMenuScene.gd` 的 `_start_battle_flow()`，由
      `tests/verify_season_elim.gd` 的 5 条 A4 断言守着。
      ③ **封盘** —— 接在 `TeamSelectScene._on_start()`，由 `tests/verify_close_lockout.gd`（8 条）守着。
      ★**闸设在 TeamSelect 不在主菜单**：`can_start_match_utc` 自带警告「调用点必须是点「开打」
      那一刻，不是点匹配 —— 摆位不限时，设在匹配就盖不住」。流程是 主菜单→摆位→`_on_start()`→匹配→战斗。
      ★★**这条门禁第一版是假的**：判据写的是「`get_tree().current_scene` 有没有变」，
      三条变异**一条都没红** —— 拆掉闸后 `_on_start()` 立刻换场景 ⇒ 树被掀 ⇒ 下一行 `await` 崩 ⇒
      **测试函数中途中止、剩余断言不跑**，于是一条 FAIL 都没打出来。
      改成量 `GameState.left_team`（产品在换场景**之前**会写它）后，三条变异才各自红。
      ★顺带：我的变异脚本也判错过 —— 只数 `[FAIL]` 行而日志里有 `SCRIPT ERROR`；
      现在改成同时看 rc + 致命 + FAIL 条数 + 有没有打 ALL PASS 四样。
- [x] A5 主菜单**与结算屏**读数 —— **两半都做齐了**（结算屏那半于 2026-09-19 补上）。
      ① 主菜单：`MainMenuScene.gd` 状态行，由 `verify_mainmenu_layout.gd` ⑦ 守着。
      ② 结算屏：`battle_hud.gd` 的 `_build_reward_chips` 加「本周场次 N / 配额」，
      由 `tests/verify_settle_quota.gd`（7 条）守着 —— 判据落在**产品渲染出来的文本**上，
      且带两条挡假的：改 `ranked_used` 读数要跟着变（挡写死）、闯关赛阶段这块必须消失
      （挡「无条件永远显示」；那时的场次不吃这个配额）。
      ★**这半原本被我用回填动作从账上抹掉过**：方案书原文写的是「主菜单与结算屏」，
      我 2026-09-18 回填时把「与结算屏」半句一起改没了 —— 比不回填更危险。
- [ ] ❌ A6 精确同场次 + 倒序取最新 —— ⑤两条反向验证（去过滤 / 去倒序）各自必红
      ★**2026-09-18 查实：真没做。** 全仓 grep `find_opponent` 附近的 round/倒序过滤为空。
      ★done-when: grep:scripts/net:exact_battles
      ★**`BUCKET_CAP` 必须和这条一起改，不许提前**（2026-09-19）：
      现值与档位宽度都**现算**，本文不抄数 —— `grep -n "BUCKET_CAP" scripts/net/backend.gd`、
      档位表见同文件的「进度档」一节。
      道理：现值是按「一个玩家在一档里只占一格」设计的；加了场次维之后同一个玩家
      **每个场次各占一格**，池子会按最宽档的场次数成倍变瘦。
      但**在 `ghost_id` 真的带上场次之前**把它调大，只是把桶撑大而无收益，
      而且注释会描述一个**不存在的状态** —— 2026-09-19 有过一次这样的提前改动，已撤回。
      （`pool_find` 加上按场次精确匹配的参数即说明落地）
- [ ] ❌ A7 补发与幂等 —— ⑥量真实字段、⑦连调两次、⑧不补成长项
      ★**2026-09-18 查实：字段在、逻辑没有 ⇒ 判「没做」。**
      `backfill_paid` 在产品代码里的出现处**全部**是声明/存档/载档/两处重置，
      **没有任何业务代码写它或读它** —— 即本项目记过的「写了没人读」。
      现算口径：`grep -rn "backfill_paid" --include=*.gd scripts/ autoload/`
      ★done-when: grep:autoload:func [a-z_]*backfill
      （现在没有任何 backfill 函数；出现即说明补发逻辑落地了）
- [ ] ❌ A8 终榜排序 —— ⑨三份快照断言顺序，反向验证比较器回退必红
      ★**2026-09-18 查实：计数在累加、比较器没有 ⇒ 判「没做」。**
      `season_sweeps` 有 A3 在写（见上），但产品代码里**没有任何排序/比较代码读它**。
      现算口径：`grep -rn "season_sweeps" --include=*.gd scripts/ autoload/`
      ★done-when: grep:scripts/net:season_sweeps
      （终榜排序在 backend.gd；它读到 season_sweeps 即说明比较器落地了）
- [ ] ❌ 受影响的旧门禁逐个改账（**不是逐个加白名单**）。名单**不抄死数字**，按 §2.2 的口径现算：
      `grep -rl "season_total_battles|is_eliminated|season_eggs_killed|pool_find|make_bot|bracket|start_new_season|record_match" tests/verify_*.gd`
- [ ] ❌ `bash run-tests.sh` 全绿（只认 `GATE_RC=`）
- [ ] 🔲 A-R1 商店锁要不要吃配额
- [ ] ❓ A-R3 「精确同场次命中率多低算太低」的阈值
- [ ] ❌ **A-R4（2026-09-18 新增·用户点出来的）**：`plans_lint` 抓不到「标记过期」。
      第 ④ 条只验「未勾项带没带分类标记」，**不验标记是否属实** ⇒ ❌ 可以在事情做完后
      继续挂着而门禁全绿（本文件 A1~A5 就这么挂了一天多）。
      ★文件头记着自动查证**试过并失败**（「按关键词去 tests/ 找证据…实测不可靠，
      把有覆盖的报成没有，拿它当门禁只会天天误报然后被白名单掏空」）。
      ⇒ 换形状而不是放松：**标 ❌ 的项必须点名一个「它做完了就会存在」的东西
      （文件/符号/门禁名）；那个东西一旦真的存在，这条 ❌ 就当场红**。
      这是把不可靠的"证明做完了"反过来做成可靠的"证明标记过期了"。

---

## 7. 决策记录

| 日期 | 决策 | 依据 |
|---|---|---|
| 2026-09-16 | 按 U8 把母方案书 §4.1 拆成本篇，与 B 阶段并行 | 用户选项回答：「A 与 B 并行」 |
| 2026-09-16 | 匹配硬条件 = 双方总场次相同；快照不加时间窗、按上传时刻倒序 | 母方案书 D5 / D10 |
| 2026-09-16 | 补发不含砍伐经验与糖果罐 | 用户：「不要补发砍伐经验和糖果罐」（U6） |
| 2026-09-16 | 0 命保持锁死，提示改「等周六观赛」，不做表演赛 | 用户：「0 命的话就只能等到周 6 周日观赛了，不再打表演赛」（U9） |
| 2026-09-16 | `pool_find` 新参数带默认值 | 本篇 §2.3 实测 10 个调用点；不带默认值会一起炸 |
| 2026-09-16 | 母方案书漏掉的 9 个门禁补进改动面 | 本篇 §2.2；口径 = 按赛季 API 关键词 grep `tests/verify_*.gd` |
