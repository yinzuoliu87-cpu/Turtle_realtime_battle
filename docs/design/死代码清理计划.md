# 实时版 死回合制代码 渐进清理计划

> 背景：本仓库从回合制版 fork。战斗已全部重写为实时(RealtimeBattle)。原回合制战斗+双路推塔流程整套成了**死代码**，但与活跃路径有交织，**不能一把删**。本文档=渐进、可验证、可回滚的清理路线。
> 立项 2026-06-27。**铁律：每步 删→验→提交，一步一 commit；验不过立即回滚该步**（说明撞到了隐藏依赖）。

## 一、依赖分析（活 vs 死）

### ✅ 活跃路径（绝不动）
- **Autoload**（project.godot）：PersistentBg / DataRegistry / GameState / Audio / PreloadCache / ShotDiff / CursorTheme
- **主场景**：MainMenu.tscn → ⚔实时战斗(RealtimeBattle) / 商店(Shop) / 背包(Inventory) / 图鉴(Codex) / 设置(Settings) / 排行(Leaderboard)
- **RealtimeBattle 依赖**：`engine/backend.gd`(preload, 本地优先抽象) · GameState 赛季接口(season_leaders/season_wins/add_season_xp/ensure_season/build_ghost_snapshot/upload_ghost) · `engine/phase2_equip_runtime.gd`(STATS 逐星属性, 实时装备复用) · DataRegistry
- **图鉴/羁绊显示**：phase2_config / phase2_types / phase2_schools（羁绊用户暂缓，但 CodexScene 显示用，**留**）

### ☠️ 死集群（清理目标，互相引用、不被活跃路径触达）
- **场景**：Battle.tscn · DualLaneMap.tscn · BattleEnd.tscn · Matchmaking.tscn · TeamSelect.tscn · Record.tscn(待核)
- **脚本**：BattleScene.gd(**1.7万行**回合制战斗) · DualLaneMapScene.gd · BattleEndScene.gd · MatchmakingScene.gd · TeamSelectScene.gd · RecordScene.gd
- **回合制引擎**（仅被 BattleScene 用）：rules.gd · turn.gd(待核) · events.gd · buffs.gd · damage.gd · skill_handlers.gd · stats_recalc.gd · synergies.gd · equipment_runtime.gd(旧e_*) · battle_stats.gd · achievement_tracker.gd · skill_text.gd
- **dot.gd**：现是实时层数DoT的**移植参照**（已照它 1:1 实装）。BattleScene 删后若无他引用→可删，但**建议保留或挪 docs/ 当参照**。
- **旧数据**：data/equipment.json(旧e_*~40件) · data/battle-rules.json — 实时版用 phase2-equipment.json，旧的若无引用可删。

### ⚠️ 残留引用（清理前先处理）
- `MainMenuScene.gd:664` `_on_test()`（debug"测试模式"按钮）→ 仍指 Matchmaking.tscn。**Phase1 改道/删**。

### ⚠️ 关键提醒：单测大半是回合制引擎的
`run-tests.sh` 的 1966 测试**大半在测回合制引擎**(BattleScene/dot/buffs/damage…)。删引擎时**对应测试一并删**，测试数会降。**删引擎后的验证基准 = 冒烟(MainMenu+RealtimeBattle 载入无报错) + 实时专项测(verify_dot_stacks 等)**，不再是"1966过"。

## 二、渐进清理阶段（每阶段一/多 commit，删→验→提交）

**验证套餐**（每步跑）：
1. `<godot> --headless --path . res://scenes/MainMenu.tscn --quit-after 200` → 无 SCRIPT/Parse ERROR
2. `<godot> --headless --path . res://scenes/RealtimeBattle.tscn --quit-after 300` → 无报错
3. 删测试前：`bash run-tests.sh` 对照基线；删引擎+测试后：剩余测试全过 + 冒烟过

### Phase 1 — UI 残留入口（最安全，先做）
- `_on_test()` debug 入口改道 RealtimeBattle 或删测试模式按钮。
- grep 全仓确认 5 个死场景无任何活跃路径引用（除死集群内部互引）。
- 验 → 提交。

### Phase 2 — 删死场景文件
- 删 Battle/DualLaneMap/BattleEnd/Matchmaking/TeamSelect(/Record) 的 `.tscn` + `.gd`。
- **逐个删 + 每删一个跑冒烟**（MainMenu/RealtimeBattle 载入），撞引用立即回滚那个。
- 提交（可分 2~3 commit）。

### Phase 3 — 删回合制引擎 + 其测试
- 按依赖叶子序删：先 skill_text/achievement_tracker/battle_stats，再 skill_handlers/synergies/equipment_runtime，再 rules/events/buffs/damage/stats_recalc。
- **每删一引擎文件，grep 全仓确认无活跃引用**（尤其 RealtimeBattle/GameState/phase2_*），无→删它+删它的测试文件→冒烟+剩余测试过→提交。
- dot.gd：确认仅 BattleScene 引用后，挪 `docs/reference/` 留作 DoT 参照（不直接删，它是层数模型的权威出处）。

### Phase 4 — 修剪 GameState 死双路逻辑
- GameState 是 autoload（活），**只删确认无活跃调用的双路函数**：grant_dual_round / dual_shop_* / 双路经济结算 / dual_passive_xp 等。
- 每删一组 grep 确认无活跃调用 + 冒烟 + 提交。保留赛季/币/配队接口（RealtimeBattle 在用）。

### Phase 5 — 旧数据文件
- grep 确认 data/equipment.json(旧e_*) / battle-rules.json 无活跃引用（DataRegistry 是否加载？若加载则先断）→ 删 → 冒烟 + 提交。
- status/synergies/shop-buffs/achievements **保留**（CodexScene/羁绊显示用）。

## 三、不做 / 暂缓
- 不动任何活跃路径文件的逻辑（只删整文件，不重构活代码）。
- 羁绊相关 phase2_* 保留（用户暂缓羁绊，CodexScene 显示在用）。
- 每阶段做完向用户报告，不一次性删完（"慢慢的"）。

## 四、进度勾选 + 实际执行结果（2026-06-27）
- [x] **Phase 1** UI 残留入口 — `f104771`（debug测试模式改道）
- [x] **Phase 2 Chunk A** 死 UI 场景 — `d2b3ed2`（删 Matchmaking/TeamSelect/DualLaneMap/BattleEnd .tscn+.gd+.uid 共12文件）
- [x] **Phase 2 Chunk B** 回合制战斗 — `4adddf5`（删 Battle.tscn + BattleScene.gd **17160行** + .uid）
- [x] **Phase 3 Chunk C** 回合制引擎+测试 — `ab29d8c`（删 rules/events/synergies/skill_handlers/equipment_runtime/battle_stats/fighter.gd 共7文件 + test_damage.*; run-tests.sh 改跑 verify_dot_stacks）
- [ ] **Phase 4** GameState 双路修剪 — 留主 Claude 手工（风险高）
- [~] **Phase 5 旧数据 — 取消（计划判断错，见下纠错）**

### ⚠ 依赖纠错（grep 删前发现活跃引用 → 保留，计划文档原判断有误）
- **`data/equipment.json` 不是纯死数据**：DataRegistry:50 加载 + CodexScene 消费品Tab(8件consumable) + shop_data + preload_cache 都读 → **保留**。
- **`data/battle-rules.json` 活跃**：DataRegistry:63 + CodexScene 规则Tab + GameState:752 → **保留**。
- **`dot.gd` 是活跃共享**（不是只当参照）：`phase2_equip_runtime.gd` preload 它（RealtimeBattle 在用）→ **留在 engine/，不移 docs**（移动会断实时战斗 — DoT 层数实装直接复用了它）。
- **`damage.gd / buffs.gd / stats_recalc.gd / slot_helpers.gd`**：被 phase2_equip_runtime / phase2_schools preload（实时运行时复用）→ **保留**。
- **`achievement_tracker.gd / shop_data.gd / skill_text.gd`**：CodexScene 在用 → **保留**。
- **`Record.tscn/RecordScene.gd`**：MainMenu"战绩"磁贴可达 → **保留**（活跃）。
- **教训**：fork 来的"引擎"很多被局外UI(CodexScene)+实时装备runtime(phase2_*)复用，不是纯回合制专属。**删前必 grep，别信文档分类**。零回滚=grep 到位。

### 复验
删完独立冒烟：MainMenu/RealtimeBattle/Codex/Shop/Inventory/Leaderboard/Settings/Record/Achievements 全 ✓；verify_dot_stacks 全过；已全部 push origin/main。
