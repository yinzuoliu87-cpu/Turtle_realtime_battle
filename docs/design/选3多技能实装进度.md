# 选3 多技能 实装进度

> 用户 2026-06-28 拍板: **保留「每龟选3技能」, 让3技在实时战斗真生效**(之前每龟只放1个固定签名招, 选的技能不生效=回合制残留)。
> 本文档 = 多技能框架 + 逐技能覆盖账本 + 待办。随实装更新。

## 一、框架 (RealtimeBattle3DScene.gd)
- **轮转模型**: 每龟读自己 loadout(选3) → `active_skills` 轮转表(`_resolve_active_skills`): `physical`/`magic` 是普攻(自动攻击)排除, 其余按选中顺序进表。
- **触发**: 沿用龟能(energy)系统 — 龟能满 → `_cast_active` 从 `skill_idx` 起找**下一个实装了的**技放(`_cast_skill`), 轮转推进。未实装的技**跳过**(不空放/不浪费龟能), 不放假招(无 placeholder)。
- **分派**: `_cast_skill` 用 `_IMPL_SKILLS` 集判实装 → juice(预备/震屏)+VFX → `_do_skill` 按**技能 type**(非龟 id)分派。
- **数据驱动伤害**: `_sk_dmg(u, tgt, opts)` 系数取自 `pets.json` 各技 `detail` 公式 `{N/M/T:...}`(N=物理 M=魔法 T=真实, ×caster ATK; hp/mr=×caster maxHp/MR 附加; hits=视觉段数封顶8防刷屏, 总伤不变; aoe=全体; rider=burn/stun/slow/curse/atkdn/mrdn)。
- **左右队 loadout**: 左队(玩家)读 `GameState.loadouts`; 右队(ghost/bot)用 `defaultSkills`。

## 二、覆盖账本 (58 type 实装 / 默认 loadout 全覆盖)
**签名招 (25, 既有 _sk_* 按 type 接入)**: turtleShieldBash, bambooHeal, angelBless, iceFrost, ninjaImpact, ghostStorm, diamondFortify, diceAllIn, gamblerBet, hunterStealth, pirateCannonBarrage, bubbleShield, lineLink, lightningSurgeBuff, phoenixShield, twoHeadFear, fortuneDice, crystalBarrier, chestCount, starMeteor, twoHeadSwitch, lavaSurge, cyberBeam, hidingDefend, shellAbsorb。
**通用 (2)**: shield(stone/rainbow/candy→`_sk_gen_shield` 全队盾), heal(stone/pirate→`_sk_gen_heal` 奶最低血)。
**数据驱动伤害 (31)**: basicBarrage, bambooLeaf, bambooSmack, angelEquality, iceSpike, ninjaShuriken, ninjaBomb, twoHeadMagicWave, ghostTouch, ghostPhantom, diamondCollide, fortuneStrike, diceAttack, rainbowStorm, gamblerCards, gamblerDraw, hunterShot, hunterBarrage, candyBarrage, lineSketch, lightningStrike, lightningBarrage, phoenixBurn, phoenixScald, lavaBolt, lavaQuake, crystalSpike, crystalBurst, chestStorm, starBeam, soulReap, shellStrike。

→ **28 龟的默认 loadout(选3)的非普攻技 全部实装**, 每龟战斗中放 2-3 个真技能(轮转)。

## 三、Batch 2 待办 (未实装 → 轮转跳过, honest 非假招)
**非伤害特殊技 (~10, 需 bespoke)**: iceBurnImmune(灼烧免疫buff)、diceFate(随机增益)、bubbleBind(束缚控制)、lineFinish(斩杀/处决)、cyberDeploy(召唤无人机)、hidingCommand(随从追击令)、shellCopy(复制属性)、starWormhole(虫洞拉拽)、chestSmash(无公式)、fortuneAllIn(梭哈, cd999)。
**自选冷门技**: 玩家若选 defaultSkills 之外的池技(索引3/4, 如 ninjaBackstab/ninjaFeet) → 暂未实装, 轮转跳过(多数玩家用默认或在显示的池内选)。
**打磨**: 逐技 type→icon 真贴图 VFX(现暂用龟签名 VFX); 多段 barrage 飘字防重叠(现封顶8段, 仍可能堆); **平衡数值必 F5**(技能频率/伤害/龟能回速/AOE 占比)。

## 四、自验
- 冒烟: `<godot> --headless --path . res://scenes/RealtimeBattle3D.tscn --quit-after 600` → 0 报错。
- 伤害量级: 全场单次伤害 0 超 300(选3 全技启用后仍均衡, 系数 ≤3.1×ATK + 暴击)。已验。
- 截图: 多技能并发放招(伤害数/闪避/治疗/弹幕粒子)正常。手感/节奏/平衡 → 待用户 F5。
