# 装备效果核实账本（2026-07-01）

> 用户质疑"每件装备都有对的效果吗，不太像做了"。经**逐件审计 + 核心复核**：59件里 **48件本就对、11件有真问题（已修）**。用户的直觉是对的——不是全做完了。

## 审计方法
- 逐件读 `data/phase2-equipment.json` 的 `effectDesc` vs `RealtimeBattle3DScene.gd` 的 `_eq_*` 分支实际行为（不是"有分支=对"）。
- 分类：OK / 不符(MISMATCH) / 缺失(MISSING) / 简化(SIMPLIFIED) / 纯属性件。

## 结果：48 OK / 11 修（commit 5a60d1e）
### CRITICAL（机制基本坏，已修）
| id | 名 | 原问题 | 修法 |
|---|---|---|---|
| p2eq_013 | 炙烤海胆 | 3★叠满硬化"把甲抗分全队"缺 | `_eq_on_target` 满20层且3★→给全队+40防+40抗(一次) |
| p2eq_017 | 不沉之锚 | 整套(受伤治疗最低血友军+攒满100充能+施法消耗充能击飞)被简化成周期击飞 | 移出tick：`_eq_on_target`受伤治疗+累积充能；`_eq_on_cast`消耗充能击飞+眩晕1.5s |
| p2eq_021 | 守护贝母 | +20龟能 & 伤害转移 缺 | +20龟能(`_eq_grant_energy`)+净化1/1/2+`dmg_redirect_to`转移25/40/60%给携带者 |
| p2eq_034 | 玩偶小熊 | 周期派小熊+攒层→召大熊 被简化成仅阵亡召大熊 | 移到tick：每2.5s派小熊冲敌(伤害+击飞)+攒层，满5/3/1层召大熊 |

### HIGH（10-20%强度，已修）
| p2eq_016 铁壁盾 | 每段非真伤固定减2/4/6 缺 | `flat_dr`字段+`_apply_damage_from`护盾前减 |
| p2eq_042 涟漪药剂 | 3★最低血友军双倍回复 缺 | 3★对最低血友军×2回已损血 |
| p2eq_052 左轮 | 敌亡+1子弹(上限6) 缺 | `_eq_on_death`任意敌亡→持有者+1子弹 |

### MEDIUM（能用但不全，已修）
| p2eq_004 暴君之牙 | 恒回40血(应回20龟能/无龟能才40血) | 有龟能系统→回20龟能，否则回40血 |
| p2eq_031 迷你水晶球B | 3★引爆溅射邻格 缺 | 3★引爆溅射90px邻敌 |
| p2eq_051 激光手枪 | 身后敌漏施流血 | 身后敌补50%流血 |
| p2eq_002 海带卷刀 | (复核本就对: DoT层数模型天然满足"可叠3层") | 仅加注释 |

## 核心管线改动（安全性已读证）
- `_apply_damage_from` 加 `flat_dr`(护盾前非真伤固定减) + `dmg_redirect_to`(伤害转移走 `_raw_lose` 无递归)。**二者严格守卫**：无该字段的单位完全不受影响 → 不破坏普通战斗。
- 新 helper：`_has_energy_system`(有主动技=有龟能) / `_eq_grant_energy`(龟能=冷却×0.075折算, 见冷却=龟能memory) / `_cleanse_n`(限量净化)。

## on-cast 实时节流（Phase 5, 保守）
- **已改**：`p2eq_050`幽灵加特林 永久减甲 → 加**每目标累计上限 15/25/40**(防实时高频放技削甲到0；对齐 bubbleBind 30上限模式)。
- **仅记 F5 观察、未盲改**：`p2eq_039`竹弓(充能类, 实时充能来源需F5确认够不够) / `p2eq_048`手铳·`p2eq_049`弩·`p2eq_053`霰弹(多发子弹, 高频放技是否刷屏/叠加过快需F5看)。平衡细调留用户 F5。

## 验证状态（诚实）
- ✅ parse 干净（headless 30帧无 SCRIPT ERROR）。
- ✅ 核心伤害管线 + 两大重写(017/034) 已逐行读证结构/守卫正确。
- ⏳ **逐效果"在战真触发对不对"留用户 F5**（11件各自触发条件不同, 未逐一在战验；不假称"全部战场验过"）。
