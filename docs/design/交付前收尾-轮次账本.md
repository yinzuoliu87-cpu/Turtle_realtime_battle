# 交付前收尾 · 轮次账本（2026-07-10 起 · /loop 60s 自驱）

> **背景**：28龟战斗线已完结（`完全实装路线图.md` 我的活=0）。本账本管**剩下的项目级收尾**。
> **纪律**：每轮 ①先核活代码/真数据，不凭印象 ②改完必验（headless/测试/截图），不说"应该没问题" ③一轮一 commit，不 push ④拿不准的**先查 transcript 原话**，绝不自造决定。
>
> ⚠ **本账本里任何"用户已定"必须附〖用户逐字原话+日期〗**。没有原话就是我的默认值，标 `▸实现默认`。
> （教训：我曾把自己写的"定稿表"当用户决定引给用户——见 `实时战斗设计-Botworld式.md` 顶部 ⛔ 横幅。）

---

## 已知事实（本轮开工前核过的）

- **实时版 = 完全自动战斗**：龟自走位/索敌/平A，主动技**龟能满自动施放**，玩家战斗中零操作。
  〖用户2026-07-10逐字〗"玩家不可以放主动技啊，这是压根没设计过的东西"
- 玩家侧待办（不归我）：气波图手动生成 / F5 眼验 / 逐龟立绘风格定调。
- 用户明确**暂缓**：羁绊 effects（12类型/11学派）。

---

## 轮次

| 轮 | 内容 | 状态 | 验收标准 |
|---|---|---|---|
| 1 | **字体：emoji 捆绑 + bold CJK 回退修正** | ✅完成 | `tests/verify_fonts` ALL PASS（关掉系统回退后 121 个 emoji/符号 + 中文 + ASCII 全有字形）|
| 2 | 设置场景完善 | ✅完成 | `tests/verify_settings` ALL PASS（含★「点重置不会立刻清档」安全断言）|
| 3 | 主菜单分层完善 | ✅完成 | `tests/verify_menu` ALL PASS（路由全通 / 调试入口 gate / REVIEW_DEMO 可关）|
| 4 | 图鉴完善 | ✅完成 | `tests/verify_codex` ALL PASS（龟能同源 / 3选1真能选3个 / 无等级解锁残留）|
| 5 | 死代码清理 Phase4（回合制残留） | ✅完成 | 迭代闭包删 11 函数/97 行；6 测试 + 6 sim + 9 场景冒烟全绿 |
| 6 | Web 导出 + itch 打包 | ✅完成 | `bash build-web.sh` 一键出包 + 5 项产物自检全过 |
| 7 | 全量回归 + 文档/memory 收尾 | 🟡下一轮 | 28龟 sim + 单测 + 账本对齐 |

---

## 轮 1 · 字体

**核过的现状**（不是猜的）
- `assets/fonts/m6x11.ttf`（英文/数字像素字）、`assets/fonts/NotoSansSC-Regular.otf`（中文，**已打包**）
- `assets/themes/default_theme.tres`：`FontVariation_main` = m6x11 打底 → fallback `[NotoSansSC, SystemFont(雅黑/苹方/…)]`
  → **CJK 早已解决**（含 Web：Noto 是打包的，不依赖系统）
- ❌ **无任何 emoji 字体**。UI 大量使用 🍬💠🗡🔼🐢 等。Windows 上靠 `allow_system_fallback` 命中 Segoe UI Emoji；
  **Web / Linux / macOS 导出会掉字形（豆腐块）**。
- ❌ `MainMenuScene._bold_font()`：中文粗体只挂 `SystemFont`，**没有打包的 Noto 作为回退** → 无雅黑的机器上粗体中文可能掉字形。

**要做**
1. 引入 **Noto Emoji**（SIL OFL，可随游戏分发）；优先单色版（体积小、Godot 稳）。
2. `default_theme.tres` 的 fallback 链尾加 emoji 字体。
3. `_bold_font()` 回退链补上打包的 Noto SC（在 SystemFont 之前）。
4. **自证**：写 `tests/verify_fonts.gd` — 关掉系统回退，逐字符 `has_char()` 检查 CJK 常用字 + 项目里实际用到的 emoji 全集（从源码里扫出来）。

### 轮1 结果（✅ 完成 · commit 见下）
- **CJK 其实早就做好了**（`NotoSansSC` 已打包 + SystemFont 回退），路线图那条"CJK+emoji 字体捆绑"里**只有 emoji 是真缺的**。
- 新增 `assets/fonts/NotoEmoji-Regular.ttf`（1.98MB，SIL OFL，附 `NotoEmoji-OFL.txt`）。
- `default_theme.tres` 回退链：`m6x11 → NotoSansSC → SystemFont(CJK+系统回退) → NotoEmoji`。
  **NotoEmoji 排最后**是有意的：桌面端先吃系统彩色 emoji（Win=Segoe UI Emoji / mac=Apple Color Emoji），
  只有 Web/Linux 取不到系统字体时才落到打包的单色版 → **既不豆腐块、桌面又是彩色**。
- 修 `MainMenuScene._bold_font()`：原来中文粗体只挂 `SystemFont`，**没有打包字体兜底** → 无雅黑的机器上粗体中文会掉字形。现补 `NotoSansSC + NotoEmoji`。
- **测试抓到 3 个真缺口**（不是猜的）：`↺`(U+21BA) `⛶`(U+26F6) `✕`(U+2715) —— Noto SC / Noto Emoji / NotoSansSymbols2 都不全覆盖。
  为 2 个字符再拖一个字体不划算 → 换成回退链已覆盖的：`↺→🔄`(U+1F504)、`✕→✖`(U+2716)；
  `⛶` 无等义替代 → **去掉图标只留文字**（"全屏 / 退出全屏"）。
- **扫描器口径**：只取 **字符串字面量** 里的字符（注释不渲染，不计入）。
  〔踩坑：我第一版扫全文，结果我自己写的"解释替换"的注释里又含 `↺⛶✕`，测试假红了一轮。〕

### ⚠ 轮1 期间造成的事故：测试覆盖了玩家真实存档
- `tests/verify_candy_jar.gd` 直接改 `GameState` 单例并调 `save()` —— 而 `save()` 写的就是 `user://savegame.json`。
- 结果：`meta_deepsea_coins` 被写成 **360**（每次打碎糖果罐 +120 × 3 次会写盘的运行；原值极可能是 0）、
  `season_leaders` 被硬设成 `["candy","stone","ninja"]`、`candy_jar_count/broken = 30/true`、
  `candy_temp_levels = {"candy":2}`、`persistent_bench` 被覆盖成 3 项测试道具、`dual_lineup top[2].temp_lv = 3`。
- **另外**：历次 headless 仿真（`REVIEW_TURTLE=… scenes/RealtimeBattle3D.tscn`）走战斗结算也会 `gs.save()`，同样在写玩家存档。
- 现场已备份：`user://savegame.json.bak-20260710-被测试污染`（**未擅自改动玩家存档**，等用户决定）。

**根治（已实装）**
- `GameState.test_mode`：置 true 后 `save()` 空转。
- **headless 下自动开启**（`DisplayServer.get_name() == "headless"`）—— 真实玩家永不 headless 运行，判定安全；
  `tests/` 里再显式设一次。
- 实证：跑一次会结算的 sim + 跑测试，存档 md5 **前后一致**。
- `verify_candy_jar` 的小将断言改成 delta（不依赖初值）→ 连跑 3 次幂等全绿。

---

## 轮 2 · 设置场景（✅ 完成 · commit f890bf3）

**核出来的 5 个真问题**（不是「完善」这种空话）
1. **「重置所有存档」一点就清空，无二次确认** — `_reset_save()` 直接调 `GameState.reset_save()`（清币/背包/统领/赛季全套）。
2. **全屏不持久** — `GameState` 里根本没有 `fullscreen` 字段，重启回窗口。
3. **全屏按钮文字不更新** — `_text_button()` 返回的 Label 没被接住，切完还写「全屏」。
4. **低画质模式是死按钮** — `perf_lite` grep 全库只在它自己的 label 里出现，什么也不做。
5. **拖音量条每帧写盘** — `_slider` 回调里每次都 `GameState.save()`；SFX 条还每帧播一次音效（爆音）。

**做的**
- 重置存档 → **二次确认弹窗**（取消 / 确认清空），写明会清什么、不会清什么。
- `GameState` 新增 `fullscreen` / `perf_lite`，进 save/load；**开机恢复全屏**（headless 跳过）。
- 全屏按钮文字同步。
- **低画质做成真开关**：战斗 SubViewport `msaa_3d = DISABLED` + `scaling_3d_scale = 0.75`（UI 层仍原生分辨率）；主菜单/图鉴/设置的 25s 常驻背景漂移 tween 不再跑。持久化。
- 音量：拖动实时生效，**松手才写盘 + 试听**一次。
- **ESC 返回**主菜单（确认框开着时 ESC 先取消）。
- `_mono_font()` 回退链补 `NotoEmoji`。
- `reset_save()` 明确**不清偏好设置**（音量/全屏/画质是偏好，不是进度）+ 「调用方必须二次确认」注释。

**验证**
- `tests/verify_settings` ALL PASS（24 条）。最关键的三条：★「点重置只弹框、不清档」/「取消后存档完好」/「确认后才真的清」。
- 回归：`verify_fonts` / `verify_candy_jar` / `verify_dot_stacks` 全绿；`basic/line/shell` sim 0 报错；**全程存档 md5 未变**。

**存档清理（用户 2026-07-10 批准）**
被测试注入的 7 项已还原：`meta_deepsea_coins 360→0` / `season_leaders→[]` / `candy_jar_count 30→0` / `candy_jar_broken→false` /
`candy_temp_levels→{}` / `persistent_bench→[]` / `dual_lineup[top][2].temp_lv` 移除。
**保留未动**（非本次测试注入，可能含真实 F5 游玩）：`hearts=8` `season_level=1` `battles_won 39/49`。
备份：`user://savegame.json.bak-20260710-被测试污染`。清理后用游戏自己的加载器验证：读得进、`auto_merge_all` 不崩、读档过程零写盘。

---

## 轮 3 · 主菜单（✅ 完成）

**核出来的真问题**
1. ★**「🛠 调试场」按钮无条件建给玩家**（`MainMenuScene._ready()` 里直接调 `_debug_arena_entry()`，没有任何 gate）
   → 正式包里玩家能点进自由摆位调试场。**已 gate 到 `OS.is_debug_build() or DEVTOOLS`**（导出 release 模板下 `is_debug_build()` 为 false）。
2. ★★**`REVIEW_DEMO := true` 会污染真实对局** —— 它不只是"评审场"开关：
   `_unit_level()` 里 `if REVIEW_DEMO: return 1` → **真打双路时全体单位被强制 Lv1，赛季等级完全不生效**。
   注释写着"上线前置 false"，但它是 `const`，一改就把评审/sim 流程全废。
   → 改成 `static func _review_demo()`，**`SHIP=1` 环境变量可就地关掉**，默认仍是评审期 true。
   两条路径都跑通（`basic/ninja/shell` × 评审 & SHIP，各 0 报错）。
3. **`_go()` 无守卫** —— 场景名打错就静默黑屏。加 `ResourceLoader.exists()` 检查 + `push_error`。
4. **陈旧注释**：`GROUP_TOP` 写着"5 入口(…排行榜…)"，实际左栏只有 4 个（开始战斗/背包/商店/设置），排行榜早挪到右侧磁贴了。

**验证**
- `tests/verify_menu` ALL PASS：8 个目标场景都存在 / 8 个子场景都有回主菜单的路径（无死胡同）/ 调试入口 gate 在调用前 / `_go()` 有守卫 / `REVIEW_DEMO` 已可关。
- 回归：`verify_fonts` `verify_candy_jar` `verify_settings` `verify_dot_stacks` 全绿；6 组 sim（3 龟 × 评审/SHIP）0 报错；**全程存档 md5 未变**。

### ★ 轮 7 交付清单（先记下，别忘）
- [ ] **`REVIEW_DEMO_DEFAULT` 改 false**（或导出时带 `SHIP=1`）。否则玩家实际战斗里赛季等级不生效、且敌方是沙包假人。
- [ ] 确认导出的 release 包里 `🛠 调试场` 按钮确实不出现（`is_debug_build()` 需实测导出模板）。

---

## 轮 4 · 图鉴（✅ 完成）

**★最重的一条：「3选1」实际是「2选1」**
- `TeamSelectScene._available_skill_indices()` 沿用回合制 PoC 的 `getPetLevel + skillUnlockLevel`：
  **`idx3` 需龟 Lv.4、`idx4` 需 Lv.7**。而 `GameState.pet_levels` 默认 **1**（注释明写"只调试面板改"），
  `clickable = not is_locked ...` → **第三个候选技点不动**。
- 与用户原话直接冲突：〖2026-06-30 13:16 逐字〗"这只龟现在是 **3个技能选一个来登场**，其他龟我也会慢慢的将4个可选改为3可选来降低复杂度和提升维护性"。
  transcript 里 **「解锁」命中 0 次** —— 用户从没要过技能等级解锁。
- → **已移除等级门槛**，全 28 龟三个候选都可选。图鉴同步去掉「🔒 Lv.4 解锁」。
- ⚠ **诚实修正**：用户自己的存档里 `pet_levels` 全是 **10**（调试面板设过），所以他本人从没撞上这个锁；
  **新玩家（默认 Lv1）才会**。这也是它一直没被发现的原因。

**龟能：图鉴在骗玩家（事实源用错）**
- 图鉴只读 `SkillEnergy.cost_of(type)`，而战斗 `_skill_cost()` 是 **pets.json `energyCost` 优先、SkillEnergy 兜底**。
- 实测两处对不上：
  - 彩虹「护盾」：战斗 **50**，图鉴显 **70**（`shield` 是多龟共用的通用 type key，一个值套不了所有龟）
  - 彩虹「反射」：`SkillEnergy` 表里**根本没有** `rainbowReflect` → 图鉴显 **龟能 0**，战斗实际 110
- → 新增 `CodexScene._skill_energy(sk)`，与战斗同口径。chip 文案改成「3选1候选 · 龟能N」。

**两条虚惊（先核后信，没冤枉代码）**
- 「79 个技能图鉴里有、战斗放不出」：**假的**。我解析 `_IMPL_SKILLS` 用 `src.find("}")`，
  被注释里的 `{N/M/T:...}` 提前截断。改成真括号配对后：`_IMPL_SKILLS` = **102 条**，**0 个放不出的技**。
- 「idx≥4 的技图鉴可见但选不到」：**不存在**。`pets.json` 已裁成每龟 4 条（idx0 普攻 + idx1..3 候选）。

**验证**
- `tests/verify_codex` ALL PASS：全 28 龟 × 全候选技逐个对龟能 / 三候选都可选 / skillPool 长度全 4 / 无 ghost 技 / 无等级锁残留。
  （踩坑：断言写成 `get_pet_level("basic")==1` 会被真机存档里的调试等级弄成假红 → 改成问一个不存在的 id 走默认分支。）
- 回归：`verify_fonts` `verify_candy_jar` `verify_settings` `verify_menu` `verify_dot_stacks` 全绿；
  6 组 sim（basic/rainbow/shell × 评审/SHIP）0 报错；**全程存档 md5 未变**。

---

## 轮 5 · 死代码清理 Phase4（✅ 完成）

**方法：迭代死码闭包，不是拍脑袋删**
- 写普查脚本：对 `GameState` + `scripts/engine/*` + `scripts/systems/*` 的每个 `func`，
  在全库（`.gd/.tscn/.tres/.cfg/.sh`）里 grep 调用方，含 `"name"` 形式的字符串/信号引用。
- **脚本先自检**：拿一个已知死函数（`on_battle_turn_economy`）和一个已知活函数（`ai_gain_coins`）当探针，
  判错就直接 assert 失败。
  〔踩坑：第一版说"0 个死函数"——因为 `os.walk` 在 Windows 给的是反斜杠路径，跟我传的 `./autoload/...` 对不上，
   **函数定义行被自己当成了调用方**。和轮4 那个 `find("}")` 是同一类错。〕
- 删除只在 **「回合制局内经济 + 地下城」命名域** 内做，双路（`dual_*` / `egg_*` / `bench_inventory`）一律不碰。
- 迭代到不动点：每删一个就重算调用图，被依赖的先留着，等它的调用方被删干净了才轮到它。

**删了什么（11 函数 / 97 行，`GameState.gd` 1243 → 1146）**
| 函数 | 为什么是死码 |
|---|---|
| `reset_battle_economy` `on_battle_turn_economy` `_interest` `grant_wealth_coin` `ai_gain_coins` | 回合制**局内**每回合 +10 币 + 利息 + 财富羁绊。实时版局内经济早改成"每5秒+1深海币"，且财富羁绊用户已删 |
| `start_dungeon` `advance_stage` `setup_next_dungeon_stage` | **地下城模式**，实时版没有 |
| `record_battle_win` `record_battle_loss` | 战斗结算里直接 `gs.battles_won += 1`，这俩没人调；且体内还有 `mode == "dungeon"` / `coins += 20`（龟币） |
| `snapshot_left_hp` | 回合制关卡间带血，零调用方 |
+ 删孤儿 `scripts/engine/phase2_pvp.gd.uid`（对应的 `.gd` 早就不存在）

**闭包证明自己没乱杀**：`reset_dungeon` / `is_dungeon_boss_stage` 虽然在 dungeon 命名域内，
但脚本查到它们**有真调用方**（`GameState.reset_dungeon()` / `if mode == "dungeon" and not is_dungeon_boss_stage()`）→ **保留**。

**故意不删（有疑虑就留下 + 写明理由）**
| 对象 | 理由 |
|---|---|
| `scripts/engine/*` 所有文件 | **一个都不能整文件删**。逐个查过引用方：`skill_text.gd` 靠 `class_name SkillText` 全局注册（路径名根本不出现，我的文件级 grep 一度误报它 0 引用）；其余都有 preload 方 |
| `sell_bench_item` / `unequip_from_turtle` / `refresh_shop` | 零调用方，但属于**双路局内商店**（`bench_inventory` / `equipped_p2` / `dual_coins`）这套自洽子系统，可能被重新接线。删单个函数会让子系统半残 |
| `damage_egg` / `egg_frac` / `dual_lane_snapshot` / `setup_dual_lane` 等 | 双路/龟蛋，`死代码清理计划.md` 自评"高风险低价值"，且 memory 记着"别碰 `try_merge_all`（双路那套）" |
| `buffs.gd` / `phase2_equip_runtime.gd` / `slot_helpers.gd` 等模块的公开 API（约 50 个零调用方 func） | 它们是**库表面**，被同一模块的其它入口按需调用；批量删是重构不是清理，收益低风险高 |
| `_unhandled_key_input` | **Godot 引擎回调**，永远"零调用方"。普查脚本已把它列进 `GODOT_CB` 白名单 |

**验证**
- `import` 零错；`verify_fonts` / `verify_candy_jar` / `verify_settings` / `verify_menu` / `verify_codex` / `verify_dot_stacks` **全绿**。
- 6 组 sim（basic/ninja/shell × 评审/SHIP=1）0 报错。
- **新增场景冒烟**：9 个场景（MainMenu/Inventory/Shop/Settings/Codex/Leaderboard/Record/TeamSelect/Matchmaking）逐个 `instantiate()` + 进树 → 全通过。
- **全程存档 md5 未变**。

---

## 轮 6 · Web 导出打包（✅ 完成）

**产出**：`bash build-web.sh` → `build/turtle-realtime-web.zip`（**153.5 MB**，9 个条目，`index.html` 在 zip 根）

**发现的真问题（导出后逐项验产物，不看退出码）**
1. ★**测试脚本被打进了正式包**。`export_filter="all_resources"` + 空 `exclude_filter` →
   导出日志里全是 `savepack | 保存文件：res://tests/verify_*.gd.remap`、`res://diff_images.gd.remap`。
   → 加 `exclude_filter="tests/*, diff_images.gd, docs/*, *.md, *.ps1, *.sh, *.py"`。
   `index.pck` 152.7 → **145.4 MB**，`res://tests/` 命中数 **0**。

**两条我差点误报、核完发现不是 bug 的**
- 「`index.js` 里有 12 处 `SharedArrayBuffer` → 是不是用了多线程模板？」
  → **不是**。实测导出的 `index.wasm` 里 `pthread_create` / `__pthread` / `wasm_worker` / `emscripten_futex`
  **全为 0 次**；且 wasm 大小 **35.95 MB** 精确匹配 `web_nothreads_release.zip`（多线程模板是 35.31 MB）。
  那 12 处只是特性探测的文档注释。**单线程确认，不需要 COOP/COEP 头，iOS Safari 能跑。**
- 「`vram_texture_compression/for_desktop=true` 而 `for_mobile=false` → iOS 上 S3TC 挂？」
  → **不成立**。全库 **400 个 `.png.import` 全是 `compress/mode=0`**（无损 / VRAM 未压缩），
  这个开关根本不产生压缩变体。**没动它。**

**体积（数据说话，不擅自压）**
- `assets` 181 MB：`sprites` 162 MB / `audio` 15 MB / `fonts` 9.9 MB。
- **素材引用普查（脚本带自检探针）：181 MB 里没有一个无引用文件**（唯一命中的是 Noto 的许可证 `.txt`）。
  → 体积就是无损美术本身，**压缩与否是美术决策，不是我能擅自定的**。
  可选杠杆（未做）：背景大图 `compress/mode` 改 Lossy(WebP)、BGM 降码率、Noto SC 字体子集化。

**新增 `build-web.sh`（一键导出 + 5 项产物自检 + 打包）**
- 自检：`index.html/wasm/pck` 存在 / `tests/` 未进包 / **wasm 内 pthread 符号必须为 0**。
- 打包用 **python `zipfile`** 而不是 `Compress-Archive` —— 后者会因 `index.pck` 被占用而失败
  （`The process cannot access the file ... being used by another process`），python 用共享读能过。
  〔踩坑：脚本里给 python 传 Git Bash 的 `/c/...` 路径 → `FileNotFoundError`；改成 `cd` 到项目根后用相对路径。
   另：python 打印 `✓` 在 Windows GBK 控制台会崩 → 改纯 ASCII。〕
- 脚本结尾会提醒：**上线前确认 `REVIEW_DEMO_DEFAULT=false` 或用 `SHIP=1` 构建**。

**验证**：`import` 零错；6 个测试套件全绿；4 组 sim（basic/shell × 评审/SHIP）0 报错；**全程存档 md5 未变**。
`build/` 已在 `.gitignore` 里，153 MB 的产物不入库。
