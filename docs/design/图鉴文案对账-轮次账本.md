# 图鉴文案 ↔ 实装 全量对账 · 轮次账本（2026-07-10 起 · /loop 60s 自驱）

> **为什么要做**：图鉴（`CodexScene`）显示的就是 `data/pets.json` 的 `brief` / `detail`。
> 收尾轮4 只修了两个机械 bug（龟能事实源、3选1 等级锁），**从没做过逐龟的文案 ↔ 代码对账**。
> "描述≠实现" 是用户反复投诉的高发点，**不能靠抽查**。

## 已知的两个方向（都真实存在，不能一律"以代码为准"）
- **代码变了、文案没跟**（已修 3 处，`12450d2`）：海盗火炮齐射（全体敌→目标800码圈）、海盗朗姆酒（2.5s/9%/5s/护甲 → 每秒4%/6s/双抗）、线条连笔（5秒/魔法 → 3秒/真实）。
- **文案超前、代码没做**（收尾各轮已补齐）：石头「+2%体型/层」、骰子「每段递减10%」、线条「30%传导+墨迹同步」、龟壳「反伤+12%」。

## 判定规则（沿用 `28龟技能设计-权威.md` §0 的 R0）
1. **用户 transcript 逐字原话** 最高优先。
2. 其次 **`28龟技能设计-权威.md`** 里已锁定的值（它本身已按 R0 校过一遍）。
3. 代码与文案冲突且两者都无原话支撑 → **以活代码为准**（它是真在跑的），并在权威文档留痕。
4. 拿不准 → **不猜**，标 `⚠` 列出证据，绝不替用户拍板。

---

## 轮次（7 批 × 4 龟，按风险排序）

| 轮 | 龟 | 状态 |
|---|---|---|
| A | `basic` `stone` `bamboo` `angel` | ✅完成 |
| B | `ice` `ninja` `two_head` `ghost` | ✅完成 |
| C | `diamond` `fortune` `dice` `rainbow` | 🟡下一轮 |
| D | `gambler` `hunter` `pirate` `candy` | ⬜ |
| E | `bubble` `line` `lightning` `phoenix` ★凤凰是已知重灾区 | ⬜ |
| F | `lava` `cyber` `crystal` `chest` | ⬜ |
| G | `space` `hiding` `headless` `shell` | ⬜ |
| H | 全量回归 + 账本/权威文档/memory 收尾 | ⬜ |

**已知重灾区**：`phoenix` 普攻文案还是回合制的「灼烧**值**」模型（`0.53×ATK 灼烧值，每秒衰减 1/3`），
而实时版是**灼烧层 + 持续喷火 channel**（`PHX_FLAME_MAG_COEF=0.2` / `PHX_FLAME_BURN_COEF=0.07`）。

## 工具
`tools/codex_audit.py <pet_id>...` —— 一次性把三方并排打出来：
`pets.json 文案` / `活代码（BASIC_ATK + _sk_* + 常量 + 龟能）` / `权威文档该龟整节`。
**脚本带自检探针**（已知阳性 + 已知阴性），判错即 `assert` 崩 —— 我已两次被自己的解析脚本骗过。

---

## 结果
（每轮追加）

---

### 轮 A · `basic` `stone` `bamboo` `angel`（✅ 完成）

**★又抓到 2 个「击飞时长」代码 bug（同石头砸地/竹刺阵那一类，我上次漏了）**
- 小龟·**龟派气波**：`vy_mult=1.8` → 实际滞空 **0.98 秒**。
  〖用户 2026-07-05 15:06 逐字〗"…**击飞1.5s**，击退大概200码" → 改 `vy_mult=2.752`（真 1.5 秒）。
- 忍者·**冲击**：`vy_mult=1.6` → 0.87 秒；〖#18〗"击飞0.8秒" → 改 `1.468`（真 0.8 秒）。
- 顺手写了个扫描器，把全库 17 处 `_knockback` 的 `vy_mult` 换算成真实滞空、和注释声称值对账 → 现在**无一处不符**。

**修正的文案（图鉴显示的就是它们）**
| 龟 | 项 | 原文案 | 实装（改后文案） |
|---|---|---|---|
| 小龟 | 龟派气波 | "智能位移…**留美术/F5**" | 已实装：12方向×3档距离采样，冲向能穿更多敌的落点，距敌 <120码 排除 |
| 石头 | 嘲讽 | "嘲讽 **5秒**"，只提永久盾 | **4秒**；补上 (0.5×护甲)% 减伤4秒、第3.5秒收尾砸地(400码 1A魔法 + **击飞2秒**)、**砸击后龟能才重新充能** |
| 竹叶 | 被动·生长 | "+**6%**maxHp 魔法 / 回**6%** / 永久 +6%攻 +3%maxHp" | +**8%**maxHp 魔法 / 回**8%** / 永久 `maxHp += 0.60×基础ATK`（**攻击力不涨**）；选竹击则 13%/12%/1.05× |
| 竹叶 | 自然恢复 | 队友"**永久**护盾" | **4秒**限时护盾 |
| 竹叶 | 竹击 | "对目标…冰寒**5秒**…击至**前排**"（回合制槽位语义） | 竹藤钩**全场最远敌** + 眩晕0.5秒 + **拉到身前** + 冰寒**4秒**(-20%攻/-20%移速) |
| 竹叶 | 竹刺阵 | "对**全体敌方**刺击**五段**" | **当前目标为心 300码圈** · 蓄力0.6秒 · `0.9A + 15%maxHp` · **击飞1.5秒**（总伤害相同，形态完全不同） |
| 天使 | 祝福 | "**永久**护盾" | **5秒**（与攻速/龟能 buff 同步） |

**核过、无需改的**：小龟 不屈/打击/过肩摔；石头 坚壁/普攻/岩石护盾/磐石之躯；竹叶 一叶刃；天使 审判/裁决/平等/飞升。

**踩坑**：给 `pets.json` 写文案时，heredoc 里的 `
` 变成了**真换行**混进 JSON 字符串 → `JSONDecodeError: Invalid control character`。
定点修复（不重写整个文件），并在每次改完 `json.load()` 断言合法性。

**验证**：import 零错 · `verify_codex/fonts/menu/settings/dot_stacks` 全绿 · 5 组 sim 0 报错 · 存档 md5 未变。

---

### 轮 B · `ice` `ninja` `two_head` `ghost`（✅ 完成）

**代码 bug：0**（本轮没抓到新的；击飞时长全库已在轮A校准过）

**文案 ≠ 实装，修 11 处**
| 龟 | 项 | 原文案 | 实装 |
|---|---|---|---|
| 寒冰 | 被动·寒域 | "登场施加冰寒 **5秒**…降低 **20%攻击力**" | 全场敌 **-30%攻速 / -30%龟能充能 / -30%移速**，**整场持续**（`spd_*_mult=0.7`，无时限） |
| 寒冰 | 普攻·冰锥 `detail` | "**六段**共 1.4A×0.5 物理+魔法"（回合制） | **单段 0.8A**，物理/魔法**逐次交替**（`alt_each`）。★brief 早就是对的，detail 还是老的 |
| 寒冰 | 冰霜 `detail` | "先削**全体敌** -25%魔抗，随后**十段**" | **当前目标处 150码圆形冰霜领域** · 5秒 · 每0.5秒一跳共10跳 · 领域内 -25%魔抗。★同样 brief 对、detail 错 |
| 寒冰 | 团队护盾 | "**永久**护盾" | **4秒**限时（`_grant_shield(o, 0.5A, 4.0)`）|
| 忍者 | 被动·忍术 | 只有 "暴击+30%/暴伤+20%/穿甲+8" | ★**「冲击」整个被动没写进图鉴**。补：500码搜最近敌 → **固定450码位移冲刺** → 主目标1.3A / 路径其余0.8A → **击飞0.8秒**，每敌10秒冷却，放技不被打断 |
| 忍者 | 背刺 `brief` | "闪现到**目标**身后" | **全场最远敌**身后（detail 本来是对的，brief 错）|
| 幽灵 | 被动·怨灵 | 只写 "**登场**诅咒" | ★**死亡诅咒漏了**（`_on_unit_death` L9734 明明有）。补：登场+死亡各施1层、可叠加、**不周期续、普攻不叠** |
| 幽灵 | 幽冥突袭 | "闪避持续 **5秒**" | **4秒**（`selfDodgeDur: 4.0`；用户 2026-07-09"闪避是4秒"）|

**核过、无需改**：忍者 手里剑/炸弹（1.6A / 1.1A+减甲25%·5秒·`aoe=全体敌` 已核）；
双头 全部（双生/普攻/技一二三·融合·双头坚韧+1甲+1抗上限20 与代码一致）；
幽灵 幽魂触碰(0.4物+0.9真)/灵魂风暴(无诅咒2段2.5A魔法·有诅咒2.5A真伤)/虚化(4秒·受物理-90%)。

**观察**：寒冰的 `brief` 是实时版的、`detail` 还是回合制的 —— **同一技能里 brief 和 detail 互相矛盾**。
说明历史上有人只改了 brief 没改 detail。后续几轮要 **brief 和 detail 都逐条读**。

**踩坑**：`pets.json` 里带 HTML `<span class=\"...\">` 的文案，用 `%` 格式化拼字符串会撞上中文百分号
（`ValueError: unsupported format character`）→ 改用 `@Q@` / `@N@` 哨兵占位再 `.replace()`。

**验证**：import 零错 · 6 个测试全绿 · `ice/ninja/two_head/ghost` sim 各 0 报错 · 存档 md5 未变。
