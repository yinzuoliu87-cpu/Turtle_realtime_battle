# 图鉴文案 ↔ 实装 全量对账 · 轮次账本（2026-07-10 起 · /loop 60s 自驱）

> **为什么要做**：图鉴（`CodexScene`）显示的就是 `data/pets.json` 的 `brief` / `detail`。
> 收尾轮4 只修了两个机械 bug（龟能事实源、3选1 等级锁），**从没做过逐龟的文案 ↔ 代码对账**。
> "描述≠实现" 是用户反复投诉的高发点，**不能靠抽查**。

## 已知的两个方向（都真实存在，不能一律"以代码为准"）
- **代码变了、文案没跟**（已修 3 处，`12450d2`）：海盗火炮齐射（全体敌→目标800码圈）、海盗朗姆酒（2.5s/9%/5s/护甲 → 每秒4%/6s/双抗）、线条连笔（5秒/魔法 → 3秒/真实）。
- **文案超前、代码没做**（收尾各轮已补齐）：石头「+2%体型/层」、骰子「每段递减10%」、线条「30%传导+墨迹同步」、龟壳「反伤+12%」。

## 判定规则（沿用 `28龟技能设计-权威.md` §0 的 R0）
1. **用户 transcript 逐字原话** 最高优先。
2. 其次 **`28龟技能设计-权威.md`** 里已锁定的值（它本身已按 R0 校过一遍）。
3. 代码与文案冲突且两者都无原话支撑 → **以活代码为准**（它是真在跑的），并在权威文档留痕。
4. 拿不准 → **不猜**，标 `⚠` 列出证据，绝不替用户拍板。

---

## 轮次（7 批 × 4 龟，按风险排序）

| 轮 | 龟 | 状态 |
|---|---|---|
| A | `basic` `stone` `bamboo` `angel` | ✅完成 |
| B | `ice` `ninja` `two_head` `ghost` | ✅完成 |
| C | `diamond` `fortune` `dice` `rainbow` | ✅完成 |
| D | `gambler` `hunter` `pirate` `candy` | ✅完成 |
| E | `bubble` `line` `lightning` `phoenix` ★凤凰是已知重灾区 | ✅完成 |
| F | `lava` `cyber` `crystal` `chest` | 🟡下一轮 |
| G | `space` `hiding` `headless` `shell` | ⬜ |
| **H′** | **`passive.brief` 补审（A–D 的 16 龟）—— ★我自己漏出来的，见轮E** | ⬜ |
| H | 全量回归 + 账本/权威文档/memory 收尾 | ⬜ |

### ★★ 轮E 抓到的**审计口径漏洞**（我自己造成的，必须补）
`passive` 有**两个都会显示**的文案字段，我前四轮只审了 `desc`：

| 字段 | 显示位置 |
|---|---|
| `passive.desc` | 图鉴被动区 `CodexScene.gd:1069` ＋ 局内信息面板 `RealtimeBattle3DScene.gd:13555` |
| **`passive.brief`** | **选龟界面 被动 chip 的 tooltip** `TeamSelectScene.gd:1155` |

而且两者**陈旧的方向还不一样**（bubble/lightning 是 `brief` 对、`desc` 旧；phoenix/line 是两个都旧）。
A–D 16 龟的 `brief` 体检（差异比 + 人工判读），确认**矛盾/漏块**的至少 6 只：

- ★★ `pirate.brief` ＝「海盗龟**无被动技能**（掠夺已移除）」—— 与 `desc`、与轮D 刚修好的死亡钩索**正面冲突**，这行字现在就显示在选龟界面。
- ★ `candy.brief` ＝「开战 **7.5秒** 对**随机**敌人」 vs `desc`＝「**登场时**对**最大生命值最高**的敌人」。
- ★ `gambler.brief` ＝「追加一次额外打击（0.5A 物理）」 vs `desc`＝「以约 3.3 倍攻速**再打一次普攻**」。
- ★ `bamboo.brief` ＝ `HP*0.06` / +6%攻 / +3%HP vs `desc`＝ `HP*0.08` / +0.60×… （数值冲突，待核代码）
- ★ `ninja.brief` / `ghost.brief` ＝ **整块漏**（忍者缺「冲击」自动冲刺斩；幽灵缺「死亡诅咒」）。
- 判读为"只是摘要、不算错"：`basic` `stone` `angel` `ice` `two_head` `diamond` `fortune` `dice` `rainbow` `hunter`。

> 教训与轮D 的「海盗朗姆酒只改 detail 没改 brief」是**同一个病**：**只审我正在看的那个字段，不先清点这一块到底有几个会显示的字段。**
> → 轮F/G 起，每只龟固定审 4 个字段：`passive.brief` / `passive.desc` / `skillPool[i].brief` / `skillPool[i].detail`。

**已知重灾区**：`phoenix` 普攻文案还是回合制的「灼烧**值**」模型（`0.53×ATK 灼烧值，每秒衰减 1/3`），
而实时版是**灼烧层 + 持续喷火 channel**（`PHX_FLAME_MAG_COEF=0.2` / `PHX_FLAME_BURN_COEF=0.07`）。

## 工具
`tools/codex_audit.py <pet_id>...` —— 一次性把三方并排打出来：
`pets.json 文案` / `活代码（BASIC_ATK + _sk_* + 常量 + 龟能）` / `权威文档该龟整节`。
**脚本带自检探针**（已知阳性 + 已知阴性），判错即 `assert` 崩 —— 我已两次被自己的解析脚本骗过。

---

## 结果
（每轮追加）

---

### 轮 A · `basic` `stone` `bamboo` `angel`（✅ 完成）

**★又抓到 2 个「击飞时长」代码 bug（同石头砸地/竹刺阵那一类，我上次漏了）**
- 小龟·**龟派气波**：`vy_mult=1.8` → 实际滞空 **0.98 秒**。
  〖用户 2026-07-05 15:06 逐字〗"…**击飞1.5s**，击退大概200码" → 改 `vy_mult=2.752`（真 1.5 秒）。
- 忍者·**冲击**：`vy_mult=1.6` → 0.87 秒；〖#18〗"击飞0.8秒" → 改 `1.468`（真 0.8 秒）。
- 顺手写了个扫描器，把全库 17 处 `_knockback` 的 `vy_mult` 换算成真实滞空、和注释声称值对账 → 现在**无一处不符**。

**修正的文案（图鉴显示的就是它们）**
| 龟 | 项 | 原文案 | 实装（改后文案） |
|---|---|---|---|
| 小龟 | 龟派气波 | "智能位移…**留美术/F5**" | 已实装：12方向×3档距离采样，冲向能穿更多敌的落点，距敌 <120码 排除 |
| 石头 | 嘲讽 | "嘲讽 **5秒**"，只提永久盾 | **4秒**；补上 (0.5×护甲)% 减伤4秒、第3.5秒收尾砸地(400码 1A魔法 + **击飞2秒**)、**砸击后龟能才重新充能** |
| 竹叶 | 被动·生长 | "+**6%**maxHp 魔法 / 回**6%** / 永久 +6%攻 +3%maxHp" | +**8%**maxHp 魔法 / 回**8%** / 永久 `maxHp += 0.60×基础ATK`（**攻击力不涨**）；选竹击则 13%/12%/1.05× |
| 竹叶 | 自然恢复 | 队友"**永久**护盾" | **4秒**限时护盾 |
| 竹叶 | 竹击 | "对目标…冰寒**5秒**…击至**前排**"（回合制槽位语义） | 竹藤钩**全场最远敌** + 眩晕0.5秒 + **拉到身前** + 冰寒**4秒**(-20%攻/-20%移速) |
| 竹叶 | 竹刺阵 | "对**全体敌方**刺击**五段**" | **当前目标为心 300码圈** · 蓄力0.6秒 · `0.9A + 15%maxHp` · **击飞1.5秒**（总伤害相同，形态完全不同） |
| 天使 | 祝福 | "**永久**护盾" | **5秒**（与攻速/龟能 buff 同步） |

**核过、无需改的**：小龟 不屈/打击/过肩摔；石头 坚壁/普攻/岩石护盾/磐石之躯；竹叶 一叶刃；天使 审判/裁决/平等/飞升。

**踩坑**：给 `pets.json` 写文案时，heredoc 里的 `
` 变成了**真换行**混进 JSON 字符串 → `JSONDecodeError: Invalid control character`。
定点修复（不重写整个文件），并在每次改完 `json.load()` 断言合法性。

**验证**：import 零错 · `verify_codex/fonts/menu/settings/dot_stacks` 全绿 · 5 组 sim 0 报错 · 存档 md5 未变。

---

### 轮 B · `ice` `ninja` `two_head` `ghost`（✅ 完成）

**代码 bug：0**（本轮没抓到新的；击飞时长全库已在轮A校准过）

**文案 ≠ 实装，修 11 处**
| 龟 | 项 | 原文案 | 实装 |
|---|---|---|---|
| 寒冰 | 被动·寒域 | "登场施加冰寒 **5秒**…降低 **20%攻击力**" | 全场敌 **-30%攻速 / -30%龟能充能 / -30%移速**，**整场持续**（`spd_*_mult=0.7`，无时限） |
| 寒冰 | 普攻·冰锥 `detail` | "**六段**共 1.4A×0.5 物理+魔法"（回合制） | **单段 0.8A**，物理/魔法**逐次交替**（`alt_each`）。★brief 早就是对的，detail 还是老的 |
| 寒冰 | 冰霜 `detail` | "先削**全体敌** -25%魔抗，随后**十段**" | **当前目标处 150码圆形冰霜领域** · 5秒 · 每0.5秒一跳共10跳 · 领域内 -25%魔抗。★同样 brief 对、detail 错 |
| 寒冰 | 团队护盾 | "**永久**护盾" | **4秒**限时（`_grant_shield(o, 0.5A, 4.0)`）|
| 忍者 | 被动·忍术 | 只有 "暴击+30%/暴伤+20%/穿甲+8" | ★**「冲击」整个被动没写进图鉴**。补：500码搜最近敌 → **固定450码位移冲刺** → 主目标1.3A / 路径其余0.8A → **击飞0.8秒**，每敌10秒冷却，放技不被打断 |
| 忍者 | 背刺 `brief` | "闪现到**目标**身后" | **全场最远敌**身后（detail 本来是对的，brief 错）|
| 幽灵 | 被动·怨灵 | 只写 "**登场**诅咒" | ★**死亡诅咒漏了**（`_on_unit_death` L9734 明明有）。补：登场+死亡各施1层、可叠加、**不周期续、普攻不叠** |
| 幽灵 | 幽冥突袭 | "闪避持续 **5秒**" | **4秒**（`selfDodgeDur: 4.0`；用户 2026-07-09"闪避是4秒"）|

**核过、无需改**：忍者 手里剑/炸弹（1.6A / 1.1A+减甲25%·5秒·`aoe=全体敌` 已核）；
双头 全部（双生/普攻/技一二三·融合·双头坚韧+1甲+1抗上限20 与代码一致）；
幽灵 幽魂触碰(0.4物+0.9真)/灵魂风暴(无诅咒2段2.5A魔法·有诅咒2.5A真伤)/虚化(4秒·受物理-90%)。

**观察**：寒冰的 `brief` 是实时版的、`detail` 还是回合制的 —— **同一技能里 brief 和 detail 互相矛盾**。
说明历史上有人只改了 brief 没改 detail。后续几轮要 **brief 和 detail 都逐条读**。

**踩坑**：`pets.json` 里带 HTML `<span class=\"...\">` 的文案，用 `%` 格式化拼字符串会撞上中文百分号
（`ValueError: unsupported format character`）→ 改用 `@Q@` / `@N@` 哨兵占位再 `.replace()`。

**验证**：import 零错 · 6 个测试全绿 · `ice/ninja/two_head/ghost` sim 各 0 报错 · 存档 md5 未变。

---

### 轮 C · `diamond` `fortune` `dice` `rainbow`（✅ 完成）

**代码 bug：0**（本轮全是文案侧）

**文案 ≠ 实装，修 22 处**（本轮最脏，`brief` 与 `detail` 大面积互相矛盾）

| 龟 | 项 | 原文案 | 实装 |
|---|---|---|---|
| 钻石 | 被动 | 断句坏（"减少 18%真实伤害不受此减免影响"）+ "全队双抗**加成效果放大** +50%" | **登场时全队双抗各 +50%**（`_buff(o,"def",0.5,pct)`）；自身受伤 -18%，**真伤/穿透不减** |
| 钻石 | 滚球 `brief` | "**100码内**可无消耗自动滚" | ★**条件写反**。〖用户#5 逐字〗"被动为当 **100码内没有目标时** 就会启动主动" → 代码 `nearest > 100.0` 正确 |
| 钻石 | 滚球 `detail` | "当前实时为 dash 近似，完整状态机**待接入(F5)**" | 陈旧：`_diamond_roll_tick` / `_diamond_roll_impact` 早已实装 |
| 钻石 | 冲撞 `detail` | "受击再减20%甲+10%抗…**待F5精修**" | 改成诚实表述：实时版未单独实装，收益已折进 +100% 双抗 |
| 财神 | 被动 | "每 **2.5秒** 获 **3~8** 枚 + 每2.5秒 **+2深海币**" | **每3秒 +4~7**〖用户 06-30 逐字"聚宝盆为4-7"〗；死亡 +9。**"+2深海币" 全库 grep 不到实装**（局内局外都没有）→ 删 |
| 财神 | 普攻 `detail` | "**6%**×攻击力×金币数"（回合制） | **2%**〖用户 06-30 逐字"普攻加成改为0.02"〗。★brief 早就是 0.02，detail 没跟 |
| 财神 | 骰子 `detail` | "释放梭哈后额外给 10%maxHp **永久护盾**" | 代码注释明写已删（3选1 下不可能同时带骰子+梭哈） |
| 财神 | 梭哈 `brief` | 每枚 **0.22A** | **0.18A**（detail 是对的，brief 错）+ 补"蓄力后逐枚投币、目标死改投最近敌" |
| 财神 | 招财进宝 | ★整块还是回合制的"**消耗20金币抽装备 + 每次涨价25%**" | 〖用户 2026-06-30 逐字〗首抽 1/2/3 费 **1★临时装备**（战后消失）→ 龟能消耗变 **160/240/460** → 每次释放 +1星 → 满 3★ 后消耗回 **60** 且每次回 1×ATK 生命。**代码早就是这么跑的** |
| 骰子 | 命运骰子 | "持续 **5秒**" | **持续到下次释放本技能**（`crit_fate_until = _t + 999.0`，再放先撤旧增益再重掷） |
| 彩虹 | 被动·绿光 | 回 "**2% 最大生命**" | 回 **2.5% 已损生命**（`(maxHp-hp)*0.025`）；蓝光护盾补"4秒" |
| 彩虹 | 普攻 | "**两段共 1.4A 魔法**" | **单段 0.9A 物理**（用户 2026-07-02 改过） |
| 彩虹 | 棱镜护盾 | 无时长 | **4秒** |
| 彩虹 | 全色风暴 `detail` | "对**全体敌**四段打击 + -15%护甲 5秒"（回合制） | **自身处半径140码风暴区 4秒**，每0.5秒 0.1A魔法+0.05A真伤，**-20%双抗**；期间龟能锁定 |
| 彩虹 | 反射 | "每弹射**递减 15%**，最低40%" | **共6段、每段相同、不递减**（权威文档 附录A-16） |

**核过、无需改**：钻石 普攻/坚不可摧/冲撞数值(DEF+MR+0.1A+9层流血)；财神 骰子(3~8金币+回8%)；
骰子 被动/普攻(0.9A+55×暴击率)/孤注一掷/稳定骰子(4+点数、递减10%)；彩虹 全色风暴 `brief`（本来就是对的）。

**★踩坑（严重）**：批量替换脚本里我在文案中写了 `\"…\"`，python 里就是**裸 ASCII 双引号**，
直接把 `pets.json` 打断（`Expecting ',' delimiter`）。因为 `json.load()` 断言写在循环**之后**，
坏文件已经落盘。→ `git checkout` 回滚，改成：
① 替换文本一律用中文引号 `「」`；② 加**安全闸**扫描替换串里的裸双引号，有就 `assert` 崩；
③ **每一步替换后立刻 `json.loads(cand)` 校验，通过才接受**，坏了当场炸、不写盘。

**验证**：import 零错 · 6 个测试全绿 · `diamond/fortune/dice/rainbow` sim 各 0 报错 · 存档 md5 未变。

---

### 轮 D · `gambler` `hunter` `pirate` `candy`（✅ 完成）

**★代码 bug：1 个（严重·改变玩法）——海盗「死亡钩索」触发条件与目标都错了**
- **原实装**：「**任意敌人**(非召唤)阵亡 → **存活的敌对海盗龟**钩索**最近敌**」
- **原版（回合制 `pets.json` passive.desc 逐字）**：「…**死亡时钩锁击杀者**，同样造成 25%最大生命值 真实伤害」
- **用户〖#15〗**：「掠夺我是说被动的**原版**海盗被动」→ 2026-07-09 加回。
- → **修为**：**海盗龟自己阵亡的瞬间** → 钩住**击杀它的单位** → 拉到尸位 90 码 + 25%**击杀者**最大生命·真实伤害。
- **自证** `tests/verify_pirate_hook`：击杀者精确掉 125（= 25%×500）/ 被拉到 90.0 码 / **非海盗阵亡不触发** / 源码级断言。
  〔踩坑：测试一开始 flaky（`got=188` vs `125`）——场景仍在 tick，**其它单位也在打 killer**，污染了伤害。
   隔离成「只留 pirate + killer 存活、清掉 killer 的 DoT」后，连跑 5 次全绿。
   另：`killer` 是评审假人，`_review_dummy` 会"受击即回满"把伤害抹平 → 测试里要显式关掉。〕
- 权威文档已留痕。

**文案 ≠ 实装，修 12 处**
| 龟 | 项 | 原文案 | 实装 |
|---|---|---|---|
| 赌神 | 被动·多重打击 | "追加一次额外打击，造成 **50%×ATK** 物理" | ★**根本不是追加一段伤害**：命中后 40% 概率**立刻以约3.3倍攻速再打一次普攻**（`atk_interval*0.30`）；连锁概率 ×0.8。补：选「命运之轮」→ 基础 60%（登场损 30%maxHp）；「赌注」→ 3秒内 +20% |
| 赌神 | 万能牌 | "**两段**共1.0A + 回 **0.25A** 生命 + **1种随机减益**" | 〖用户 2026-07-09〗"只造成**1段**伤害" → 1段 1.0A；回 **5% maxHp**；固定使目标**攻击力 -15%** |
| 赌神 | 赌注 | 没写时长 | 补：7段**穿透减伤**；释放后 **3秒**内多重 +20% |
| 赌神 | 命运之轮 | "**每2.5秒**抽取一个花色" | ★是**主动技**（龟能满释放才抽）。补：选此技**登场损 30% maxHp** 换基础概率 40%→60%；玩家抽到的花色**本大轮跨场累积**。删掉"深海闯关"（地下城早已删除） |
| 猎人 | 狩猎弹幕 `detail` | "可挡弹道**待接入**，当前直击近似" | 陈旧：`_basic_first_blocker` 早已接上 |
| 海盗 | 被动·掠夺 | `{HP}` 指代不清 + 钩索语义错 | 明确"**目标/击杀者**最大生命"；死亡钩索改成原版语义 |
| 海盗 | 朗姆酒 `brief` | "每 **2.5秒** 回9%、持续5秒、提升**护甲**" | ★**上一轮我只改了 detail，brief 漏了**。→ 每秒 4%、6秒、**双抗** |
| 糖果 | 被动·甜蜜吸取 | "**开战7.5秒**对**随机**敌人" | **登场时**对**最大生命最高**的敌人，扣其 25% maxHp 并**全额回复自己**（不杀，最低留1血）|

**我自己在这一轮里写错又当场改回的**：糖果被动我一度写成"偷取其 25% **攻击力**"，
核代码才发现是"扣其 **25% 最大生命值** → 全额回复糖果龟"。**写前必须核，不能凭印象。**

**核过、无需改**：赌神 卡牌射击；猎人 全部（猎杀被动/射箭+残血追猎/精准射击三合一/隐蔽薇恩Q式）；
海盗 弯刀(1.0A+自愈0.2A)/火炮齐射/海盗船；糖果 糖果拳/糖果锤/糖衣炮弹/糖果炸弹。

**验证**：import 零错 · 7 个测试全绿（新增 `verify_pirate_hook`）· 8 组 sim（4龟×评审/SHIP）0 报错 · 存档 md5 未变。

---

## 轮 E ✅ `bubble` `line` `lightning` `phoenix`（2026-07-10）

**★代码 bug：0 处。** 这一轮四只龟的**实装全部与用户原话吻合**，错的全在文案和我写的文档/注释。

**★文档/注释 bug（我自己写岔的）：2 处**
| 位置 | 原文 | 真相 |
|---|---|---|
| `RealtimeBattle3DScene.gd:4561` 注释 | 闪电改造普攻「魔法 **1.15×ATK**」 | 活代码 `_lightning_hop` ＝ `_atk_dmg(u, 0.6*fr)` → 主目标 **0.6A**。回合制 `lightningStrike: atkScale=1.15` 是**技能5段的总加成**，被我错抄成实时单道系数 |
| `28龟技能设计-权威.md` L1296 / A-24 | 同上，且标成"回合制原值" | 同上。**同一份文档 L1265 写的却是对的**（0.6+0.36+0.216≈1.176A，恰好逼近回合制 1.15A 总量）—— 自相矛盾 |
> 用户〖2026-06-28〗逐字只说：「得改造，是一次攻击一道，并有连锁闪电和叠被动」——**没给系数**。
> 故 0.6 / ×0.6 / 260码 ＝ T3 实装默认值，已归入 附录A-24（可调，不阻塞）。**没有动代码**（擅自改成 1.15 = 静默 +92% 强度，无用户授权）。

**文案 ≠ 实装，修 25 处**（`data/pets.json`，含 2 个直接漏给玩家看的占位符）
| 龟 | 项 | 原文案 | 实装（活代码） |
|---|---|---|---|
| 线条 | **画龙点睛 brief/detail** | ★★字面就是 `_lineFinishBrief_` / `_lineFinishDetail_` —— **占位符直接显示给玩家**（全 `pets.json` 仅此 2 处） | 打**墨迹层数最多**的敌人，(0.7 + 0.45×层数)×ATK 物理，**不消耗墨迹**〖用户封板〗 |
| 线条 | 被动·墨迹 | 上限 **5** 层；额外承受 **5% 魔法伤害** | 上限 **7**（选「墨水炸弹」→ **10**，`_ink_cap`）；**原伤害之外**每层额外 **5% 真实伤害**〖用户 2026-07-10 订死〗 |
| 线条 | 素描（普攻） | **三笔共 1.5A 物理** | **单段 1.0A 魔法**（`BASIC_ATK["line"]={"magic":1.0,"hits":1}`） |
| 线条 | 墨水炸弹 | 4段共 1.0A **物理** | 4段共 1.0A **魔法**；且选它会把墨迹上限抬到 10 |
| 泡泡 | 被动·泡沫 | 每 **2.5秒**，消 **15%** 回血 + **35%** 化魔法打**随机**敌 | 每 **3秒**，消 **10%** 回血 + **10%** 化魔法打**最近**敌（共消耗 20%） |
| 泡泡 | 泡泡束缚 | 束缚 **1.5秒** | **3秒**（`_cc_dur(tgt, 3.0)` + `bind_until`） |
| 泡泡 | 泡泡爆破 | 消耗**全部**泡泡值；打**目标所在竖排**；伤害 = **40%×消耗量** | 消耗当前泡泡值的 **40%**；打目标周围 **200码**内；魔法伤害 = **等同消耗量**（吃魔抗）+ 0.8A 物理 |
| 闪电 | 被动·雷电 `desc` | 每 **2.5秒**自动电击 | 每 **4秒**（`_ltimer >= 4.0`）。`brief` 本来就写的 4秒 —— 两字段互相矛盾 |
| 闪电 | 雷盾 | **0.9A** 护盾，**没写时长** | **3.0×ATK 护盾 / 5秒**〖用户 2026-07-07「雷盾120龟能，盾量等于3ATK」〗 |
| 闪电 | 普攻 detail | "连锁至最近 2 个敌人" | 是**依次接力**：主目标 → 其 260码内最近敌 → 再最近敌（不是都取施法者最近） |
| 凤凰 | 被动·涅槃 | 施加「**{0.67*ATK} 灼烧值**」 | 回合制模型残留。实时是**灼烧层**：`_default_burn_stacks` = round(0.67×ATK) **层** |
| 凤凰 | 普攻 detail | 「0.9A 魔法 + 0.53×ATK **灼烧值**，灼烧值**每秒衰减约 1/3**」 | ★整块陈旧。实时＝**持续喷火 channel**：每 0.5s 对身前 70° 扇形 `0.2×ATK×(1+攻速)` 魔法 + `0.07×ATK` 层灼烧；喷火时移速 ×0.5。灼烧每秒 =（层数 + 0.1%×maxHp×层数）魔法，**层数衰减到 80%**〖用户 2026-06-30「这三个都改为每次衰减到80%」〗。`BASIC_ATK["phoenix"]` 是**死数据**（凤凰走 `_phoenix_flame_channel`，从不进 `_do_basic`） |
| 凤凰 | 普攻 brief | 灼烧 **0.05×ATK** 层 | **0.07×ATK**（`PHX_FLAME_BURN_COEF`）〖用户 2026-06-30「每次普攻加灼烧层0.07ATK」〗 |
| 凤凰 | 熔岩盾 | **0.75A** 护盾 / **5秒** | **3.5×ATK / 4秒**〖用户 2026-07-07「熔岩盾改为3.5ATK」〗 |
| 凤凰 | 烫伤 detail | 0.7A 魔法 + 0.67×ATK **灼烧值** | ★brief 与 detail 互相打架。实装＝先破 50% 盾 → **1.5A 魔法** → **1.0×ATK 层灼烧** + 攻/防/抗 −15% + 治疗削减 50%（5秒）〖用户 2026-06-30「1.5ATK魔法伤害，灼烧层=1ATK」〗 |

**核过、无需改**：泡泡 普攻(3段1.5A物理)/泡泡盾(最脆友军1.8A·4秒·爆裂2.0A)/束缚减抗(X=1或2·上限30)；
线条 连笔(3秒/0.8A物理/30%真伤传导/墨迹同步)；闪电 涌动(5秒+50%真伤·立即1.23A)/雷暴(20道×0.11A魔法·各叠1层)；凤凰 强化涅槃(+50%攻速移速4秒·100%复活·永久+20%攻)。

**踩坑**
1. `line` 的 `passive.brief` 与 `passive.desc` **逐字相同**，`json.dumps` token 在原文里出现 **2 次** → 替换脚本的 `count==1` 断言当场崩。**这个断言救了我**：否则我会只换掉一处，留下另一处陈旧。→ 加了 `passive_both` 分支（断言 `count==2` 且 `brief==desc`）。
2. 顺着这个崩溃才发现 **`passive.brief` 是会显示的**（选龟界面 tooltip）→ 见上文「审计口径漏洞」，轮 H′ 补。

**验证**：`--import` 零错 · 7 个测试套件全绿 · **8 组 sim（4龟 × 评审/`SHIP=1`）rc=0、脚本报错 0** · 存档 md5 `3d954183…` 未变 · `_lineFinish` 占位符 0 残留 · phoenix 全字段无「灼烧值」残留。
> 注：`verify_dot_stacks` 是**纯报告型**（打印 `════ 自证完成 ════`，无 `ALL PASS` 标记，exit=0）。我第一次跑时按 `ALL PASS` 判它 FAIL —— **是我的判定 grep 错了，不是回归。**
