# RealtimeBattle3DScene.gd 拆分方案（2026-07-19）

> 起点 23329 行 → 当前 **22149 行**（−1180）。已完成三刀见下表。
> 本文给出剩余候选的**实测依赖数据**，供决定拆哪些、按什么顺序。
> 依赖数据由脚本扫出（非估计）：统计每个板块调用了多少主文件函数、用到多少主文件成员变量。

## 已完成

| 刀 | 内容 | 行数 | 风险 | 结果 |
|---|---|---|---|---|
| 死代码 | 24 个零调用函数 + `detect_sets()` + `_sk_gen_shield`/`_cur_skill_data` | −372 | 低 | 3 局压力局全绿 |
| 拆分① | 34 个贴图生成器 → `scripts/engine/vfx_textures.gd`（`class_name VfxTex`） | −667 | 低 | 函数总数对账零丢失 |
| 拆分② | 战中📊统计浮层 → `scripts/scenes/dmg_stats_panel.gd`（`class_name DmgStatsPanel`） | −165 | 低 | 抓图确认渲染一致；耦合全走依赖注入 |

## 剩余候选（按"风险/收益比"排序）

### A. 沙漏时停 `_ts_*` — 推荐优先
- **18 个函数 / 320 行**
- 外部函数依赖 **5 个**：`_eq_grant_energy` `_float_text` `_has_energy_system` `_skill_ring` `_world_pos`
- 外部成员变量 **6 个**：`_cam` `_follow_vfx` `_sim_tweens` `_t` `_units` `_world`
- **阻力**：`_ts_active` / `_ts_remaining` 被主循环 `_process` 直接读写（每帧判断是否进入时停分支）。拆出去后需要主循环通过接口查询，属**双向耦合**。
- 备注：体检报告估计此块 817 行，实测按 `_ts_*` 函数统计只有 320 行（3291–4107 那个区间里混着别的函数）。

### B. 调试场编辑器 `_edit_*`
- **40 个函数 / 631 行**，且是 **DEV-only 代码躺在出货战斗场里**
- 外部依赖仅 5 个：`_units` `_data_by_id()` `_make_unit()` `_cam` `_spool`
- **阻力**：`_unhandled_input` 里与统计面板 / 双路放置 / 触屏 pinch 的输入分发混在一起，**要先抽一层输入路由**才能整块搬走。
- 收益最大（行数 + 概念纯度），但改动面也最大。

### C. 战中统计浮层 — ✅ 已完成（2026-07-19）
抽出 6 个函数 + 4 个成员变量 + 6 个常量 → `DmgStatsPanel`，主文件 −165 行。
新类对外只有 `setup(ui_layer, units_of)` / `toggle()` / `render()`，不认识 `_units`／`_world`／战斗状态。

**纠正一处此前的归类错误**：`_stats_column` 属于**结算统计表**（`_build_stats_panel`，7 列表格），
不是战中浮层——早先的依赖扫描把两者混在了一起。且它用 `await get_tree()` 需要 Node 上下文，
与 RefCounted 类不兼容。故本次只抽战中浮层，结算表整体未动。

**结算统计表仍可作为后续候选**（`_build_stats_panel` / `_center_panel_deferred` / `_stats_column`，约 90 行），
但需以 Node 形态承载；且注释写明其样式是"1:1 照抄回合制 BattleEndScene._stats_table"，
即同样的表格构建代码在项目里仍有两份，抽出可跨版本复用。

### D. Sprite3D 样板（**已评估后暂缓**）
- `Sprite3D.new()` 全文 335 次，其中约 231 处是同一套 10 行模板
- 理论收益最大（−1500 行左右）
- **暂缓理由**：它最主要的价值——防止"tween 被 kill → `queue_free` 回调不执行 → 特效跨场泄漏"——**已被 2026-07-19 的"建场快照 + 换路兜底扫场"从根上解决**（实测每次换路扫掉 35–38 个遗留节点）。剩下只是行数收益，而 231 处写法各异，在 22k 行文件里盲改的风险与收益不对等，且无法逐一眼验 28 只龟的技能表现。

## 拆分作业的两条硬教训（2026-07-19 踩过）

1. **切函数块的边界扫描，遇到任何列首注释就必须停**。第一版只在"分隔符注释（`# ===` / `# ───` / `# ═══`）"处停，遇到普通 `#` 说明注释会继续往下吃，结果把紧跟其后的 `_reticle_flash` 连注释带函数头一起卷走，函数体残留在上一个函数里 → `Parse Error: already a variable named "r"`。
   **护栏**：①每个块内 `func` 行必须恰好 1 个（它自己）②块之间不得重叠 ③拆完做**函数总数对账**（原 N = 主文件 + 新文件）。

2. **新建带 `class_name` 的脚本，必须先跑一次 `--headless --import`** 注册全局类，否则运行期报 `Identifier "XXX" not declared`（与新增 PNG 需要导入是同一类问题）。

---

## 2026-07-24 · 从"搬板块"升级到"sim/演出分离"（大厂视角·结构治理）

> 起因：用户「新人完全接不了手·一个商业游戏这么管理吗」。去读了大厂做本品类(自走棋)的一手工程
> —— Riot《Determinism in LoL: Implementation》· Cliffski 确定性太空自走棋 · Godot 原生架构(abmarnie) · Gaffer《Fix Your Timestep》——
> 并**实测本文件当下的纠缠**。这组数字比"行数"更能解释为什么接手难：

| 病征 | 实测(RealtimeBattle3DScene.gd) | 大厂标准 |
|---|--:|---|
| 文件规模 | **25,206 行 = 全项目 66%**（2026-07-19 拆到 22149 后又**涨回来**——功能增生 > 抽层，抽层不持续就白抽） | 各系统独立模块 |
| sim/演出纠缠 | **1,354** 个 `tween_` · 627 `add_child` · 440 节点 `.new()` | sim 无节点·演出只读 sim 来渲染 |
| 伤害路径 | 两条(`_apply_damage` 13 调用 + `_apply_damage_from` 178 调用) | 一条 sim 伤害函数 |
| 单位=裸 dict | `u["hp"]` 式取值 **355** 处 | 类型化单位(根除 dict-做-key 递归哈希坑) |
| 时钟碎片 | `_t` **328** · `create_timer` 20 | 一个统一时钟(Riot 花 6 个月做的正是这件事) |
| RNG 未播种 | **388** 裸 randf/randi · `seed()` **0 次** | 单一受控 PRNG·种子化(Riot 用 game-id 播种) |

**关键认知**：CLAUDE.md §3 那串"地雷"(两条伤害路径要都改 / `_t` 跨路不重置 / 单位字典不能做 key / CI 偶发红)
**不是本项目的怪癖，是本品类被 Riot/Cliffski 命名过的已知失败模式**——每颗地雷 = 一个缺失的抽象。
Riot 的确定性坑(指针比较/地址序·碎片时钟·未播种 RNG)与本项目地雷**逐条对上**。补上边界，§3 一半地雷不复存在。

### 本次已落地（每刀独立·门禁全绿·prove-fail·本地 commit 不 push）
- **切片0 文档治理**(`995ea6d`)：归档 22 篇废弃档 → `archive/`，活树 81→59。零代码风险。
- **切片1 匹配单一受控 PRNG**(`90c1def`)：`Backend.make_match_rng()`（默认 `randomize()` 与线上一致，`TURTLE_SEED` 时确定）
  → 同种子同对手·灭「CI 随机敌队 vs 本地存档队」偶发红的根。`verify_match_seed`(5 断言)。门禁 82/82。
- **切片2 纯数学抽 SIM 模块(新抽取轴)**：新建 `scripts/combat/`，把散落重复的**纯算式**收进无节点、可 headless 单测的 SIM 模块。
  每个 primitive：主场景改调 + 单测(已知例 + 边界 + **与 god file 原式 200 采样逐位一致**) + **prove-fail** + 门禁全绿 + 本地 commit。**全程零玩法改动**(与原地逐位一致)。

  | 模块 | primitive | 去重站点 | commit |
  |---|---|--:|---|
  | `DamageMath` | `crit_multiplier` 暴击倍率 | 3 | `5c83e95` |
  | `DamageMath` | `resist_multiplier` 护甲/魔抗减伤曲线(K=40) | 3 | `f37d1d4` |
  | `DamageMath` | `effective_resist` 穿透后净抗性 | 3 | `3047317` |
  | `UnitScaling` | `level_multiplier` 等级乘数(+5%/级) | 2 | `b9bf83c` |
  | `CombatMath` | `decay_stacks` 叠层衰减80% · `hp_frac` 血量分数 · `cooldown_ready` 就绪度 | 9 | (本刀) |

  合计 **7 primitive · 去重 20 站点 · 3 个 SIM 模块**。`DamageMath` 已覆盖伤害结算数学核心(暴击/护甲曲线/穿透)。

### 这条轴的边界(已到)：清白高价值的抽取做完了
本文原有 A/B/C/D 是**搬板块**(时停/调试场/结算表/Sprite3D 模板)。上面是**另一条轴**(纯算式)。
频率扫描已确认：剩下的重复要么是 **演出/GUI**(`maxf(_hitstop,…)`)、要么是 **stdlib 平凡包装**(`minf(hp,maxHp)` 抽出比原地还长=纯凑数)、
要么是 **各技能各自的调参乘数**(`maxHp*0.20 / *0.08…` 系数各异·不是共享公式)。**没有更多"有名字的"纯 primitive 了**——再抽即凑数，故停。

### 剩余·为何必须人盯（不在自定步循环里 blind 完成）
**合并两条伤害路径 / 类型化单位 / 统一时钟 / 战斗 RNG 全种子化(388 处)** = 会改**玩法手感**，
且要逐一眼验 28 龟技能表现——headless 门禁覆盖不到「手感/像素/28 龟平衡」。这些必须**用户 F5 盯着改**。
安全的抽取(纯算式、去重、行为与原式逐位一致)可继续自动推；一旦触及会改手感的，停下等人。

---

## 2026-07-25 · 装上棘轮——让"抽层不白抽"（本轮·规范化）

> 起因：用户「你搞了快 20 小时，一看什么都没动？不是说好像大厂一样所有东西有规范吗」。
> 复盘发现真问题不是"没拆"，是**拆了会涨回来**：本文档 2026-07-19 把主文件 23329→22149(−1180)，
> 到 2026-07-25 又涨回 **25272**——**没有强制，抽层就是白抽**。大厂靠的不是自觉，是门禁。

### 已落地（本轮·纯工具/文档·零玩法风险·门禁全绿）
- **架构预算棘轮 `tools/arch_budget.py` + `arch_budget.json`（进门禁）**：
  主文件冻结在 **25272 行**——**只减不增**。往里加代码(哪怕 +1 行)门禁就红 → 逼"想加先拆"。
  真拆小了把台账数字改到新值(改小才过) = 单调下降、可审计的还债账。单函数 ≤250 行同理(2 个超标者登记在册)。
  prove-fail 已验(把台账调低/删豁免 → 红)。**这是本文档三刀拆完又涨回来这件事的根治。**
- **代码风格门禁 `tools/style_lint.py`**：全 tab / snake_case / PascalCase 三个当前 100% 成立的不变量焊死，永不漂移。
- **工程规范单一权威 `docs/工程规范.md`**：每条规范→它的强制门禁，收编散落约定；Definition of Done。

### 下一步还债（棘轮已就位·每拆一刀把 arch_budget.json 的 25272 改小）
候选与依赖数据见本文 §A/B/C/D。**均需 F5 盯**（理由见上一节）：
- **`_edit_*` 调试场(51 函数/812 行·L21763~22575)**：收益最大，但 `_edit_unit_at_screen` **被正式对局点龟弹面板复用**、
  且 `_unhandled_input` 里编辑/统计/双路/触屏 pinch 混在一起——**必须先抽输入路由层**，且有 ~24 函数要架桥。dev-only+`verify_debug_arena` 兜底，但输入路由碰正式对局，需人盯。
- **`_ts_*` 沙漏时停(16 函数/304 行·L4946~5253)**：近乎连续块，但 15 个状态变量被 `_process` 每帧直接读写(双向耦合)，
  且**视觉半边(overlay/粒子/时钟/glow)确定性门禁盖不到**、时停 t=10s 才触发(冒烟/确定性测试都没跑到)→ 抽了要 F5 看时停演出没坏。
- 安全网：纯代码搬迁行为不变 → `verify_battle_determinism` 指纹逐字节不变即证 sim 等价；但**视觉/手感仍须 F5**。
