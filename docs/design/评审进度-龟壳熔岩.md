# 龟壳 / 熔岩 评审 + 自主推进进度 (2026-06-30)

> 上线 11 龟最后 2 只。龟壳已审定提交，熔岩自主推进中。含本轮新增的全局规则 + 资源条系统性修复 + 决策日志。

## 状态总览
- **龟壳 shell** ✅ 审定提交（commit 55ab898）— 待用户 F5 复审
- **熔岩 lava** ✅ 审定（自主审计+修，commit d7a80ae）— 待用户 F5 审
- **资源条系统** ✅ 系统性修复（全龟受益）
- 上线 11 龟：9 只已审定（小龟/石头/竹叶/天使/寒冰 + 财神/彩虹/闪电/凤凰）+ 龟壳 = 10，剩熔岩

## 龟壳 shell（SSS · 近战 射程70）
- **普攻**：1ATK，**物理↔真实交替**（连续普攻轮流），主目标 **120px 内其他敌溅射 50%**（同类型）。〔改自原回合制网格"相邻"溅射 → 实时范围溅射〕
- **被动 气场觉醒**：
  - 觉醒（不变）：登场 10 秒 → ATK/护甲/魔抗/最大生命/吸血/反伤 各 +12% + 暴击 +25%
  - 储能循环（大改）：储能 6 秒（受伤 100% 存，上限 50% maxHp）→ **缓慢黄色能量冲击波**（扩散 520px/1.8s，触碰敌一次 储能×40% 物理）+ **金色储能护盾**（储能×80%，5 秒线性流失，`_auraShieldVal` 单独吸收）→ 15 秒冷却 → 循环。开战即起储能。
- **视觉**：脚下白方块/接触波纹去掉，**只留干净深色软影**（影子贴图从 GradientTexture2D FILL_RADIAL〔方角 bug〕改 Image 真圆）。

## 本轮新增全局规则
- **真实伤害也吃暴击**（`_apply_damage_from` raw 路径；对齐"暴击全龟通用"；真伤仍无视护甲/减伤）。DoT（burn 走 `_apply_damage`）不暴击。
- **灼烧/中毒/流血 每秒衰减统一 80%**（原 burn 2/3，毒/血 3/4）；灼烧飘字改标准魔蓝 #4dabf7。

## 资源条系统（系统性修，原实时版全没接通）
- 病根：实时单位字典**没接 `passive` 键** + 资源字段名跟 HpBar 回合制端口对不上（实时 `rage`/`star_energy`/`store_energy`/`bubble_store` vs 端口 `_lavaRage`/`_starEnergy`/`_auraEnergy`/`bubbleStore`）→ 所有资源条都不显示。
- 修：单位字典补 `"passive": d.get("passive",{})` + `update_state` 前镜像 `_auraEnergy←store_energy` / `_lavaRage←rage` / `_starEnergy←star_energy` / `bubbleStore←bubble_store` / `_stoneDefGained←(base_def-stone_init_def)` + `_initDef←stone_init_def`。
- → **龟壳储能条 / 熔岩怒气条 / 石头护甲进度条 / 星能 / 泡泡 全部显示**（颜色走 HpBar 端口：储能/星能橙黄、怒气橙红、护甲 tan/金）。

## 评审工具
- **REVIEW_DUMMY_ATTACKS**（默认 true）：假人会还手（去 no_basic/no_move）+ 受审龟免死 → 看挨打类被动（龟壳储能/熔岩怒气）的完整循环。
- **主菜单「🛠 调试场」**（commit 2fba9b3）：运行时自由摆位测试场（选龟/左右队/点击摆/拖拽/右键删/假人血±100/掉死/开始-编辑-清空）。

## 决策日志（自主/用户拍板）
| 项 | 决策 | 来源 |
|---|---|---|
| 龟壳储能/护盾系数 | 冲击波 储能×40% / 护盾 储能×80% / 上限 50%maxHp | 用户"按回合制数值" |
| 首次储能起点 | 开战即起 | 用户 |
| 真伤暴击 | 全局吃暴击（选项1） | 用户 |
| 龟壳冲击波颜色 | 黄色能量波 | 用户 |
| 龟壳脚下 | 只留影子（去白接触波纹/队色环） | 用户 |
| 龟壳普攻溅射 | 50% / 120px | 用户认默认 |

## 熔岩 lava（S · 远程）✅ 审定（自主推进 commit d7a80ae，待用户 F5）
- **普攻 熔岩弹**：0.9ATK魔 + 8%目标最大生命 + 0.67ATK灼烧（层数DoT）〔原丢了8%HP分量、灼烧错用0.5系数 → 修〕
- **地裂**：全体0.6ATK魔 + 魔抗-20%/5s ｜ **岩浆涌动**：1.5ATK魔 + 0.8ATK永久盾 ｜ **熔岩喷射**：全体3段0.2ATK魔 + 0.67ATK灼烧〔原**根本没派发**，落回地裂 → 补〕 ｜ **强化熔岩之心**：开战即100怒气立即变身〔原**没实装** → 补〕
- **被动 熔岩之心**：怒气（造伤25%+承伤20%，满100）→ 变身**火山龟15秒**（maxHP+ATK×2.5、攻防抗+ATK×0.2 flat，数据驱动）+ 火山技能组 + 入场爆发 → 回退。〔变身时长 6s→15s、属性从瞎写%改数据驱动、**变身回退射程bug修(230→400)**、HP漂移修〕
- 灼烧全走层数DoT；怒气条已显（橙红，HP条下）。
- ⚠️ F5 调感：取景把单位挤左上（远程 kite + 假人追，非熔岩 bug）；15秒变身时长 + 爆发数值 + 变身 melee 冲锋手感 需真机看。
