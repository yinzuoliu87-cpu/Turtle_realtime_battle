# 待定计划 · 局外/输入/双路（用户 2026-07-11 反馈批次）

> 用户一批实机反馈 + 追加澄清。能解决的已解决（见「已修」），需真机验证/待设计的进待定清单。

## ✅ 已修
- **#1 点龟开信息面板→关掉后屏幕整个灰**：`_show_unit_info_panel` 建了全屏灰底 `backdrop`(ColorRect 0,0,0,.45)，把面板作为它的子节点；`_close_info_panel` 只 free 了面板(子)没 free backdrop(父)→灰底残留。修：关闭时 free 面板的父(backdrop)。
- **#5 装了装备左右头像框什么都没显示**：头像框渲染+装备注入都对，但 `_build_team_panels()` **只在非双路分支调**，正式对局走双路早退绕过→双路整套头像框(连装备)从没建。修：`_spawn_dual_lane` 补 `_build_team_panels()`。
- **#2 调试场加装备**：加 `REVIEW_EQUIP` 常量(baked·不用 env)。非空时给受审龟装上→看显示+效果。本板设 `["p2eq_001","p2eq_016","p2eq_034"]` 演示。
- **#2b 调试场装备星级可调**：加 `REVIEW_EQUIP_STAR` 常量(1-3, 默认2)。改它即换测试星级。
- **#3 还在打召唤物却突然弹下一战场**（用户澄清「不合理」）：团灭判定原**故意排除所有召唤体**→有战斗型召唤(大熊/海螺虫/骷髅/小将/机甲/浮游炮)活着也判「团灭」提前破蛋。修：`_dl_side_alive` 改为**只排除惰性召唤**(`_DL_INERT_SUMMON` = 糖果炸弹/水晶球，否则会续着卡死破蛋窗口)，战斗型召唤算存活→打完召唤物才推进。（`_check_end` 单路判定本板未动，正式对局走双路；如单路也要一致再说。）
- **#6 安卓点击位置下移**（用户澄清：龟爪【尖】对着手指是对的，但判定落在龟爪【中心】=偏低半个光标）：查实**龟爪光标**用 `get_viewport().get_mouse_position()`(爪尖落此点=手指，正确)，而**命中**用 `event.position`(安卓触摸时偏到爪中心)→两者在安卓上不同源。修：命中改用 `get_mouse_position()`，与光标同源。**桌面两者相等无影响**；⏳ **需真机确认**是否已对齐（若仍有残余偏移=SubViewport 缩放，另查）。

## 🔵 待做（design/需方向）
- **#2c 调试场「实时」选技能/星级**（用户：调试场也选不了技能、装备选不了星级）—— 现状是 **baked 常量**(`REVIEW_SKILL_IDX` 选技 / `REVIEW_EQUIP` + `REVIEW_EQUIP_STAR` 选装备星级)，改常量重出包即换，但**不能在 APK 内实时点选**。要在调试场内实时切，需做一个**调试面板 UI**（技能 0-3 单选 + 装备多选 + 星级 1-3），设本地变量→重载场景生效。是一块小 UI 工程，待排期。**当前**：告诉我要测哪个技/哪几件装备/几星，我改常量重出包。
- **#4 背包(局外 InventoryScene)UI 待设计** —— 功能可用但 UI 未成型（无可视格边框/龟身槽是文字/无拖拽）。设计工程，待用户给方向后专门做一轮。
