# 待定计划 · 局外/输入/双路（用户 2026-07-11 反馈批次）

> 用户一批实机反馈。能解决的已解决（见下「已修」），不能立即定/需真机的进本待定清单。

## ✅ 已修（本板）
- **#1 点龟开信息面板→关掉后屏幕整个灰**：`_show_unit_info_panel` 建了全屏灰底 `backdrop`(ColorRect 0,0,0,.45)，把面板作为它的子节点；`_close_info_panel` 只 `queue_free` 了面板(子)没 free backdrop(父)→灰底残留。修：关闭时 free 面板的父(backdrop)。
- **#5 装了装备左右头像框什么都没显示**：头像框渲染(`_make_team_frame` 含装备图标行)+装备注入(`_inject_equipment`)都对，但 `_build_team_panels()` **只在非双路分支(L1051)调**，正式对局走双路(`_spawn_dual_lane`)早退绕过→双路整套头像框(连装备)从没建。修：`_spawn_dual_lane` 补 `_build_team_panels()`。
- **#2 调试场加装备去调试**：加 `REVIEW_EQUIP` 常量(baked·不用 env)。非空时给受审龟装上→看装备显示+效果。本板设了 `["p2eq_001","p2eq_016","p2eq_034"]` 演示；改这个常量即换测试装备（空[]=裸装看纯技能）。

## 🟡 待用户拍板
- **#3 场上有召唤物活着但龟蛋围栏已打开** —— **是有意设计，不是纯 bug**。双路团灭判定 `_dl_side_alive`(L1503) 与单路 `_check_end`(L11906) 都**故意排除召唤体**(`is_summon`)，注释「召唤体不计入胜负判定」。理由：被动/惰性召唤(水晶球 crystalball / 糖果炸弹 candybomb / 无人机 drone)否则会把队伍「续」着→破蛋10s窗口卡死(softlock)。
  - **选项 A（保持现状）**：召唤物不算数，龟统领+小将(小将算数)全死即破蛋。简单、防卡死。
  - **选项 B（战斗型召唤算数）**：让**能打的**召唤(大熊 bear/海螺虫 worm/骷髅 skeleton/召唤小将 minion)保持围栏关，只排除惰性的(水晶球/糖果炸弹/无人机)。更符直觉，但要按 `summon_kind` 分类 + 决定单路 `_check_end` 是否同步改，稍复杂。
  - ⏳ **待用户选 A/B**（选 B 我按 summon_kind 白名单实现）。

## 🔵 待做（design/需真机）
- **#4 背包(局外 InventoryScene)UI 待设计** —— 功能可用但 UI 未成型（无可视格边框/龟身槽是文字/无拖拽）。是一块**设计工程**，非快修。待用户给设计方向（布局/交互）后专门做一轮。
- **#6 安卓触摸点击位置下移（龟爪光标不准）** —— 取点数学自洽（光标/血条/命中同坐标系，桌面正常），偏移几乎肯定在**安卓触摸→画布映射**：`stretch=canvas_items`+`aspect=expand`+沉浸模式下，触摸坐标可能含**状态栏/刘海安全区顶部内边距**没被补偿→恒定下移。
  - **候选修**：读 `DisplayServer.get_display_safe_area()` 算顶部内边距，从点击/光标位置减掉；或直接处理 `InputEventScreenTouch`（别依赖 emulate_mouse_from_touch）。
  - ⏳ **需真机诊断确认**（桌面测不出）：真机上看①龟爪尖 vs 手指②血条 vs 龟身③log `event.position`/`get_display_safe_area()`/`window.size` 的 Y 差 = 状态栏高？确认后再落修（防瞎补反而更偏）。
