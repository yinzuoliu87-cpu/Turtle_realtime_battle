# 项目全量清理 · 轮次账本（2026-07-10 起）

> 用户〖2026-07-10〗:「之后你就自己排轮次清理整个项目，确保完全干净，所有部分同步到唯一知识库里，
> 知识库不再有什么分歧点而是完全拿属性和效果说话，没有任何分歧」

## 目标（可验收，不是口号）
1. **代码干净**：没有死函数 / 死数据 / 撒谎的注释 / 开发术语泄漏到玩家面前。
2. **文档唯一**：`docs/design/28龟技能设计-权威.md` 是**唯一**知识库；其余账本要么合并进去、要么明确标成「历史记录，不是事实源」。
3. **零分歧**：知识库里的每个数值都能**直接写进代码**，且与 `pets.json` 文案、活代码**三方一致**。
   不出现「待你拍 / 参考回合制 / 大概 / 近似」。
4. **机器守卫**：一致性由脚本/测试保证，不靠我"这次很仔细"。

## 判定规则（沿用 §0 R0，不变）
用户 transcript 逐字原话 > 权威文档已锁定值 > 活代码。拿不准 → 标 ⚠ 列证据，**绝不替用户拍板**。

## 轮次
| 轮 | 内容 | 状态 |
|---|---|---|
| **K1** | **★素材树治理**（删零引用 / 去重 / 长条图重排成网格 ≤4096 / 修循环 tween / 收紧导出）| ✅完成 |
| **K2** | 重出 APK v3（170.2→**127.3 MB**）+ 6 项出包自检全过；**等用户真机确认** | ✅完成(待真机确认) |
| **J1** | **死代码 / 死数据清理** | ✅完成 |
| **J2** | **三方一致性对账器**：写脚本把 `权威文档 ↔ pets.json ↔ 活代码` 的每个数值拉平对账，输出差异表；差异清零 | ⬜ |
| **J3** | **知识库同步**：把轮 E~H′ + 轮 I 抓到的全部实装事实回填权威文档；附录A 调参表补齐；删掉一切模糊措辞 | ⬜ |
| **J4** | **文档收口**：陈旧账本标注/归档，`MEMORY.md` 与 memory 文件同步，最终全量回归 + 收官报告 | ⬜ |

## K1 结果（实测）
| 项 | 结果 |
|---|---|
| 删除资源 | **250 个**（121 零引用 + 111 个 `_sm/` + 11 中文名重复副本），保留 `NotoEmoji-OFL.txt` 许可证 |
| `assets` 体积 | 181 MB → **129 MB** |
| 超 4096 的贴图 | 11 张 → **0 张**（重排成网格，像素逐帧断言逐字节不变）|
| `Infinite loop` 报错 | 120 秒战斗 2 条 → **0 条**（8 处循环 tween 全部 `bind_node`）|
| **APK** | 170.2 MB → **127.3 MB**（`turtle-realtime-v3.apk`）|
| 新增机器守卫 | `verify_sprite_sheets`（贴图不许超 4096 / 帧数不许变）；`Infinite loop detected` 进 FATAL 表 |
| 回归 | `run-tests.sh` **ALL PASS (11/11)**，存档 md5 未变 |

## K1 事实底账（工具 `c:/tmp/asset_audit.py`，带阳性/阴性自检探针）

> ⚠ 我上一次的素材普查结论是「**没有一个无引用文件**」，**那是错的**。
> 病因：只做全路径比对，而代码/json 里大量是**相对路径**（`SPRITE_DIR + "skills/x.png"`、`"icon": "skills/x.png"`）。
> 第一版工具把 491 个文件误判成无引用；改成**后缀匹配**并加探针后才稳定。**结果太吓人时先怀疑工具。**

| 项 | 数字 |
|---|---|
| 资源总数 | 690 |
| 被引用 | 377 |
| **零引用** | **313 个 / 69.4 MB** |
| **内容重复组** | **37 组**（`angel.png == 天使龟v1.png`、`equip/x.png == equip/_sm/x.png` …）|
| 未压缩 RGBA8 全量展开 | **945.1 MB** |
| 宽 > 8192 的立绘（几乎所有安卓机必黑） | 7 张（`shell` 10000、`fortune` 9000、`headless`/幽灵 8500）|
| 宽 4097~8192（4096 上限的机器会黑） | 16 张 |

零引用的大头：回合制的竞技场背景 `bg-sakura/ruins/shipwreck/firefly/cave-alt/underwater/oasis/ice`（实时版是 3D 场地，不用）、
`giant-crab*`、`dungeon-*` 装备图、`_sm/` 缩略图副本、中文名立绘副本。
**它们全部被打进了 APK**（`export_filter="all_resources"`）。

### 与回合制版的隔离：**已经是隔离的**
全仓库对 `turtle-battle` / `poc-phaser` 的 6 处提及**全在注释里**（出处标注），
无任何真实路径依赖：`autoload/DataRegistry.gd:12`、`scripts/systems/visual_constants.gd:5`、`data/_meta.json:3`。
实时版有自己的 `assets/` 与 `data/`。**乱的是 fork 时整包拷进来的残留，不是跨仓库耦合。**

### K1 执行顺序（每步单独 commit，可回滚）
1. 删 313 个零引用文件 —— **删前先跑 `--import` + `run-tests.sh` + 9 场景冒烟**，证明没有动态引用漏网。
2. 去重 37 组 —— 保留代码实际引用的那份，另一份删。
3. 长条图 → 网格重排（**像素逐字节不变**），最大边 ≤ 4096；加载端补 `vframes` 推导。
4. `export_filter` 从 `all_resources` 收紧，或确保 `.gdignore`/排除规则覆盖。

## J1 待查清单（已在轮 E~I 顺手记下的可疑死物，**每条都要先证明**）
- `BASIC_ATK` 里的死条目：`phoenix`（走 `_phoenix_flame_channel`，从不进 `_do_basic`）、`cyber`（走贯穿激光分支）、
  `headless` / `shell` / `chest`（注释已自称"兜底"）。→ 证明后删或改成显式 `null` + 注释。
- `lavaEnhancedRage`：`RealtimeBattle3DScene.gd:6150` 注释自称"已废弃"，且不在任何 `skillPool` → `_apply_spawn_passives` 里的分支永不可达。
- `_sk_crystal_spike` 的分派（`crystalSpike` → `_sk_dmg`）：水晶刺是普攻，走 `BASIC_ATK` + `_on_basic_hit`，该分派可能永不触发。
- `_sk_phoenix_purify`（火焰净化）：用户〖#17〗"去掉火焰净化"，函数还在。
- `cyber_ai_charge`：**只写不读**（用户没定义收益）→ 不删，标 ⚠ 保留 + 文档写明「待设计」。
- `skill_energy.gd` 与 `pets.json.energyCost` 的重复事实源（轮7 已对齐 3 处，需复查全表）。
- `_review_*` 常量族：`REVIEW_SHOWCASE` / `REVIEW_SKILL_IDX` 等是否仍有引用。
- 未引用资源普查（上次结论是"没有一个无引用文件"，需在删代码后复跑）。

## 用户待拍板（我不猜，挂在这里）
1. **缩头召唤池**含 SS 级的**无头龟**，与旧文案「A级及以下」冲突；且 A 级及以下实际 19 只，池里只有 12 只。要调整吗？
2. **水晶球在水晶龟阵亡后不会消失**（全库只有缩头龟做了随从同步死亡）。要加同步死亡吗？
3. `REVIEW_DEMO_DEFAULT` 目前仍是 `true`（编辑器/F5 里进评审场）。你审完龟之后要不要翻成 `false`？
   （**不影响 APK/Web**：release 包已经靠 `OS.is_debug_build()` 自动关掉。）

## 铁律（每轮都适用）
- 只动实时版 repo，绝不碰回合制版。一轮一 commit，**不 push**。
- 删任何东西前，先用**带阳性/阴性自检探针**的脚本证明它是死的；工具先自证，再信结论。
- 写「已实装 / 已实测」前必须真跑出来。`bash run-tests.sh` 必须 ALL PASS，存档 md5 不变。
