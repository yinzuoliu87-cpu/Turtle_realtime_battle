# 装备演示 / 测试配置登记表（EQDEMO）

> 每件需要专属演示的装备，在这里登记它的**完整测试配置** + **一键运行命令**。
> 以后要看某件装备效果 = 照抄命令、设好环境变量、实际跑起来看，不用每次重新搭场景。

## 机制：EQDEMO 测试台

战斗场景 `RealtimeBattle3DScene` 内置一套 env 门控的装备演示台（`REVIEW_DEMO` 评审模式的一个分支，正常游戏不受影响）。设 `EQDEMO_EQUIP=<装备id>` 即启用：

| 环境变量 | 作用 | 默认 |
|---|---|---|
| `EQDEMO_EQUIP` | 要演示的装备 id（如 `p2eq_034`）；**必填**，设了才进演示台 | — |
| `EQDEMO_STAR` | 装备星级 1/2/3 | `2` |
| `EQDEMO_CARRIER` | 携带者龟 id | `basic`（友方假人） |
| `EQDEMO_DUMMYHP` | 右侧敌方假人血量 | `5000` |
| `EQDEMO_FAST` | =1 时大熊约5秒快速出现放波（专给看特效，仅 p2eq_034） | 关 |
| `EQDEMO_WAVEROT` | 冲击波波纹朝向偏移（度），调弹道方向用 | `0`（顺行进方向） |
| `EQDEMO_ATTACKER` | =1 携带者正常移动+普攻+技能（测 on-hit/on-cast 类件；否则是不动的持装备假人）| 关 |
| `EQDEMO_SKILL` | 强制携带者只放指定技能 key（如 `rainbowStorm`），测 AoE 技×装备交互 | 无（AI自选）|
| `EQDEMO_COUNT` | 给携带者塞 N 件同款（测多件叠加）| `1` |
| `EQDEMO_ENEMY1` | 敌1距携带者的码数 | `210` |
| `EQDEMO_GAP` | 两敌间距（码）；溅射/AoE 类件把假人摆近 | `500` |
| `EQDEMO_ENEMY_ATTACKS` | =1 敌方假人逼近+普攻打携带者（携带者转不死沙包）；演**受伤/挨打类**(硬化/荆棘/铁壁/锚/冰封) | 关 |
| `EQDEMO_ALLIES` | 友方假人个数（半血裸装，站桩会掉血）；演**团队增益/治疗类**(全队盾/连接/全队回血) | `0` |

**默认场景布局**（写在 `_spawn_teams` 里）：
- **左 = 1 只友方假人携带者**：`no_move` + `no_basic` + 移速0 + 50万血，只负责持装备触发效果（不会自己乱跑/攻击，方便观察召唤/触发）。
- **右 = 2 只敌方假人**：`no_move` / 无技能 / 30 双抗 / 会掉血；**相距 500 码**（`_cx+60 + i*500`），方便看直线/位移/穿透类效果。
- 装备注入：`_inject_equipment` 对 `REVIEW_DEMO` 早返回，但 **EQDEMO 例外**（否则携带者拿不到装备）。

## 运行命令

Godot 可执行文件：`C:\Users\Louis\Desktop\Godot_v4.6.3-stable_win64.exe`
仓库：`c:\Users\Louis\Documents\GitHub\turtle-realtime-godot`

**开窗口看（推荐，墙钟卡时长，能看动作/位移）**——Git Bash：
```bash
timeout 42 env EQDEMO_EQUIP=p2eq_034 EQDEMO_STAR=2 \
  "/c/Users/Louis/Desktop/Godot_v4.6.3-stable_win64.exe" \
  --path "c:/Users/Louis/Documents/GitHub/turtle-realtime-godot" \
  res://scenes/RealtimeBattle3D.tscn --resolution 1280x720
```
> ⚠ 别用 `--quit-after N`（按帧算，窗口没锁 60fps 会提前关）；用 `timeout <秒>` 卡墙钟时长。
> ⚠ headless（`--headless`）只用来抓 parse / 运行时报错，**sim 时间不可靠**，看不到真实节奏。

## 已登记装备

### p2eq_034 玩偶小熊（5费传说） — 大熊召唤 + 小菊式冲击波
- **配置**：`EQDEMO_EQUIP=p2eq_034 EQDEMO_STAR=2`（默认即此配置：左友方假人携带者移速0，右2敌假人相距500码）。
- **预期时间线**（2星，需3层）：≈4/8/12s 各出一只小熊踢击飞 → 满层后携带者**金光蓄力1.2s** → 召**大熊** → 大熊走到假人**熊掌**（攻速0.5=每2秒一下）攒2层 → **冲击波**（600码、波速500px/s 慢）直线穿过相距500的两假人：1.5ATK物理 + 击飞0.8s + 滞空期朝熊平滑拉回（击飞态不能移动/充能）。
- **看点**：小熊节奏、蓄力停顿、大熊攻速、冲击波速度/射程/穿透、拉回是否平滑不重叠。

### p2eq_001 锈蚀短剑（1费普通） — 就绪型武器(射程=携带者射程)
- **配置**：`EQDEMO_EQUIP=p2eq_001 EQDEMO_STAR=2 EQDEMO_CARRIER=hunter`（远程携带者射程400；敌1@210码在射程内、敌2@710码在射程外）。近战演示改 `EQDEMO_CARRIER=basic EQDEMO_ENEMY1=60`（射程70内）。
- **预期**：每3秒就绪，射程(=携带者射程)内有敌→斜砍(0.6/0.75/1.0×ATK+40/60/100×暴击率)物理；面向镜头斜砍斩弧。
- **看点**：3秒节奏、只打射程内的敌(远的不打)、斜砍弧角度。

### p2eq_003 锋利鲨齿（3费稀有） — 命中溅射(150码)+圈圈扩散
- **配置**：`EQDEMO_EQUIP=p2eq_003 EQDEMO_STAR=2 EQDEMO_ATTACKER=1 EQDEMO_GAP=70`（攻击型携带者普攻→溅射旁边70码的假人）。
- **AoE 技交互演示**：`EQDEMO_CARRIER=rainbow EQDEMO_SKILL=rainbowStorm EQDEMO_ATTACKER=1 EQDEMO_GAP=70`（彩虹龟全色风暴每 tick 命中→触发溅射+扩散环）。
- **预期**：每次命中→对目标 150 码内相邻敌溅射 15/28/50%×本段伤害物理 + 金色扩散环；多件叠加。
- **看点**：溅射到相邻假人、圈圈扩散、AoE 技每段都触发。

### p2eq_004 暴君之牙（4费史诗） — 处决 + -999999真伤大字
- **配置**：`EQDEMO_EQUIP=p2eq_004 EQDEMO_STAR=2 EQDEMO_CARRIER=hunter EQDEMO_ATTACKER=1 EQDEMO_DUMMYHP=500`（低血假人才够快打进斩杀线）。
- **预期**：hunter 打假人到血 <斩杀线(5/7/10%maxHP+10/15/40%×暴击率) → 处决秒杀 + 跳固定 **-999999 真伤大字**（实际伤害=剩余血）+ 回 20 龟能飘字。
- **看点**：处决触发时机、-999999 大字、回龟能。

### p2eq_006 千刃风暴（4费史诗） — 蓄力→身后召一排剑→前移穿全场
- **配置**：`EQDEMO_EQUIP=p2eq_006 EQDEMO_STAR=2 EQDEMO_CARRIER=rainbow EQDEMO_ATTACKER=1 EQDEMO_SKILL=rainbowStorm`（施法触发 on_cast → 剑阵；用 rainbowStorm 保证稳定放技）。
- **预期**：施法 → 蓄力 0.45s → 身后一排 7 剑错峰淡入放大生成 → 调转方向对准目标 → 剑阵 650px/s 前移穿过全体敌，逐个命中。
- **看点**：生成→调转→飞出三段节奏、剑尖朝向、命中全体。

### p2eq_008 双穿珊瑚刺（1费普通） — 每5次普攻射珊瑚刺弹体(物理+魔法)
- **配置**：`EQDEMO_EQUIP=p2eq_008 EQDEMO_STAR=2 EQDEMO_CARRIER=hunter EQDEMO_ATTACKER=1 EQDEMO_DUMMYHP=1000`（攻击型携带者攒5次普攻→射；1000血好让两段数字都清楚）。
- **预期**：每 5 次普攻（不算多段命中）→ 射一发珊瑚刺弹体飞向最远敌 → 物理红字(下,倍率×ATK过护甲) + 魔法蓝字(上,%目标maxHP过魔抗) **同帧贴近堆叠**跳。
- **看点**：5次普攻计数、弹体飞行、两段颜色(红物理下/蓝魔法上)、贴近不重合。

### p2eq_009 宽刃弯刀（3费稀有） — 刃能满→预警扇区→月光斩
- **配置**：`EQDEMO_EQUIP=p2eq_009 EQDEMO_STAR=2 EQDEMO_CARRIER=hunter EQDEMO_ATTACKER=1 EQDEMO_ENEMY1=300 EQDEMO_GAP=350`（近敌300码给hunter普攻攒刃能、远敌650码在500~800扇区内被斩）。
- **预期**：普攻攒刃能(图标下充能条)满100 → 预警环形扇区(500~800码×60°朝敌方) → 5帧黄色月光弯月闪电斩 → 区内敌真伤(白上)+物理(红下)同帧贴近堆叠。
- **看点**：充能条、预警扇区形状/朝向、月光斩5帧动画、两段数字白上红下贴近。

### p2eq_010 激光长刃（5费传说） — 第二普攻(激光长刃)：扇形扫斩+单体竖劈冲击波
- **配置**：`EQDEMO_EQUIP=p2eq_010 EQDEMO_STAR=2 EQDEMO_CARRIER=hunter EQDEMO_ATTACKER=1 EQDEMO_ENEMY1=200 EQDEMO_GAP=520`（hunter射程400；敌1@200码在扇形内、敌2@720码在外→扇形只命中1人→触发竖劈；看完整链路）。
  - **看扇形扫多人**变体：`EQDEMO_ENEMY1=200 EQDEMO_GAP=150`（两敌都在射程内→扇形同扫2人、不触发竖劈）。
  - **看3★大扇形**：`EQDEMO_STAR=3`（半径×2=800码）。
- **预期**：独立计时器每 0.7s（hunter攻速）触发一次激光长刃 → **120°红色扇形斩击**(半径=射程400, 白热芯激光刃扫过+扇形辉光)对区内敌 15/32/200+0.6/1/8×ATK物理 → **若只命中1人**：0.2s金光蓄力 → **竖劈红白激光刃**(带剑气拖尾残影)冲出2×射程、半宽80、命中再来一发+短暂击飞0.1s → 携带者回血=本次总伤害的35/80/100%。
- **看点**：第二普攻节奏(=攻速)、扇形红激光刃质感/朝向(朝敌方质心)、单体触发竖劈、竖劈剑气飞行拖尾、3★大扇形、回血飘字。VFX 自验窗口：`VFXPREVIEW=laser_sweep`（扫斩）/ `VFXPREVIEW=laser_chop`（竖劈），`VFXPREVIEW_FOV` 调远近。

### p2eq_036 温泉蛋（3费稀有） — 孵化进度→临时等级(+5%基础属性×3)→孵满全队护盾
- **配置**：`EQDEMO_EQUIP=p2eq_036 EQDEMO_STAR=2 EQDEMO_CARRIER=hunter EQDEMO_ATTACKER=1 EQDEMO_DUMMYHP=8000 EQDEMO_GAP=120`（高血假人让战斗持续够久看满+3级；打不死时靠 +5/tick+造成×0.1 累积）。
  - **看敌死+10源**：`EQDEMO_DUMMYHP=150`（假人被打死→每死+10进度，快速升级，但战斗会提前结束）。
- **预期**：孵化进度累积(4源: +5/2.5s、敌死+10、己死+15、造成×0.1、承受×0.1)→每满100跳 `孵化 Lv+N` 金字+成长环、base属性×1.05(攻/防/抗/血)→满+3级(孵满)时全队均摊 300/400/600 护盾一次+粒子迸发。
- **看点**：升级飘字/成长环、属性真涨(点单位看面板)、+3孵满全队护盾。**我拍板项(待你确认)**：+5%做成复利、孵满=到+3级才发护盾（见审查进度账本）。

<!-- 新增装备演示配置照此格式往下加：id + 名 + 配置命令 + 预期时间线 + 看点 -->

## 演示环境设计约定（2026-07-02，用户授权我定）
按件类型选布局，核心=**看得清、打不完、层数可见**：
- **友方**：默认 1 携带者（持受审装备，50万血/站桩观察）。团队增益/治疗类加 `EQDEMO_ALLIES=N`（友方假人半血裸装→看盾/治疗刷到队友）。
- **敌方**：默认 2 个假人（`EQDEMO_GAP` 调间距；溅射/AoE 摆近 `EQDEMO_GAP=70`）。默认站桩不还手（干净看输出），会掉血跳伤害数字但**不死沙包**（回满，打不完看完整循环）。
- **行为三态**：①默认站桩观察（自发/召唤/周期件）②`EQDEMO_ATTACKER=1` 携带者普攻+技能（on-hit/on-cast/施法件，可加 `EQDEMO_CARRIER=rainbow EQDEMO_SKILL=rainbowStorm`）③`EQDEMO_ENEMY_ATTACKS=1` 敌打携带者（受伤/挨打件，携带者不死沙包）。
- **血量**：假人高血+不死沙包；受伤类携带者也不死；友方假人起手 40% 血（看治疗回填）。
- **层数显示=是**：所有层数/计数类件在头像装备格**右下角显数字徽章**（硬化0-20/齿轮/子弹0-6/吐息0-3/临时等级0-3/生长充能/海葵层/巨浪层/大熊层），充能类显充能条（弯刀刃能/雷杖/火珊瑚/锚/温泉蛋孵化）。`PANEL_COUNT`/`PANEL_CHARGE` 登记。
- **通用复审工具**：`REVIEW_TURTLE=<id> REVIEW_SKILL=<skillPool索引>` 任意龟/技上场；`VFXPREVIEW=<effect>` 隐藏单位放大看特效帧。
