# V2 改造 · 阶段 0 实施规格 —— 清死代码

> 路线图阶段 0（见 `docs/design/V2模式策划-异步PvP生命赛季.md` §十五）。
> 目标：清掉 V2 用不到的死/旧代码，腾干净地基。**事实底座 = 已核实代码（2026-06-26 调查）**。
> 这是"实施级 spec"样板：精确到 file:line + 验收断言 + 开工前决策点。照此应能直接动手、不冒结构性疑问。

---

## ⚠ 关键修正（调查推翻对账的乐观判断）
对账曾说"synergies.gd 10 pet-tag 可整删、零损失"。**核代码后不成立**，两个原因：
1. **老 single 模式仍活**：主菜单「教程」磁贴 → `_on_tutorial()` 设 `mode="single"` → `Battle.tscn` → `Synergies.apply_team` **真执行**（`MainMenuScene.gd:551/652/697`）。pet-tag 在教程里仍会触发。
2. **synergies.gd 还托管着双路在用的工具方法**：`apply_shift`（双头龟换形 / 机甲变身 / 熔岩变身，C-GROUP 机制，**双路在用**）被 `skill_handlers.gd:896` + `BattleScene.gd:4518/5500` 调用。整删文件会**连带砍掉双路的换形/变身**。

→ **结论：synergies.gd 这个文件阶段 0 不能删**（它不是纯 pet-tag 容器）。好在 pet-tag 部分在双路本就被 `BattleScene:808 mode!=duallane` 门控隔离 —— **"双路去 pet-tag 羁绊"这个目标零改动即已达成**。阶段 0 实际只做"清显示残留 + 删 phase2_pvp"。

---

## 范围
- **做**：删 `phase2_pvp.gd` 全链 · 清老 pet-tag 羁绊**显示**残留（TeamSelect 死代码 / HUD 老 chip / 开场日志）。
- **不做**：不删 `synergies.gd`（托管双路 换形/变身）· 不碰 12 类型 / 11 学派显示 · 不碰 CodexScene（已切 Phase2Schools）· 不碰双路战斗逻辑。

## 开工前决策点 —— ✅ 已定：保留教程（2026-06-26 用户拍板）
synergies.gd 原样留，pet-tag 仅教程触发、双路已门控隔离。
**后果（已折入下方任务）**：教程（single 模式）的 HUD/日志**仍在用老 pet-tag 显示链**，是活代码 → **任务 1.3 不删 `_render_synergy` 链**（教程要）；**任务 1.4 改成"加 mode 门控"而非删除**（教程要日志、只挡双路误打印）。
整删 synergies.gd = 独立大改，挂 backlog（仅当未来教程退役才做）。

---

## 档 1 · 无条件做（零风险）

### 任务 1.1 — 删 phase2_pvp.gd 全链
**改动：**
- 删文件 `scripts/engine/phase2_pvp.gd`（53 行，零生产引用，已复核）
- 删 `tests/test_damage.gd:8` 的 `const PvP := preload(...)`
- 删 `tests/test_damage.gd:3499-3516`（PvP 断言内联块，在 `_test_phase2_equip_shell` 末尾，删后函数尾自然收口）

**验收：**
- `bash run-tests.sh` 全绿（用例数相应减少）
- `grep -r phase2_pvp .` → 0 命中；`grep -rE 'PvP\.(commit|verify_reveal|make_action)' .` → 0 命中

### 任务 1.2 — 清 TeamSelect 死显示（纯死代码，不改可见 UI）
**现状**：`_refresh_synergy()` 第 1015 行 `if _synergy_box == null: return` 永远早退（构建已于 316 注释掉）。整链死。
**改动：**
- 删 `TeamSelectScene.gd:1014-1038` `_refresh_synergy()` 整函数
- 删 `TeamSelectScene.gd:1043+` `_make_synergy_chip()` 整函数
- 删所有 `_refresh_synergy()` 调用点（先 `grep _refresh_synergy(` 定位）

**验收：** 编译过；`grep _refresh_synergy` 0 命中；选龟界面视觉无变化（羁绊 chip 早就不显）

### 任务 1.3 — HUD 老羁绊显示【教程保留 → 改为不动】
**现状**：dispatcher `_refresh_school_chips_all`（`BattleScene.gd:13496-13501`）按 mode 分流：duallane 走新 `_render_school_chips`（学派/类型 chip），else（single 教程）走老 `_render_synergy`（pet-tag chip）。
**因保留教程**：else 分支是教程的**活显示**，`_render_synergy`/`_show_synergy_detail`/`_mk_synergy_tier_block` 三函数**保留勿删**。
**改动**：**无**。双路本就走新路径、显示已正确，本条仅核对、不动代码。
**验收**：双路 HUD 学派/类型 chip 正常；教程 HUD 仍显 pet-tag chip（不变）。

### 任务 1.4 — _log_init 老协同日志【教程保留 → 加 mode 门控，不删】
**现状**：`_log_init`(16230-16247) 无 mode 门控，16241-16246 `Synergies.calc_active` 在 duallane 也跑、错误打印"协同激活"。教程（single）打印是对的、要留。
**改动**：把 `BattleScene.gd:16233-16246`（两段 `for s in Synergies.calc_active` + `left_tagged/right_tagged` 收集）整体包进 `if GameState.mode != "duallane":`；保留 16232 `"战斗开始!\n"` + 16247 status_bar + 函数本身。
**验收**：双路开场日志不再出现老协同行；教程开场仍打印 pet-tag 协同；编译过。

---

## 执行顺序 + 自验闸
- 4 个任务相互独立，逐个：**改 → `bash run-tests.sh` 绿 → grep 验删净 → 一任务一 commit**。
- 全档 1 完成后总验：双路一整局（选龟→双路→终极→结算）跑通无崩、HUD chip 正常、测试全绿。手感/视觉这步本就无变化，无需 F5。

## 附 · synergies.gd consumer 分支清单（仅"教程退役"子任务才动，阶段 0 不碰）
读 `_synergy*` flag 的点（双路本就 no-op，因 flag 只由门控住的 apply_team 写）：`damage.gd:68` · `dot.gd:127` · `equipment_runtime.gd:362-363` · `skill_handlers.gd:2439` · `shop_data.gd:66-71` · `BattleScene.gd:3531(已门控)/4814/4899/6862/7207/15152` · `_grant_luck_synergy`(864-903)。⚠ `_test_synergies`(1826-1946) 混了 3 组无关断言（元素 DoT 1868-1875 / 潮汐 ripple 装备 1880-1887 / 反伤地板回归 1919-1924），退役时须先迁出再删函数。
