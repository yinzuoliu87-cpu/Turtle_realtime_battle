# 信息面板 / HUD 资源条 细节修复批 (用户 2026-06-24, 全自主)

> 来源: 用户"信息面板按等级显格子数" + "龟壳储能蓝条下没显" + "除龟壳都查查"。资源条审计(只读)已逐条比对 PoC, 区分真 bug vs PoC 有意取舍。

## 待修 (都写 BattleScene.gd / hp_bar.gd, 与 VFX 批串行)
- [x] **① 信息面板格子数**: `BattleScene.gd:13080-13086` `for i in range(10)` **写死 10 格** → 改成 `Phase2Config.equip_slots_for_level(该龟 side 的 dual_level)`(等级 2/4/6/8/10 → 1/2/3/4/5 槽)。按实际战斗规则显示真实可用格子数。**已修**: 新增 `eq_cap = maxi(equip_slots_for_level(...), eq_ids.size())`(防御已装>cap), 循环/列数/"装备 N/cap"标签全用 eq_cap。
- [x] **② 龟壳气场储能 HUD 空条 (BUG1, 用户报的就是这个)**: `hp_bar.gd:95` auraAwaken 分支读 `_starEnergy`/`_storedEnergy`(龟壳从不写这俩→恒空), 引擎实际写 `_auraEnergy`(`damage.gd:313`)。**改**: auraAwaken 读 `_auraEnergy`(三级回退 `_starEnergy`→`_auraEnergy`→`_storedEnergy`)。cap 计算(hp_bar:97-100)已对, 只改字段读取。**已修**: `f.get("_starEnergy", f.get("_auraEnergy", f.get("_storedEnergy", 0)))`。
- [x] **③ lavaRage 熔岩怒气 HUD 重复显示 (BUG2, 新发现)**: `_lavaRage` 同时进能量条(`BS:1343-1346`)+ 特殊条(`hp_bar:90-93`)= 两条橙。**已修**(用户定"一条就好"): 删 `BattleScene.gd` 资源条创建里的 lavaRage 分支(原 1343-1346 的 `elif passive.type=="lavaRage"`→`res_field="_lavaRage"`/橙色那段), 熔岩龟不再建能量条; 保留 hp_bar `_compute_special_bars` 怒气特殊条(PoC turtle-hud 有 rage 特殊条=保真)。其它龟正常龟能(`_maxEnergy`)蓝条不受影响 — 条件只删 lavaRage 分支, `if _maxEnergy>0` 那支原样。熔岩龟现只剩**一条**怒气特殊条。1607/1607 测过, import 无 script/parse 报错。视觉需 F5。

- [x] **④ 法器法力条刷新链不命中 (潜伏 BUG, 孵化器 agent 挖出)**: `staff_mana_bars` meta 误存在 **hud 兄弟节点**上, 而 `_refresh_slot`/`_refresh_staff_mana_for` 都查 **slot 节点(root)** → 那条刷新链从不命中 → **法器法力条建出来后不更新**(只显初值)。**修**: 把 staff_mana_bars meta 改存在 slot 节点(root)上(对齐孵化器条的做法), 或让 refresh 查 hud。孵化器条已规避此坑可参照。**已修**: `_build_equip_column` 改签名 `(slot_node, hud, ...)`(原只收 hud, 调用方 BS:1368 传 `root,hud`); 视觉(框/图标/法力条)仍 `hud.add_child`, 但 `staff_mana_bars` meta 改存 `slot_node`(get/set/末尾 `_update_staff_mana_bars` 三处, BS:1527-1530)→ 与 `_refresh_slot`(BS:10649)/`_refresh_staff_mana_for`(BS:1556 传 `slot_nodes[i]`) 同节点, 刷新链命中。回合开始 +25 法力后 `_refresh_staff_mana_for` 真驱动条变化。1607/1607 测过, 无 script/parse 报错。

## 非 bug (PoC 一致, 别动)
starEnergy 详情面板无块 · 财宝 HUD 无条 · 法器法力 · 龟能蓝条 · 坚壁 stoneWall · 泡泡 bubbleStore —— 字段写读全一致, 刷新链(`_refresh_slot`→`update_state`/`_update_energy_bar`)完整。

## 进度: ①②③④ 全修。③ 已删能量条那份 lavaRage 渲染(留特殊条), 熔岩龟一条怒气。视觉需 F5。
